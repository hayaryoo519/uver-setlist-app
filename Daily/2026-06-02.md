<!-- Conversation Start: 019e7899-7332-7180-8c3d-49619e6b375d -->
### 🤖 エージェント対話ログ (2026/06/03 08:25)

* **Conversation ID**: `019e7899-7332-7180-8c3d-49619e6b375d`
* **作業スペース**: `kakeibo`

#### 💬 ユーザーからの指示・要望
> <environment_context>
>   <cwd>/home/haya-ryoo/apps/kakeibo</cwd>
>   <shell>bash</shell>
>   <current_date>2026-05-30</current_date>
>   <timezone>Asia/Tokyo</timezone>
> </environment_context>

> 作業候補を挙げて

> それですすめて
> 

> 次の作業候補は？
> 

> 優先度高　から行こうか
> 

> 変更を整理してコミット
> 

> 次の作業候補
> 

> これいったん行こうか
> 品質改善
> 7. Playwrightで主要導線のスモークテストを追加。
> 

> 入力欄が常時表示で、醜いので、
> 改善案を3つ考えて
> 

> OK　案②で
> 

> <environment_context>
>   <current_date>2026-05-31</current_date>
>   <timezone>Asia/Tokyo</timezone>
> </environment_context>

> 復旧したので続けて
> 

> 動作確認したい
> 使用していないポートは閉じてほしい
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://127.0.0.1:3021/
> 
> ## My request for Codex:
> This site can't be reached
> 127.0.0.1 refused to connect
> Try:
> 
> Checking the connection
> Checking the proxy, firewall, and DNS configuration
> ERR_CONNECTION_REFUSED
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://127.0.0.1:3021/
> 
> ## My request for Codex:
> だめっぽいね、Linuxさーばーだからかな
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://127.0.0.1:3021/
> 
> ## My request for Codex:
> PlayWrigitdeの確認よろしく
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://127.0.0.1:3021/
> 
> ## My request for Codex:
> 今はどのポートで動いている？
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://127.0.0.1:3021/
> 
> ## My request for Codex:
> 確認したいので再度起動してほしい
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> ボタンがレスポンシブで大きさ変わるのはよい！
> 画面が広いときにボタンが大きくなりすぎるね
> 

> 次の作業をよろしく

> 再開して

> 次の作業を

> <environment_context>
>   <current_date>2026-06-01</current_date>
>   <timezone>Asia/Tokyo</timezone>
> </environment_context>

> 整理してコミット

> issueとして、整理して
> 金額分け方
> 大まかに
>  5.5 4.6とか
> 細かく
>  レシートから起票
> ユーザー登録からカップルユーザーの仕組み

> こなれくらいの粒度で、作業候補を10個あげて

> シチュエーションを想像して
> お店での支払いや給料日なて
> 
> ユーザーストーリー
> それぞれの場面であったらいいなという機能を3つずつ

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> OK,その機能をすべてissueしてほしい
> 
> あとは、使用料をグラフで確認できるように、
> このリポジトリを参考にしたい。
> [team-mirai/marumie](https://github.com/team-mirai/marumie)
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> 何から行こうか。
> DBなどの整理からかな？
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> よしそれでいこう
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> 整理してコミット
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> つぎへ
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> OK
> 
> 次へ
> 
> どんどんいこう！
> 

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> プッシュして、isuueのクローズ
> 字作業を進めて
> 

> <environment_context>
>   <current_date>2026-06-02</current_date>
>   <timezone>Asia/Tokyo</timezone>
> </environment_context>

> 次作業

> つっきを

> 変更したもので、仕様書の整理をして欲しい

> issueにもあると思うけど、分析ページ（仮）
> として、詳細に状況をグラフで可視化したい
> 
> まる見えのやつグラフ見やすいので、そのまま活用したい（権利関係確認してね）
> 一応私用利用なので大丈夫なつもり

> おけ、コミット

> 目標達成のモチベーションになりそうな、
> デザイン、Uiを既存のものから探して欲しい
> 
> それを元に貯金目標とか節約目標のUiを作りたい

> タブいっぱい増えちゃうよね
> ごちゃごちゃするのは避けたい

> それで実装進めて

> 続きを！

> それでいきう

> よろしく

> プッシュして、次作業へ

> 
> # In app browser:
> - The user has the in-app browser open.
> - Current URL: http://192.168.0.13:3021/
> 
> ## My request for Codex:
> よろしく
> 

> 字作業を
> 

> つづきを
> 

> <environment_context>
>   <current_date>2026-06-03</current_date>
>   <timezone>Asia/Tokyo</timezone>
> </environment_context>

> 整理してコミット
> 
> 残りはデザイン変更のやつ中身確認して、コミットして
> 目的はAi感を無くすこと

> ブランチを切ってデザイン修正をしようか
> 
> デザインmdにそう形で、まずは修正箇所を確認して

> おけい、それですすめたいけど
> 
> デザインのAI感を定義してから、それに当てはまらないようにして欲しい

> 次の作業は

> コミットよろしく、
> ただ確認したいので、メインには入れないでおこうかな
> 
> メインの方の作業候補を

> その順でどんどんすすめて

> 続けて

> 次作業すすめて

> 次作業候補を上げて

> それをすすめて

#### 💻 実行した主要コマンド
```bash
pwd && rg --files -g '!node_modules' -g '!dist' -g '!build' | sed -n '1,220p'
git status --short --branch && git log --oneline -8
rg -n "TODO|FIXME|HACK|XXX|未実装|課題|タスク|roadmap|Roadmap" -g '!node_modules' -g '!dist' -g '!build' .
ls -la && find . -maxdepth 2 \( -name README.md -o -name package.json -o -name AGENTS.md -o -name '*.md' \) -print
find src tests .github supabase postgres -type f | sort
cat package.json && printf '\n-- README --\n' && sed -n '1,240p' README.md
grep -RInE "TODO|FIXME|HACK|XXX|未実装|課題|タスク|roadmap|Roadmap|今後|MVP" --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git .
sed -n '1,260p' Docs/product-spec.md && printf '\n-- MVP --\n' && sed -n '1,240p' Docs/mvp-policy.md && printf '\n-- DIARY --\n' && sed -n '1,280p' Docs/development-diary.md
sed -n '260,430p' Docs/development-diary.md && printf '\n-- SCREENS --\n' && sed -n '1,300p' Docs/screens.md && printf '\n-- CI --\n' && sed -n '1,140p' Docs/cicd.md
sed -n '1,300p' src/components/kakeibo-dashboard.tsx && printf '\n-- DASHBOARD API --\n' && sed -n '1,240p' src/app/api/dashboard/route.ts && printf '\n-- EXPENSE API --\n' && sed -n '1,260p' src/app/api/expenses/route.ts
sed -n '1,260p' src/features/dashboard/dashboard.mapper.ts && printf '\n-- CALCULATOR --\n' && sed -n '1,300p' src/features/settlement/settlement.calculator.ts && printf '\n-- TYPES --\n' && sed -n '1,220p' src/features/settlement/settlement.types.ts && printf '\n-- UTILS --\n' && sed -n '1,240p' src/lib/expense-utils.ts && printf '\n-- DB --\n' && sed -n '1,200p' src/lib/db.ts
sed -n '1,260p' tests/expenses/dashboard.mapper.test.ts && printf '\n-- UTILS TEST --\n' && sed -n '1,260p' tests/expenses/expense-utils.test.ts && printf '\n-- SETTLEMENT TEST --\n' && sed -n '1,340p' tests/settlement/settlement.calculator.test.ts && printf '\n-- WORKFLOW --\n' && sed -n '1,220p' .github/workflows/ci.yml
sed -n '300,680p' src/components/kakeibo-dashboard.tsx && printf '\n-- PAGE --\n' && sed -n '1,120p' src/app/page.tsx && printf '\n-- TSCONFIG --\n' && cat tsconfig.json
sed -n '1,320p' postgres/migrations/001_initial_schema.sql && printf '\n-- SEED --\n' && sed -n '1,320p' postgres/migrations/003_seed_local_data.sql && printf '\n-- DATA MODEL --\n' && sed -n '1,250p' Docs/data-model.md
npm test
npm run build
nl -ba src/app/api/dashboard/route.ts | sed -n '1,180p' && printf '\n-- EXPENSE API --\n' && nl -ba src/app/api/expenses/route.ts | sed -n '1,280p' && printf '\n-- COMPONENT --\n' && nl -ba src/components/kakeibo-dashboard.tsx | sed -n '45,185p'
nl -ba src/features/dashboard/dashboard.mapper.ts | sed -n '1,180p' && printf '\n-- SCREENS --\n' && nl -ba Docs/screens.md | sed -n '1,180p' && printf '\n-- CI DOC --\n' && nl -ba Docs/cicd.md | sed -n '48,90p'
grep -n "error\|composer\|chip" src/app/globals.css | sed -n '1,220p'
sed -n '1,280p' src/app/globals.css && printf '\n-- rest --\n' && sed -n '280,620p' src/app/globals.css
git status --short
apply_patch <<'PATCH'
*** Begin Patch
*** Update File: src/lib/expense-utils.ts
@@
 export type ExpenseScope = "shared" | "personal" | "review";
 export type MemberId = "a" | "b";
 export type SplitPreset = "equal" | "a70" | "b70";
+export type SplitRates = Record<MemberId, number>;
@@
   note?: string;
   createdAt: string;
   splitPreset?: SplitPreset;
+  splitRates?: SplitRates;
@@
-function sharesForPreset(preset: SplitPreset | undefined) {
+export function splitRatesForPreset(preset: SplitPreset | undefined): SplitRates {
   if (preset === "a70") {
-    return [
-      { memberId: "a", rate: 0.7 },
-      { memberId: "b", rate: 0.3 },
-    ];
+    return { a: 0.7, b: 0.3 };
   }
 
   if (preset === "b70") {
-    return [
-      { memberId: "a", rate: 0.3 },
-      { memberId: "b", rate: 0.7 },
-    ];
+    return { a: 0.3, b: 0.7 };
   }
 
-  return undefined;
+  return { a: 0.5, b: 0.5 };
+}
+
+function sharesFor(expense: Expense) {
+  const rates = expense.splitRates ?? splitRatesForPreset(expense.splitPreset);
+
+  return [
+    { memberId: "a", rate: rates.a },
+    { memberId: "b", rate: rates.b },
+  ];
 }
@@
       amount: expense.amount,
       paidByMemberId: expense.paidBy,
       scope: expense.scope,
-      shares: sharesForPreset(expense.splitPreset),
+      shares: sharesFor(expense),
     })),
   );
*** Update File: src/features/dashboard/dashboard.mapper.ts
@@
-import type { Expense, MemberId } from "../../lib/expense-utils.ts";
+import type { Expense, MemberId, SplitPreset, SplitRates } from "../../lib/expense-utils.ts";
+
+export type DashboardExpenseSplitRow = {
+  member_id: string;
+  rate: number | string;
+};
@@
   expense_date: string | Date;
   created_at: string | Date;
+  splits?: DashboardExpenseSplitRow[] | null;
@@
 function noteFor(title: string, memo: string | null) {
@@
 }
 
+function splitDataFor(splits: DashboardExpenseSplitRow[] | null | undefined, memberAliases: Map<string, MemberId>) {
+  const splitRates: Partial<SplitRates> = {};
+
+  for (const split of splits ?? []) {
+    const alias = memberAliases.get(split.member_id);
+    const rate = Number(split.rate);
+    if (alias && Number.isFinite(rate)) {
+      splitRates[alias] = rate;
+    }
+  }
+
+  if (splitRates.a === undefined || splitRates.b === undefined) {
+    return {};
+  }
+
+  let splitPreset: SplitPreset | undefined;
+  if (splitRates.a === 0.5 && splitRates.b === 0.5) splitPreset = "equal";
+  if (splitRates.a === 0.7 && splitRates.b === 0.3) splitPreset = "a70";
+  if (splitRates.a === 0.3 && splitRates.b === 0.7) splitPreset = "b70";
+
+  return {
+    splitPreset,
+    splitRates: splitRates as SplitRates,
+  };
+}
+
 export function mapDashboardExpenses(memberRows: DashboardMemberRow[], expenseRows: DashboardExpenseRow[]): Expense[] {
@@
-  return expenseRows.map((expense) => ({
-    id: expense.id,
-    title: expense.title,
-    amount: expense.amount,
-    paidBy: expense.paid_by_member_id ? (memberAliases.get(expense.paid_by_member_id) ?? "a") : "a",
-    scope: expense.scope,
-    category: expense.category,
-    note: noteFor(expense.title, expense.memo),
-    createdAt: isoDateTime(expense.created_at ?? expense.expense_date),
-  }));
+  return expenseRows.map((expense) => {
+    const splitData = splitDataFor(expense.splits, memberAliases);
+
+    return {
+      id: expense.id,
+      title: expense.title,
+      amount: expense.amount,
+      paidBy: expense.paid_by_member_id ? (memberAliases.get(expense.paid_by_member_id) ?? "a") : "a",
+      scope: expense.scope,
+      category: expense.category,
+      note: noteFor(expense.title, expense.memo),
+      createdAt: isoDateTime(expense.created_at ?? expense.expense_date),
+      ...splitData,
+    };
+  });
 }
*** Update File: src/app/api/dashboard/route.ts
@@
     e.memo,
     e.expense_date::text,
-    e.expense_date::text as created_at
+    e.expense_date::text as created_at,
+    coalesce((
+      select json_agg(
+        json_build_object(
+          'member_id', es.member_id::text,
+          'rate', es.rate::float8
+        )
+        order by hm.role asc, hm.created_at asc
+      )
+      from expense_splits es
+      join household_members hm on hm.id = es.member_id
+      where es.expense_id = e.id and es.rate is not null
+    ), '[]'::json) as splits
*** Add File: src/app/api/expenses/[id]/route.ts
+import { NextResponse } from "next/server";
+import { db } from "../../../../lib/db";
+import type { ExpenseScope, SplitPreset } from "../../../../lib/expense-utils";
+
+export const runtime = "nodejs";
+export const dynamic = "force-dynamic";
+
+type UpdateExpenseRequest = {
+  scope?: unknown;
+  splitPreset?: unknown;
+};
+
+type HouseholdRow = { id: string };
+type MemberRow = { id: string };
+
+const householdQuery = `
+  select id
+  from households
+  order by created_at asc
+  limit 1
+`;
+
+const membersQuery = `
+  select id
+  from household_members
+  where household_id = $1
+  order by role asc, created_at asc
+`;
+
+const expenseQuery = `
+  select id
+  from expenses
+  where id = $1 and household_id = $2
+`;
+
+const updateExpenseQuery = `
+  update expenses
+  set scope = $2
+  where id = $1
+`;
+
+const deleteSplitsQuery = `
+  delete from expense_splits
+  where expense_id = $1
+`;
+
+const insertSplitQuery = `
+  insert into expense_splits (expense_id, member_id, rate)
+  values ($1, $2, $3)
+`;
+
+function parseScope(value: unknown): ExpenseScope | null {
+  if (value === "shared" || value === "personal" || value === "review") return value;
+  return null;
+}
+
+function parseSplitPreset(value: unknown): SplitPreset {
+  if (value === "a70" || value === "b70") return value;
+  return "equal";
+}
+
+function splitRatesFor(preset: SplitPreset) {
+  if (preset === "a70") return [0.7, 0.3];
+  if (preset === "b70") return [0.3, 0.7];
+  return [0.5, 0.5];
+}
+
+export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) {
+  try {
+    const { id } = await context.params;
+    const body = (await request.json()) as UpdateExpenseRequest;
+    const scope = parseScope(body.scope);
+    const splitPreset = parseSplitPreset(body.splitPreset);
+
+    if (!scope) {
+      return NextResponse.json({ error: "invalid_expense_update" }, { status: 400 });
+    }
+
+    const pool = db();
+    const client = await pool.connect();
+
+    try {
+      await client.query("begin");
+
+      const household = (await client.query<HouseholdRow>(householdQuery)).rows[0];
+      if (!household) {
+        await client.query("rollback");
+        return NextResponse.json({ error: "household_not_found" }, { status: 404 });
+      }
+
+      const expense = (await client.query(expenseQuery, [id, household.id])).rows[0];
+      if (!expense) {
+        await client.query("rollback");
+        return NextResponse.json({ error: "expense_not_found" }, { status: 404 });
+      }
+
+      await client.query(updateExpenseQuery, [id, scope]);
+      await client.query(deleteSplitsQuery, [id]);
+
+      if (scope === "shared") {
+        const members = (await client.query<MemberRow>(membersQuery, [household.id])).rows;
+        const rates = splitRatesFor(splitPreset);
+
+        if (members.length !== rates.length) {
+          await client.query("rollback");
+          return NextResponse.json({ error: "invalid_household_members" }, { status: 400 });
+        }
+
+        for (const [index, member] of members.entries()) {
+          await client.query(insertSplitQuery, [id, member.id, rates[index]]);
+        }
+      }
+
+      await client.query("commit");
+      return NextResponse.json({
+        expense: {
+          id,
+          scope,
+          splitPreset: scope === "shared" ? splitPreset : undefined,
+        },
+      });
+    } catch (error) {
+      await client.query("rollback");
+      throw error;
+    } finally {
+      client.release();
+    }
+  } catch (error) {
+    console.error("Failed to update expense", error);
+    return NextResponse.json({ error: "expense_update_failed" }, { status: 500 });
+  }
+}
*** End Patch
PATCH
python3 <<'PY'
from pathlib import Path

root = Path('/home/haya-ryoo/apps/kakeibo')

def replace(path, old, new):
    file = root / path
    text = file.read_text()
    if old not in text:
        raise SystemExit(f'missing expected text in {path}: {old[:80]!r}')
    file.write_text(text.replace(old, new, 1))

replace('src/lib/expense-utils.ts',
'''export type SplitPreset = "equal" | "a70" | "b70";\n''',
'''export type SplitPreset = "equal" | "a70" | "b70";\nexport type SplitRates = Record<MemberId, number>;\n''')
replace('src/lib/expense-utils.ts',
'''  splitPreset?: SplitPreset;\n};\n''',
'''  splitPreset?: SplitPreset;\n  splitRates?: SplitRates;\n};\n''')
replace('src/lib/expense-utils.ts',
'''function sharesForPreset(preset: SplitPreset | undefined) {\n  if (preset === "a70") {\n    return [\n      { memberId: "a", rate: 0.7 },\n      { memberId: "b", rate: 0.3 },\n    ];\n  }\n\n  if (preset === "b70") {\n    return [\n      { memberId: "a", rate: 0.3 },\n      { memberId: "b", rate: 0.7 },\n    ];\n  }\n\n  return undefined;\n}\n''',
'''export function splitRatesForPreset(preset: SplitPreset | undefined): SplitRates {\n  if (preset === "a70") {\n    return { a: 0.7, b: 0.3 };\n  }\n\n  if (preset === "b70") {\n    return { a: 0.3, b: 0.7 };\n  }\n\n  return { a: 0.5, b: 0.5 };\n}\n\nfunction sharesFor(expense: Expense) {\n  const rates = expense.splitRates ?? splitRatesForPreset(expense.splitPreset);\n\n  return [\n    { memberId: "a", rate: rates.a },\n    { memberId: "b", rate: rates.b },\n  ];\n}\n''')
replace('src/lib/expense-utils.ts', '      shares: sharesForPreset(expense.splitPreset),\n', '      shares: sharesFor(expense),\n')

replace('src/features/dashboard/dashboard.mapper.ts',
'''import type { Expense, MemberId } from "../../lib/expense-utils.ts";\n''',
'''import type { Expense, MemberId, SplitPreset, SplitRates } from "../../lib/expense-utils.ts";\n\nexport type DashboardExpenseSplitRow = {\n  member_id: string;\n  rate: number | string;\n};\n''')
replace('src/features/dashboard/dashboard.mapper.ts',
'''  created_at: string | Date;\n};\n''',
'''  created_at: string | Date;\n  splits?: DashboardExpenseSplitRow[] | null;\n};\n''')
replace('src/features/dashboard/dashboard.mapper.ts',
'''export function mapDashboardExpenses(memberRows: DashboardMemberRow[], expenseRows: DashboardExpenseRow[]): Expense[] {\n''',
'''function splitDataFor(splits: DashboardExpenseSplitRow[] | null | undefined, memberAliases: Map<string, MemberId>) {\n  const splitRates: Partial<SplitRates> = {};\n\n  for (const split of splits ?? []) {\n    const alias = memberAliases.get(split.member_id);\n    const rate = Number(split.rate);\n    if (alias && Number.isFinite(rate)) {\n      splitRates[alias] = rate;\n    }\n  }\n\n  if (splitRates.a === undefined || splitRates.b === undefined) {\n    return {};\n  }\n\n  let splitPreset: SplitPreset | undefined;\n  if (splitRates.a === 0.5 && splitRates.b === 0.5) splitPreset = "equal";\n  if (splitRates.a === 0.7 && splitRates.b === 0.3) splitPreset = "a70";\n  if (splitRates.a === 0.3 && splitRates.b === 0.7) splitPreset = "b70";\n\n  return {\n    splitPreset,\n    splitRates: splitRates as SplitRates,\n  };\n}\n\nexport function mapDashboardExpenses(memberRows: DashboardMemberRow[], expenseRows: DashboardExpenseRow[]): Expense[] {\n''')
replace('src/features/dashboard/dashboard.mapper.ts',
'''  return expenseRows.map((expense) => ({\n    id: expense.id,\n    title: expense.title,\n    amount: expense.amount,\n    paidBy: expense.paid_by_member_id ? (memberAliases.get(expense.paid_by_member_id) ?? "a") : "a",\n    scope: expense.scope,\n    category: expense.category,\n    note: noteFor(expense.title, expense.memo),\n    createdAt: isoDateTime(expense.created_at ?? expense.expense_date),\n  }));\n''',
'''  return expenseRows.map((expense) => {\n    const splitData = splitDataFor(expense.splits, memberAliases);\n\n    return {\n      id: expense.id,\n      title: expense.title,\n      amount: expense.amount,\n      paidBy: expense.paid_by_member_id ? (memberAliases.get(expense.paid_by_member_id) ?? "a") : "a",\n      scope: expense.scope,\n      category: expense.category,\n      note: noteFor(expense.title, expense.memo),\n      createdAt: isoDateTime(expense.created_at ?? expense.expense_date),\n      ...splitData,\n    };\n  });\n''')

replace('src/app/api/dashboard/route.ts',
'''    e.expense_date::text,\n    e.expense_date::text as created_at\n''',
'''    e.expense_date::text,\n    e.expense_date::text as created_at,\n    coalesce((\n      select json_agg(\n        json_build_object(\n          'member_id', es.member_id::text,\n          'rate', es.rate::float8\n        )\n        order by hm.role asc, hm.created_at asc\n      )\n      from expense_splits es\n      join household_members hm on hm.id = es.member_id\n      where es.expense_id = e.id and es.rate is not null\n    ), '[]'::json) as splits\n''')

file = root / 'src/app/api/expenses/[id]/route.ts'
file.parent.mkdir(parents=True, exist_ok=True)
file.write_text('''import { NextResponse } from "next/server";\nimport { db } from "../../../../lib/db";\nimport type { ExpenseScope, SplitPreset } from "../../../../lib/expense-utils";\n\nexport const runtime = "nodejs";\nexport const dynamic = "force-dynamic";\n\ntype UpdateExpenseRequest = {\n  scope?: unknown;\n  splitPreset?: unknown;\n};\n\ntype HouseholdRow = { id: string };\ntype MemberRow = { id: string };\n\nconst householdQuery = `\n  select id\n  from households\n  order by created_at asc\n  limit 1\n`;\n\nconst membersQuery = `\n  select id\n  from household_members\n  where household_id = $1\n  order by role asc, created_at asc\n`;\n\nconst expenseQuery = `\n  select id\n  from expenses\n  where id = $1 and household_id = $2\n`;\n\nconst updateExpenseQuery = `\n  update expenses\n  set scope = $2\n  where id = $1\n`;\n\nconst deleteSplitsQuery = `\n  delete from expense_splits\n  where expense_id = $1\n`;\n\nconst insertSplitQuery = `\n  insert into expense_splits (expense_id, member_id, rate)\n  values ($1, $2, $3)\n`;\n\nfunction parseScope(value: unknown): ExpenseScope | null {\n  if (value === "shared" || value === "personal" || value === "review") return value;\n  return null;\n}\n\nfunction parseSplitPreset(value: unknown): SplitPreset {\n  if (value === "a70" || value === "b70") return value;\n  return "equal";\n}\n\nfunction splitRatesFor(preset: SplitPreset) {\n  if (preset === "a70") return [0.7, 0.3];\n  if (preset === "b70") return [0.3, 0.7];\n  return [0.5, 0.5];\n}\n\nexport async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) {\n  try {\n    const { id } = await context.params;\n    const body = (await request.json()) as UpdateExpenseRequest;\n    const scope = parseScope(body.scope);\n    const splitPreset = parseSplitPreset(body.splitPreset);\n\n    if (!scope) {\n      return NextResponse.json({ error: "invalid_expense_update" }, { status: 400 });\n    }\n\n    const pool = db();\n    const client = await pool.connect();\n\n    try {\n      await client.query("begin");\n\n      const household = (await client.query<HouseholdRow>(householdQuery)).rows[0];\n      if (!household) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "household_not_found" }, { status: 404 });\n      }\n\n      const expense = (await client.query(expenseQuery, [id, household.id])).rows[0];\n      if (!expense) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "expense_not_found" }, { status: 404 });\n      }\n\n      await client.query(updateExpenseQuery, [id, scope]);\n      await client.query(deleteSplitsQuery, [id]);\n\n      if (scope === "shared") {\n        const members = (await client.query<MemberRow>(membersQuery, [household.id])).rows;\n        const rates = splitRatesFor(splitPreset);\n\n        if (members.length !== rates.length) {\n          await client.query("rollback");\n          return NextResponse.json({ error: "invalid_household_members" }, { status: 400 });\n        }\n\n        for (const [index, member] of members.entries()) {\n          await client.query(insertSplitQuery, [id, member.id, rates[index]]);\n        }\n      }\n\n      await client.query("commit");\n      return NextResponse.json({\n        expense: {\n          id,\n          scope,\n          splitPreset: scope === "shared" ? splitPreset : undefined,\n        },\n      });\n    } catch (error) {\n      await client.query("rollback");\n      throw error;\n    } finally {\n      client.release();\n    }\n  } catch (error) {\n    console.error("Failed to update expense", error);\n    return NextResponse.json({ error: "expense_update_failed" }, { status: 500 });\n  }\n}\n''')
PY
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')

def replace(path, old, new):
    file = root / path
    text = file.read_text()
    if old not in text:
        raise SystemExit(f'missing expected text in {path}: {old[:100]!r}')
    file.write_text(text.replace(old, new, 1))

replace('src/components/kakeibo-dashboard.tsx',
'''  splitPresetLabel,\n  summaryFor,\n''',
'''  splitPresetLabel,\n  splitRatesForPreset,\n  summaryFor,\n''')
replace('src/components/kakeibo-dashboard.tsx',
'''function splitLabelFor(expense: Expense) {\n  if (expense.scope !== "shared") return null;\n  return splitPresetLabel(expense.splitPreset);\n}\n''',
'''function splitLabelFor(expense: Expense) {\n  if (expense.scope !== "shared") return null;\n  if (expense.splitPreset) return splitPresetLabel(expense.splitPreset);\n  if (expense.splitRates) return `葵 ${expense.splitRates.a * 10} : 蓮 ${expense.splitRates.b * 10}`;\n  return splitPresetLabel();\n}\n''')
replace('src/components/kakeibo-dashboard.tsx',
'''  const [defaultSplitPreset, setDefaultSplitPreset] = useState<SplitPreset>("equal");\n  const [expenses, setExpenses] = useState<Expense[]>(initialExpenses);\n''',
'''  const [defaultSplitPreset, setDefaultSplitPreset] = useState<SplitPreset>("equal");\n  const [expenses, setExpenses] = useState<Expense[]>(initialExpenses);\n  const [saveError, setSaveError] = useState<string | null>(null);\n''')
replace('src/components/kakeibo-dashboard.tsx',
'''    try {\n      const response = await fetch("/api/expenses", {\n''',
'''    setSaveError(null);\n\n    try {\n      const response = await fetch("/api/expenses", {\n''')
replace('src/components/kakeibo-dashboard.tsx',
'''      } else {\n        setExpenses((current) => [fallbackExpense, ...current]);\n      }\n    } catch {\n      setExpenses((current) => [fallbackExpense, ...current]);\n    }\n\n    setComposerValue("");\n  };\n\n  const updateScope = (expenseId: string, scope: ExpenseScope) => {\n    setExpenses((current) =>\n      current.map((expense) =>\n        expense.id === expenseId\n          ? {\n              ...expense,\n              scope,\n              splitPreset: scope === "shared" ? (expense.splitPreset ?? defaultSplitPreset) : undefined,\n              note: scope === "review" ? expense.note ?? "扱いを決める" : undefined,\n            }\n          : expense,\n      ),\n    );\n  };\n\n  const updateSplitPreset = (expenseId: string, splitPreset: SplitPreset) => {\n    setExpenses((current) =>\n      current.map((expense) =>\n        expense.id === expenseId\n          ? {\n              ...expense,\n              scope: "shared",\n              splitPreset,\n              note: undefined,\n            }\n          : expense,\n      ),\n    );\n  };\n''',
'''      } else {\n        setSaveError("支出を保存できませんでした。通信状態を確認して、もう一度お試しください。");\n        return;\n      }\n    } catch {\n      setSaveError("支出を保存できませんでした。通信状態を確認して、もう一度お試しください。");\n      return;\n    }\n\n    setComposerValue("");\n  };\n\n  const saveExpenseUpdate = async (previousExpense: Expense, nextExpense: Expense) => {\n    setSaveError(null);\n    setExpenses((current) => current.map((expense) => expense.id === nextExpense.id ? nextExpense : expense));\n\n    try {\n      const response = await fetch(`/api/expenses/${nextExpense.id}`, {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({\n          scope: nextExpense.scope,\n          splitPreset: nextExpense.splitPreset,\n        }),\n      });\n\n      if (!response.ok) throw new Error("expense_update_failed");\n    } catch {\n      setExpenses((current) => current.map((expense) => expense.id === previousExpense.id ? previousExpense : expense));\n      setSaveError("変更を保存できませんでした。表示を元に戻しました。");\n    }\n  };\n\n  const updateScope = (expenseId: string, scope: ExpenseScope) => {\n    const expense = expenses.find((item) => item.id === expenseId);\n    if (!expense) return;\n\n    const splitPreset = scope === "shared" ? (expense.splitPreset ?? defaultSplitPreset) : undefined;\n    const nextExpense: Expense = {\n      ...expense,\n      scope,\n      splitPreset,\n      splitRates: scope === "shared" ? splitRatesForPreset(splitPreset) : undefined,\n      note: scope === "review" ? expense.note ?? "扱いを決める" : undefined,\n    };\n\n    void saveExpenseUpdate(expense, nextExpense);\n  };\n\n  const updateSplitPreset = (expenseId: string, splitPreset: SplitPreset) => {\n    const expense = expenses.find((item) => item.id === expenseId);\n    if (!expense) return;\n\n    void saveExpenseUpdate(expense, {\n      ...expense,\n      scope: "shared",\n      splitPreset,\n      splitRates: splitRatesForPreset(splitPreset),\n      note: undefined,\n    });\n  };\n''')
replace('src/components/kakeibo-dashboard.tsx',
'''        <div className="composer__note">LINE っぽく軽く入れて、あとで要確認を整える。</div>\n''',
'''        {saveError ? <div className="composer__error" role="alert">{saveError}</div> : null}\n        <div className="composer__note">LINE っぽく軽く入れて、あとで要確認を整える。</div>\n''')

replace('src/app/globals.css',
'''.composer__note {\n  display: none;\n  margin-top: 10px;\n  color: var(--muted);\n  font-size: 12px;\n}\n''',
'''.composer__note {\n  display: none;\n  margin-top: 10px;\n  color: var(--muted);\n  font-size: 12px;\n}\n\n.composer__error {\n  margin-top: 8px;\n  color: var(--danger);\n  font-size: 12px;\n  font-weight: 600;\n}\n''')
PY
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')

def replace(path, old, new):
    file = root / path
    text = file.read_text()
    if old not in text:
        raise SystemExit(f'missing expected text in {path}: {old[:100]!r}')
    file.write_text(text.replace(old, new, 1))

replace('tests/expenses/dashboard.mapper.test.ts',
'''  });\n});\n''',
'''  });\n\n  it("keeps persisted custom split rates for settlement", () => {\n    const [expense] = mapDashboardExpenses(\n      [\n        { id: "member-a", display_name: "葵" },\n        { id: "member-b", display_name: "蓮" },\n      ],\n      [\n        {\n          id: "expense-rent",\n          title: "家賃",\n          amount: 124000,\n          paid_by_member_id: "member-a",\n          scope: "shared",\n          category: "家賃",\n          memo: "5月分の家賃",\n          expense_date: "2026-05-01",\n          created_at: "2026-05-01T00:00:00.000Z",\n          splits: [\n            { member_id: "member-a", rate: "0.5500" },\n            { member_id: "member-b", rate: "0.4500" },\n          ],\n        },\n      ],\n    );\n\n    assert.deepEqual(expense.splitRates, { a: 0.55, b: 0.45 });\n    assert.equal(expense.splitPreset, undefined);\n  });\n});\n''')

replace('tests/expenses/expense-utils.test.ts',
'''  });\n});\n''',
'''  });\n\n  it("applies persisted custom split rates to settlement", () => {\n    const expenses: Expense[] = [\n      {\n        id: "rent",\n        title: "家賃",\n        amount: 10000,\n        paidBy: "b",\n        scope: "shared",\n        category: "家賃",\n        createdAt: "2026-05-01T00:00:00.000Z",\n        splitRates: { a: 0.55, b: 0.45 },\n      },\n    ];\n\n    const summary = summaryFor(expenses);\n\n    assert.equal(summary.balanceA, -5500);\n    assert.equal(summary.balanceB, 5500);\n    assert.equal(summary.debtor, "a");\n    assert.equal(summary.creditor, "b");\n    assert.equal(summary.settlement, 5500);\n  });\n});\n''')
PY

git diff --stat
git status --short
git diff -- src/app/api/dashboard/route.ts src/app/api/expenses/'[id]'/route.ts src/components/kakeibo-dashboard.tsx src/features/dashboard/dashboard.mapper.ts src/lib/expense-utils.ts tests/expenses/dashboard.mapper.test.ts tests/expenses/expense-utils.test.ts src/app/globals.css
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/components/kakeibo-dashboard.tsx')
text = file.read_text()
old = '''    const expense = expenses.find((item) => item.id === expenseId);\n    if (!expense) return;\n\n    const splitPreset = scope === "shared" ? (expense.splitPreset ?? defaultSplitPreset) : undefined;\n'''
new = '''    const expense = expenses.find((item) => item.id === expenseId);\n    if (!expense || expense.scope === scope) return;\n\n    const splitPreset = scope === "shared" ? (expense.splitPreset ?? defaultSplitPreset) : undefined;\n'''
if old not in text:
    raise SystemExit('expected updateScope block not found')
file.write_text(text.replace(old, new, 1))
PY

sed -n '1,240p' src/app/api/expenses/'[id]'/route.ts
git status --short
git diff --check && docker compose ps
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run dev -- --host 127.0.0.1 --port 3021
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run dev -- --hostname 127.0.0.1 --port 3021
set -e
expense_id=dddddddd-dddd-4ddd-8ddd-ddddddddddd4
restore_review() {
  curl -fsS -X PATCH "http://127.0.0.1:3021/api/expenses/$expense_id" -H 'Content-Type: application/json' --data '{"scope":"review"}' >/dev/null
}
trap restore_review EXIT
curl -fsS -X PATCH "http://127.0.0.1:3021/api/expenses/$expense_id" -H 'Content-Type: application/json' --data '{"scope":"shared","splitPreset":"b70"}'
printf '\n-- dashboard after patch --\n'
curl -fsS http://127.0.0.1:3021/api/dashboard
printf '\n'
curl -fsS http://127.0.0.1:3021/api/dashboard
git status --short && printf '\n-- diff stat --\n' && git diff --stat && printf '\n-- new route --\n' && find src/app/api/expenses/'[id]' -type f -maxdepth 1 -print
sed -n '1,180p' src/app/api/dashboard/route.ts && printf '\n-- update route --\n' && sed -n '1,260p' src/app/api/expenses/'[id]'/route.ts && printf '\n-- component selected --\n' && sed -n '1,210p' src/components/kakeibo-dashboard.tsx && printf '\n-- settlement selected --\n' && sed -n '340,470p' src/components/kakeibo-dashboard.tsx
sed -n '130,270p' postgres/migrations/001_initial_schema.sql && printf '\n-- seed settlement --\n' && sed -n '220,330p' postgres/migrations/003_seed_local_data.sql && printf '\n-- package --\n' && cat package.json && printf '\n-- git status --\n' && git status --short
find tests -type f -maxdepth 3 -print | sort && printf '\n-- docker compose --\n' && sed -n '1,180p' docker-compose.yml && printf '\n-- env example --\n' && sed -n '1,160p' .env.example
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')

(root / 'src/app/api/dashboard/route.ts').write_text('''import { NextResponse } from "next/server";\nimport { db } from "../../../lib/db";\nimport { mapDashboardExpenses, type DashboardExpenseRow, type DashboardMemberRow } from "../../../features/dashboard/dashboard.mapper";\n\nexport const runtime = "nodejs";\nexport const dynamic = "force-dynamic";\n\ntype SettlementPeriodStatus = "open" | "reviewing" | "settled";\n\nconst householdQuery = `\n  select id\n  from households\n  order by created_at asc\n  limit 1\n`;\n\nconst membersQuery = `\n  select id, display_name\n  from household_members\n  where household_id = $1\n  order by role asc, created_at asc\n`;\n\nconst expensesQuery = `\n  select\n    e.id::text,\n    case when c.kind = 'rent' then c.name else coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name) end as title,\n    e.amount,\n    e.paid_by_member_id::text,\n    e.scope,\n    c.name as category,\n    e.memo,\n    e.expense_date::text,\n    e.expense_date::text as created_at,\n    coalesce((\n      select json_agg(\n        json_build_object(\n          'member_id', es.member_id::text,\n          'rate', es.rate::float8\n        )\n        order by hm.role asc, hm.created_at asc\n      )\n      from expense_splits es\n      join household_members hm on hm.id = es.member_id\n      where es.expense_id = e.id and es.rate is not null\n    ), '[]'::json) as splits\n  from expenses e\n  join categories c on c.id = e.category_id\n  where e.household_id = $1\n    and e.expense_date >= make_date($2, $3, 1)\n    and e.expense_date < make_date($2, $3, 1) + interval '1 month'\n  order by e.expense_date desc, e.created_at desc\n`;\n\nconst settlementPeriodQuery = `\n  select status\n  from settlement_periods\n  where household_id = $1 and year = $2 and month = $3\n`;\n\nfunction parsePeriod(request: Request) {\n  const url = new URL(request.url);\n  const now = new Date();\n  const year = Number(url.searchParams.get("year") ?? now.getFullYear());\n  const month = Number(url.searchParams.get("month") ?? now.getMonth() + 1);\n\n  if (!Number.isInteger(year) || year < 2000 || year > 9999 || !Number.isInteger(month) || month < 1 || month > 12) {\n    return null;\n  }\n\n  return { year, month };\n}\n\nexport async function GET(request: Request) {\n  try {\n    const period = parsePeriod(request);\n    if (!period) {\n      return NextResponse.json({ error: "invalid_period" }, { status: 400 });\n    }\n\n    const pool = db();\n    const householdResult = await pool.query<{ id: string }>(householdQuery);\n    const household = householdResult.rows[0];\n\n    if (!household) {\n      return NextResponse.json({ members: [], expenses: [], period: { ...period, status: "open" } });\n    }\n\n    const [memberResult, expenseResult, settlementPeriodResult] = await Promise.all([\n      pool.query<DashboardMemberRow>(membersQuery, [household.id]),\n      pool.query<DashboardExpenseRow>(expensesQuery, [household.id, period.year, period.month]),\n      pool.query<{ status: SettlementPeriodStatus }>(settlementPeriodQuery, [household.id, period.year, period.month]),\n    ]);\n\n    return NextResponse.json({\n      members: memberResult.rows,\n      expenses: mapDashboardExpenses(memberResult.rows, expenseResult.rows),\n      period: {\n        ...period,\n        status: settlementPeriodResult.rows[0]?.status ?? "open",\n      },\n    });\n  } catch (error) {\n    console.error("Failed to load dashboard data", error);\n\n    return NextResponse.json(\n      { error: "dashboard_load_failed" },\n      { status: 500 },\n    );\n  }\n}\n''')

settlement = root / 'src/app/api/settlements/route.ts'
settlement.parent.mkdir(parents=True, exist_ok=True)
settlement.write_text('''import { NextResponse } from "next/server";\nimport { calculateSettlement } from "../../../features/settlement/settlement.calculator";\nimport type { SettlementExpense } from "../../../features/settlement/settlement.types";\nimport { db } from "../../../lib/db";\n\nexport const runtime = "nodejs";\nexport const dynamic = "force-dynamic";\n\ntype CreateSettlementRequest = {\n  year?: unknown;\n  month?: unknown;\n};\n\ntype HouseholdRow = { id: string };\ntype MemberRow = { id: string; display_name: string };\ntype ExpenseRow = {\n  id: string;\n  amount: number;\n  paid_by_member_id: string | null;\n  scope: SettlementExpense["scope"];\n  splits: { member_id: string; rate: number | string }[];\n};\n\nconst householdQuery = `\n  select id\n  from households\n  order by created_at asc\n  limit 1\n`;\n\nconst membersQuery = `\n  select id::text, display_name\n  from household_members\n  where household_id = $1\n  order by role asc, created_at asc\n`;\n\nconst expensesQuery = `\n  select\n    e.id::text,\n    e.amount,\n    e.paid_by_member_id::text,\n    e.scope,\n    coalesce((\n      select json_agg(\n        json_build_object(\n          'member_id', es.member_id::text,\n          'rate', es.rate::float8\n        )\n        order by hm.role asc, hm.created_at asc\n      )\n      from expense_splits es\n      join household_members hm on hm.id = es.member_id\n      where es.expense_id = e.id and es.rate is not null\n    ), '[]'::json) as splits\n  from expenses e\n  where e.household_id = $1\n    and e.expense_date >= make_date($2, $3, 1)\n    and e.expense_date < make_date($2, $3, 1) + interval '1 month'\n  order by e.expense_date asc, e.created_at asc\n`;\n\nconst upsertPeriodQuery = `\n  insert into settlement_periods (household_id, year, month, status, settled_at)\n  values ($1, $2, $3, 'settled', now())\n  on conflict (household_id, year, month) do update\n  set status = 'settled', settled_at = now()\n  returning id::text\n`;\n\nconst deleteSettlementsQuery = `\n  delete from settlements\n  where period_id = $1\n`;\n\nconst insertSettlementQuery = `\n  insert into settlements (period_id, from_member_id, to_member_id, amount, status)\n  values ($1, $2, $3, $4, 'pending')\n`;\n\nfunction parsePeriod(body: CreateSettlementRequest) {\n  const year = typeof body.year === "number" ? body.year : Number.NaN;\n  const month = typeof body.month === "number" ? body.month : Number.NaN;\n\n  if (!Number.isInteger(year) || year < 2000 || year > 9999 || !Number.isInteger(month) || month < 1 || month > 12) {\n    return null;\n  }\n\n  return { year, month };\n}\n\nfunction settlementExpenses(rows: ExpenseRow[]): SettlementExpense[] {\n  return rows.map((expense) => ({\n    id: expense.id,\n    amount: expense.amount,\n    paidByMemberId: expense.paid_by_member_id,\n    scope: expense.scope,\n    shares: expense.splits.map((split) => ({\n      memberId: split.member_id,\n      rate: Number(split.rate),\n    })),\n  }));\n}\n\nexport async function POST(request: Request) {\n  try {\n    const period = parsePeriod((await request.json()) as CreateSettlementRequest);\n    if (!period) {\n      return NextResponse.json({ error: "invalid_period" }, { status: 400 });\n    }\n\n    const pool = db();\n    const client = await pool.connect();\n\n    try {\n      await client.query("begin");\n\n      const household = (await client.query<HouseholdRow>(householdQuery)).rows[0];\n      if (!household) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "household_not_found" }, { status: 404 });\n      }\n\n      const [memberResult, expenseResult] = await Promise.all([\n        client.query<MemberRow>(membersQuery, [household.id]),\n        client.query<ExpenseRow>(expensesQuery, [household.id, period.year, period.month]),\n      ]);\n      const summary = calculateSettlement(\n        memberResult.rows.map((member) => ({ id: member.id, name: member.display_name })),\n        settlementExpenses(expenseResult.rows),\n      );\n\n      if (summary.reviewCount > 0) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "review_expenses_remaining", reviewCount: summary.reviewCount }, { status: 409 });\n      }\n\n      const settlementPeriod = (await client.query<{ id: string }>(upsertPeriodQuery, [household.id, period.year, period.month])).rows[0];\n      await client.query(deleteSettlementsQuery, [settlementPeriod.id]);\n\n      for (const transfer of summary.transfers) {\n        await client.query(insertSettlementQuery, [settlementPeriod.id, transfer.fromMemberId, transfer.toMemberId, transfer.amount]);\n      }\n\n      await client.query("commit");\n      return NextResponse.json({ period: { ...period, status: "settled" }, summary }, { status: 201 });\n    } catch (error) {\n      await client.query("rollback");\n      throw error;\n    } finally {\n      client.release();\n    }\n  } catch (error) {\n    console.error("Failed to settle period", error);\n    return NextResponse.json({ error: "settlement_create_failed" }, { status: 500 });\n  }\n}\n''')
PY
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/components/kakeibo-dashboard.tsx')
text = file.read_text()

def replace(old, new):
    global text
    if old not in text:
        raise SystemExit(f'missing expected text: {old[:120]!r}')
    text = text.replace(old, new, 1)

replace('''import { Camera, Check, Home, List, Plus, ReceiptText, Send, Settings, Wallet } from "lucide-react";\n''', '''import { Camera, Check, ChevronLeft, ChevronRight, Home, List, Plus, ReceiptText, Send, Settings, Wallet } from "lucide-react";\n''')
replace('''type DashboardResponse = {\n  expenses: Expense[];\n};\n''', '''type SettlementPeriodStatus = "open" | "reviewing" | "settled";\n\ntype Period = {\n  year: number;\n  month: number;\n};\n\ntype DashboardResponse = {\n  expenses: Expense[];\n  period: Period & { status: SettlementPeriodStatus };\n};\n''')
replace('''function splitLabelFor(expense: Expense) {\n''', '''function currentPeriod(): Period {\n  const now = new Date();\n  return { year: now.getFullYear(), month: now.getMonth() + 1 };\n}\n\nfunction movePeriod(period: Period, offset: number): Period {\n  const date = new Date(period.year, period.month - 1 + offset, 1);\n  return { year: date.getFullYear(), month: date.getMonth() + 1 };\n}\n\nfunction periodLabel(period: Period) {\n  return `${period.year}年${period.month}月`;\n}\n\nfunction splitLabelFor(expense: Expense) {\n''')
replace('''  const [expenses, setExpenses] = useState<Expense[]>(initialExpenses);\n  const [saveError, setSaveError] = useState<string | null>(null);\n''', '''  const [expenses, setExpenses] = useState<Expense[]>(initialExpenses);\n  const [selectedPeriod, setSelectedPeriod] = useState<Period>(currentPeriod);\n  const [periodStatus, setPeriodStatus] = useState<SettlementPeriodStatus>("open");\n  const [saveError, setSaveError] = useState<string | null>(null);\n  const [isSettling, setIsSettling] = useState(false);\n''')
replace('''        const response = await fetch("/api/dashboard");\n        if (!response.ok) return;\n\n        const data = (await response.json()) as DashboardResponse;\n        if (active && data.expenses.length > 0) {\n          setExpenses(data.expenses);\n        }\n''', '''        const response = await fetch(`/api/dashboard?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);\n        if (!response.ok) return;\n\n        const data = (await response.json()) as DashboardResponse;\n        if (active) {\n          setExpenses(data.expenses);\n          setPeriodStatus(data.period.status);\n        }\n''')
replace('''  }, []);\n\n  const stats = summaryFor(expenses);\n''', '''  }, [selectedPeriod]);\n\n  const stats = summaryFor(expenses);\n  const nowPeriod = currentPeriod();\n  const isCurrentPeriod = selectedPeriod.year === nowPeriod.year && selectedPeriod.month === nowPeriod.month;\n''')
replace('''  const addExpense = async (title: string) => {\n    const draft = parseExpenseInput(title, selectedPayer);\n''', '''  const addExpense = async (title: string) => {\n    if (!isCurrentPeriod) {\n      setSaveError("過去または未来の月を表示中です。支出追加は今月へ戻ってから行ってください。");\n      return;\n    }\n\n    const draft = parseExpenseInput(title, selectedPayer);\n''')
replace('''  const expenseCard = (expense: Expense, options?: { showSplitPicker?: boolean }) => (\n''', '''  const settlePeriod = async () => {\n    setSaveError(null);\n    setIsSettling(true);\n\n    try {\n      const response = await fetch("/api/settlements", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify(selectedPeriod),\n      });\n\n      if (!response.ok) throw new Error("settlement_create_failed");\n      setPeriodStatus("settled");\n    } catch {\n      setSaveError("精算を確定できませんでした。要確認の支出と通信状態を確認してください。");\n    } finally {\n      setIsSettling(false);\n    }\n  };\n\n  const expenseCard = (expense: Expense, options?: { showSplitPicker?: boolean }) => (\n''')
replace('''        <button className="action-button" type="button" disabled={stats.reviewCount > 0}>\n          精算済みにする\n        </button>\n''', '''        <button className="action-button" type="button" disabled={stats.reviewCount > 0 || periodStatus === "settled" || isSettling} onClick={() => void settlePeriod()}>\n          {periodStatus === "settled" ? "精算済み" : isSettling ? "確定中..." : "精算済みにする"}\n        </button>\n''')
replace('''              <div className="eyebrow">\n                <Wallet size={14} />\n                2026年5月\n              </div>\n''', '''              <div className="period-switcher">\n                <button className="period-switcher__button" type="button" aria-label="前月を見る" onClick={() => setSelectedPeriod((period) => movePeriod(period, -1))}>\n                  <ChevronLeft size={16} />\n                </button>\n                <div className="eyebrow">\n                  <Wallet size={14} />\n                  {periodLabel(selectedPeriod)}\n                </div>\n                <button className="period-switcher__button" type="button" aria-label="翌月を見る" onClick={() => setSelectedPeriod((period) => movePeriod(period, 1))}>\n                  <ChevronRight size={16} />\n                </button>\n              </div>\n''')
replace('''            value={composerValue}\n            onChange={(event) => setComposerValue(event.target.value)}\n''', '''            value={composerValue}\n            disabled={!isCurrentPeriod}\n            onChange={(event) => setComposerValue(event.target.value)}\n''')
replace('''          <button className="composer__submit" type="submit" aria-label="記録する">\n''', '''          <button className="composer__submit" type="submit" aria-label="記録する" disabled={!isCurrentPeriod}>\n''')
replace('''        <div className="composer__quick">\n''', '''        <div className="composer__quick" aria-disabled={!isCurrentPeriod}>\n''')
replace('''            <button key={item} className="chip" type="button" onClick={() => setComposerValue(item)}>\n''', '''            <button key={item} className="chip" type="button" disabled={!isCurrentPeriod} onClick={() => setComposerValue(item)}>\n''')
replace('''              type="button"\n              onClick={() => setSelectedPayer(member.id)}\n''', '''              type="button"\n              disabled={!isCurrentPeriod}\n              onClick={() => setSelectedPayer(member.id)}\n''')
file.write_text(text)

css = Path('/home/haya-ryoo/apps/kakeibo/src/app/globals.css')
text = css.read_text()
old = '''.eyebrow {\n  display: inline-flex;\n'''
new = '''.period-switcher {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n}\n\n.period-switcher__button {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 34px;\n  height: 34px;\n  border: 1px solid var(--border);\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.74);\n  color: var(--muted);\n}\n\n.eyebrow {\n  display: inline-flex;\n'''
if old not in text:
    raise SystemExit('missing css eyebrow')
text = text.replace(old, new, 1)
old = '''.composer__submit {\n  display: inline-flex;\n'''
new = '''.composer__submit {\n  display: inline-flex;\n'''
# append disabled rules after submit block marker before quick
marker = '''.composer__quick {\n'''
insert = '''.composer__submit:disabled,\n.composer__input:disabled,\n.composer__quick[aria-disabled="true"] {\n  opacity: 0.55;\n}\n\n'''
if marker not in text:
    raise SystemExit('missing composer quick css')
text = text.replace(marker, insert + marker, 1)
css.write_text(text)
PY
sed -n '1,260p' src/app/api/settlements/route.ts && printf '\n-- ui state --\n' && sed -n '1,250p' src/components/kakeibo-dashboard.tsx && printf '\n-- ui lower --\n' && sed -n '390,590p' src/components/kakeibo-dashboard.tsx
git diff --check && git status --short
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/app/api/settlements/route.ts')
text = file.read_text()
old = '''    shares: expense.splits.map((split) => ({\n      memberId: split.member_id,\n      rate: Number(split.rate),\n    })),\n'''
new = '''    shares: expense.splits.length > 0\n      ? expense.splits.map((split) => ({\n          memberId: split.member_id,\n          rate: Number(split.rate),\n        }))\n      : undefined,\n'''
if old not in text:
    raise SystemExit('expected settlement shares block not found')
file.write_text(text.replace(old, new, 1))
PY
mkdir -p tests/integration
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/tests/integration/api.integration.test.ts')
file.write_text('''import assert from "node:assert/strict";\nimport { after, before, describe, it } from "node:test";\n\nimport { Pool } from "pg";\n\nconst databaseUrl = process.env.DATABASE_URL_TEST;\nconst describeWithDatabase = databaseUrl ? describe : describe.skip;\n\ndescribeWithDatabase("database-backed API routes", () => {\n  const pool = new Pool({ connectionString: databaseUrl });\n  const testExpenseId = "77777777-7777-4777-8777-777777777771";\n  const testPeriod = { year: 2098, month: 12 };\n\n  before(async () => {\n    process.env.DATABASE_URL = databaseUrl;\n    const { rows } = await pool.query<{ household_id: string; paid_by_member_id: string; payment_source_id: string; category_id: string }>(`\n      select\n        e.household_id::text,\n        e.paid_by_member_id::text,\n        e.payment_source_id::text,\n        e.category_id::text\n      from expenses e\n      where e.paid_by_member_id is not null\n      order by e.created_at asc\n      limit 1\n    `);\n    const source = rows[0];\n    assert.ok(source);\n\n    await pool.query(`\n      insert into expenses (id, household_id, paid_by_member_id, payment_source_id, category_id, amount, expense_date, scope, memo)\n      values ($1, $2, $3, $4, $5, 10000, date '2098-12-10', 'review', '統合テスト支出')\n      on conflict (id) do update\n      set scope = 'review', amount = 10000, expense_date = date '2098-12-10', memo = '統合テスト支出'\n    `, [testExpenseId, source.household_id, source.paid_by_member_id, source.payment_source_id, source.category_id]);\n    await pool.query("delete from expense_splits where expense_id = $1", [testExpenseId]);\n    await pool.query("delete from settlement_periods where household_id = $1 and year = $2 and month = $3", [source.household_id, testPeriod.year, testPeriod.month]);\n  });\n\n  after(async () => {\n    await pool.query("delete from expenses where id = $1", [testExpenseId]);\n    await pool.query("delete from settlement_periods where year = $1 and month = $2", [testPeriod.year, testPeriod.month]);\n    await pool.end();\n    const { db } = await import("../../src/lib/db.ts");\n    await db().end();\n    globalThis.kakeiboPgPool = undefined;\n  });\n\n  it("filters dashboard expenses by month and validates period input", async () => {\n    const { GET } = await import("../../src/app/api/dashboard/route.ts");\n    const response = await GET(new Request(`http://localhost/api/dashboard?year=${testPeriod.year}&month=${testPeriod.month}`));\n    assert.equal(response.status, 200);\n    const body = await response.json();\n    assert.deepEqual(body.expenses.map((expense: { id: string }) => expense.id), [testExpenseId]);\n\n    const invalidResponse = await GET(new Request("http://localhost/api/dashboard?year=2098&month=13"));\n    assert.equal(invalidResponse.status, 400);\n  });\n\n  it("persists expense scope and split preset updates", async () => {\n    const { PATCH } = await import("../../src/app/api/expenses/[id]/route.ts");\n    const response = await PATCH(\n      new Request(`http://localhost/api/expenses/${testExpenseId}`, {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ scope: "shared", splitPreset: "a70" }),\n      }),\n      { params: Promise.resolve({ id: testExpenseId }) },\n    );\n    assert.equal(response.status, 200);\n\n    const { rows } = await pool.query<{ rate: string }>("select rate::text from expense_splits where expense_id = $1 order by rate desc", [testExpenseId]);\n    assert.deepEqual(rows.map((row) => row.rate), ["0.7000", "0.3000"]);\n  });\n\n  it("rejects settlement while review expenses remain, then stores the calculated transfer", async () => {\n    const { PATCH } = await import("../../src/app/api/expenses/[id]/route.ts");\n    const { POST } = await import("../../src/app/api/settlements/route.ts");\n\n    await PATCH(\n      new Request(`http://localhost/api/expenses/${testExpenseId}`, {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ scope: "review" }),\n      }),\n      { params: Promise.resolve({ id: testExpenseId }) },\n    );\n    const blocked = await POST(new Request("http://localhost/api/settlements", {\n      method: "POST",\n      headers: { "Content-Type": "application/json" },\n      body: JSON.stringify(testPeriod),\n    }));\n    assert.equal(blocked.status, 409);\n\n    await PATCH(\n      new Request(`http://localhost/api/expenses/${testExpenseId}`, {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ scope: "shared", splitPreset: "equal" }),\n      }),\n      { params: Promise.resolve({ id: testExpenseId }) },\n    );\n    const settled = await POST(new Request("http://localhost/api/settlements", {\n      method: "POST",\n      headers: { "Content-Type": "application/json" },\n      body: JSON.stringify(testPeriod),\n    }));\n    assert.equal(settled.status, 201);\n\n    const { rows } = await pool.query<{ status: string; amount: number }>(`\n      select sp.status, s.amount\n      from settlement_periods sp\n      join settlements s on s.period_id = sp.id\n      where sp.year = $1 and sp.month = $2\n    `, [testPeriod.year, testPeriod.month]);\n    assert.deepEqual(rows, [{ status: "settled", amount: 5000 }]);\n  });\n});\n''')
PY
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/.github/workflows/ci.yml')
text = file.read_text()
old = '''      - name: Test\n        run: npm test\n'''
new = '''      - name: Test\n        env:\n          DATABASE_URL_TEST: postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test\n        run: npm test\n'''
if old not in text:
    raise SystemExit('expected CI test step not found')
file.write_text(text.replace(old, new, 1))
PY
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')
replacements = {
  'src/app/api/dashboard/route.ts': [
    ('from "next/server"', 'from "next/server.js"'),
    ('from "../../../lib/db"', 'from "../../../lib/db.ts"'),
    ('from "../../../features/dashboard/dashboard.mapper"', 'from "../../../features/dashboard/dashboard.mapper.ts"'),
  ],
  'src/app/api/expenses/route.ts': [
    ('from "next/server"', 'from "next/server.js"'),
  ],
  'src/app/api/expenses/[id]/route.ts': [
    ('from "next/server"', 'from "next/server.js"'),
    ('from "../../../../lib/db"', 'from "../../../../lib/db.ts"'),
    ('from "../../../../lib/expense-utils"', 'from "../../../../lib/expense-utils.ts"'),
  ],
  'src/app/api/settlements/route.ts': [
    ('from "next/server"', 'from "next/server.js"'),
    ('from "../../../features/settlement/settlement.calculator"', 'from "../../../features/settlement/settlement.calculator.ts"'),
    ('from "../../../features/settlement/settlement.types"', 'from "../../../features/settlement/settlement.types.ts"'),
    ('from "../../../lib/db"', 'from "../../../lib/db.ts"'),
  ],
}
for path, edits in replacements.items():
  file = root / path
  text = file.read_text()
  for old, new in edits:
    if old not in text:
      raise SystemExit(f'missing {old!r} in {path}')
    text = text.replace(old, new, 1)
  file.write_text(text)
PY
git diff --check && printf '\n-- integration cleanup --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select count(*) from expenses where id = '77777777-7777-4777-8777-777777777771'; select count(*) from settlement_periods where year = 2098 and month = 12;" && printf '\n-- status --\n' && git status --short
git status --short --branch && printf '\n-- diff stat --\n' && git diff --stat && printf '\n-- untracked --\n' && git ls-files --others --exclude-standard
git diff -- .gitignore .github/workflows/ci.yml && printf '\n-- tracked changes --\n' && git diff --name-only
git log --oneline -8
git add .github/workflows/ci.yml src/app/api/dashboard/route.ts src/app/api/expenses/route.ts 'src/app/api/expenses/[id]/route.ts' src/app/api/settlements/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx src/features/dashboard/dashboard.mapper.ts src/lib/expense-utils.ts tests/expenses/dashboard.mapper.test.ts tests/expenses/expense-utils.test.ts tests/integration/api.integration.test.ts && git diff --cached --stat && printf '\n-- staged files --\n' && git diff --cached --name-status && printf '\n-- unstaged --\n' && git status --short
git diff --cached --check && git commit -m "月次精算フローをDB保存に接続する" && printf '\n-- latest commit --\n' && git log -1 --oneline --stat && printf '\n-- remaining status --\n' && git status --short --branch
cat package.json && printf '\n-- lock playwright --\n' && grep -n 'playwright\|@playwright/test' package-lock.json | sed -n '1,100p' && printf '\n-- ci --\n' && sed -n '1,180p' .github/workflows/ci.yml && printf '\n-- status --\n' && git status --short --branch
npm ls @playwright/test playwright --depth=0 || true
command -v chromium || true
command -v chromium-browser || true
command -v google-chrome || true
command -v google-chrome-stable || true
sed -n '1,160p' postgres/migrations/002_prepare_local_environments.sh && printf '\n-- seed relevant --\n' && sed -n '80,250p' postgres/migrations/003_seed_local_data.sql && printf '\n-- component controls --\n' && grep -n "精算済み\|要確認\|前月を見る\|翌月を見る\|記録する\|支出を入力" src/components/kakeibo-dashboard.tsx
npm ls @playwright/test playwright --depth=0
npm install --save-dev @playwright/test
mkdir -p tests/e2e
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')

package = root / 'package.json'
text = package.read_text()
old = '''    "start": "next start",\n    "test": "node --test --experimental-strip-types tests/**/*.test.ts"\n'''
new = '''    "start": "next start",\n    "test": "node --test --experimental-strip-types tests/**/*.test.ts",\n    "test:e2e": "playwright test"\n'''
if old not in text:
    raise SystemExit('expected package scripts not found')
package.write_text(text.replace(old, new, 1))

(root / 'playwright.config.ts').write_text('''import { defineConfig, devices } from "@playwright/test";\n\nconst testDatabaseUrl = process.env.DATABASE_URL_TEST\n  ?? "postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test";\n\nexport default defineConfig({\n  testDir: "./tests/e2e",\n  fullyParallel: false,\n  workers: 1,\n  retries: process.env.CI ? 2 : 0,\n  reporter: process.env.CI ? "github" : "list",\n  use: {\n    baseURL: "http://127.0.0.1:3022",\n    trace: "on-first-retry",\n  },\n  webServer: {\n    command: "npm run dev -- --hostname 127.0.0.1 --port 3022",\n    env: {\n      DATABASE_URL: testDatabaseUrl,\n    },\n    url: "http://127.0.0.1:3022",\n    reuseExistingServer: !process.env.CI,\n    timeout: 120_000,\n  },\n  projects: [\n    {\n      name: "chromium",\n      use: { ...devices["Desktop Chrome"] },\n    },\n  ],\n});\n''')

(root / 'tests/e2e/kakeibo.spec.ts').write_text('''import { execFileSync } from "node:child_process";\n\nimport { expect, test } from "@playwright/test";\n\nfunction resetTestDatabase() {\n  execFileSync("docker", [\n    "compose",\n    "exec",\n    "-T",\n    "db",\n    "/docker-entrypoint-initdb.d/002_prepare_local_environments.sh",\n  ], { stdio: "ignore" });\n}\n\ntest.beforeEach(() => {\n  resetTestDatabase();\n});\n\ntest.afterAll(() => {\n  resetTestDatabase();\n});\n\ntest("月を切り替えると過去月では支出追加を無効化する", async ({ page }) => {\n  await page.goto("/");\n\n  await expect(page.getByText("2026年5月")).toBeVisible();\n  await expect(page.getByText("駅前イタリアン")).toBeVisible();\n\n  await page.getByRole("button", { name: "前月を見る" }).click();\n\n  await expect(page.getByText("2026年4月")).toBeVisible();\n  await expect(page.getByLabel("支出を入力")).toBeDisabled();\n  await expect(page.getByRole("button", { name: "記録する" })).toBeDisabled();\n\n  await page.getByRole("button", { name: "翌月を見る" }).click();\n  await expect(page.getByText("2026年5月")).toBeVisible();\n  await expect(page.getByLabel("支出を入力")).toBeEnabled();\n});\n\ntest("要確認を共通費へ整理して月次精算を確定する", async ({ page }) => {\n  await page.goto("/");\n\n  const reviewCard = page.locator(".review-card").filter({ hasText: "駅前イタリアン" });\n  await expect(reviewCard).toBeVisible();\n  await reviewCard.getByRole("button", { name: "共通費", exact: true }).click();\n  await expect(reviewCard).toHaveCount(0);\n\n  await page.getByRole("button", { name: "精算", exact: true }).click();\n  const settleButton = page.getByRole("button", { name: "精算済みにする" });\n  await expect(settleButton).toBeEnabled();\n  await settleButton.click();\n\n  await expect(page.getByRole("button", { name: "精算済み", exact: true })).toBeDisabled();\n});\n''')

workflow = root / '.github/workflows/ci.yml'
text = workflow.read_text()
old = '''      - name: Build\n        run: npm run build\n\n      - name: Stop PostgreSQL\n'''
new = '''      - name: Build\n        run: npm run build\n\n      - name: Install Playwright browser\n        run: npx playwright install --with-deps chromium\n\n      - name: Smoke test\n        env:\n          DATABASE_URL_TEST: postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test\n        run: npm run test:e2e\n\n      - name: Stop PostgreSQL\n'''
if old not in text:
    raise SystemExit('expected CI build step not found')
workflow.write_text(text.replace(old, new, 1))
PY

git diff --stat
git status --short
npx playwright install chromium
git diff --check && sed -n '1,180p' playwright.config.ts && printf '\n-- e2e --\n' && sed -n '1,240p' tests/e2e/kakeibo.spec.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run test:e2e
ps -ef | grep -E 'playwright|next dev|next-server' | grep -v grep
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')

file = root / 'src/components/kakeibo-dashboard.tsx'
text = file.read_text()
text = text.replace('''  initialExpenses,\n''', '')
old = '''  const [expenses, setExpenses] = useState<Expense[]>(initialExpenses);\n  const [selectedPeriod, setSelectedPeriod] = useState<Period>(currentPeriod);\n'''
new = '''  const [expenses, setExpenses] = useState<Expense[]>([]);\n  const [selectedPeriod, setSelectedPeriod] = useState<Period>(currentPeriod);\n'''
if old not in text:
    raise SystemExit('expenses state not found')
text = text.replace(old, new, 1)
old = '''  const [saveError, setSaveError] = useState<string | null>(null);\n  const [isSettling, setIsSettling] = useState(false);\n'''
new = '''  const [saveError, setSaveError] = useState<string | null>(null);\n  const [isDashboardLoading, setIsDashboardLoading] = useState(true);\n  const [isSettling, setIsSettling] = useState(false);\n'''
if old not in text:
    raise SystemExit('state block not found')
text = text.replace(old, new, 1)
old = '''    async function loadDashboard() {\n      try {\n        const response = await fetch(`/api/dashboard?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);\n        if (!response.ok) return;\n\n        const data = (await response.json()) as DashboardResponse;\n        if (active) {\n          setExpenses(data.expenses);\n          setPeriodStatus(data.period.status);\n        }\n      } catch {\n        // Keep the seed-aligned local fallback when the API is unavailable.\n      }\n    }\n'''
new = '''    async function loadDashboard() {\n      setIsDashboardLoading(true);\n      setSaveError(null);\n\n      try {\n        const response = await fetch(`/api/dashboard?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);\n        if (!response.ok) throw new Error("dashboard_load_failed");\n\n        const data = (await response.json()) as DashboardResponse;\n        if (active) {\n          setExpenses(data.expenses);\n          setPeriodStatus(data.period.status);\n        }\n      } catch {\n        if (active) {\n          setExpenses([]);\n          setSaveError("支出を読み込めませんでした。通信状態を確認してください。");\n        }\n      } finally {\n        if (active) setIsDashboardLoading(false);\n      }\n    }\n'''
if old not in text:
    raise SystemExit('load dashboard block not found')
text = text.replace(old, new, 1)
old = '''          <div className="timeline">\n            {reviewExpenses.map((expense) => (\n'''
new = '''          <div className="timeline">\n            {isDashboardLoading ? <p className="section__meta">読み込み中...</p> : null}\n            {reviewExpenses.map((expense) => (\n'''
if old not in text:
    raise SystemExit('review timeline not found')
text = text.replace(old, new, 1)
# Disable composer while dashboard is loading as well as non-current periods.
text = text.replace('''disabled={!isCurrentPeriod}''', '''disabled={!isCurrentPeriod || isDashboardLoading}''')
text = text.replace('''aria-disabled={!isCurrentPeriod}''', '''aria-disabled={!isCurrentPeriod || isDashboardLoading}''')
file.write_text(text)

file = root / 'src/app/api/expenses/[id]/route.ts'
text = file.read_text()
old = '''function parseScope(value: unknown): ExpenseScope | null {\n'''
new = '''function isUuid(value: string) {\n  return /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value);\n}\n\nfunction parseScope(value: unknown): ExpenseScope | null {\n'''
if old not in text:
    raise SystemExit('parseScope marker not found')
text = text.replace(old, new, 1)
old = '''    const body = (await request.json()) as UpdateExpenseRequest;\n    const scope = parseScope(body.scope);\n'''
new = '''    const body = (await request.json()) as UpdateExpenseRequest;\n    const scope = parseScope(body.scope);\n\n    if (!isUuid(id)) {\n      return NextResponse.json({ error: "invalid_expense_id" }, { status: 400 });\n    }\n'''
if old not in text:
    raise SystemExit('id parse block not found')
text = text.replace(old, new, 1)
file.write_text(text)

file = root / 'tests/e2e/kakeibo.spec.ts'
text = file.read_text()
text = text.replace('''  await expect(page.getByText("駅前イタリアン")).toBeVisible();\n''', '''  await expect(page.getByText("駅前イタリアン", { exact: true })).toBeVisible();\n''')
file.write_text(text)

file = root / '.gitignore'
text = file.read_text()
append = '''\n# Playwright\n/test-results/\n/playwright-report/\n/blob-report/\n'''
if '/test-results/' not in text:
    file.write_text(text.rstrip() + '\n' + append)
PY

git diff --check
git status --short
sed -n '1,240p' test-results/kakeibo-要確認を共通費へ整理して月次精算を確定する-chromium/error-context.md
docker compose exec -T db psql -U kakeibo -d kakeibo_test -x -c "select id, scope from expenses order by expense_date; select year, month, status, settled_at from settlement_periods order by year, month; select period_id, from_member_id, to_member_id, amount, status from settlements order by created_at;"
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/components/kakeibo-dashboard.tsx')
text = file.read_text()
old = '''  const [isDashboardLoading, setIsDashboardLoading] = useState(true);\n  const [isSettling, setIsSettling] = useState(false);\n'''
new = '''  const [isDashboardLoading, setIsDashboardLoading] = useState(true);\n  const [pendingExpenseUpdates, setPendingExpenseUpdates] = useState(0);\n  const [isSettling, setIsSettling] = useState(false);\n'''
if old not in text:
    raise SystemExit('state marker not found')
text = text.replace(old, new, 1)
old = '''  const saveExpenseUpdate = async (previousExpense: Expense, nextExpense: Expense) => {\n    setSaveError(null);\n    setExpenses((current) => current.map((expense) => expense.id === nextExpense.id ? nextExpense : expense));\n\n    try {\n'''
new = '''  const saveExpenseUpdate = async (previousExpense: Expense, nextExpense: Expense) => {\n    setSaveError(null);\n    setPendingExpenseUpdates((current) => current + 1);\n    setExpenses((current) => current.map((expense) => expense.id === nextExpense.id ? nextExpense : expense));\n\n    try {\n'''
if old not in text:
    raise SystemExit('save update start not found')
text = text.replace(old, new, 1)
old = '''    } catch {\n      setExpenses((current) => current.map((expense) => expense.id === previousExpense.id ? previousExpense : expense));\n      setSaveError("変更を保存できませんでした。表示を元に戻しました。");\n    }\n  };\n'''
new = '''    } catch {\n      setExpenses((current) => current.map((expense) => expense.id === previousExpense.id ? previousExpense : expense));\n      setSaveError("変更を保存できませんでした。表示を元に戻しました。");\n    } finally {\n      setPendingExpenseUpdates((current) => Math.max(0, current - 1));\n    }\n  };\n'''
if old not in text:
    raise SystemExit('save update catch not found')
text = text.replace(old, new, 1)
old = '''        <button className="action-button" type="button" disabled={stats.reviewCount > 0 || periodStatus === "settled" || isSettling} onClick={() => void settlePeriod()}>\n          {periodStatus === "settled" ? "精算済み" : isSettling ? "確定中..." : "精算済みにする"}\n'''
new = '''        <button className="action-button" type="button" disabled={stats.reviewCount > 0 || pendingExpenseUpdates > 0 || periodStatus === "settled" || isSettling} onClick={() => void settlePeriod()}>\n          {periodStatus === "settled" ? "精算済み" : isSettling ? "確定中..." : pendingExpenseUpdates > 0 ? "保存中..." : "精算済みにする"}\n'''
if old not in text:
    raise SystemExit('settle button not found')
file.write_text(text.replace(old, new, 1))
PY

git diff --check
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/tests/integration/api.integration.test.ts')
text = file.read_text()
marker = '''  it("persists expense scope and split preset updates", async () => {\n'''
insert = '''  it("rejects malformed expense ids before querying PostgreSQL", async () => {\n    const { PATCH } = await import("../../src/app/api/expenses/[id]/route.ts");\n    const response = await PATCH(\n      new Request("http://localhost/api/expenses/not-a-uuid", {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ scope: "shared", splitPreset: "equal" }),\n      }),\n      { params: Promise.resolve({ id: "not-a-uuid" }) },\n    );\n\n    assert.equal(response.status, 400);\n  });\n\n'''
if marker not in text:
    raise SystemExit('integration test marker not found')
file.write_text(text.replace(marker, insert + marker, 1))
PY

git diff --check
git diff --check && printf '\n-- cleanup --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select count(*) from expenses where id = '77777777-7777-4777-8777-777777777771'; select count(*) from settlement_periods where year = 2098 and month = 12;" && printf '\n-- status --\n' && git status --short --branch && printf '\n-- diff stat --\n' && git diff --stat
git diff --check && printf '\n-- ci diff --\n' && git diff -- .github/workflows/ci.yml && printf '\n-- gitignore diff --\n' && git diff -- .gitignore && printf '\n-- status --\n' && git status --short --branch && printf '\n-- untracked --\n' && git ls-files --others --exclude-standard
sed -n '1,150p' src/app/globals.css && printf '\n-- composer css --\n' && sed -n '280,480p' src/app/globals.css && printf '\n-- composer jsx --\n' && sed -n '540,625p' src/components/kakeibo-dashboard.tsx && printf '\n-- state --\n' && sed -n '70,125p' src/components/kakeibo-dashboard.tsx
sed -n '1,150p' tests/e2e/kakeibo.spec.ts && printf '\n-- status --\n' && git status --short --branch
git status --short --branch && printf '\n-- composer state --\n' && sed -n '82,130p' src/components/kakeibo-dashboard.tsx && printf '\n-- composer jsx --\n' && sed -n '552,615p' src/components/kakeibo-dashboard.tsx
sed -n '310,410p' src/app/globals.css && printf '\n-- desktop composer --\n' && sed -n '440,490p' src/app/globals.css && printf '\n-- e2e --\n' && sed -n '1,140p' tests/e2e/kakeibo.spec.ts
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')

file = root / 'src/components/kakeibo-dashboard.tsx'
text = file.read_text()
old = '''  const [selectedPayer, setSelectedPayer] = useState<MemberId>("a");\n  const [composerValue, setComposerValue] = useState("スーパーライフ 4280");\n'''
new = '''  const [selectedPayer, setSelectedPayer] = useState<MemberId>("a");\n  const [composerValue, setComposerValue] = useState("");\n  const [isComposerExpanded, setIsComposerExpanded] = useState(false);\n'''
if old not in text:
    raise SystemExit('composer state marker not found')
text = text.replace(old, new, 1)
old = '''    setComposerValue("");\n  };\n'''
new = '''    setComposerValue("");\n    setIsComposerExpanded(false);\n  };\n'''
if old not in text:
    raise SystemExit('composer reset marker not found')
text = text.replace(old, new, 1)
old = '''            <button className="eyebrow" type="button" onClick={() => setComposerValue("ファミリーマート 1420 要確認")}>\n'''
new = '''            <button className="eyebrow" type="button" onClick={() => {\n              setComposerValue("ファミリーマート 1420 要確認");\n              setIsComposerExpanded(true);\n            }}>\n'''
if old not in text:
    raise SystemExit('ocr button marker not found')
text = text.replace(old, new, 1)
old = '''      <form\n        className="composer"\n        onSubmit={(event) => {\n          event.preventDefault();\n          void addExpense(composerValue);\n        }}\n      >\n        <div className="composer__row">\n          <div className="eyebrow" style={{ marginRight: 2 }}>\n'''
new = '''      <form\n        className="composer"\n        data-expanded={isComposerExpanded}\n        onFocusCapture={() => setIsComposerExpanded(true)}\n        onBlurCapture={(event) => {\n          if (!event.currentTarget.contains(event.relatedTarget as Node | null)) {\n            setIsComposerExpanded(false);\n          }\n        }}\n        onSubmit={(event) => {\n          event.preventDefault();\n          void addExpense(composerValue);\n        }}\n      >\n        <div className="composer__row">\n          <div className="eyebrow composer__payer" style={{ marginRight: 2 }}>\n'''
if old not in text:
    raise SystemExit('form marker not found')
text = text.replace(old, new, 1)
text = text.replace('''            placeholder="スーパーライフ 4280"\n''', '''            placeholder="支出を追加..."\n''', 1)
file.write_text(text)

file = root / 'src/app/globals.css'
text = file.read_text()
text = text.replace('''  --composer-height: 112px;\n''', '''  --composer-height: 68px;\n''', 1)
old = '''.composer__quick {\n  display: flex;\n'''
new = '''.composer:not([data-expanded="true"]) .composer__payer,\n.composer:not([data-expanded="true"]) .composer__submit,\n.composer:not([data-expanded="true"]) .composer__quick,\n.composer:not([data-expanded="true"]) .composer__note {\n  display: none;\n}\n\n.composer__quick {\n  display: flex;\n'''
if old not in text:
    raise SystemExit('composer quick marker not found')
text = text.replace(old, new, 1)
# Keep desktop collapsed height compact too; expanded content temporarily overlays the page.
text = text.replace('''  :root {\n    --composer-height: 92px;\n  }\n\n''', '', 1)
file.write_text(text)

file = root / 'tests/e2e/kakeibo.spec.ts'
text = file.read_text()
marker = '''test("月を切り替えると過去月では支出追加を無効化する", async ({ page }) => {\n'''
insert = '''test("支出入力は通常時に畳まれ、フォーカスすると候補を展開する", async ({ page }) => {\n  await page.goto("/");\n\n  const input = page.getByLabel("支出を入力");\n  const quickInput = page.getByRole("button", { name: "スーパーライフ 4280", exact: true });\n  await expect(input).toHaveAttribute("placeholder", "支出を追加...");\n  await expect(quickInput).toBeHidden();\n\n  await input.focus();\n  await expect(quickInput).toBeVisible();\n\n  await page.getByRole("heading", { name: "今月の流れ" }).click();\n  await expect(quickInput).toBeHidden();\n});\n\n'''
if marker not in text:
    raise SystemExit('e2e marker not found')
file.write_text(text.replace(marker, insert + marker, 1))
PY

git diff --check
git status --short
ps -ef | grep -E 'playwright test|next dev --hostname 127.0.0.1 --port 3022|next-server' | grep -v grep
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3022 || true
ps -ef | grep -E '002_prepare|psql|workerProcessEntry|playwright test' | grep -v grep
for file in test-results/*/error-context.md; do printf '\n-- %s --\n' "$file"; sed -n '1,180p' "$file"; done
ps -ef | grep -E 'playwright|chrome|chromium|next dev --hostname 127.0.0.1 --port 3022|next-server' | grep -v grep
docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select id || ':' || scope from expenses order by expense_date; select year || '-' || month || ':' || status from settlement_periods order by year, month;"
python3 <<'PY'
from pathlib import Path
root = Path('/home/haya-ryoo/apps/kakeibo')
file = root / 'playwright.config.ts'
text = file.read_text()
old = '''  fullyParallel: false,\n  workers: 1,\n  retries: process.env.CI ? 2 : 0,\n'''
new = '''  fullyParallel: false,\n  workers: 1,\n  timeout: 120_000,\n  expect: {\n    timeout: 20_000,\n  },\n  retries: process.env.CI ? 2 : 0,\n'''
if old not in text:
    raise SystemExit('config marker not found')
file.write_text(text.replace(old, new, 1))

file = root / 'tests/e2e/kakeibo.spec.ts'
text = file.read_text()
old = '''  await expect(input).toHaveAttribute("placeholder", "支出を追加...");\n  await expect(quickInput).toBeHidden();\n\n  await input.focus();\n'''
new = '''  await expect(input).toHaveAttribute("placeholder", "支出を追加...");\n  await expect(input).toBeEnabled();\n  await expect(quickInput).toBeHidden();\n\n  await input.focus();\n'''
if old not in text:
    raise SystemExit('e2e input marker not found')
file.write_text(text.replace(old, new, 1))
PY

git diff --check
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/tests/e2e/kakeibo.spec.ts')
text = file.read_text()
old = '''  await expect(page.getByRole("button", { name: "記録する" })).toBeDisabled();\n'''
new = '''  await expect(page.locator('button[aria-label="記録する"]')).toBeDisabled();\n'''
if old not in text:
    raise SystemExit('submit assertion not found')
file.write_text(text.replace(old, new, 1))
PY

git diff --check
ss -ltnp | sed -n '1,240p'
ps -ef | grep -E '/home/haya-ryoo/apps/kakeibo|next dev --hostname 127.0.0.1 --port 3022|playwright test' | grep -v grep
git status --short --branch
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3021 && printf '\n-- kakeibo listeners --\n' && ss -ltnp | grep -E ':3021|:3022' || true
curl -fsS 'http://127.0.0.1:3021/api/dashboard?year=2026&month=5' | sed -n '1,4p'
ss -ltnp | grep -E ':3021|:3022' || true
ps -ef | grep -E '/home/haya-ryoo/apps/kakeibo|next dev --hostname 127.0.0.1 --port 3021' | grep -v grep || true
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run dev -- --hostname 0.0.0.0 --port 3021
curl -fsS -o /dev/null -w '127.0.0.1: %{http_code}\n' http://127.0.0.1:3021 && curl -fsS -o /dev/null -w 'localhost: %{http_code}\n' http://localhost:3021
hostname -I && printf '\n-- routes --\n' && ip -4 route && printf '\n-- listeners --\n' && ss -ltnp | grep ':3021' || true
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3021
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh >/dev/null && docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select id || ':' || scope from expenses order by expense_date"
curl -fsS -o /dev/null -w 'localhost: %{http_code}\n' http://127.0.0.1:3021 && curl -fsS -o /dev/null -w 'LAN: %{http_code}\n' http://192.168.0.13:3021
sed -n '210,310p' src/app/globals.css && printf '\n-- lower controls --\n' && sed -n '470,570p' src/app/globals.css
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/app/globals.css')
text = file.read_text()
marker = '''  .composer__note {\n    display: block;\n  }\n}\n'''
insert = '''  .composer__note {\n    display: block;\n  }\n\n  .timeline-item__actions,\n  .review-card__actions {\n    grid-template-columns: repeat(3, minmax(108px, 148px));\n    justify-content: start;\n  }\n\n  .timeline-item__actions .chip:last-child,\n  .review-card__actions .chip:last-child {\n    grid-column: auto;\n  }\n\n  .split-picker {\n    grid-template-columns: repeat(3, minmax(108px, 148px));\n    justify-content: start;\n  }\n}\n'''
if marker not in text:
    raise SystemExit('desktop media marker not found')
file.write_text(text.replace(marker, insert, 1))
PY

git diff --check
git diff -- src/app/globals.css | sed -n '1,220p'
git status --short --branch && printf '\n-- diff stat --\n' && git diff --stat && printf '\n-- expense update api --\n' && sed -n '1,260p' src/app/api/expenses/'[id]'/route.ts && printf '\n-- expense create api --\n' && sed -n '1,280p' src/app/api/expenses/route.ts
sed -n '1,260p' src/components/kakeibo-dashboard.tsx && printf '\n-- cards and views --\n' && sed -n '260,560p' src/components/kakeibo-dashboard.tsx && printf '\n-- composer tail --\n' && sed -n '560,660p' src/components/kakeibo-dashboard.tsx
sed -n '1,220p' tests/integration/api.integration.test.ts && printf '\n-- e2e --\n' && sed -n '1,180p' tests/e2e/kakeibo.spec.ts && printf '\n-- schema expenses --\n' && sed -n '100,175p' postgres/migrations/001_initial_schema.sql
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/app/api/expenses/[id]/route.ts')
text = file.read_text()

def replace(old, new):
    global text
    if old not in text:
        raise SystemExit(f'missing marker: {old[:100]!r}')
    text = text.replace(old, new, 1)

replace('''const updateExpenseBodySchema = z.object({\n  scope: z.enum(["shared", "personal", "review"]),\n  splitPreset: z.enum(["equal", "a70", "b70"]).optional().transform(val => val || "equal"),\n});\n''', '''const updateExpenseBodySchema = z.object({\n  scope: z.enum(["shared", "personal", "review"]),\n  splitPreset: z.enum(["equal", "a70", "b70"]).optional().transform(val => val || "equal"),\n  title: z.string().trim().min(1).max(100).optional(),\n  amount: z.number().int().positive().optional(),\n  paidBy: z.enum(["a", "b"]).optional(),\n  category: z.string().trim().min(1).max(50).optional(),\n  note: z.string().trim().max(200).optional(),\n  expenseDate: z.string().date().optional(),\n});\n''')
replace('''type HouseholdRow = { id: string };\ntype MemberRow = { id: string };\n''', '''type HouseholdRow = { id: string };\ntype MemberRow = { id: string };\ntype CategoryRow = { id: string };\ntype PaymentSourceRow = { id: string };\n''')
replace('''const expenseQuery = `\n  select id\n  from expenses\n  where id = $1 and household_id = $2\n`;\n\nconst updateExpenseQuery = `\n  update expenses\n  set scope = $2\n  where id = $1\n`;\n''', '''const expenseQuery = `\n  select id, memo\n  from expenses\n  where id = $1 and household_id = $2\n`;\n\nconst updateExpenseQuery = `\n  update expenses\n  set\n    paid_by_member_id = coalesce($2, paid_by_member_id),\n    payment_source_id = coalesce($3, payment_source_id),\n    category_id = coalesce($4, category_id),\n    amount = coalesce($5, amount),\n    expense_date = coalesce($6, expense_date),\n    scope = $7,\n    memo = coalesce($8, memo)\n  where id = $1\n`;\n\nconst deleteExpenseQuery = `\n  delete from expenses\n  where id = $1 and household_id = $2\n  returning id\n`;\n\nconst categoryQuery = `\n  select id\n  from categories\n  where household_id = $1 and name = $2\n  limit 1\n`;\n\nconst createCategoryQuery = `\n  insert into categories (household_id, name, kind, default_expense_scope, default_split_type)\n  values ($1, $2, 'other', $3, 'equal')\n  on conflict (household_id, name) do update\n  set name = excluded.name\n  returning id\n`;\n\nconst paymentSourceQuery = `\n  select id\n  from payment_sources\n  where household_id = $1 and owner_member_id = $2 and type = 'member_personal'\n  order by created_at asc\n  limit 1\n`;\n\nconst fallbackPaymentSourceQuery = `\n  select id\n  from payment_sources\n  where household_id = $1\n  order by type desc, created_at asc\n  limit 1\n`;\n''')
replace('''function splitRatesFor(preset: SplitPreset) {\n  if (preset === "a70") return [0.7, 0.3];\n  if (preset === "b70") return [0.3, 0.7];\n  return [0.5, 0.5];\n}\n''', '''function splitRatesFor(preset: SplitPreset) {\n  if (preset === "a70") return [0.7, 0.3];\n  if (preset === "b70") return [0.3, 0.7];\n  return [0.5, 0.5];\n}\n\nfunction memoFor(title: string, note: string) {\n  return note ? `${title} ${note}` : title;\n}\n''')
replace('''    const { scope, splitPreset } = result.data;\n''', '''    const { scope, splitPreset, title, amount, paidBy, category, note, expenseDate } = result.data;\n''')
replace('''      const expense = (await client.query(expenseQuery, [id, household.id])).rows[0];\n''', '''      const expense = (await client.query<{ id: string; memo: string | null }>(expenseQuery, [id, household.id])).rows[0];\n''')
replace('''      await client.query(updateExpenseQuery, [id, scope]);\n      await client.query(deleteSplitsQuery, [id]);\n\n      if (scope === "shared") {\n        const members = (await client.query<MemberRow>(membersQuery, [household.id])).rows;\n        const rates = splitRatesFor(splitPreset);\n''', '''      const members = (await client.query<MemberRow>(membersQuery, [household.id])).rows;\n      const payer = paidBy ? (paidBy === "a" ? members[0] : members[1]) : undefined;\n      if (paidBy && !payer) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "payer_not_found" }, { status: 400 });\n      }\n\n      const categoryId = category\n        ? (await client.query<CategoryRow>(categoryQuery, [household.id, category])).rows[0]?.id\n          ?? (await client.query<CategoryRow>(createCategoryQuery, [household.id, category, scope])).rows[0]?.id\n        : undefined;\n      if (category && !categoryId) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "category_not_found" }, { status: 400 });\n      }\n\n      const paymentSourceId = payer\n        ? (await client.query<PaymentSourceRow>(paymentSourceQuery, [household.id, payer.id])).rows[0]?.id\n          ?? (await client.query<PaymentSourceRow>(fallbackPaymentSourceQuery, [household.id])).rows[0]?.id\n        : undefined;\n      if (payer && !paymentSourceId) {\n        await client.query("rollback");\n        return NextResponse.json({ error: "payment_source_not_found" }, { status: 400 });\n      }\n\n      const memo = title !== undefined || note !== undefined\n        ? memoFor(title ?? expense.memo?.split(" ")[0] ?? "支出", note ?? "")\n        : undefined;\n\n      await client.query(updateExpenseQuery, [id, payer?.id, paymentSourceId, categoryId, amount, expenseDate, scope, memo]);\n      await client.query(deleteSplitsQuery, [id]);\n\n      if (scope === "shared") {\n        const rates = splitRatesFor(splitPreset);\n''')
text += '''\nexport async function DELETE(_request: Request, context: { params: Promise<{ id: string }> }) {\n  try {\n    const { id } = await context.params;\n    if (!uuidSchema.safeParse(id).success) {\n      return NextResponse.json({ error: "invalid_expense_id" }, { status: 400 });\n    }\n\n    const pool = db();\n    const household = (await pool.query<HouseholdRow>(householdQuery)).rows[0];\n    if (!household) {\n      return NextResponse.json({ error: "household_not_found" }, { status: 404 });\n    }\n\n    const deleted = (await pool.query(deleteExpenseQuery, [id, household.id])).rows[0];\n    if (!deleted) {\n      return NextResponse.json({ error: "expense_not_found" }, { status: 404 });\n    }\n\n    return NextResponse.json({ deletedExpenseId: id });\n  } catch (error) {\n    console.error("Failed to delete expense", error);\n    return NextResponse.json({ error: "expense_delete_failed" }, { status: 500 });\n  }\n}\n'''
file.write_text(text)
PY

git diff --check
git diff -- src/app/api/expenses/'[id]'/route.ts | sed -n '1,280p'
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/components/kakeibo-dashboard.tsx')
text = file.read_text()

def replace(old, new):
    global text
    if old not in text:
        raise SystemExit(f'missing marker: {old[:120]!r}')
    text = text.replace(old, new, 1)

replace('''import { Camera, Check, ChevronLeft, ChevronRight, Home, List, Plus, ReceiptText, Send, Settings, Wallet } from "lucide-react";\n''', '''import { Camera, Check, ChevronLeft, ChevronRight, Home, List, Pencil, Plus, ReceiptText, Save, Send, Settings, Trash2, Wallet, X } from "lucide-react";\n''')
replace('''type CreateExpenseResponse = {\n  expense: Expense;\n};\n''', '''type CreateExpenseResponse = {\n  expense: Expense;\n};\n\ntype ExpenseEditDraft = {\n  title: string;\n  amount: string;\n  paidBy: MemberId;\n  category: string;\n  note: string;\n  expenseDate: string;\n};\n''')
replace('''  const [pendingExpenseUpdates, setPendingExpenseUpdates] = useState(0);\n  const [isSettling, setIsSettling] = useState(false);\n''', '''  const [pendingExpenseUpdates, setPendingExpenseUpdates] = useState(0);\n  const [editingExpenseId, setEditingExpenseId] = useState<string | null>(null);\n  const [expenseEditDraft, setExpenseEditDraft] = useState<ExpenseEditDraft | null>(null);\n  const [isSettling, setIsSettling] = useState(false);\n''')
marker = '''  const settlePeriod = async () => {\n'''
insert = '''  const startExpenseEdit = (expense: Expense) => {\n    setEditingExpenseId(expense.id);\n    setExpenseEditDraft({\n      title: expense.title,\n      amount: String(expense.amount),\n      paidBy: expense.paidBy,\n      category: expense.category,\n      note: expense.note ?? "",\n      expenseDate: expense.createdAt.slice(0, 10),\n    });\n  };\n\n  const cancelExpenseEdit = () => {\n    setEditingExpenseId(null);\n    setExpenseEditDraft(null);\n  };\n\n  const saveExpenseEdit = async (expense: Expense) => {\n    if (!expenseEditDraft) return;\n\n    const amount = Number(expenseEditDraft.amount);\n    if (!Number.isInteger(amount) || amount <= 0 || !expenseEditDraft.title.trim() || !expenseEditDraft.category.trim()) {\n      setSaveError("金額、タイトル、カテゴリを確認してください。");\n      return;\n    }\n\n    const nextExpense: Expense = {\n      ...expense,\n      title: expenseEditDraft.title.trim(),\n      amount,\n      paidBy: expenseEditDraft.paidBy,\n      category: expenseEditDraft.category.trim(),\n      note: expenseEditDraft.note.trim() || undefined,\n      createdAt: `${expenseEditDraft.expenseDate}T00:00:00.000Z`,\n    };\n\n    setSaveError(null);\n    setPendingExpenseUpdates((current) => current + 1);\n\n    try {\n      const response = await fetch(`/api/expenses/${expense.id}`, {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({\n          scope: expense.scope,\n          splitPreset: expense.splitPreset,\n          title: nextExpense.title,\n          amount: nextExpense.amount,\n          paidBy: nextExpense.paidBy,\n          category: nextExpense.category,\n          note: nextExpense.note ?? "",\n          expenseDate: expenseEditDraft.expenseDate,\n        }),\n      });\n\n      if (!response.ok) throw new Error("expense_edit_failed");\n      setExpenses((current) => current.map((item) => item.id === expense.id ? nextExpense : item));\n      cancelExpenseEdit();\n    } catch {\n      setSaveError("支出を更新できませんでした。入力内容を確認してください。");\n    } finally {\n      setPendingExpenseUpdates((current) => Math.max(0, current - 1));\n    }\n  };\n\n  const deleteExpense = async (expense: Expense) => {\n    if (!window.confirm(`${expense.title}を削除しますか？`)) return;\n\n    setSaveError(null);\n    setPendingExpenseUpdates((current) => current + 1);\n\n    try {\n      const response = await fetch(`/api/expenses/${expense.id}`, { method: "DELETE" });\n      if (!response.ok) throw new Error("expense_delete_failed");\n      setExpenses((current) => current.filter((item) => item.id !== expense.id));\n      if (editingExpenseId === expense.id) cancelExpenseEdit();\n    } catch {\n      setSaveError("支出を削除できませんでした。通信状態を確認してください。");\n    } finally {\n      setPendingExpenseUpdates((current) => Math.max(0, current - 1));\n    }\n  };\n\n'''
if marker not in text:
    raise SystemExit('settle marker missing')
text = text.replace(marker, insert + marker, 1)
replace('''  const expenseCard = (expense: Expense, options?: { showSplitPicker?: boolean }) => (\n''', '''  const expenseCard = (expense: Expense, options?: { showSplitPicker?: boolean; showManagement?: boolean }) => (\n''')
replace('''      <div className="timeline-item__actions">\n''', '''      {options?.showManagement ? (\n        <div className="timeline-item__manage">\n          <button className="chip" type="button" onClick={() => startExpenseEdit(expense)}>\n            <Pencil size={14} />\n            編集\n          </button>\n          <button className="chip chip--danger" type="button" onClick={() => void deleteExpense(expense)}>\n            <Trash2 size={14} />\n            削除\n          </button>\n        </div>\n      ) : null}\n\n      {editingExpenseId === expense.id && expenseEditDraft ? (\n        <form className="expense-editor" onSubmit={(event) => {\n          event.preventDefault();\n          void saveExpenseEdit(expense);\n        }}>\n          <label>\n            <span>タイトル</span>\n            <input value={expenseEditDraft.title} onChange={(event) => setExpenseEditDraft({ ...expenseEditDraft, title: event.target.value })} />\n          </label>\n          <label>\n            <span>金額</span>\n            <input inputMode="numeric" value={expenseEditDraft.amount} onChange={(event) => setExpenseEditDraft({ ...expenseEditDraft, amount: event.target.value })} />\n          </label>\n          <label>\n            <span>日付</span>\n            <input type="date" value={expenseEditDraft.expenseDate} onChange={(event) => setExpenseEditDraft({ ...expenseEditDraft, expenseDate: event.target.value })} />\n          </label>\n          <label>\n            <span>支払者</span>\n            <select value={expenseEditDraft.paidBy} onChange={(event) => setExpenseEditDraft({ ...expenseEditDraft, paidBy: event.target.value as MemberId })}>\n              {members.map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}\n            </select>\n          </label>\n          <label>\n            <span>カテゴリ</span>\n            <input value={expenseEditDraft.category} onChange={(event) => setExpenseEditDraft({ ...expenseEditDraft, category: event.target.value })} />\n          </label>\n          <label className="expense-editor__wide">\n            <span>メモ</span>\n            <input value={expenseEditDraft.note} onChange={(event) => setExpenseEditDraft({ ...expenseEditDraft, note: event.target.value })} />\n          </label>\n          <div className="expense-editor__actions">\n            <button className="chip chip--active" type="submit">\n              <Save size={14} />\n              保存\n            </button>\n            <button className="chip" type="button" onClick={cancelExpenseEdit}>\n              <X size={14} />\n              キャンセル\n            </button>\n          </div>\n        </form>\n      ) : null}\n\n      <div className="timeline-item__actions">\n''')
replace('''      <div className="timeline timeline--wide">{timeline.map((expense) => expenseCard(expense))}</div>\n''', '''      <div className="timeline timeline--wide">{timeline.map((expense) => expenseCard(expense, { showManagement: true }))}</div>\n''')
file.write_text(text)
PY

python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/src/app/globals.css')
text = file.read_text()
marker = '''.timeline-item__actions {\n  display: grid;\n'''
insert = '''.timeline-item__manage,\n.expense-editor__actions {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  margin-top: 12px;\n}\n\n.chip--danger {\n  color: var(--danger);\n  border-color: rgba(220, 38, 38, 0.24);\n  background: rgba(220, 38, 38, 0.06);\n}\n\n.expense-editor {\n  display: grid;\n  grid-template-columns: repeat(2, minmax(0, 1fr));\n  gap: 10px;\n  margin-top: 12px;\n  padding: 12px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: rgba(248, 250, 252, 0.72);\n}\n\n.expense-editor label {\n  display: grid;\n  gap: 4px;\n  color: var(--muted);\n  font-size: 12px;\n  font-weight: 600;\n}\n\n.expense-editor input,\n.expense-editor select {\n  min-width: 0;\n  min-height: 40px;\n  padding: 0 10px;\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fff;\n  color: var(--text);\n}\n\n.expense-editor__wide,\n.expense-editor__actions {\n  grid-column: 1 / -1;\n}\n\n'''
if marker not in text:
    raise SystemExit('css marker not found')
text = text.replace(marker, insert + marker, 1)
file.write_text(text)
PY

git diff --check
python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/tests/integration/api.integration.test.ts')
text = file.read_text()

def replace(old, new):
    global text
    if old not in text:
        raise SystemExit(f'missing marker: {old[:120]!r}')
    text = text.replace(old, new, 1)

replace('''  const testExpenseId = "77777777-7777-4777-8777-777777777771";\n  const testPeriod = { year: 2098, month: 12 };\n''', '''  const testExpenseId = "77777777-7777-4777-8777-777777777771";\n  const editExpenseId = "77777777-7777-4777-8777-777777777772";\n  const deleteExpenseId = "77777777-7777-4777-8777-777777777773";\n  const testPeriod = { year: 2098, month: 12 };\n''')
replace('''    await pool.query("delete from expense_splits where expense_id = $1", [testExpenseId]);\n''', '''    await pool.query("delete from expense_splits where expense_id = $1", [testExpenseId]);\n    await pool.query(`\n      insert into expenses (id, household_id, paid_by_member_id, payment_source_id, category_id, amount, expense_date, scope, memo)\n      values\n        ($1, $3, $4, $5, $6, 2400, date '2098-11-10', 'personal', '編集前 メモ'),\n        ($2, $3, $4, $5, $6, 3200, date '2098-10-10', 'personal', '削除対象')\n      on conflict (id) do update\n      set amount = excluded.amount, expense_date = excluded.expense_date, scope = excluded.scope, memo = excluded.memo\n    `, [editExpenseId, deleteExpenseId, source.household_id, source.paid_by_member_id, source.payment_source_id, source.category_id]);\n''')
replace('''    await pool.query("delete from expenses where id = $1", [testExpenseId]);\n''', '''    await pool.query("delete from expenses where id = any($1::uuid[])", [[testExpenseId, editExpenseId, deleteExpenseId]]);\n''')
marker = '''  it("rejects settlement while review expenses remain, then stores the calculated transfer", async () => {\n'''
insert = '''  it("edits expense details and deletes an expense", async () => {\n    const { DELETE, PATCH } = await import("../../src/app/api/expenses/[id]/route.ts");\n    const editResponse = await PATCH(\n      new Request(`http://localhost/api/expenses/${editExpenseId}`, {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({\n          scope: "personal",\n          title: "編集後",\n          amount: 3600,\n          paidBy: "b",\n          category: "交通費",\n          note: "電車代",\n          expenseDate: "2098-11-12",\n        }),\n      }),\n      { params: Promise.resolve({ id: editExpenseId }) },\n    );\n    assert.equal(editResponse.status, 200);\n\n    const { rows } = await pool.query<{ amount: number; expense_date: string; memo: string; category: string }>(`\n      select e.amount, e.expense_date::text, e.memo, c.name as category\n      from expenses e\n      join categories c on c.id = e.category_id\n      where e.id = $1\n    `, [editExpenseId]);\n    assert.deepEqual(rows, [{ amount: 3600, expense_date: "2098-11-12", memo: "編集後 電車代", category: "交通費" }]);\n\n    const deleteResponse = await DELETE(\n      new Request(`http://localhost/api/expenses/${deleteExpenseId}`, { method: "DELETE" }),\n      { params: Promise.resolve({ id: deleteExpenseId }) },\n    );\n    assert.equal(deleteResponse.status, 200);\n    assert.equal((await pool.query("select id from expenses where id = $1", [deleteExpenseId])).rowCount, 0);\n  });\n\n'''
if marker not in text:
    raise SystemExit('settlement test marker missing')
text = text.replace(marker, insert + marker, 1)
file.write_text(text)
PY

python3 <<'PY'
from pathlib import Path
file = Path('/home/haya-ryoo/apps/kakeibo/tests/e2e/kakeibo.spec.ts')
text = file.read_text()
text += '''\n\ntest("履歴から支出を編集して削除する", async ({ page }) => {\n  await page.goto("/");\n  await expect(page.getByText("スーパーライフ", { exact: true })).toBeVisible();\n\n  await page.getByRole("button", { name: "履歴", exact: true }).click();\n  const expenseCard = page.locator(".timeline-item").filter({ hasText: "スーパーライフ" });\n  await expenseCard.getByRole("button", { name: "編集", exact: true }).click();\n  await expenseCard.getByLabel("タイトル").fill("スーパー編集後");\n  await expenseCard.getByLabel("金額").fill("5300");\n  await expenseCard.getByRole("button", { name: "保存", exact: true }).click();\n  await expect(page.getByText("スーパー編集後", { exact: true })).toBeVisible();\n\n  page.once("dialog", (dialog) => dialog.accept());\n  await expenseCard.getByRole("button", { name: "削除", exact: true }).click();\n  await expect(page.getByText("スーパー編集後", { exact: true })).toHaveCount(0);\n});\n'''
file.write_text(text)
PY

git diff --check
ps -ef | grep -E '/home/haya-ryoo/apps/kakeibo|next build|next dev --hostname 127.0.0.1 --port 3022|next-server' | grep -v grep && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3021|:3022' || true
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run dev -- --hostname 127.0.0.1 --port 3022
ps -ef | grep -E 'playwright|chrome|chromium|workerProcessEntry|next dev --hostname 127.0.0.1 --port 3022' | grep -v grep
pkill -f '/home/haya-ryoo/apps/kakeibo/node_modules/.bin/playwright test' || true
ss -ltnp | grep -E ':3021|:3022' || true && printf '\n-- playwright remnants --\n' && ps -ef | grep -E '/home/haya-ryoo/apps/kakeibo/node_modules/.bin/playwright test|playwright/lib/worker/workerProcessEntry' | grep -v grep || true
git diff --check && printf '\n-- status --\n' && git status --short --branch && printf '\n-- stat --\n' && git diff --stat
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh >/dev/null && docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select count(*) from expenses where id = any(array['77777777-7777-4777-8777-777777777771','77777777-7777-4777-8777-777777777772','77777777-7777-4777-8777-777777777773']::uuid[]); select count(*) from settlement_periods where year = 2098 and month = 12;"
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '履歴から支出を編集して削除する'
sed -n '1,260p' test-results/kakeibo-履歴から支出を編集して削除する-chromium/error-context.md && printf '\n-- db row --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_test -x -c "select e.id, e.amount, e.expense_date, e.memo, c.name as category from expenses e join categories c on c.id = e.category_id where e.id = 'dddddddd-dddd-4ddd-8ddd-ddddddddddd2';"
sed -n '1,280p' test-results/kakeibo-履歴から支出を編集して削除する-chromium/error-context.md && printf '\n-- db row --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_test -x -c "select e.id, e.amount, e.expense_date, e.memo, c.name as category from expenses e join categories c on c.id = e.category_id where e.id = 'dddddddd-dddd-4ddd-8ddd-ddddddddddd2';"
ss -ltnp | grep -E ':3021|:3022' || true && printf '\n-- status --\n' && git status --short --branch
sed -n '130,270p' src/components/kakeibo-dashboard.tsx && printf '\n-- editor --\n' && sed -n '270,430p' src/components/kakeibo-dashboard.tsx && printf '\n-- e2e tail --\n' && sed -n '65,120p' tests/e2e/kakeibo.spec.ts
sed -n '1,240p' 'src/app/api/expenses/[id]/route.ts'
sed -n '1,240p' tests/e2e/kakeibo.spec.ts
sed -n '1,280p' tests/integration/api.integration.test.ts
python3 - <<'PY'
from pathlib import Path

route = Path('src/app/api/expenses/[id]/route.ts')
text = route.read_text()
old = '''    const json = await request.json();
    const result = updateExpenseBodySchema.safeParse(json);
'''
new = '''    let json: unknown;
    try {
      json = await request.json();
    } catch {
      return NextResponse.json({ error: "invalid_expense_update" }, { status: 400 });
    }

    const result = updateExpenseBodySchema.safeParse(json);
'''
if old not in text:
    raise SystemExit('route target not found')
route.write_text(text.replace(old, new, 1))

e2e = Path('tests/e2e/kakeibo.spec.ts')
text = e2e.read_text()
old = '''  await expenseCard.getByLabel("タイトル").fill("スーパー編集後");
  await expenseCard.getByLabel("金額").fill("5300");
  await expenseCard.getByRole("button", { name: "保存", exact: true }).click();
  await expect(page.getByText("スーパー編集後", { exact: true })).toBeVisible();

  page.once("dialog", (dialog) => dialog.accept());
  await expenseCard.getByRole("button", { name: "削除", exact: true }).click();
  await expect(page.getByText("スーパー編集後", { exact: true })).toHaveCount(0);
'''
new = '''  await expenseCard.getByLabel("タイトル").fill("スーパー編集後");
  await expenseCard.getByLabel("金額").fill("5300");
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "PATCH" && response.ok()),
    expenseCard.getByRole("button", { name: "保存", exact: true }).click(),
  ]);
  await expect(page.getByText("スーパー編集後", { exact: true })).toBeVisible();

  page.once("dialog", (dialog) => dialog.accept());
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "DELETE" && response.ok()),
    expenseCard.getByRole("button", { name: "削除", exact: true }).click(),
  ]);
  await expect(page.getByText("スーパー編集後", { exact: true })).toHaveCount(0);
'''
if old not in text:
    raise SystemExit('e2e target not found')
e2e.write_text(text.replace(old, new, 1))

test = Path('tests/integration/api.integration.test.ts')
text = test.read_text()
needle = '''  it("persists expense scope and split preset updates", async () => {
'''
insert = '''  it("rejects malformed expense update JSON", async () => {
    const { PATCH } = await import("../../src/app/api/expenses/[id]/route.ts");
    const response = await PATCH(
      new Request(`http://localhost/api/expenses/${testExpenseId}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: "{",
      }),
      { params: Promise.resolve({ id: testExpenseId }) },
    );

    assert.equal(response.status, 400);
  });

'''
if needle not in text:
    raise SystemExit('integration test insertion point not found')
test.write_text(text.replace(needle, insert + needle, 1))
PY

git diff --check
ps -eo pid,pcpu,stat,etime,cmd --sort=-pcpu | head -n 12
sed -n '1,220p' playwright.config.ts && printf '\n-- ports --\n' && ss -ltnp | grep -E ':(3021|3022)\b' || true
ps -eo pid,ppid,pcpu,stat,etime,cmd --sort=-pcpu | head -n 16
ss -ltnp | grep -E ':(3021|3022)\b' || true
ps -eo pid,ppid,pcpu,stat,etime,cmd --sort=-pcpu | grep -E 'next dev|playwright|next-server|PID' | head -n 16
curl -fsS --max-time 90 -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3022/
ps -eo pid,ppid,pcpu,stat,etime,cmd --sort=-pcpu | grep -E 'next dev|playwright|chrome|chromium|next-server|PID' | head -n 24
sed -n '1,260p' test-results/kakeibo-履歴から支出を編集して削除する-chromium/error-context.md
grep -R "function deleteExpense\|const deleteExpense\|window.confirm\|DELETE" -n src/components/kakeibo-dashboard.tsx src/app/api/expenses/'[id]'/route.ts
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
old = '''  await expect(page.getByText("スーパー編集後", { exact: true })).toBeVisible();

  page.once("dialog", (dialog) => dialog.accept());
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "DELETE" && response.ok()),
    expenseCard.getByRole("button", { name: "削除", exact: true }).click(),
  ]);
'''
new = '''  await expect(page.getByText("スーパー編集後", { exact: true })).toBeVisible();

  const editedExpenseCard = page.locator(".timeline-item").filter({ hasText: "スーパー編集後" });
  page.once("dialog", (dialog) => dialog.accept());
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "DELETE" && response.ok()),
    editedExpenseCard.getByRole("button", { name: "削除", exact: true }).click(),
  ]);
'''
if old not in text:
    raise SystemExit('target not found')
path.write_text(text.replace(old, new, 1))
PY

git diff --check
ss -ltnp | grep -E ':(3021|3022)\b' || true
git status --short --branch && git diff --stat
git diff --check
grep -n "export async function PATCH\|export async function DELETE\|let json: unknown" 'src/app/api/expenses/[id]/route.ts'
grep -n "startExpenseEdit\|saveExpenseEdit\|deleteExpense\|showManagement" src/components/kakeibo-dashboard.tsx
grep -n "履歴から支出を編集して削除する\|rejects malformed expense update JSON\|edits expense details" tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git status --short --branch && printf '\n-- files --\n' && find . -maxdepth 3 -type f | sed 's#^./##' | sort | head -n 240
grep -RInE 'TODO|FIXME|未実装|今後|次|MVP|認証|settled|精算済' --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git . | head -n 240
git log --oneline --decorate -n 12 && printf '\n-- package --\n' && sed -n '1,220p' package.json
sed -n '1,280p' src/app/api/expenses/route.ts && printf '\n-- settlements --\n' && sed -n '1,240p' src/app/api/settlements/route.ts
sed -n '1,230p' src/components/kakeibo-dashboard.tsx && printf '\n-- dashboard middle --\n' && sed -n '230,700p' src/components/kakeibo-dashboard.tsx
sed -n '1,250p' src/app/api/dashboard/route.ts && printf '\n-- schema periods --\n' && grep -RIn "create table settlement_periods\|settlement_periods" postgres/migrations supabase/migrations | head -n 80
sed -n '700,940p' src/components/kakeibo-dashboard.tsx
sed -n '1,270p' tests/integration/api.integration.test.ts && printf '\n-- e2e --\n' && sed -n '1,180p' tests/e2e/kakeibo.spec.ts
sed -n '185,225p' postgres/migrations/001_initial_schema.sql && printf '\n-- seeds --\n' && sed -n '180,215p' postgres/migrations/003_seed_local_data.sql
python3 - <<'PY'
from pathlib import Path

path = Path('src/app/api/expenses/route.ts')
text = path.read_text()
text = text.replace("type InsertedExpenseRow = DashboardExpenseRow;\n", "type InsertedExpenseRow = DashboardExpenseRow;\ntype SettlementPeriodRow = { settled: boolean };\n")
needle = '''const fallbackPaymentSourceQuery = `
  select id
  from payment_sources
  where household_id = $1
  order by type desc, created_at asc
  limit 1
`;
'''
insert = needle + '''
const currentPeriodSettledQuery = `
  select exists (
    select 1
    from settlement_periods
    where household_id = $1
      and year = extract(year from current_date)::int
      and month = extract(month from current_date)::int
      and status = 'settled'
  ) as settled
`;
'''
if needle not in text:
    raise SystemExit('expenses route query insertion point missing')
text = text.replace(needle, insert, 1)
needle = '''      const memberResult = await client.query<DashboardMemberRow>(membersQuery, [household.id]);
'''
insert = '''      const period = (await client.query<SettlementPeriodRow>(currentPeriodSettledQuery, [household.id])).rows[0];
      if (period?.settled) {
        await client.query("rollback");
        return NextResponse.json({ error: "settled_period_locked" }, { status: 409 });
      }

''' + needle
if needle not in text:
    raise SystemExit('expenses route period check insertion point missing')
path.write_text(text.replace(needle, insert, 1))

path = Path('src/app/api/expenses/[id]/route.ts')
text = path.read_text()
text = text.replace("type PaymentSourceRow = { id: string };\n", "type PaymentSourceRow = { id: string };\ntype SettlementPeriodRow = { settled: boolean };\n")
text = text.replace('''const expenseQuery = `
  select id, memo
  from expenses
  where id = $1 and household_id = $2
`;
''', '''const expenseQuery = `
  select id, memo, expense_date::text
  from expenses
  where id = $1 and household_id = $2
`;

const settledPeriodQuery = `
  select exists (
    select 1
    from settlement_periods
    where household_id = $1
      and status = 'settled'
      and (
        (year = extract(year from $2::date)::int and month = extract(month from $2::date)::int)
        or (year = extract(year from $3::date)::int and month = extract(month from $3::date)::int)
      )
  ) as settled
`;
''')
needle = '''      const expense = (await client.query<{ id: string; memo: string | null }>(expenseQuery, [id, household.id])).rows[0];
      if (!expense) {
        await client.query("rollback");
        return NextResponse.json({ error: "expense_not_found" }, { status: 404 });
      }

'''
replace = '''      const expense = (await client.query<{ id: string; memo: string | null; expense_date: string }>(expenseQuery, [id, household.id])).rows[0];
      if (!expense) {
        await client.query("rollback");
        return NextResponse.json({ error: "expense_not_found" }, { status: 404 });
      }

      const period = (await client.query<SettlementPeriodRow>(settledPeriodQuery, [household.id, expense.expense_date, expenseDate ?? expense.expense_date])).rows[0];
      if (period?.settled) {
        await client.query("rollback");
        return NextResponse.json({ error: "settled_period_locked" }, { status: 409 });
      }

'''
if needle not in text:
    raise SystemExit('patch expense lookup target missing')
text = text.replace(needle, replace, 1)
needle = '''    const deleted = (await pool.query(deleteExpenseQuery, [id, household.id])).rows[0];
'''
replace = '''    const expense = (await pool.query<{ id: string; expense_date: string }>(expenseQuery, [id, household.id])).rows[0];
    if (!expense) {
      return NextResponse.json({ error: "expense_not_found" }, { status: 404 });
    }

    const period = (await pool.query<SettlementPeriodRow>(settledPeriodQuery, [household.id, expense.expense_date, expense.expense_date])).rows[0];
    if (period?.settled) {
      return NextResponse.json({ error: "settled_period_locked" }, { status: 409 });
    }

    const deleted = (await pool.query(deleteExpenseQuery, [id, household.id])).rows[0];
'''
if needle not in text:
    raise SystemExit('delete insertion target missing')
path.write_text(text.replace(needle, replace, 1))
PY

git diff --check
python3 - <<'PY'
from pathlib import Path
path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('''  const isCurrentPeriod = selectedPeriod.year === nowPeriod.year && selectedPeriod.month === nowPeriod.month;
''', '''  const isCurrentPeriod = selectedPeriod.year === nowPeriod.year && selectedPeriod.month === nowPeriod.month;
  const isPeriodLocked = periodStatus === "settled";
  const isComposerDisabled = !isCurrentPeriod || isDashboardLoading || isPeriodLocked;
''', 1)
text = text.replace('''    if (!isCurrentPeriod) {
      setSaveError("過去または未来の月を表示中です。支出追加は今月へ戻ってから行ってください。");
      return;
    }

''', '''    if (!isCurrentPeriod) {
      setSaveError("過去または未来の月を表示中です。支出追加は今月へ戻ってから行ってください。");
      return;
    }

    if (isPeriodLocked) {
      setSaveError("精算済みの月には支出を追加できません。");
      return;
    }

''', 1)
text = text.replace('''  const saveExpenseUpdate = async (previousExpense: Expense, nextExpense: Expense) => {
    setSaveError(null);
''', '''  const saveExpenseUpdate = async (previousExpense: Expense, nextExpense: Expense) => {
    if (isPeriodLocked) {
      setSaveError("精算済みの月は変更できません。");
      return;
    }

    setSaveError(null);
''', 1)
text = text.replace('''  const startExpenseEdit = (expense: Expense) => {
    setEditingExpenseId(expense.id);
''', '''  const startExpenseEdit = (expense: Expense) => {
    if (isPeriodLocked) return;

    setEditingExpenseId(expense.id);
''', 1)
text = text.replace('''  const saveExpenseEdit = async (expense: Expense) => {
    if (!expenseEditDraft) return;
''', '''  const saveExpenseEdit = async (expense: Expense) => {
    if (!expenseEditDraft || isPeriodLocked) return;
''', 1)
text = text.replace('''  const deleteExpense = async (expense: Expense) => {
    if (!window.confirm(`${expense.title}を削除しますか？`)) return;
''', '''  const deleteExpense = async (expense: Expense) => {
    if (isPeriodLocked) return;
    if (!window.confirm(`${expense.title}を削除しますか？`)) return;
''', 1)
text = text.replace('''          <button className="chip" type="button" onClick={() => startExpenseEdit(expense)}>
''', '''          <button className="chip" type="button" disabled={isPeriodLocked} onClick={() => startExpenseEdit(expense)}>
''', 1)
text = text.replace('''          <button className="chip chip--danger" type="button" onClick={() => void deleteExpense(expense)}>
''', '''          <button className="chip chip--danger" type="button" disabled={isPeriodLocked} onClick={() => void deleteExpense(expense)}>
''', 1)
text = text.replace('''        <button className={scopeClass(expense.scope, expense.scope === "shared")} type="button" onClick={() => updateScope(expense.id, "shared")}>
''', '''        <button className={scopeClass(expense.scope, expense.scope === "shared")} type="button" disabled={isPeriodLocked} onClick={() => updateScope(expense.id, "shared")}>
''', 1)
text = text.replace('''        <button className={scopeClass(expense.scope, expense.scope === "personal")} type="button" onClick={() => updateScope(expense.id, "personal")}>
''', '''        <button className={scopeClass(expense.scope, expense.scope === "personal")} type="button" disabled={isPeriodLocked} onClick={() => updateScope(expense.id, "personal")}>
''', 1)
text = text.replace('''        <button className={scopeClass(expense.scope, expense.scope === "review")} type="button" onClick={() => updateScope(expense.id, "review")}>
''', '''        <button className={scopeClass(expense.scope, expense.scope === "review")} type="button" disabled={isPeriodLocked} onClick={() => updateScope(expense.id, "review")}>
''', 1)
text = text.replace('''              type="button"
              onClick={() => updateSplitPreset(expense.id, preset)}
''', '''              type="button"
              disabled={isPeriodLocked}
              onClick={() => updateSplitPreset(expense.id, preset)}
''', 1)
text = text.replace('''                  <button className="chip" type="button" onClick={() => updateScope(expense.id, "shared")}>
''', '''                  <button className="chip" type="button" disabled={isPeriodLocked} onClick={() => updateScope(expense.id, "shared")}>
''', 1)
text = text.replace('''                  <button className="chip" type="button" onClick={() => updateScope(expense.id, "personal")}>
''', '''                  <button className="chip" type="button" disabled={isPeriodLocked} onClick={() => updateScope(expense.id, "personal")}>
''', 1)
text = text.replace('''                  <button className="chip chip--review" type="button" onClick={() => updateSplitPreset(expense.id, defaultSplitPreset)}>
''', '''                  <button className="chip chip--review" type="button" disabled={isPeriodLocked} onClick={() => updateSplitPreset(expense.id, defaultSplitPreset)}>
''', 1)
text = text.replace('''                    <button key={preset} className="chip" type="button" onClick={() => updateSplitPreset(expense.id, preset)}>
''', '''                    <button key={preset} className="chip" type="button" disabled={isPeriodLocked} onClick={() => updateSplitPreset(expense.id, preset)}>
''', 1)
text = text.replace('''            <button className="eyebrow" type="button" onClick={() => {
''', '''            <button className="eyebrow" type="button" disabled={isComposerDisabled} onClick={() => {
''', 1)
text = text.replace('''            disabled={!isCurrentPeriod || isDashboardLoading}
''', '''            disabled={isComposerDisabled}
''', 1)
text = text.replace('''          <button className="composer__submit" type="submit" aria-label="記録する" disabled={!isCurrentPeriod || isDashboardLoading}>
''', '''          <button className="composer__submit" type="submit" aria-label="記録する" disabled={isComposerDisabled}>
''', 1)
text = text.replace('''        <div className="composer__quick" aria-disabled={!isCurrentPeriod || isDashboardLoading}>
''', '''        <div className="composer__quick" aria-disabled={isComposerDisabled}>
''', 1)
text = text.replace('''            <button key={item} className="chip" type="button" disabled={!isCurrentPeriod || isDashboardLoading} onClick={() => setComposerValue(item)}>
''', '''            <button key={item} className="chip" type="button" disabled={isComposerDisabled} onClick={() => setComposerValue(item)}>
''', 1)
text = text.replace('''              disabled={!isCurrentPeriod || isDashboardLoading}
''', '''              disabled={isComposerDisabled}
''', 1)
text = text.replace('''        <div className="composer__note">LINE っぽく軽く入れて、あとで要確認を整える。</div>
''', '''        <div className="composer__note">{isPeriodLocked ? "精算済みの月は変更できません。" : "LINE っぽく軽く入れて、あとで要確認を整える。"}</div>
''', 1)
path.write_text(text)
PY

git diff --check
grep -n "isPeriodLocked\|isComposerDisabled\|disabled={isPeriodLocked}\|settled_period_locked" src/components/kakeibo-dashboard.tsx src/app/api/expenses/route.ts 'src/app/api/expenses/[id]/route.ts'
sed -n '180,290p' 'src/app/api/expenses/[id]/route.ts' && printf '\n-- ui cards --\n' && sed -n '360,490p' src/components/kakeibo-dashboard.tsx && printf '\n-- ui composer --\n' && sed -n '700,835p' src/components/kakeibo-dashboard.tsx
sed -n '135,195p' 'src/app/api/expenses/[id]/route.ts' && printf '\n-- create --\n' && sed -n '135,180p' src/app/api/expenses/route.ts
python3 - <<'PY'
from pathlib import Path

path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
needle = '''  it("persists expense scope and split preset updates", async () => {
'''
insert = '''  it("rejects expense creation for a settled current period", async () => {
    const { POST } = await import("../../src/app/api/expenses/route.ts");
    const household = (await pool.query<{ id: string }>("select id::text from households order by created_at asc limit 1")).rows[0];
    assert.ok(household);

    await pool.query(`
      insert into settlement_periods (household_id, year, month, status, settled_at)
      values ($1, extract(year from current_date)::int, extract(month from current_date)::int, 'settled', now())
      on conflict (household_id, year, month) do update
      set status = 'settled', settled_at = now()
    `, [household.id]);

    try {
      const response = await POST(new Request("http://localhost/api/expenses", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          title: "精算後の支出",
          amount: 1200,
          paidBy: "a",
          scope: "shared",
          category: "食費",
        }),
      }));

      assert.equal(response.status, 409);
    } finally {
      await pool.query(`
        update settlement_periods
        set status = 'open', settled_at = null
        where household_id = $1
          and year = extract(year from current_date)::int
          and month = extract(month from current_date)::int
      `, [household.id]);
    }
  });

'''
if needle not in text:
    raise SystemExit('integration insertion point missing')
text = text.replace(needle, insert + needle, 1)
needle = '''    assert.deepEqual(rows, [{ status: "settled", amount: 5000 }]);
'''
insert = '''    assert.deepEqual(rows, [{ status: "settled", amount: 5000 }]);

    const lockedUpdate = await PATCH(
      new Request(`http://localhost/api/expenses/${testExpenseId}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ scope: "personal" }),
      }),
      { params: Promise.resolve({ id: testExpenseId }) },
    );
    assert.equal(lockedUpdate.status, 409);

    const lockedMove = await PATCH(
      new Request(`http://localhost/api/expenses/${editExpenseId}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ scope: "personal", expenseDate: "2098-12-12" }),
      }),
      { params: Promise.resolve({ id: editExpenseId }) },
    );
    assert.equal(lockedMove.status, 409);

    const { DELETE } = await import("../../src/app/api/expenses/[id]/route.ts");
    const lockedDelete = await DELETE(
      new Request(`http://localhost/api/expenses/${testExpenseId}`, { method: "DELETE" }),
      { params: Promise.resolve({ id: testExpenseId }) },
    );
    assert.equal(lockedDelete.status, 409);
'''
if needle not in text:
    raise SystemExit('settled test assertion missing')
text = text.replace(needle, insert, 1)
path.write_text(text)

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
needle = '''  await expect(page.getByRole("button", { name: "精算済み", exact: true })).toBeDisabled();
'''
replace = '''  await expect(page.getByRole("button", { name: "精算済み", exact: true })).toBeDisabled();
  await expect(page.getByLabel("支出を入力")).toBeDisabled();

  await page.getByRole("button", { name: "履歴", exact: true }).click();
  await expect(page.getByRole("button", { name: "編集", exact: true }).first()).toBeDisabled();
  await expect(page.getByRole("button", { name: "削除", exact: true }).first()).toBeDisabled();
'''
if needle not in text:
    raise SystemExit('e2e settlement assertion missing')
path.write_text(text.replace(needle, replace, 1))
PY

git diff --check
git status --short --branch && printf '\n-- diff stat --\n' && git diff --stat
python3 - <<'PY'
from pathlib import Path
path = Path('src/app/api/expenses/route.ts')
text = path.read_text()
text = text.replace('from "../../../features/dashboard/dashboard.mapper";', 'from "../../../features/dashboard/dashboard.mapper.ts";')
text = text.replace('from "../../../lib/db";', 'from "../../../lib/db.ts";')
text = text.replace('from "../../../lib/expense-utils";', 'from "../../../lib/expense-utils.ts";')
path.write_text(text)
PY

git diff --check
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '要確認を共通費へ整理して月次精算を確定する'
grep -n "currentPeriodSettledQuery\|settled_period_locked" src/app/api/expenses/route.ts 'src/app/api/expenses/[id]/route.ts' && printf '\n-- ui --\n' && grep -n "isPeriodLocked\|isComposerDisabled" src/components/kakeibo-dashboard.tsx && printf '\n-- tests --\n' && grep -n "rejects expense creation for a settled\|lockedUpdate\|支出を入力.*toBeDisabled" tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git status --short --branch && printf '\n-- stat --\n' && git diff --stat && printf '\n-- names --\n' && git diff --name-only
git diff -- src/app/api/expenses/route.ts 'src/app/api/expenses/[id]/route.ts' tests/integration/api.integration.test.ts
git diff -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/e2e/kakeibo.spec.ts
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
text = text.replace('});\n\n\ntest("履歴から支出を編集して削除する"', '});\n\ntest("履歴から支出を編集して削除する"', 1)
path.write_text(text)
PY

git diff --check
git status --short
git add 'src/app/api/expenses/[id]/route.ts' src/app/api/expenses/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && printf '\n-- status --\n' && git status --short
git commit -m '支出編集と精算済み月の保護を追加する' -m '- 履歴画面から支出詳細を編集・削除できるようにする
- 精算済み月の支出追加・更新・削除をAPIと画面の両方で拒否する
- 統合テストとPlaywrightで編集・削除・精算後ロックを確認する'
git log -1 --format='%h%n%s%n%b' && printf '\n-- show stat --\n' && git show --stat --oneline --summary HEAD
sed -n '1,240p' /home/haya-ryoo/.codex/plugins/cache/openai-curated/github/6188456f/skills/github/SKILL.md
git remote -v
gh auth status
gh issue list --limit 100 --state all --json number,title,state,url
gh issue create --title '負担割合を大まかなプリセットから選べるようにする' --body $'## 概要\n共通費の金額の分け方を、毎回細かく入力せず大まかな割合から選べるようにする。\n\n## 背景\n現在は均等または限定的な割合を扱っている。実運用では、厳密な金額指定よりも `5:5`、`4:6`、`6:4` などの大まかな負担割合を素早く選べる方が使いやすい。\n\n## 要件案\n- 共通費の登録・編集時に負担割合プリセットを選択できる\n- 少なくとも `5:5`、`4:6`、`6:4` を扱う\n- 月次精算の計算に選択した割合を反映する\n- 履歴と精算画面で適用中の割合を確認できる\n- 既存データは `5:5` として扱う\n\n## 完了条件\n- 支出ごとに割合を保存できる\n- 精算額が選択した割合に基づいて計算される\n- API・計算ロジック・主要導線のテストが追加されている\n\n## 補足\n将来的に任意割合入力を追加できる構造にするが、最初はプリセット選択を優先する。'
gh issue create --title 'レシート明細から支出を起票できるようにする' --body $'## 概要\nレシートを起点に、支出を明細単位で起票できるようにする。\n\n## 背景\n1枚のレシートに共通費と個人費が混在する場合、合計金額だけでは正確に分けられない。細かく管理したいケースでは、レシート明細から支出候補を作成し、必要な行だけ分類できるようにしたい。\n\n## 要件案\n- レシート画像を登録できる\n- OCR 結果から店舗名、日付、合計金額、明細行を支出候補として表示する\n- 明細行ごとに共通費・個人費・要確認を選択できる\n- 認識誤りを手動修正できる\n- 確定前に合計金額と明細合計の差分を確認できる\n- OCR に失敗した場合も手入力へ切り替えられる\n\n## 完了条件\n- レシート画像から複数の支出候補を作成できる\n- 明細単位の分類と修正ができる\n- OCR 失敗時のフォールバックがある\n- 主要導線のテストが追加されている\n\n## 検討事項\n- OCR サービスの選定\n- 画像保存先と保持期間\n- レシート画像に含まれる個人情報の扱い\n- MVP では画像アップロードと手動補正を優先し、自動分類は後続でもよい'
gh issue create --title 'ユーザー登録とカップル連携の仕組みを追加する' --body $'## 概要\nユーザー登録から、2人で同じ世帯データを利用できるカップル連携までの仕組みを追加する。\n\n## 背景\n現在の API は最古の世帯を暗黙的に選択する MVP 実装であり、ユーザーごとのデータ分離がない。実利用には認証と世帯単位のアクセス制御が必要。\n\n## 要件案\n- ユーザー登録、ログイン、ログアウトを提供する\n- 初回登録時に世帯を作成できる\n- 招待 URL または招待コードでパートナーを世帯へ追加できる\n- 1世帯に紐づくメンバーと権限を管理する\n- API はログイン中ユーザーの所属世帯だけを参照する\n- 他世帯の支出・精算データへアクセスできない\n- 招待の期限切れ、再送、無効化を扱う\n\n## 完了条件\n- 2ユーザーが同じ世帯へ参加できる\n- 世帯ごとに支出・精算データが分離される\n- 未認証アクセスと他世帯アクセスが拒否される\n- 認証、招待、世帯分離の統合テストが追加されている\n\n## 検討事項\n- Supabase Auth と独自認証の比較\n- 招待フローの UI\n- 世帯を離脱・解消する場合のデータ保持方針\n- RLS を利用する場合の API 構成'
gh issue list --limit 10 --state open --json number,title,url --jq 'sort_by(.number)[] | "#\(.number)\t\(.title)\t\(.url)"'
sed -n '1,220p' /home/haya-ryoo/.codex/plugins/cache/openai-curated/github/6188456f/skills/github/SKILL.md
gh issue list --limit 100 --state all --json number,title,state,url --jq 'sort_by(.number)[] | "#\(.number)\t\(.state)\t\(.title)\t\(.url)"'
gh api repos/team-mirai/marumie/git/trees/develop?recursive=1 --jq '.tree[].path' | grep -Ei 'chart|graph|visual|dashboard|recharts|apex|nivo' | head -n 160
gh api repos/team-mirai/marumie/contents/webapp/package.json?ref=develop --jq '.content' | base64 -d | grep -Ei 'recharts|apex|nivo|chart'
gh api repos/team-mirai/marumie/contents/webapp/src/client/components/top-page/features/charts/MonthlyChart.tsx?ref=develop --jq '.content' | base64 -d | sed -n '1,260p'
gh api repos/team-mirai/marumie/contents/webapp/src/client/components/top-page/features/charts/BalanceSheetChart.tsx?ref=develop --jq '.content' | base64 -d | sed -n '1,260p'
gh api repos/team-mirai/marumie/contents/webapp/src/client/components/top-page/features/charts/SankeyChart.tsx?ref=develop --jq '.content' | base64 -d | sed -n '1,260p'
gh issue create --title '店舗とカテゴリの入力候補を表示する' --body $'## ユーザーストーリー\nスーパーやドラッグストアで支払った直後に、店名と金額だけで素早く記録したい。毎回同じカテゴリを選ぶ手間は減らしたい。\n\n## 要件案\n- 過去の入力履歴から店舗名を候補表示する\n- 店舗ごとによく使うカテゴリを初期選択する\n- 候補は手動で上書きできる\n- 個人費・共通費・要確認の初期値も店舗履歴から提案する\n\n## 完了条件\n- 店舗名の入力途中に候補が表示される\n- 候補選択後もカテゴリと分類を変更できる\n- 候補がない店舗でも従来通り登録できる\n\n## 関連\n- #1 レシート明細から支出を起票できるようにする'
gh issue create --title '外食時のおごり指定と支払い確認共有を追加する' --body $'## ユーザーストーリー\n二人で外食して片方がまとめて支払った。割り勘だけでなく、今回は自分のおごりにしたり、登録内容を相手へ軽く共有したい。\n\n## 要件案\n- 負担割合に `今回は自分のおごり` を追加する\n- 支払者と負担者が異なるケースを保存できる\n- 支出内容を確認できる共有リンクを生成する\n- 共有リンクからアプリを開いた場合、対象支出を表示する\n\n## 完了条件\n- おごり指定が月次精算へ正しく反映される\n- 共有リンクから対象支出を確認できる\n- 認証導入後は同じ世帯のメンバーだけが詳細を閲覧できる\n\n## 関連\n- #2 負担割合を大まかなプリセットから選べるようにする\n- #3 ユーザー登録とカップル連携の仕組みを追加する'
gh issue create --title '少額支出の仮登録とまとめ整理を追加する' --body $'## ユーザーストーリー\nコンビニなどの少額支出は、その場で細かく分類すると面倒だが、後回しにすると忘れる。数秒で仮登録して後からまとめて整えたい。\n\n## 要件案\n- 金額と店舗名だけで仮登録できる\n- PWA のホーム画面ショートカットから入力画面を直接開ける\n- 未分類・要確認の支出を一覧でまとめて整理できる\n- まとめ整理画面で分類と割合を連続編集できる\n\n## 完了条件\n- 最小入力で支出を保存できる\n- 未整理件数がホーム画面で分かる\n- 複数件を画面遷移なしで整理できる'
gh issue create --title '定期支出の自動起票と引き落とし通知を追加する' --body $'## ユーザーストーリー\n家賃、光熱費、サブスクなど、毎月発生する支出を何度も手入力したくない。金額の変化と引き落とし日だけ確認したい。\n\n## 要件案\n- 定期支出の名称、カテゴリ、支払者、予定日、標準金額を登録する\n- 毎月の支出候補を自動生成する\n- 前月との差額を表示する\n- 引き落とし予定日前に通知する\n- 確定、スキップ、金額修正を行える\n\n## 完了条件\n- 毎月の候補が二重登録されない\n- 前月差分が確認できる\n- 通知の有効・無効を設定できる'
gh issue create --title '給料日と共通口座への入金管理を追加する' --body $'## ユーザーストーリー\n給料日に、今月の生活費として共通口座へいくら入れるか決めたい。収入差がある月も無理のない負担にしたい。\n\n## 要件案\n- メンバーごとの給料日と収入を登録する\n- 給料日前後に通知する\n- 収入比率に応じた共通生活費の負担額を提案する\n- 共通口座への入金予定と完了状態を管理する\n- 月ごとに提案額を手動調整できる\n\n## 完了条件\n- 収入比率から入金提案額が計算される\n- 二人の入金状況を一覧で確認できる\n- 収入額の公開範囲を検討し、仕様として明記する\n\n## 関連\n- #2 負担割合を大まかなプリセットから選べるようにする'
gh issue create --title 'ネット通販の注文を商品単位で分割登録する' --body $'## ユーザーストーリー\nネット通販で、共用品、自分の服、プレゼントをまとめて注文した。注文合計だけでなく商品単位で分類したい。\n\n## 要件案\n- 1件の注文から複数の支出明細を作成する\n- 商品ごとに共通費・個人費・要確認を指定する\n- 購入日、注文 URL、メモを保存する\n- 明細合計と注文合計の差分を確認する\n- 送料や割引を調整行として扱う\n\n## 完了条件\n- 注文単位と商品明細を関連付けて保存できる\n- 明細ごとの分類が月次精算へ反映される\n- 合計不一致を確定前に検知できる\n\n## 関連\n- #1 レシート明細から支出を起票できるようにする'
gh issue create --title '月次精算を取り消して再編集できるようにする' --body $'## ユーザーストーリー\n月末精算を確定した後に、登録漏れや分類ミスに気付いた。確定済みデータを黙って書き換えず、取り消し理由を残して修正したい。\n\n## 要件案\n- 精算済み月に `精算を取り消す` 操作を追加する\n- 取り消し理由と実行者、日時を保存する\n- 取り消し後は支出の追加・編集・削除を再び許可する\n- 再確定時に精算額を再計算する\n- 過去の確定額と取り消し履歴を確認できる\n\n## 完了条件\n- 明示的な取り消しなしで精算済み月を変更できない\n- 取り消しと再確定の履歴が残る\n- API と主要導線のテストが追加されている'
gh issue create --title '旅行や引っ越し向けのイベント会計を追加する' --body $'## ユーザーストーリー\n旅行や引っ越しで臨時支出が増えた。普段の生活費と混ぜず、イベント全体の予算と精算額を確認したい。\n\n## 要件案\n- 旅行、引っ越しなどのイベントを作成する\n- 支出をイベントへ紐付ける\n- イベント期間中に使える簡易入力モードを提供する\n- イベント単位で合計、カテゴリ内訳、精算額を表示する\n- イベント終了時に個別精算できる\n\n## 完了条件\n- 通常の月次精算とイベント精算を区別できる\n- イベントの支出一覧と合計を確認できる\n- 終了後も履歴として参照できる'
gh issue create --title '要確認支出にコメントとリマインドを追加する' --body $'## ユーザーストーリー\n共通費か個人費か、その場では判断できない支出がある。勝手に決めず、相手へ確認して月末までに整理したい。\n\n## 要件案\n- 要確認支出にコメントを追加する\n- 確認済み、同意、保留などのリアクションを追加する\n- 未解決支出の件数を表示する\n- 月末前に未解決項目を通知する\n- 通知から対象支出へ直接移動できる\n\n## 完了条件\n- 支出ごとに会話履歴が残る\n- 未解決項目だけを絞り込める\n- 通知の頻度を設定できる\n\n## 関連\n- #3 ユーザー登録とカップル連携の仕組みを追加する'
gh issue create --title '月別・カテゴリ別の使用料グラフを追加する' --body $'## ユーザーストーリー\n今月どれくらい使ったか、先月より増えたか、何にお金を使っているかを直感的に確認したい。\n\n## 要件案\n- 月別の支出推移を棒グラフで表示する\n- カテゴリ別の支出内訳を表示する\n- 共通費、個人費、要確認を切り替えて確認できる\n- 月、カテゴリ、支払者で絞り込める\n- スマートフォンでも読みやすい表示にする\n- データがない場合の空状態を用意する\n\n## 参考実装\n- [team-mirai/marumie](https://github.com/team-mirai/marumie)\n- [MonthlyChart.tsx](https://github.com/team-mirai/marumie/blob/develop/webapp/src/client/components/top-page/features/charts/MonthlyChart.tsx): `react-apexcharts` を動的 import し、SSR を避けながら月次推移を表示している\n- [SankeyChart.tsx](https://github.com/team-mirai/marumie/blob/develop/webapp/src/client/components/top-page/features/charts/SankeyChart.tsx): `@nivo/sankey` で収支フローを可視化している\n- `marumie` の資金フロー表示は将来候補。最初は月別推移とカテゴリ内訳を優先する\n\n## 完了条件\n- 月別支出の増減を確認できる\n- カテゴリ内訳を確認できる\n- ローディング、空状態、モバイル表示が確認できる\n- 集計 API と主要導線のテストが追加されている'
gh issue list --limit 100 --state open --json number,title,url --jq 'sort_by(.number)[] | "#\(.number)\t\(.title)\t\(.url)"'
sed -n '1,220p' /home/haya-ryoo/.codex/plugins/cache/openai-curated/github/6188456f/skills/github/SKILL.md && printf '\n-- issue --\n' && gh issue view 9 --json number,title,body,url,state
git status --short --branch && printf '\n-- migrations --\n' && find postgres/migrations supabase/migrations -maxdepth 1 -type f | sort && printf '\n-- db scripts --\n' && find postgres -maxdepth 3 -type f | sort && printf '\n-- compose --\n' && sed -n '1,240p' docker-compose.yml
sed -n '1,280p' src/app/api/settlements/route.ts && printf '\n-- dashboard api --\n' && sed -n '1,240p' src/app/api/dashboard/route.ts
sed -n '1,300p' tests/integration/api.integration.test.ts && printf '\n-- e2e --\n' && sed -n '1,170p' tests/e2e/kakeibo.spec.ts
grep -RIn "settlement_period\|settlements" src/db postgres supabase Docs --exclude='*.md' | head -n 240 && printf '\n-- ui settlement --\n' && sed -n '320,700p' src/components/kakeibo-dashboard.tsx
sed -n '1,280p' postgres/migrations/002_prepare_local_environments.sh && printf '\n-- schema tail --\n' && sed -n '220,340p' postgres/migrations/001_initial_schema.sql && printf '\n-- supabase policies --\n' && sed -n '240,470p' supabase/migrations/202605240001_initial_schema.sql
grep -n "action-button\|section__meta\|composer__error\|panel" src/app/globals.css | head -n 120 && printf '\n-- css relevant --\n' && sed -n '120,330p' src/app/globals.css
python3 - <<'PY'
from pathlib import Path

local_schema = Path('postgres/migrations/001_initial_schema.sql')
text = local_schema.read_text()
needle = '''create table settlements (
  id uuid primary key default gen_random_uuid(),
  period_id uuid not null references settlement_periods(id) on delete cascade,
  from_member_id uuid not null references household_members(id) on delete cascade,
  to_member_id uuid not null references household_members(id) on delete cascade,
  amount integer not null,
  status settlement_status not null default 'pending',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint settlements_amount_positive check (amount > 0),
  constraint settlements_members_different check (from_member_id <> to_member_id)
);
'''
insert = needle + '''
create table settlement_period_events (
  id uuid primary key default gen_random_uuid(),
  period_id uuid not null references settlement_periods(id) on delete cascade,
  action text not null,
  reason text,
  acted_by_member_id uuid references household_members(id) on delete set null,
  settlement_snapshot jsonb,
  created_at timestamptz not null default now(),
  constraint settlement_period_events_action_valid check (action in ('settled', 'reopened')),
  constraint settlement_period_events_reopen_reason_required check (action <> 'reopened' or nullif(trim(reason), '') is not null)
);
'''
if needle not in text:
    raise SystemExit('local schema table insertion point missing')
text = text.replace(needle, insert, 1)
text = text.replace('''create index settlements_period_id_idx on settlements(period_id);
''', '''create index settlements_period_id_idx on settlements(period_id);
create index settlement_period_events_period_id_idx on settlement_period_events(period_id, created_at desc);
''', 1)
local_schema.write_text(text)

local_migration = Path('postgres/migrations/004_add_settlement_period_events.sql')
local_migration.write_text('''create table if not exists settlement_period_events (
  id uuid primary key default gen_random_uuid(),
  period_id uuid not null references settlement_periods(id) on delete cascade,
  action text not null,
  reason text,
  acted_by_member_id uuid references household_members(id) on delete set null,
  settlement_snapshot jsonb,
  created_at timestamptz not null default now(),
  constraint settlement_period_events_action_valid check (action in ('settled', 'reopened')),
  constraint settlement_period_events_reopen_reason_required check (action <> 'reopened' or nullif(trim(reason), '') is not null)
);

create index if not exists settlement_period_events_period_id_idx
  on settlement_period_events(period_id, created_at desc);
''')

prep = Path('postgres/migrations/002_prepare_local_environments.sh')
text = prep.read_text()
text = text.replace('''seed_file="/docker-entrypoint-initdb.d/003_seed_local_data.sql"
''', '''seed_file="/docker-entrypoint-initdb.d/003_seed_local_data.sql"
settlement_events_migration_file="/docker-entrypoint-initdb.d/004_add_settlement_period_events.sql"
''', 1)
needle = '''seed_database() {
'''
insert = '''apply_incremental_migrations() {
  database_name="$1"

  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$settlement_events_migration_file"
}

'''
if needle not in text:
    raise SystemExit('prep insertion point missing')
text = text.replace(needle, insert + needle, 1)
text = text.replace('''  apply_schema_if_missing "$database_name"
  seed_database "$database_name"
''', '''  apply_schema_if_missing "$database_name"
  apply_incremental_migrations "$database_name"
  seed_database "$database_name"
''', 1)
prep.write_text(text)

supabase = Path('supabase/migrations/202606010001_add_settlement_period_events.sql')
supabase.write_text('''create table public.settlement_period_events (
  id uuid primary key default gen_random_uuid(),
  period_id uuid not null references public.settlement_periods(id) on delete cascade,
  action text not null,
  reason text,
  acted_by_member_id uuid references public.household_members(id) on delete set null,
  settlement_snapshot jsonb,
  created_at timestamptz not null default now(),
  constraint settlement_period_events_action_valid check (action in ('settled', 'reopened')),
  constraint settlement_period_events_reopen_reason_required check (action <> 'reopened' or nullif(trim(reason), '') is not null)
);

create index settlement_period_events_period_id_idx
  on public.settlement_period_events(period_id, created_at desc);

alter table public.settlement_period_events enable row level security;

create policy "members can manage settlement period events"
on public.settlement_period_events
for all
using (
  exists (
    select 1
    from public.settlement_periods sp
    where sp.id = period_id
      and public.is_household_member(sp.household_id)
  )
)
with check (
  exists (
    select 1
    from public.settlement_periods sp
    where sp.id = period_id
      and public.is_household_member(sp.household_id)
  )
);
''')
PY

git diff --check
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
python3 - <<'PY'
from pathlib import Path

path = Path('src/app/api/settlements/route.ts')
text = path.read_text()
needle = '''const insertSettlementQuery = `
  insert into settlements (period_id, from_member_id, to_member_id, amount, status)
  values ($1, $2, $3, $4, 'pending')
`;
'''
insert = needle + '''
const insertSettlementEventQuery = `
  insert into settlement_period_events (period_id, action, settlement_snapshot)
  values ($1, 'settled', $2::jsonb)
`;
'''
if needle not in text:
    raise SystemExit('settlement query insertion point missing')
text = text.replace(needle, insert, 1)
needle = '''      for (const transfer of summary.transfers) {
        await client.query(insertSettlementQuery, [settlementPeriod.id, transfer.fromMemberId, transfer.toMemberId, transfer.amount]);
      }

      await client.query("commit");
'''
replace = '''      for (const transfer of summary.transfers) {
        await client.query(insertSettlementQuery, [settlementPeriod.id, transfer.fromMemberId, transfer.toMemberId, transfer.amount]);
      }

      await client.query(insertSettlementEventQuery, [settlementPeriod.id, JSON.stringify(summary)]);
      await client.query("commit");
'''
if needle not in text:
    raise SystemExit('settlement event insertion point missing')
path.write_text(text.replace(needle, replace, 1))

path = Path('src/app/api/dashboard/route.ts')
text = path.read_text()
text = text.replace('''type SettlementPeriodStatus = "open" | "reviewing" | "settled";
''', '''type SettlementPeriodStatus = "open" | "reviewing" | "settled";
type SettlementPeriodEventRow = {
  action: "settled" | "reopened";
  reason: string | null;
  settlement_snapshot: unknown;
  created_at: string;
};
''', 1)
needle = '''const settlementPeriodQuery = `
  select status
  from settlement_periods
  where household_id = $1 and year = $2 and month = $3
`;
'''
insert = needle + '''
const settlementPeriodEventsQuery = `
  select spe.action, spe.reason, spe.settlement_snapshot, spe.created_at::text
  from settlement_period_events spe
  join settlement_periods sp on sp.id = spe.period_id
  where sp.household_id = $1 and sp.year = $2 and sp.month = $3
  order by spe.created_at desc
`;
'''
if needle not in text:
    raise SystemExit('dashboard event query insertion point missing')
text = text.replace(needle, insert, 1)
text = text.replace('''    const [memberResult, expenseResult, settlementPeriodResult] = await Promise.all([
''', '''    const [memberResult, expenseResult, settlementPeriodResult, settlementPeriodEventsResult] = await Promise.all([
''', 1)
text = text.replace('''      pool.query<{ status: SettlementPeriodStatus }>(settlementPeriodQuery, [household.id, period.year, period.month]),
''', '''      pool.query<{ status: SettlementPeriodStatus }>(settlementPeriodQuery, [household.id, period.year, period.month]),
      pool.query<SettlementPeriodEventRow>(settlementPeriodEventsQuery, [household.id, period.year, period.month]),
''', 1)
text = text.replace('''      period: {
        ...period,
        status: settlementPeriodResult.rows[0]?.status ?? "open",
      },
''', '''      period: {
        ...period,
        status: settlementPeriodResult.rows[0]?.status ?? "open",
        events: settlementPeriodEventsResult.rows.map((event) => ({
          action: event.action,
          reason: event.reason,
          settlementSnapshot: event.settlement_snapshot,
          createdAt: event.created_at,
        })),
      },
''', 1)
path.write_text(text)

reopen = Path('src/app/api/settlements/reopen/route.ts')
reopen.parent.mkdir(parents=True, exist_ok=True)
reopen.write_text('''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const reopenSettlementSchema = z.object({
  year: z.number().int().min(2000).max(9999),
  month: z.number().int().min(1).max(12),
  reason: z.string().trim().min(1).max(200),
});

type HouseholdRow = { id: string };
type SettlementPeriodRow = { id: string };

// TODO(security): 認証統合時に、ログイン中ユーザーの所属世帯と acted_by_member_id をセッションから解決する。
const householdQuery = `
  select id
  from households
  order by created_at asc
  limit 1
`;

const settledPeriodQuery = `
  select id::text
  from settlement_periods
  where household_id = $1 and year = $2 and month = $3 and status = 'settled'
  for update
`;

const settlementSnapshotQuery = `
  select coalesce(json_agg(json_build_object(
    'fromMemberId', from_member_id::text,
    'toMemberId', to_member_id::text,
    'amount', amount,
    'status', status
  ) order by created_at asc), '[]'::json) as transfers
  from settlements
  where period_id = $1
`;

const reopenPeriodQuery = `
  update settlement_periods
  set status = 'open', settled_at = null
  where id = $1
`;

const deleteSettlementsQuery = `
  delete from settlements
  where period_id = $1
`;

const insertSettlementEventQuery = `
  insert into settlement_period_events (period_id, action, reason, settlement_snapshot)
  values ($1, 'reopened', $2, $3::jsonb)
`;

export async function POST(request: Request) {
  let json: unknown;
  try {
    json = await request.json();
  } catch {
    return NextResponse.json({ error: "invalid_reopen_request" }, { status: 400 });
  }

  const result = reopenSettlementSchema.safeParse(json);
  if (!result.success) {
    return NextResponse.json({ error: "invalid_reopen_request" }, { status: 400 });
  }

  const { year, month, reason } = result.data;
  const pool = db();
  const client = await pool.connect();

  try {
    await client.query("begin");

    const household = (await client.query<HouseholdRow>(householdQuery)).rows[0];
    if (!household) {
      await client.query("rollback");
      return NextResponse.json({ error: "household_not_found" }, { status: 404 });
    }

    const period = (await client.query<SettlementPeriodRow>(settledPeriodQuery, [household.id, year, month])).rows[0];
    if (!period) {
      await client.query("rollback");
      return NextResponse.json({ error: "settled_period_not_found" }, { status: 404 });
    }

    const snapshot = (await client.query<{ transfers: unknown }>(settlementSnapshotQuery, [period.id])).rows[0]?.transfers ?? [];
    await client.query(insertSettlementEventQuery, [period.id, reason, JSON.stringify({ transfers: snapshot })]);
    await client.query(deleteSettlementsQuery, [period.id]);
    await client.query(reopenPeriodQuery, [period.id]);
    await client.query("commit");

    return NextResponse.json({ period: { year, month, status: "open" } });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to reopen settlement period", error);
    return NextResponse.json({ error: "settlement_reopen_failed" }, { status: 500 });
  } finally {
    client.release();
  }
}
''')
PY

git diff --check
python3 - <<'PY'
from pathlib import Path
path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('''type DashboardResponse = {
  expenses: Expense[];
  period: Period & { status: SettlementPeriodStatus };
};
''', '''type SettlementPeriodEvent = {
  action: "settled" | "reopened";
  reason: string | null;
  settlementSnapshot: unknown;
  createdAt: string;
};

type DashboardResponse = {
  expenses: Expense[];
  period: Period & { status: SettlementPeriodStatus; events: SettlementPeriodEvent[] };
};
''', 1)
text = text.replace('''  const [periodStatus, setPeriodStatus] = useState<SettlementPeriodStatus>("open");
''', '''  const [periodStatus, setPeriodStatus] = useState<SettlementPeriodStatus>("open");
  const [periodEvents, setPeriodEvents] = useState<SettlementPeriodEvent[]>([]);
''', 1)
text = text.replace('''  const [isSettling, setIsSettling] = useState(false);
''', '''  const [isSettling, setIsSettling] = useState(false);
  const [isReopening, setIsReopening] = useState(false);
''', 1)
text = text.replace('''          setExpenses(data.expenses);
          setPeriodStatus(data.period.status);
''', '''          setExpenses(data.expenses);
          setPeriodStatus(data.period.status);
          setPeriodEvents(data.period.events ?? []);
''', 1)
text = text.replace('''          setExpenses([]);
          setSaveError("支出を読み込めませんでした。通信状態を確認してください。");
''', '''          setExpenses([]);
          setPeriodEvents([]);
          setSaveError("支出を読み込めませんでした。通信状態を確認してください。");
''', 1)
needle = '''  const expenseCard = (expense: Expense, options?: { showSplitPicker?: boolean; showManagement?: boolean }) => (
'''
insert = '''  const reopenPeriod = async () => {
    const reason = window.prompt("精算を取り消す理由を入力してください。");
    if (!reason?.trim()) return;

    setSaveError(null);
    setIsReopening(true);

    try {
      const response = await fetch("/api/settlements/reopen", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ ...selectedPeriod, reason: reason.trim() }),
      });

      if (!response.ok) throw new Error("settlement_reopen_failed");
      setPeriodStatus("open");
      setPeriodEvents((current) => [{
        action: "reopened",
        reason: reason.trim(),
        settlementSnapshot: null,
        createdAt: new Date().toISOString(),
      }, ...current]);
    } catch {
      setSaveError("精算を取り消せませんでした。通信状態を確認してください。");
    } finally {
      setIsReopening(false);
    }
  };

'''
if needle not in text:
    raise SystemExit('UI function insertion point missing')
text = text.replace(needle, insert + needle, 1)
needle = '''        <button className="action-button" type="button" disabled={stats.reviewCount > 0 || pendingExpenseUpdates > 0 || periodStatus === "settled" || isSettling} onClick={() => void settlePeriod()}>
          {periodStatus === "settled" ? "精算済み" : isSettling ? "確定中..." : pendingExpenseUpdates > 0 ? "保存中..." : "精算済みにする"}
        </button>
'''
replace = '''        <div className="settlement-actions">
          <button className="action-button" type="button" disabled={stats.reviewCount > 0 || pendingExpenseUpdates > 0 || periodStatus === "settled" || isSettling || isReopening} onClick={() => void settlePeriod()}>
            {periodStatus === "settled" ? "精算済み" : isSettling ? "確定中..." : pendingExpenseUpdates > 0 ? "保存中..." : "精算済みにする"}
          </button>
          {periodStatus === "settled" ? (
            <button className="chip chip--danger" type="button" disabled={isReopening} onClick={() => void reopenPeriod()}>
              {isReopening ? "取り消し中..." : "精算を取り消す"}
            </button>
          ) : null}
        </div>
'''
if needle not in text:
    raise SystemExit('settlement actions target missing')
text = text.replace(needle, replace, 1)
needle = '''      <div className="timeline timeline--wide">{timeline.map((expense) => expenseCard(expense, { showSplitPicker: expense.scope !== "personal" }))}</div>
'''
replace = '''      <div className="timeline timeline--wide">{timeline.map((expense) => expenseCard(expense, { showSplitPicker: expense.scope !== "personal" }))}</div>

      {periodEvents.length > 0 ? (
        <div className="settlement-history">
          <h3 className="section__title">精算履歴</h3>
          <div className="timeline">
            {periodEvents.map((event, index) => (
              <article key={`${event.action}-${event.createdAt}-${index}`} className="timeline-item">
                <p className="timeline-item__name">{event.action === "settled" ? "精算を確定" : "精算を取り消し"}</p>
                <p className="timeline-item__detail">
                  {new Date(event.createdAt).toLocaleString("ja-JP")}
                  {event.reason ? ` ・ 理由: ${event.reason}` : ""}
                </p>
              </article>
            ))}
          </div>
        </div>
      ) : null}
'''
if needle not in text:
    raise SystemExit('settlement history insertion target missing')
path.write_text(text.replace(needle, replace, 1))

css = Path('src/app/globals.css')
text = css.read_text()
needle = '''.action-button:disabled {
'''
insert = '''.settlement-actions {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 8px;
}

.settlement-history {
  margin-top: 18px;
}

.settlement-history .section__title {
  margin-bottom: 10px;
}

'''
if needle not in text:
    raise SystemExit('css insertion target missing')
css.write_text(text.replace(needle, insert + needle, 1))
PY

git diff --check
python3 - <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
needle = '''    const lockedDelete = await DELETE(
      new Request(`http://localhost/api/expenses/${testExpenseId}`, { method: "DELETE" }),
      { params: Promise.resolve({ id: testExpenseId }) },
    );
    assert.equal(lockedDelete.status, 409);
'''
replace = needle + '''
    const { POST: reopenPeriod } = await import("../../src/app/api/settlements/reopen/route.ts");
    const invalidReopen = await reopenPeriod(new Request("http://localhost/api/settlements/reopen", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ ...testPeriod, reason: "" }),
    }));
    assert.equal(invalidReopen.status, 400);

    const reopened = await reopenPeriod(new Request("http://localhost/api/settlements/reopen", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ ...testPeriod, reason: "登録漏れを修正する" }),
    }));
    assert.equal(reopened.status, 200);

    const reopenedPeriod = (await pool.query<{ status: string; settled_at: string | null }>(`
      select status, settled_at::text
      from settlement_periods
      where year = $1 and month = $2
    `, [testPeriod.year, testPeriod.month])).rows[0];
    assert.deepEqual(reopenedPeriod, { status: "open", settled_at: null });
    assert.equal((await pool.query(`
      select s.id
      from settlements s
      join settlement_periods sp on sp.id = s.period_id
      where sp.year = $1 and sp.month = $2
    `, [testPeriod.year, testPeriod.month])).rowCount, 0);

    const reopenedUpdate = await PATCH(
      new Request(`http://localhost/api/expenses/${testExpenseId}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ scope: "shared", splitPreset: "a70" }),
      }),
      { params: Promise.resolve({ id: testExpenseId }) },
    );
    assert.equal(reopenedUpdate.status, 200);

    const resettled = await POST(new Request("http://localhost/api/settlements", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(testPeriod),
    }));
    assert.equal(resettled.status, 201);

    const events = (await pool.query<{ action: string; reason: string | null }>(`
      select spe.action, spe.reason
      from settlement_period_events spe
      join settlement_periods sp on sp.id = spe.period_id
      where sp.year = $1 and sp.month = $2
      order by spe.created_at asc
    `, [testPeriod.year, testPeriod.month])).rows;
    assert.deepEqual(events, [
      { action: "settled", reason: null },
      { action: "reopened", reason: "登録漏れを修正する" },
      { action: "settled", reason: null },
    ]);
'''
if needle not in text:
    raise SystemExit('integration reopen insertion target missing')
path.write_text(text.replace(needle, replace, 1))

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
needle = '''  await page.getByRole("button", { name: "履歴", exact: true }).click();
  await expect(page.getByRole("button", { name: "編集", exact: true }).first()).toBeDisabled();
  await expect(page.getByRole("button", { name: "削除", exact: true }).first()).toBeDisabled();
'''
replace = needle + '''
  await page.getByRole("button", { name: "精算", exact: true }).click();
  page.once("dialog", (dialog) => dialog.accept("登録漏れを修正する"));
  await page.getByRole("button", { name: "精算を取り消す", exact: true }).click();
  await expect(page.getByRole("button", { name: "精算済みにする", exact: true })).toBeEnabled();
  await expect(page.getByText("精算を取り消し", { exact: true })).toBeVisible();
  await expect(page.getByText(/理由: 登録漏れを修正する/)).toBeVisible();
  await expect(page.getByLabel("支出を入力")).toBeEnabled();
'''
if needle not in text:
    raise SystemExit('e2e reopen insertion target missing')
path.write_text(text.replace(needle, replace, 1))
PY

git diff --check
git status --short --branch && printf '\n-- stat --\n' && git diff --stat
date '+%Y-%m-%d %Z' && printf '\n-- seeded expense dates --\n' && grep -n "2026-0[456]" postgres/migrations/003_seed_local_data.sql
sed -n '1,280p' postgres/migrations/003_seed_local_data.sql
python3 - <<'PY'
from pathlib import Path
path = Path('postgres/migrations/003_seed_local_data.sql')
text = path.read_text()
text = text.replace("date '2026-05-01'", "date_trunc('month', current_date)::date")
text = text.replace("date '2026-05-12'", "date_trunc('month', current_date)::date + 11")
text = text.replace("date '2026-05-14'", "date_trunc('month', current_date)::date + 13")
text = text.replace("date '2026-05-18'", "date_trunc('month', current_date)::date + 17")
text = text.replace("'5月分の家賃'", "'今月分の家賃'")
text = text.replace("  2026,\n  5,", "  extract(year from current_date)::int,\n  extract(month from current_date)::int,")
text = text.replace("date '2026-05-05'", "date_trunc('month', current_date)::date + 4")
path.write_text(text)

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
insert = '''function periodLabel(date: Date) {
  return `${date.getFullYear()}年${date.getMonth() + 1}月`;
}

const currentMonthLabel = periodLabel(new Date());
const previousMonthLabel = periodLabel(new Date(new Date().getFullYear(), new Date().getMonth() - 1, 1));

'''
needle = '''test.beforeEach(() => {
'''
if needle not in text:
    raise SystemExit('e2e helper insertion point missing')
text = text.replace(needle, insert + needle, 1)
text = text.replace('''  await expect(page.getByText("2026年5月")).toBeVisible();
''', '''  await expect(page.getByText(currentMonthLabel)).toBeVisible();
''')
text = text.replace('''  await expect(page.getByText("2026年4月")).toBeVisible();
''', '''  await expect(page.getByText(previousMonthLabel)).toBeVisible();
''')
path.write_text(text)
PY

git diff --check
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
python3 - <<'PY'
from pathlib import Path
path = Path('postgres/migrations/003_seed_local_data.sql')
text = path.read_text()
start = text.index("insert into settlement_periods")
end = text.index("insert into shared_wallet_contributions", start)
replacement = '''insert into settlement_periods (household_id, year, month, status, settled_at)
values (
  'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',
  extract(year from current_date)::int,
  extract(month from current_date)::int,
  'open',
  null
)
on conflict (household_id, year, month) do update
set
  status = excluded.status,
  settled_at = excluded.settled_at;

'''
path.write_text(text[:start] + replacement + text[end:])
PY

git diff --check
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
sed -n '1,260p' test-results/kakeibo-要確認を共通費へ整理して月次精算を確定する-chromium/error-context.md
docker compose exec -T db psql -U kakeibo -d kakeibo_test -c "select year, month, status, settled_at from settlement_periods order by year desc, month desc;" -c "select expense_date, scope, memo from expenses where id = 'dddddddd-dddd-4ddd-8ddd-ddddddddddd4';"
python3 - <<'PY'
from pathlib import Path

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
text = text.replace('''  await reviewCard.getByRole("button", { name: "共通費", exact: true }).click();
  await expect(reviewCard).toHaveCount(0);
''', '''  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "PATCH" && response.ok()),
    reviewCard.getByRole("button", { name: "共通費", exact: true }).click(),
  ]);
  await expect(reviewCard).toHaveCount(0);
''', 1)
text = text.replace('''  page.once("dialog", (dialog) => dialog.accept("登録漏れを修正する"));
  await page.getByRole("button", { name: "精算を取り消す", exact: true }).click();
''', '''  page.once("dialog", (dialog) => dialog.accept("登録漏れを修正する"));
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/settlements/reopen") && response.request().method() === "POST" && response.ok()),
    page.getByRole("button", { name: "精算を取り消す", exact: true }).click(),
  ]);
''', 1)
path.write_text(text)

path = Path('postgres/migrations/003_seed_local_data.sql')
text = path.read_text()
needle = '''on conflict (household_id, year, month) do update
set
  status = excluded.status,
  settled_at = excluded.settled_at;

insert into shared_wallet_contributions'''
replace = '''on conflict (household_id, year, month) do update
set
  status = excluded.status,
  settled_at = excluded.settled_at;

delete from settlement_period_events
where period_id in (
  select id
  from settlement_periods
  where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'
    and year = extract(year from current_date)::int
    and month = extract(month from current_date)::int
);

delete from settlements
where period_id in (
  select id
  from settlement_periods
  where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'
    and year = extract(year from current_date)::int
    and month = extract(month from current_date)::int
);

insert into shared_wallet_contributions'''
if needle not in text:
    raise SystemExit('seed cleanup insertion point missing')
path.write_text(text.replace(needle, replace, 1))
PY

git diff --check
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
ss -ltnp | grep -E ':(3021|3022)\b' || true && printf '\n-- diff check --\n' && git diff --check && printf '\n-- status --\n' && git status --short --branch
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test
git diff --check && printf '\n-- tracked status --\n' && git status --short --branch && printf '\n-- untracked detail --\n' && find .agent scripts -maxdepth 3 -type f 2>/dev/null | sort
grep -n "settlement_period_events\|apply_incremental_migrations" postgres/migrations/001_initial_schema.sql postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/004_add_settlement_period_events.sql supabase/migrations/202606010001_add_settlement_period_events.sql && printf '\n-- api --\n' && grep -n "insertSettlementEventQuery\|settlementPeriodEventsQuery\|export async function POST" src/app/api/settlements/route.ts src/app/api/settlements/reopen/route.ts src/app/api/dashboard/route.ts && printf '\n-- ui --\n' && grep -n "reopenPeriod\|精算を取り消す\|精算履歴" src/components/kakeibo-dashboard.tsx
git status --short --branch && printf '\n-- tracked diff stat --\n' && git diff --stat && printf '\n-- untracked --\n' && git ls-files --others --exclude-standard
git diff -- postgres/migrations/001_initial_schema.sql postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/api/dashboard/route.ts src/app/api/settlements/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && printf '\n-- new migrations --\n' && sed -n '1,180p' postgres/migrations/004_add_settlement_period_events.sql && printf '\n-- reopen route --\n' && sed -n '1,180p' src/app/api/settlements/reopen/route.ts && printf '\n-- supabase migration --\n' && sed -n '1,180p' supabase/migrations/202606010001_add_settlement_period_events.sql
git add postgres/migrations/001_initial_schema.sql postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/004_add_settlement_period_events.sql supabase/migrations/202606010001_add_settlement_period_events.sql src/app/api/dashboard/route.ts src/app/api/settlements/route.ts src/app/api/settlements/reopen/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && printf '\n-- status --\n' && git status --short
git commit -m '月次精算の取り消し履歴を追加する' -m '- 精算確定と取り消しを履歴イベントとして保存する
- 理由を必須にした精算再オープンAPIと画面操作を追加する
- seedを現在月へ追従させ、月替わりでもE2Eを安定させる
- 統合テストとPlaywrightで再編集・再確定まで確認する'
git log -2 --format='%h %s' && printf '\n-- latest stat --\n' && git show --stat --oneline --summary HEAD
gh issue view 2 --json number,title,body,url,state && printf '\n-- status --\n' && git status --short --branch
sed -n '1,320p' src/lib/expense-utils.ts && printf '\n-- tests --\n' && sed -n '1,280p' tests/expenses/expense-utils.test.ts && printf '\n-- split tests --\n' && sed -n '1,240p' tests/expenses/summary.presets.test.ts
grep -RIn "SplitPreset\|splitPreset\|a70\|b70\|equal" src tests --exclude-dir=.next | head -n 260
sed -n '1,250p' src/features/dashboard/dashboard.mapper.ts && printf '\n-- calculator --\n' && sed -n '1,280p' src/features/settlement/settlement.calculator.ts && printf '\n-- types --\n' && sed -n '1,220p' src/features/settlement/settlement.types.ts
python3 - <<'PY'
from pathlib import Path

path = Path('src/lib/expense-utils.ts')
text = path.read_text()
text = text.replace('export type SplitPreset = "equal" | "a70" | "b70";', 'export type SplitPreset = "equal" | "a60" | "b60" | "a70" | "b70";')
text = text.replace('''export function splitPresetLabel(preset: SplitPreset = "equal") {
  if (preset === "a70") return "葵 7 : 蓮 3";
  if (preset === "b70") return "葵 3 : 蓮 7";
  return "折半 5 : 5";
}

export function splitRatesForPreset(preset: SplitPreset | undefined): SplitRates {
  if (preset === "a70") {
    return { a: 0.7, b: 0.3 };
  }

  if (preset === "b70") {
    return { a: 0.3, b: 0.7 };
  }

  return { a: 0.5, b: 0.5 };
}
''', '''export function splitPresetLabel(preset: SplitPreset = "equal") {
  if (preset === "a60") return "葵 6 : 蓮 4";
  if (preset === "b60") return "葵 4 : 蓮 6";
  if (preset === "a70") return "葵 7 : 蓮 3";
  if (preset === "b70") return "葵 3 : 蓮 7";
  return "折半 5 : 5";
}

export function splitRatesForPreset(preset: SplitPreset | undefined): SplitRates {
  if (preset === "a60") {
    return { a: 0.6, b: 0.4 };
  }

  if (preset === "b60") {
    return { a: 0.4, b: 0.6 };
  }

  if (preset === "a70") {
    return { a: 0.7, b: 0.3 };
  }

  if (preset === "b70") {
    return { a: 0.3, b: 0.7 };
  }

  return { a: 0.5, b: 0.5 };
}
''')
path.write_text(text)

path = Path('src/features/dashboard/dashboard.mapper.ts')
text = path.read_text()
text = text.replace('''  if (splitRates.a === 0.5 && splitRates.b === 0.5) splitPreset = "equal";
  if (splitRates.a === 0.7 && splitRates.b === 0.3) splitPreset = "a70";
  if (splitRates.a === 0.3 && splitRates.b === 0.7) splitPreset = "b70";
''', '''  if (splitRates.a === 0.5 && splitRates.b === 0.5) splitPreset = "equal";
  if (splitRates.a === 0.6 && splitRates.b === 0.4) splitPreset = "a60";
  if (splitRates.a === 0.4 && splitRates.b === 0.6) splitPreset = "b60";
  if (splitRates.a === 0.7 && splitRates.b === 0.3) splitPreset = "a70";
  if (splitRates.a === 0.3 && splitRates.b === 0.7) splitPreset = "b70";
''')
path.write_text(text)

path = Path('src/app/api/expenses/route.ts')
text = path.read_text()
text = text.replace('z.enum(["equal", "a70", "b70"])', 'z.enum(["equal", "a60", "b60", "a70", "b70"])')
text = text.replace('''  if (preset === "a70") {
''', '''  if (preset === "a60") {
    return [
      { memberId: first.id, rate: 0.6 },
      { memberId: second.id, rate: 0.4 },
    ];
  }

  if (preset === "b60") {
    return [
      { memberId: first.id, rate: 0.4 },
      { memberId: second.id, rate: 0.6 },
    ];
  }

  if (preset === "a70") {
''', 1)
path.write_text(text)

path = Path('src/app/api/expenses/[id]/route.ts')
text = path.read_text()
text = text.replace('z.enum(["equal", "a70", "b70"])', 'z.enum(["equal", "a60", "b60", "a70", "b70"])')
text = text.replace('''function splitRatesFor(preset: SplitPreset) {
  if (preset === "a70") return [0.7, 0.3];
  if (preset === "b70") return [0.3, 0.7];
  return [0.5, 0.5];
}
''', '''function splitRatesFor(preset: SplitPreset) {
  if (preset === "a60") return [0.6, 0.4];
  if (preset === "b60") return [0.4, 0.6];
  if (preset === "a70") return [0.7, 0.3];
  if (preset === "b70") return [0.3, 0.7];
  return [0.5, 0.5];
}
''')
path.write_text(text)

path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('const splitPresets: SplitPreset[] = ["equal", "a70", "b70"];', 'const splitPresets: SplitPreset[] = ["equal", "a60", "b60", "a70", "b70"];')
text = text.replace('''      <div className="timeline timeline--wide">{timeline.map((expense) => expenseCard(expense, { showManagement: true }))}</div>
''', '''      <div className="timeline timeline--wide">{timeline.map((expense) => expenseCard(expense, { showManagement: true, showSplitPicker: expense.scope === "shared" }))}</div>
''')
path.write_text(text)
PY

git diff --check
sed -n '100,165p' src/app/api/expenses/route.ts && printf '\n-- patch route --\n' && sed -n '118,145p' 'src/app/api/expenses/[id]/route.ts' && printf '\n-- mapper --\n' && sed -n '68,92p' src/features/dashboard/dashboard.mapper.ts && printf '\n-- ui history --\n' && sed -n '720,750p' src/components/kakeibo-dashboard.tsx
sed -n '1,150p' tests/expenses/dashboard.mapper.test.ts && printf '\n-- integration relevant --\n' && sed -n '130,160p' tests/integration/api.integration.test.ts && printf '\n-- e2e tail --\n' && sed -n '90,145p' tests/e2e/kakeibo.spec.ts
python3 - <<'PY'
from pathlib import Path

path = Path('tests/expenses/expense-utils.test.ts')
text = path.read_text()
text = text.replace('''import { initialExpenses, memberName, summaryFor, type Expense } from "../../src/lib/expense-utils.ts";
''', '''import { initialExpenses, memberName, splitPresetLabel, splitRatesForPreset, summaryFor, type Expense } from "../../src/lib/expense-utils.ts";
''', 1)
needle = '''  it("applies persisted custom split rates to settlement", () => {
'''
insert = '''  it("supports rough 6:4 split presets", () => {
    assert.equal(splitPresetLabel("a60"), "葵 6 : 蓮 4");
    assert.equal(splitPresetLabel("b60"), "葵 4 : 蓮 6");
    assert.deepEqual(splitRatesForPreset("a60"), { a: 0.6, b: 0.4 });
    assert.deepEqual(splitRatesForPreset("b60"), { a: 0.4, b: 0.6 });
  });

'''
if needle not in text:
    raise SystemExit('utils test insertion point missing')
text = text.replace(needle, insert + needle, 1)
text = text.replace('''        splitPreset: "a70",
''', '''        splitPreset: "a60",
''', 1)
text = text.replace('''    assert.equal(summary.balanceA, -7000);
    assert.equal(summary.balanceB, 7000);
''', '''    assert.equal(summary.balanceA, -6000);
    assert.equal(summary.balanceB, 6000);
''', 1)
text = text.replace('''    assert.equal(summary.settlement, 7000);
''', '''    assert.equal(summary.settlement, 6000);
''', 1)
path.write_text(text)

path = Path('tests/expenses/dashboard.mapper.test.ts')
text = path.read_text()
needle = '''  it("keeps persisted custom split rates for settlement", () => {
'''
insert = '''  it("restores rough split presets from persisted rates", () => {
    const [expense] = mapDashboardExpenses(
      [
        { id: "member-a", display_name: "葵" },
        { id: "member-b", display_name: "蓮" },
      ],
      [
        {
          id: "expense-dinner",
          title: "駅前イタリアン",
          amount: 6200,
          paid_by_member_id: "member-b",
          scope: "shared",
          category: "外食",
          memo: "駅前イタリアン",
          expense_date: "2026-05-18",
          created_at: "2026-05-18T00:00:00.000Z",
          splits: [
            { member_id: "member-a", rate: "0.6000" },
            { member_id: "member-b", rate: "0.4000" },
          ],
        },
      ],
    );

    assert.equal(expense.splitPreset, "a60");
    assert.deepEqual(expense.splitRates, { a: 0.6, b: 0.4 });
  });

'''
if needle not in text:
    raise SystemExit('mapper test insertion point missing')
path.write_text(text.replace(needle, insert + needle, 1))

path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
text = text.replace('''        body: JSON.stringify({ scope: "shared", splitPreset: "a70" }),
''', '''        body: JSON.stringify({ scope: "shared", splitPreset: "a60" }),
''', 1)
text = text.replace('''    assert.deepEqual(rows.map((row) => row.rate), ["0.7000", "0.3000"]);
''', '''    assert.deepEqual(rows.map((row) => row.rate), ["0.6000", "0.4000"]);
''', 1)
path.write_text(text)

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
needle = '''  const expenseCard = page.locator(".timeline-item").filter({ hasText: "スーパーライフ" });
  await expenseCard.getByRole("button", { name: "編集", exact: true }).click();
'''
replace = '''  const expenseCard = page.locator(".timeline-item").filter({ hasText: "スーパーライフ" });
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "PATCH" && response.ok()),
    expenseCard.getByRole("button", { name: "葵 4 : 蓮 6", exact: true }).click(),
  ]);
  await expect(expenseCard.locator(".timeline-item__detail")).toContainText("葵 4 : 蓮 6");

  await expenseCard.getByRole("button", { name: "編集", exact: true }).click();
'''
if needle not in text:
    raise SystemExit('e2e split insertion point missing')
path.write_text(text.replace(needle, replace, 1))
PY

git diff --check
grep -RIn 'z.enum(\["equal"\|SplitPreset =\|splitPresets:' src --exclude-dir=.next && printf '\n-- status --\n' && git status --short --branch
grep -n 'SplitPreset\|a60\|b60' src/lib/expense-utils.ts src/features/dashboard/dashboard.mapper.ts src/app/api/expenses/route.ts 'src/app/api/expenses/[id]/route.ts' src/components/kakeibo-dashboard.tsx && printf '\n-- tests --\n' && grep -n 'rough 6:4\|rough split\|葵 4 : 蓮 6\|0.6000' tests/expenses/expense-utils.test.ts tests/expenses/dashboard.mapper.test.ts tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
gh issue view 10 --json number,title,body,url,state
git add 'src/app/api/expenses/[id]/route.ts' src/app/api/expenses/route.ts src/components/kakeibo-dashboard.tsx src/features/dashboard/dashboard.mapper.ts src/lib/expense-utils.ts tests/e2e/kakeibo.spec.ts tests/expenses/dashboard.mapper.test.ts tests/expenses/expense-utils.test.ts tests/integration/api.integration.test.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && git commit -m '負担割合プリセットに4対6と6対4を追加する' -m '- 共通費の負担割合に4:6と6:4を追加する
- DB保存済みの割合からプリセットを復元する
- 履歴画面から割合を変更できるようにする
- 単体・統合・Playwrightで保存と表示を確認する' && printf '\n-- status --\n' && git status --short --branch
sed -n '1,270p' src/app/api/expenses/route.ts && printf '\n-- dashboard input --\n' && sed -n '140,230p' src/components/kakeibo-dashboard.tsx && printf '\n-- composer --\n' && sed -n '780,860p' src/components/kakeibo-dashboard.tsx
sed -n '1,360p' tests/integration/api.integration.test.ts && printf '\n-- e2e --\n' && sed -n '1,155p' tests/e2e/kakeibo.spec.ts && printf '\n-- css composer --\n' && sed -n '360,500p' src/app/globals.css
find src/app/api -maxdepth 4 -type f | sort && printf '\n-- api docs excerpts --\n' && grep -RIn "GET /api\|POST /api/expenses\|dashboard" Docs README.md | head -n 180
python3 - <<'PY'
from pathlib import Path

route = Path('src/app/api/expenses/suggestions/route.ts')
route.parent.mkdir(parents=True, exist_ok=True)
route.write_text('''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../../lib/db.ts";
import type { ExpenseScope } from "../../../../lib/expense-utils.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const suggestionsQuerySchema = z.object({
  q: z.string().trim().max(100).optional().transform((value) => value ?? ""),
});

type HouseholdRow = { id: string };
type SuggestionRow = {
  title: string;
  category: string;
  scope: ExpenseScope;
  latest_amount: number;
  usage_count: number;
};

// TODO(security): 認証統合時に、ログイン中ユーザーの所属世帯をセッションから解決する。
const householdQuery = `
  select id
  from households
  order by created_at asc
  limit 1
`;

const suggestionsQuery = `
  with expense_history as (
    select
      coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name) as title,
      c.name as category,
      e.scope,
      e.amount,
      e.created_at,
      count(*) over (
        partition by coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name), c.name, e.scope
      ) as variant_count
    from expenses e
    join categories c on c.id = e.category_id
    where e.household_id = $1
      and ($2 = '' or coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name) ilike '%' || $2 || '%')
  ),
  ranked_history as (
    select
      title,
      category,
      scope,
      amount as latest_amount,
      count(*) over (partition by title) as usage_count,
      row_number() over (
        partition by title
        order by variant_count desc, created_at desc
      ) as preference_rank
    from expense_history
  )
  select title, category, scope, latest_amount, usage_count::int
  from ranked_history
  where preference_rank = 1
  order by usage_count desc, title asc
  limit 6
`;

export async function GET(request: Request) {
  try {
    const url = new URL(request.url);
    const result = suggestionsQuerySchema.safeParse({ q: url.searchParams.get("q") ?? undefined });
    if (!result.success) {
      return NextResponse.json({ error: "invalid_suggestion_query" }, { status: 400 });
    }

    const pool = db();
    const household = (await pool.query<HouseholdRow>(householdQuery)).rows[0];
    if (!household) {
      return NextResponse.json({ suggestions: [] });
    }

    const suggestions = (await pool.query<SuggestionRow>(suggestionsQuery, [household.id, result.data.q])).rows;
    return NextResponse.json({
      suggestions: suggestions.map((suggestion) => ({
        title: suggestion.title,
        category: suggestion.category,
        scope: suggestion.scope,
        latestAmount: suggestion.latest_amount,
        usageCount: suggestion.usage_count,
      })),
    });
  } catch (error) {
    console.error("Failed to load expense suggestions", error);
    return NextResponse.json({ error: "expense_suggestions_failed" }, { status: 500 });
  }
}
''')

path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('''type CreateExpenseResponse = {
  expense: Expense;
};
''', '''type CreateExpenseResponse = {
  expense: Expense;
};

type ExpenseSuggestion = {
  title: string;
  category: string;
  scope: ExpenseScope;
  latestAmount: number;
  usageCount: number;
};
''', 1)
text = text.replace('''  const [composerValue, setComposerValue] = useState("");
''', '''  const [composerValue, setComposerValue] = useState("");
  const [composerSuggestions, setComposerSuggestions] = useState<ExpenseSuggestion[]>([]);
  const [selectedSuggestion, setSelectedSuggestion] = useState<ExpenseSuggestion | null>(null);
''', 1)
needle = '''  const stats = summaryFor(expenses);
'''
insert = '''  useEffect(() => {
    if (!isComposerExpanded || !isCurrentPeriod || isPeriodLocked) {
      setComposerSuggestions([]);
      return;
    }

    const query = composerValue.replace(/\\d[\\d,]*/g, "").trim();
    const controller = new AbortController();
    const timer = window.setTimeout(async () => {
      try {
        const response = await fetch(`/api/expenses/suggestions?q=${encodeURIComponent(query)}`, { signal: controller.signal });
        if (!response.ok) return;
        const data = (await response.json()) as { suggestions: ExpenseSuggestion[] };
        setComposerSuggestions(data.suggestions);
      } catch (error) {
        if ((error as Error).name !== "AbortError") setComposerSuggestions([]);
      }
    }, 150);

    return () => {
      window.clearTimeout(timer);
      controller.abort();
    };
  }, [composerValue, isComposerExpanded, isCurrentPeriod, isPeriodLocked]);

'''
if needle not in text:
    raise SystemExit('UI suggestion effect insertion point missing')
text = text.replace(needle, insert + needle, 1)
text = text.replace('''    const draft = parseExpenseInput(title, selectedPayer);
    if (!draft) return;

    const fallbackExpense: Expense = {
''', '''    const draft = parseExpenseInput(title, selectedPayer);
    if (!draft) return;
    const suggestion = selectedSuggestion?.title === draft.title ? selectedSuggestion : null;

    const fallbackExpense: Expense = {
''', 1)
text = text.replace('''      scope: draft.scope,
      category: draft.category,
''', '''      scope: suggestion?.scope ?? draft.scope,
      category: suggestion?.category ?? draft.category,
''', 1)
text = text.replace('''    setComposerValue("");
    setIsComposerExpanded(false);
''', '''    setComposerValue("");
    setSelectedSuggestion(null);
    setComposerSuggestions([]);
    setIsComposerExpanded(false);
''', 1)
text = text.replace('''            onChange={(event) => setComposerValue(event.target.value)}
''', '''            onChange={(event) => {
              setComposerValue(event.target.value);
              setSelectedSuggestion(null);
            }}
''', 1)
needle = '''        <div className="composer__quick" aria-disabled={isComposerDisabled}>
'''
insert = '''        {composerSuggestions.length > 0 ? (
          <div className="composer__suggestions" aria-label="店舗候補">
            {composerSuggestions.map((suggestion) => (
              <button
                key={`${suggestion.title}-${suggestion.category}-${suggestion.scope}`}
                className="composer-suggestion"
                type="button"
                disabled={isComposerDisabled}
                onClick={() => {
                  setComposerValue(`${suggestion.title} ${suggestion.latestAmount}`);
                  setSelectedSuggestion(suggestion);
                }}
              >
                <span>{suggestion.title}</span>
                <small>{suggestion.category} ・ {suggestion.scope === "shared" ? "共通費" : suggestion.scope === "personal" ? "個人費" : "要確認"}</small>
              </button>
            ))}
          </div>
        ) : null}
'''
if needle not in text:
    raise SystemExit('UI suggestion list insertion point missing')
text = text.replace(needle, insert + needle, 1)
path.write_text(text)

css = Path('src/app/globals.css')
text = css.read_text()
needle = '''.composer__quick {
'''
insert = '''.composer__suggestions {
  display: flex;
  gap: 8px;
  margin-top: 10px;
  overflow-x: auto;
  scrollbar-width: none;
}

.composer__suggestions::-webkit-scrollbar {
  display: none;
}

.composer-suggestion {
  display: grid;
  min-width: max-content;
  gap: 2px;
  padding: 8px 10px;
  border: 1px solid rgba(15, 118, 110, 0.18);
  border-radius: 10px;
  background: var(--primary-ghost);
  color: var(--text);
  text-align: left;
}

.composer-suggestion span {
  font-size: 13px;
  font-weight: 700;
}

.composer-suggestion small {
  color: var(--muted);
  font-size: 11px;
}

.composer:not([data-expanded="true"]) .composer__suggestions {
  display: none;
}

'''
if needle not in text:
    raise SystemExit('CSS suggestion insertion point missing')
css.write_text(text.replace(needle, insert + needle, 1))
PY

git diff --check
sed -n '105,215p' src/components/kakeibo-dashboard.tsx && printf '\n-- composer --\n' && sed -n '830,925p' src/components/kakeibo-dashboard.tsx
sed -n '1,240p' src/app/api/expenses/suggestions/route.ts && printf '\n-- status --\n' && git status --short --branch
python3 - <<'PY'
from pathlib import Path
path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
derived = '''  const stats = summaryFor(expenses);
  const nowPeriod = currentPeriod();
  const isCurrentPeriod = selectedPeriod.year === nowPeriod.year && selectedPeriod.month === nowPeriod.month;
  const isPeriodLocked = periodStatus === "settled";
  const isComposerDisabled = !isCurrentPeriod || isDashboardLoading || isPeriodLocked;
  const timeline = [...expenses].sort((left, right) => right.createdAt.localeCompare(left.createdAt));
  const reviewExpenses = expenses.filter((expense) => expense.scope === "review");

'''
if text.count(derived) != 1:
    raise SystemExit('derived state block not found exactly once')
text = text.replace(derived, '', 1)
needle = '''  useEffect(() => {
    if (!isComposerExpanded || !isCurrentPeriod || isPeriodLocked) {
'''
if needle not in text:
    raise SystemExit('suggestion effect not found')
text = text.replace(needle, derived + needle, 1)
text = text.replace('''              setComposerValue("ファミリーマート 1420 要確認");
              setIsComposerExpanded(true);
''', '''              setComposerValue("ファミリーマート 1420 要確認");
              setSelectedSuggestion(null);
              setIsComposerExpanded(true);
''', 1)
text = text.replace('''            <button key={item} className="chip" type="button" disabled={isComposerDisabled} onClick={() => setComposerValue(item)}>
''', '''            <button key={item} className="chip" type="button" disabled={isComposerDisabled} onClick={() => {
              setComposerValue(item);
              setSelectedSuggestion(null);
            }}>
''', 1)
path.write_text(text)
PY

git diff --check
python3 - <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
needle = '''  it("rejects malformed expense ids before querying PostgreSQL", async () => {
'''
insert = '''  it("suggests stores with their usual category and scope", async () => {
    const { GET } = await import("../../src/app/api/expenses/suggestions/route.ts");
    const response = await GET(new Request("http://localhost/api/expenses/suggestions?q=スーパー"));
    assert.equal(response.status, 200);

    const body = await response.json();
    assert.deepEqual(body.suggestions, [{
      title: "スーパーライフ",
      category: "食費",
      scope: "shared",
      latestAmount: 4280,
      usageCount: 1,
    }]);

    const invalidResponse = await GET(new Request(`http://localhost/api/expenses/suggestions?q=${"a".repeat(101)}`));
    assert.equal(invalidResponse.status, 400);
  });

'''
if needle not in text:
    raise SystemExit('integration suggestion insertion point missing')
path.write_text(text.replace(needle, insert + needle, 1))

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
needle = '''test("月を切り替えると過去月では支出追加を無効化する", async ({ page }) => {
'''
insert = '''test("店舗名の入力途中にカテゴリ付き候補を表示する", async ({ page }) => {
  await page.goto("/");

  const input = page.getByLabel("支出を入力");
  await input.fill("スーパー");
  const suggestion = page.getByRole("button", { name: /スーパーライフ.*食費.*共通費/ });
  await expect(suggestion).toBeVisible();
  await suggestion.click();
  await expect(input).toHaveValue("スーパーライフ 4280");
});

'''
if needle not in text:
    raise SystemExit('e2e suggestion insertion point missing')
path.write_text(text.replace(needle, insert + needle, 1))
PY

git diff --check
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep
kill 465691 465808 2>/dev/null || true
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
kill 465808 471771 2>/dev/null || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
printf '\n-- remaining kakeibo node --\n'
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
while ps -p 474825 >/dev/null 2>&1; do sleep 2; done
printf 'external build finished\n'
ss -ltnp | grep -E ':(3021|3022)\b' || true
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '店舗名の入力途中にカテゴリ付き候補を表示する'
sed -n '1,240p' src/app/api/expenses/suggestions/route.ts
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
python3 - <<'PY'
from pathlib import Path
path = Path('src/app/api/expenses/suggestions/route.ts')
text = path.read_text()
old = '''const suggestionsQuery = `
  with expense_history as (
    select
      coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name) as title,
      c.name as category,
      e.scope,
      e.amount,
      e.created_at,
      count(*) over (
        partition by coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name), c.name, e.scope
      ) as variant_count
    from expenses e
    join categories c on c.id = e.category_id
    where e.household_id = $1
      and ($2 = '' or coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name) ilike '%' || $2 || '%')
  ),
'''
new = '''const suggestionsQuery = `
  with expense_sources as (
    select
      case
        when c.kind = 'rent' then c.name
        else coalesce(nullif(split_part(e.memo, ' ', 1), ''), c.name)
      end as title,
      c.name as category,
      e.scope,
      e.amount,
      e.created_at
    from expenses e
    join categories c on c.id = e.category_id
    where e.household_id = $1
  ),
  expense_history as (
    select
      title,
      category,
      scope,
      amount,
      created_at,
      count(*) over (
        partition by title, category, scope
      ) as variant_count
    from expense_sources
    where $2 = '' or title ilike '%' || $2 || '%'
  ),
'''
if old not in text:
    raise SystemExit('expected SQL block not found')
path.write_text(text.replace(old, new))
PY
git diff -- src/app/api/expenses/suggestions/route.ts
sed -n '1,180p' src/app/api/expenses/suggestions/route.ts
git diff --check
git status --short --branch
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
npm run build
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test
ss -ltnp | grep -E ':(3021|3022)\b' || true
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
git diff --check
git diff --stat
git status --short --branch
git add src/app/api/expenses/suggestions/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git diff --cached --check
git diff --cached --stat
git status --short --branch
git commit -m 'feat: 店舗とカテゴリの入力候補を追加する' -m '- 支出履歴から店舗ごとのカテゴリと分類候補を集計する
- 入力途中の店舗候補を表示し、直近金額を差し込む
- 統合テストとPlaywrightで候補検索と選択を確認する'
git status --short --branch
git log -1 --oneline
gh issue view 11 --json number,title,body,state,url
grep -R "composer\|addExpense\|ExpenseScope\|review" -n src/components src/app/api tests | head -n 220
sed -n '1,280p' src/components/kakeibo-dashboard.tsx
sed -n '820,940p' src/components/kakeibo-dashboard.tsx
find src -maxdepth 4 -type f | sort
printf '\n-- public --\n'
find public -maxdepth 3 -type f | sort 2>/dev/null || true
printf '\n-- config refs --\n'
grep -R "manifest\|viewport\|metadata\|searchParams" -n src public 2>/dev/null || true
sed -n '1,260p' src/lib/expense-utils.ts
printf '\n-- expense api --\n'
sed -n '1,200p' src/app/api/expenses/route.ts
printf '\n-- layout/page --\n'
sed -n '1,180p' src/app/layout.tsx
sed -n '1,160p' src/app/page.tsx
sed -n '1,180p' tests/e2e/kakeibo.spec.ts
printf '\n-- css composer --\n'
grep -n "composer" src/app/globals.css | head -n 80
sed -n '760,930p' src/app/globals.css
sed -n '1,120p' playwright.config.ts
printf '\n-- package --\n'
sed -n '1,140p' package.json
printf '\n-- integration imports --\n'
sed -n '1,100p' tests/integration/api.integration.test.ts
sed -n '340,500p' src/app/globals.css
printf '\n-- test files --\n'
find tests -maxdepth 3 -type f | sort
python3 - <<'PY'
from pathlib import Path

path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('import { useEffect, useState } from "react";', 'import { useEffect, useRef, useState } from "react";')
text = text.replace('  const [isReopening, setIsReopening] = useState(false);\n', '  const [isReopening, setIsReopening] = useState(false);\n  const composerInputRef = useRef<HTMLInputElement>(null);\n  const didHandleQuickCompose = useRef(false);\n')
old = '''  const timeline = [...expenses].sort((left, right) => right.createdAt.localeCompare(left.createdAt));
  const reviewExpenses = expenses.filter((expense) => expense.scope === "review");

  useEffect(() => {
    if (!isComposerExpanded || !isCurrentPeriod || isPeriodLocked) {
'''
new = '''  const timeline = [...expenses].sort((left, right) => right.createdAt.localeCompare(left.createdAt));
  const reviewExpenses = expenses.filter((expense) => expense.scope === "review");

  useEffect(() => {
    if (didHandleQuickCompose.current || isComposerDisabled) return;
    if (new URLSearchParams(window.location.search).get("compose") !== "quick") return;

    didHandleQuickCompose.current = true;
    setIsComposerExpanded(true);
    window.requestAnimationFrame(() => composerInputRef.current?.focus());
  }, [isComposerDisabled]);

  useEffect(() => {
    if (!isComposerExpanded || !isCurrentPeriod || isPeriodLocked) {
'''
if old not in text:
    raise SystemExit('derived constants anchor not found')
text = text.replace(old, new)
text = text.replace('  const addExpense = async (title: string) => {', '  const addExpense = async (title: string, options?: { forceReview?: boolean }) => {')
old = '''    const suggestion = selectedSuggestion?.title === draft.title ? selectedSuggestion : null;

    const fallbackExpense: Expense = {
      id: `${Date.now()}`,
      title: draft.title,
      amount: draft.amount,
      paidBy: draft.paidBy,
      scope: suggestion?.scope ?? draft.scope,
      category: suggestion?.category ?? draft.category,
      createdAt: new Date().toISOString(),
      note: draft.scope === "review" ? "扱いを決める" : undefined,
      splitPreset: draft.scope === "shared" ? defaultSplitPreset : undefined,
    };
'''
new = '''    const suggestion = selectedSuggestion?.title === draft.title ? selectedSuggestion : null;
    const fallbackScope = options?.forceReview ? "review" : suggestion?.scope ?? draft.scope;

    const fallbackExpense: Expense = {
      id: `${Date.now()}`,
      title: draft.title,
      amount: draft.amount,
      paidBy: draft.paidBy,
      scope: fallbackScope,
      category: suggestion?.category ?? draft.category,
      createdAt: new Date().toISOString(),
      note: fallbackScope === "review" ? "扱いを決める" : undefined,
      splitPreset: fallbackScope === "shared" ? defaultSplitPreset : undefined,
    };
'''
if old not in text:
    raise SystemExit('fallback expense anchor not found')
text = text.replace(old, new)
text = text.replace('''          <input
            className="composer__input"
''', '''          <input
            ref={composerInputRef}
            className="composer__input"
''')
old = '''        <div className="composer__quick" aria-disabled={isComposerDisabled}>
          {quickInputs.map((item) => (
'''
new = '''        <div className="composer__quick" aria-disabled={isComposerDisabled}>
          <button className="chip chip--review" type="button" disabled={isComposerDisabled || !composerValue.trim()} onClick={() => void addExpense(composerValue, { forceReview: true })}>
            仮登録
          </button>
          {quickInputs.map((item) => (
'''
if old not in text:
    raise SystemExit('quick inputs anchor not found')
text = text.replace(old, new)
path.write_text(text)

manifest = Path('src/app/manifest.ts')
manifest.write_text('''import type { MetadataRoute } from "next";\n\nexport default function manifest(): MetadataRoute.Manifest {\n  return {\n    name: "Kakeibo",\n    short_name: "Kakeibo",\n    description: "同棲生活の精算ストレスを減らす共有費精算アプリ",\n    start_url: "/",\n    display: "standalone",\n    background_color: "#f8fafc",\n    theme_color: "#0f766e",\n    shortcuts: [\n      {\n        name: "支出を仮登録",\n        short_name: "仮登録",\n        description: "少額支出を要確認として素早く記録する",\n        url: "/?compose=quick",\n      },\n    ],\n  };\n}\n''')

spec = Path('tests/e2e/kakeibo.spec.ts')
text = spec.read_text()
anchor = '''test("店舗名の入力途中にカテゴリ付き候補を表示する", async ({ page }) => {\n'''
addition = '''test("PWAショートカットから少額支出を仮登録する", async ({ page }) => {\n  const manifestResponse = await page.request.get("/manifest.webmanifest");\n  expect(manifestResponse.ok()).toBeTruthy();\n  await expect(manifestResponse.json()).resolves.toMatchObject({\n    shortcuts: [{ name: "支出を仮登録", url: "/?compose=quick" }],\n  });\n\n  await page.goto("/?compose=quick");\n  const input = page.getByLabel("支出を入力");\n  await expect(input).toBeFocused();\n  await input.fill("ファミリーマート 680");\n  await Promise.all([\n    page.waitForResponse((response) => response.url().endsWith("/api/expenses") && response.request().method() === "POST" && response.ok()),\n    page.getByRole("button", { name: "仮登録", exact: true }).click(),\n  ]);\n\n  await expect(page.locator(".review-card").filter({ hasText: "ファミリーマート" })).toBeVisible();\n});\n\n'''
if anchor not in text:
    raise SystemExit('e2e anchor not found')
spec.write_text(text.replace(anchor, addition + anchor))
PY

git diff --check
git status --short --branch
git diff --stat
git diff -- src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts
printf '\n-- manifest --\n'
sed -n '1,140p' src/app/manifest.ts
ps -eo pid,ppid,etime,stat,cmd | grep -E 'npm test|node --test|docker|next|playwright|/home/haya-ryoo/apps/kakeibo' | grep -v grep || true
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep 'PWAショートカットから少額支出を仮登録する'
sed -n '1,220p' compose.yml 2>/dev/null || sed -n '1,220p' docker-compose.yml 2>/dev/null || true
printf '\n-- env --\n'
sed -n '1,120p' .env 2>/dev/null || true
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
old = '''  await expect(page.locator(".review-card").filter({ hasText: "ファミリーマート" })).toBeVisible();
});
'''
new = '''  const reviewCard = page.locator(".review-card").filter({ hasText: "ファミリーマート" });
  await expect(reviewCard).toBeVisible();
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "PATCH" && response.ok()),
    reviewCard.getByRole("button", { name: "個人費", exact: true }).click(),
  ]);
  await expect(reviewCard).toHaveCount(0);
});
'''
if old not in text:
    raise SystemExit('cleanup anchor not found')
path.write_text(text.replace(old, new))
PY
node --input-type=module <<'JS'
import { Pool } from 'pg';
const pool = new Pool({ connectionString: 'postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test' });
const result = await pool.query("delete from expenses where amount = 680 and memo = 'ファミリーマート'");
console.log(`deleted test expenses: ${result.rowCount}`);
await pool.end();
JS
git diff --check
git diff -- tests/e2e/kakeibo.spec.ts
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
old = '''  await input.fill("ファミリーマート 680");
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/expenses") && response.request().method() === "POST" && response.ok()),
    page.getByRole("button", { name: "仮登録", exact: true }).click(),
  ]);

  const reviewCard = page.locator(".review-card").filter({ hasText: "ファミリーマート" });
  await expect(reviewCard).toBeVisible();
  await Promise.all([
    page.waitForResponse((response) => response.url().includes("/api/expenses/") && response.request().method() === "PATCH" && response.ok()),
    reviewCard.getByRole("button", { name: "個人費", exact: true }).click(),
  ]);
  await expect(reviewCard).toHaveCount(0);
'''
new = '''  await input.fill("E2Eコンビニ仮登録 680");
  const createResponsePromise = page.waitForResponse((response) => response.url().endsWith("/api/expenses") && response.request().method() === "POST" && response.ok());
  await page.getByRole("button", { name: "仮登録", exact: true }).click();
  const createResponse = await createResponsePromise;
  const body = (await createResponse.json()) as { expense: { id: string } };

  try {
    await expect(page.locator(".review-card").filter({ hasText: "E2Eコンビニ仮登録" })).toBeVisible();
  } finally {
    const cleanupResponse = await page.request.delete(`/api/expenses/${body.expense.id}`);
    expect(cleanupResponse.ok()).toBeTruthy();
  }
'''
if old not in text:
    raise SystemExit('previous e2e cleanup block not found')
path.write_text(text.replace(old, new))
PY
node --input-type=module <<'JS'
import { Pool } from 'pg';
const pool = new Pool({ connectionString: 'postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test' });
const before = await pool.query("select id::text, memo from expenses where amount = 680 and memo like 'ファミリーマート%'");
console.log(`matching stale test expenses: ${before.rowCount}`);
const result = await pool.query("delete from expenses where amount = 680 and memo like 'ファミリーマート%'");
console.log(`deleted stale test expenses: ${result.rowCount}`);
await pool.end();
JS
git diff --check
git diff -- tests/e2e/kakeibo.spec.ts
sed -n '1,240p' test-results/kakeibo-PWAショートカットから少額支出を仮登録する-chromium/error-context.md 2>/dev/null || true
grep -n "currentView\|reviewExpenses\|review-card\|activeTab" src/components/kakeibo-dashboard.tsx | head -n 100
sed -n '560,670p' src/components/kakeibo-dashboard.tsx
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text().replace('E2Eコンビニ仮登録 680', 'テストコンビニ仮登録 680').replace('E2Eコンビニ仮登録', 'テストコンビニ仮登録')
path.write_text(text)
PY
git diff --check
grep -n "仮登録" tests/e2e/kakeibo.spec.ts
git add src/components/kakeibo-dashboard.tsx src/app/manifest.ts tests/e2e/kakeibo.spec.ts
git diff --cached --check
git diff --cached --stat
git status --short --branch
git commit -m 'feat: 少額支出の仮登録導線を追加する' -m '- 入力内容を要確認として保存する仮登録操作を追加する
- PWAショートカットから入力欄へ直接フォーカスする
- Playwrightでショートカット経由の仮登録を確認する'
git status --short --branch
git log -2 --oneline
sed -n '1,220p' /home/haya-ryoo/.codex/plugins/cache/openai-curated/github/6188456f/skills/yeet/SKILL.md
git status --short --branch
git branch --show-current
git remote -v
git log --oneline origin/main..HEAD
gh --version
gh auth status
gh issue view 10 --json number,title,state,url
gh issue view 11 --json number,title,state,url
git push origin main
gh issue close 10 --comment '実装済みです。店舗履歴からカテゴリ・分類候補を集計し、入力途中の候補表示と直近金額の差し込みを追加しました。commit: 1b8b016'
gh issue view 10 --json number,state,url
gh issue close 11 --comment '実装済みです。少額支出を要確認として保存する仮登録操作と、PWAショートカットから入力欄へ直接フォーカスする導線を追加しました。commit: 48266df'
gh issue view 11 --json number,state,url
gh issue list --state open --limit 30 --json number,title,url,labels
git status --short --branch
git log --oneline --max-count=7
gh issue view 2 --json number,title,body,state,url
gh issue view 9 --json number,title,body,state,url
gh issue view 7 --json number,title,body,state,url
git diff -- README.md package.json
git status --short --branch
gh issue close 2 --comment '実装済みです。5:5、4:6、6:4を含む負担割合プリセットを追加し、API・精算計算・履歴表示へ反映しました。commit: f5dfa50'
gh issue view 2 --json number,state,url
gh issue close 9 --comment '実装済みです。精算済み月の保護、取り消し理由の保存、履歴表示、取り消し後の再編集を追加しました。commit: d8066d8'
gh issue view 9 --json number,state,url
sed -n '1,280p' src/db/schema.sql
printf '\n-- seed refs --\n'
find . -path './node_modules' -prune -o -type f -print | grep -E 'seed|prepare_local|schema' | sort
sed -n '1,220p' src/app/api/dashboard/route.ts
printf '\n-- mapper --\n'
sed -n '1,240p' src/features/dashboard/dashboard.mapper.ts
sed -n '280,840p' src/components/kakeibo-dashboard.tsx
printf '\n-- css cards --\n'
sed -n '1,340p' src/app/globals.css
sed -n '100,320p' tests/integration/api.integration.test.ts
printf '\n-- e2e tail --\n'
sed -n '1,220p' tests/e2e/kakeibo.spec.ts
sed -n '1,260p' postgres/migrations/003_seed_local_data.sql
printf '\n-- prepare --\n'
sed -n '1,220p' postgres/migrations/002_prepare_local_environments.sh
sed -n '1,220p' src/lib/db.ts
printf '\n-- globals tail --\n'
tail -n 140 src/app/globals.css
mkdir -p src/app/api/analytics
python3 - <<'PY'
from pathlib import Path

Path('src/app/api/analytics/route.ts').write_text('''import { NextResponse } from "next/server.js";\nimport { z } from "zod";\nimport { db } from "../../../lib/db.ts";\n\nexport const runtime = "nodejs";\nexport const dynamic = "force-dynamic";\n\ntype MonthlyRow = { period: string; month: number; total: number };\ntype CategoryRow = { category: string; total: number };\ntype MemberRow = { id: string };\n\nconst analyticsQuerySchema = z.object({\n  year: z.string().transform(Number).pipe(z.number().int().min(2000).max(9999)),\n  month: z.string().transform(Number).pipe(z.number().int().min(1).max(12)),\n  scope: z.enum(["all", "shared", "personal", "review"]).optional().default("all"),\n  paidBy: z.enum(["all", "a", "b"]).optional().default("all"),\n  category: z.string().trim().max(50).optional().default(""),\n});\n\n// TODO(security): 認証統合時に、ログイン中ユーザーの所属世帯をセッションから解決する。\nconst householdQuery = `\n  select id\n  from households\n  order by created_at asc\n  limit 1\n`;\n\nconst membersQuery = `\n  select id\n  from household_members\n  where household_id = $1\n  order by role asc, created_at asc\n`;\n\nconst monthlyQuery = `\n  with months as (\n    select generate_series(\n      make_date($2, $3, 1) - interval '5 months',\n      make_date($2, $3, 1),\n      interval '1 month'\n    )::date as month_start\n  )\n  select\n    to_char(months.month_start, 'YYYY-MM') as period,\n    extract(month from months.month_start)::int as month,\n    coalesce(sum(e.amount), 0)::int as total\n  from months\n  left join expenses e\n    on e.household_id = $1\n    and e.expense_date >= months.month_start\n    and e.expense_date < months.month_start + interval '1 month'\n    and ($4 = 'all' or e.scope = $4)\n    and ($5::uuid is null or e.paid_by_member_id = $5::uuid)\n    and ($6 = '' or exists (\n      select 1\n      from categories filtered_category\n      where filtered_category.id = e.category_id and filtered_category.name = $6\n    ))\n  group by months.month_start\n  order by months.month_start asc\n`;\n\nconst categoriesQuery = `\n  select c.name as category, sum(e.amount)::int as total\n  from expenses e\n  join categories c on c.id = e.category_id\n  where e.household_id = $1\n    and e.expense_date >= make_date($2, $3, 1)\n    and e.expense_date < make_date($2, $3, 1) + interval '1 month'\n    and ($4 = 'all' or e.scope = $4)\n    and ($5::uuid is null or e.paid_by_member_id = $5::uuid)\n    and ($6 = '' or c.name = $6)\n  group by c.name\n  order by total desc, c.name asc\n`;\n\nconst categoryOptionsQuery = `\n  select distinct name\n  from categories\n  where household_id = $1\n  order by name asc\n`;\n\nexport async function GET(request: Request) {\n  try {\n    const url = new URL(request.url);\n    const result = analyticsQuerySchema.safeParse({\n      year: url.searchParams.get("year") ?? "",\n      month: url.searchParams.get("month") ?? "",\n      scope: url.searchParams.get("scope") ?? undefined,\n      paidBy: url.searchParams.get("paidBy") ?? undefined,\n      category: url.searchParams.get("category") ?? undefined,\n    });\n\n    if (!result.success) {\n      return NextResponse.json({ error: "invalid_analytics_query" }, { status: 400 });\n    }\n\n    const pool = db();\n    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];\n    if (!household) {\n      return NextResponse.json({ monthly: [], categories: [], categoryOptions: [] });\n    }\n\n    const { year, month, scope, paidBy, category } = result.data;\n    const members = (await pool.query<MemberRow>(membersQuery, [household.id])).rows;\n    const paidByMemberId = paidBy === "a" ? members[0]?.id ?? null : paidBy === "b" ? members[1]?.id ?? null : null;\n    const params = [household.id, year, month, scope, paidByMemberId, category];\n    const [monthly, categories, categoryOptions] = await Promise.all([\n      pool.query<MonthlyRow>(monthlyQuery, params),\n      pool.query<CategoryRow>(categoriesQuery, params),\n      pool.query<{ name: string }>(categoryOptionsQuery, [household.id]),\n    ]);\n\n    return NextResponse.json({\n      monthly: monthly.rows.map((item) => ({ ...item, label: `${item.month}月` })),\n      categories: categories.rows,\n      categoryOptions: categoryOptions.rows.map((item) => item.name),\n    });\n  } catch (error) {\n    console.error("Failed to load expense analytics", error);\n    return NextResponse.json({ error: "expense_analytics_failed" }, { status: 500 });\n  }\n}\n''')

path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
anchor = '''type ExpenseEditDraft = {\n  title: string;\n  amount: string;\n  paidBy: MemberId;\n  category: string;\n  note: string;\n  expenseDate: string;\n};\n'''
addition = '''\ntype AnalyticsScope = "all" | ExpenseScope;\ntype AnalyticsPayer = "all" | MemberId;\ntype AnalyticsResponse = {\n  monthly: { period: string; label: string; total: number }[];\n  categories: { category: string; total: number }[];\n  categoryOptions: string[];\n};\n'''
if anchor not in text:
    raise SystemExit('type anchor not found')
text = text.replace(anchor, anchor + addition)
anchor = '''  const [isReopening, setIsReopening] = useState(false);\n  const composerInputRef = useRef<HTMLInputElement>(null);\n'''
addition = '''  const [analyticsScope, setAnalyticsScope] = useState<AnalyticsScope>("all");\n  const [analyticsPayer, setAnalyticsPayer] = useState<AnalyticsPayer>("all");\n  const [analyticsCategory, setAnalyticsCategory] = useState("");\n  const [analytics, setAnalytics] = useState<AnalyticsResponse | null>(null);\n  const [isAnalyticsLoading, setIsAnalyticsLoading] = useState(true);\n  const [analyticsError, setAnalyticsError] = useState<string | null>(null);\n'''
if anchor not in text:
    raise SystemExit('state anchor not found')
text = text.replace(anchor, '  const [isReopening, setIsReopening] = useState(false);\n' + addition + '  const composerInputRef = useRef<HTMLInputElement>(null);\n')
anchor = '''  useEffect(() => {\n    if (didHandleQuickCompose.current || isComposerDisabled) return;\n'''
addition = '''  useEffect(() => {\n    let active = true;\n    const params = new URLSearchParams({\n      year: String(selectedPeriod.year),\n      month: String(selectedPeriod.month),\n      scope: analyticsScope,\n      paidBy: analyticsPayer,\n    });\n    if (analyticsCategory) params.set("category", analyticsCategory);\n\n    setIsAnalyticsLoading(true);\n    setAnalyticsError(null);\n    void fetch(`/api/analytics?${params.toString()}`)\n      .then((response) => {\n        if (!response.ok) throw new Error("analytics_load_failed");\n        return response.json() as Promise<AnalyticsResponse>;\n      })\n      .then((data) => {\n        if (active) setAnalytics(data);\n      })\n      .catch(() => {\n        if (active) {\n          setAnalytics(null);\n          setAnalyticsError("使用料グラフを読み込めませんでした。");\n        }\n      })\n      .finally(() => {\n        if (active) setIsAnalyticsLoading(false);\n      });\n\n    return () => {\n      active = false;\n    };\n  }, [selectedPeriod, analyticsScope, analyticsPayer, analyticsCategory, expenses]);\n\n'''
if anchor not in text:
    raise SystemExit('effect anchor not found')
text = text.replace(anchor, addition + anchor)
anchor = '''  const homeView = (\n    <div className="layout-grid">\n      <section>\n'''
addition = '''  const monthlyMax = Math.max(...(analytics?.monthly.map((item) => item.total) ?? [0]), 1);\n  const categoryTotal = analytics?.categories.reduce((total, item) => total + item.total, 0) ?? 0;\n  const analyticsPanel = (\n    <section className="section analytics-panel" aria-label="使用料グラフ">\n      <div className="section__header">\n        <div>\n          <h2 className="section__title">使用料グラフ</h2>\n          <p className="section__meta">月別推移とカテゴリ内訳をざっくり確認する</p>\n        </div>\n      </div>\n      <div className="analytics-filters">\n        <select aria-label="分類で絞り込む" value={analyticsScope} onChange={(event) => setAnalyticsScope(event.target.value as AnalyticsScope)}>\n          <option value="all">すべての分類</option>\n          <option value="shared">共通費のみ</option>\n          <option value="personal">個人費のみ</option>\n          <option value="review">要確認のみ</option>\n        </select>\n        <select aria-label="支払者で絞り込む" value={analyticsPayer} onChange={(event) => setAnalyticsPayer(event.target.value as AnalyticsPayer)}>\n          <option value="all">すべての支払者</option>\n          {members.map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}\n        </select>\n        <select aria-label="カテゴリで絞り込む" value={analyticsCategory} onChange={(event) => setAnalyticsCategory(event.target.value)}>\n          <option value="">すべてのカテゴリ</option>\n          {(analytics?.categoryOptions ?? []).map((category) => <option key={category} value={category}>{category}</option>)}\n        </select>\n      </div>\n      {isAnalyticsLoading ? <p className="analytics-empty">読み込み中...</p> : analyticsError ? <p className="analytics-empty">{analyticsError}</p> : (analytics?.categories.length ?? 0) === 0 ? <p className="analytics-empty">この条件の支出はまだありません。</p> : (\n        <div className="analytics-grid">\n          <article className="analytics-card">\n            <h3>直近6か月</h3>\n            <div className="monthly-chart">\n              {analytics?.monthly.map((item) => (\n                <div key={item.period} className="monthly-chart__item" aria-label={`${item.period}の支出 ${formatMoney(item.total)}`}>\n                  <strong>{formatMoney(item.total)}</strong>\n                  <div className="monthly-chart__track"><span style={{ height: `${Math.max(item.total / monthlyMax * 100, item.total > 0 ? 4 : 0)}%` }} /></div>\n                  <small>{item.label}</small>\n                </div>\n              ))}\n            </div>\n          </article>\n          <article className="analytics-card">\n            <h3>カテゴリ内訳</h3>\n            <div className="category-chart">\n              {analytics?.categories.map((item) => (\n                <div key={item.category} className="category-chart__item">\n                  <div><span>{item.category}</span><strong>{formatMoney(item.total)}</strong></div>\n                  <div className="category-chart__track"><span style={{ width: `${categoryTotal ? item.total / categoryTotal * 100 : 0}%` }} /></div>\n                </div>\n              ))}\n            </div>\n          </article>\n        </div>\n      )}\n    </section>\n  );\n\n'''
if anchor not in text:
    raise SystemExit('home anchor not found')
text = text.replace(anchor, addition + anchor)
old = '''      <section>\n        <div className="section">\n          <div className="section__header">\n            <div>\n              <h2 className="section__title">今月の流れ</h2>\n'''
new = '''      <section>\n        {analyticsPanel}\n        <div className="section">\n          <div className="section__header">\n            <div>\n              <h2 className="section__title">今月の流れ</h2>\n'''
if old not in text:
    raise SystemExit('home section insertion anchor not found')
text = text.replace(old, new)
path.write_text(text)

css = Path('src/app/globals.css')
css.write_text(css.read_text() + '''\n.analytics-panel {\n  padding: 14px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.analytics-filters,\n.analytics-grid {\n  display: grid;\n  gap: 8px;\n}\n\n.analytics-filters select {\n  min-height: 40px;\n  padding: 0 10px;\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fff;\n  color: var(--text);\n}\n\n.analytics-grid {\n  margin-top: 14px;\n}\n\n.analytics-card {\n  min-width: 0;\n  padding: 12px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: rgba(255, 255, 255, 0.64);\n}\n\n.analytics-card h3 {\n  margin: 0 0 12px;\n  color: var(--muted);\n  font-size: 13px;\n}\n\n.monthly-chart {\n  display: grid;\n  grid-template-columns: repeat(6, minmax(38px, 1fr));\n  gap: 8px;\n  overflow-x: auto;\n}\n\n.monthly-chart__item {\n  display: grid;\n  min-width: 38px;\n  gap: 6px;\n  text-align: center;\n}\n\n.monthly-chart__item strong {\n  overflow: hidden;\n  color: var(--muted);\n  font-size: 10px;\n  text-overflow: ellipsis;\n}\n\n.monthly-chart__item small {\n  color: var(--muted);\n  font-size: 11px;\n}\n\n.monthly-chart__track {\n  display: flex;\n  align-items: flex-end;\n  height: 116px;\n  border-radius: 6px;\n  background: rgba(15, 118, 110, 0.08);\n  overflow: hidden;\n}\n\n.monthly-chart__track span {\n  width: 100%;\n  border-radius: 6px 6px 0 0;\n  background: var(--primary);\n}\n\n.category-chart {\n  display: grid;\n  gap: 10px;\n}\n\n.category-chart__item > div:first-child {\n  display: flex;\n  justify-content: space-between;\n  gap: 8px;\n  font-size: 13px;\n}\n\n.category-chart__track {\n  height: 8px;\n  margin-top: 5px;\n  border-radius: 999px;\n  background: rgba(15, 118, 110, 0.08);\n  overflow: hidden;\n}\n\n.category-chart__track span {\n  display: block;\n  height: 100%;\n  border-radius: inherit;\n  background: var(--primary);\n}\n\n.analytics-empty {\n  margin: 14px 0 0;\n  color: var(--muted);\n  font-size: 13px;\n}\n\n@media (min-width: 720px) {\n  .analytics-filters {\n    grid-template-columns: repeat(3, minmax(0, 1fr));\n  }\n\n  .analytics-grid {\n    grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);\n  }\n}\n''')

path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
anchor = '''  it("rejects malformed expense ids before querying PostgreSQL", async () => {\n'''
addition = '''  it("aggregates monthly and category expense analytics", async () => {\n    const { GET } = await import("../../src/app/api/analytics/route.ts");\n    const now = new Date();\n    const response = await GET(new Request(`http://localhost/api/analytics?year=${now.getFullYear()}&month=${now.getMonth() + 1}`));\n    assert.equal(response.status, 200);\n\n    const body = await response.json();\n    assert.equal(body.monthly.length, 6);\n    assert.equal(body.monthly.at(-1).total, 136460);\n    assert.deepEqual(body.categories, [\n      { category: "家賃", total: 124000 },\n      { category: "外食", total: 6200 },\n      { category: "食費", total: 4280 },\n      { category: "日用品", total: 1980 },\n    ]);\n\n    const invalidResponse = await GET(new Request(`http://localhost/api/analytics?year=${now.getFullYear()}&month=${now.getMonth() + 1}&scope=unknown`));\n    assert.equal(invalidResponse.status, 400);\n  });\n\n'''
if anchor not in text:
    raise SystemExit('integration anchor not found')
path.write_text(text.replace(anchor, addition + anchor))

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
anchor = '''test("月を切り替えると過去月では支出追加を無効化する", async ({ page }) => {\n'''
addition = '''test("月別とカテゴリ別の使用料グラフを表示して分類で絞り込む", async ({ page }) => {\n  await page.goto("/");\n\n  const panel = page.getByLabel("使用料グラフ");\n  await expect(panel.getByRole("heading", { name: "使用料グラフ" })).toBeVisible();\n  await expect(panel.getByText("家賃", { exact: true })).toBeVisible();\n  await panel.getByLabel("分類で絞り込む").selectOption("review");\n  await expect(panel.getByText("外食", { exact: true })).toBeVisible();\n  await expect(panel.getByText("家賃", { exact: true })).toHaveCount(0);\n});\n\n'''
if anchor not in text:
    raise SystemExit('e2e anchor not found')
path.write_text(text.replace(anchor, addition + anchor))
PY

git diff --check
git status --short --branch
git diff --stat -- src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
sed -n '1,240p' src/app/api/analytics/route.ts
git diff --check -- src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
sed -n '95,280p' src/components/kakeibo-dashboard.tsx
printf '\n-- analytics panel --\n'
sed -n '590,760p' src/components/kakeibo-dashboard.tsx
python3 - <<'PY'
from pathlib import Path
path = Path('src/app/api/analytics/route.ts')
text = path.read_text().replace("and ($4 = 'all' or e.scope = $4)", "and ($4 = 'all' or e.scope::text = $4)")
path.write_text(text)
PY
git diff --check -- src/app/api/analytics/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
grep -n "scope::text" src/app/api/analytics/route.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '月別とカテゴリ別の使用料グラフを表示して分類で絞り込む'
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
old = '''  await expect(panel.getByText("家賃", { exact: true })).toBeVisible();\n  await panel.getByLabel("分類で絞り込む").selectOption("review");\n  await expect(panel.getByText("外食", { exact: true })).toBeVisible();\n  await expect(panel.getByText("家賃", { exact: true })).toHaveCount(0);\n'''
new = '''  const categories = panel.locator(".category-chart");\n  await expect(categories.getByText("家賃", { exact: true })).toBeVisible();\n  await panel.getByLabel("分類で絞り込む").selectOption("review");\n  await expect(categories.getByText("外食", { exact: true })).toBeVisible();\n  await expect(categories.getByText("家賃", { exact: true })).toHaveCount(0);\n'''
if old not in text:
    raise SystemExit('analytics e2e locator anchor not found')
path.write_text(text.replace(old, new))
PY
git diff --check -- src/app/api/analytics/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '月別とカテゴリ別の使用料グラフを表示して分類で絞り込む'
ps -eo pid,ppid,etime,stat,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
ps -eo pid,ppid,etime,stat,cmd | grep -E 'npx playwright|playwright test|npm exec|node .*playwright' | grep -v grep || true
git diff --check -- src/app/api/analytics/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git status --short --branch
git diff --stat -- src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git add src/app/api/analytics/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git diff --cached --check
git diff --cached --stat
git status --short --branch
git commit -m 'feat: 月別とカテゴリ別の使用料グラフを追加する' -m '- 直近6か月とカテゴリ内訳を返す分析APIを追加する
- 分類、支払者、カテゴリで絞り込めるグラフをホーム画面へ追加する
- 統合テストとPlaywrightで集計と分類切替を確認する'
git status --short --branch
git log -2 --oneline
gh issue close 7 --comment '実装済みです。直近6か月の棒グラフ、カテゴリ内訳、分類・支払者・カテゴリの絞り込み、ローディングと空状態を追加しました。commit: 1942f27'
gh issue view 7 --json number,state,url
git status --short --branch
git log --oneline origin/main..HEAD
ss -ltnp | grep -E ':(3021|3022)\b' || true
sed -n '1,180p' /home/haya-ryoo/.codex/plugins/cache/openai-curated/github/6188456f/skills/github/SKILL.md
gh issue list --state open --limit 30 --json number,title,url,body
git status --short --branch
grep -R "create table recurring_expenses\|recurring_expenses\|notification" -n postgres supabase src tests | head -n 240
sed -n '1,260p' postgres/migrations/001_initial_schema.sql
printf '\n-- expense create remainder --\n'
sed -n '120,300p' src/app/api/expenses/route.ts
printf '\n-- tabs and current view --\n'
grep -n "const tabs\|settingsView\|currentView" src/components/kakeibo-dashboard.tsx
git diff -- README.md package.json
printf '\n-- migrations --\n'
find postgres/migrations -maxdepth 1 -type f | sort
printf '\n-- API files --\n'
find src/app/api -type f | sort
sed -n '1,220p' postgres/migrations/004_add_settlement_period_events.sql
printf '\n-- supabase migrations --\n'
find supabase/migrations -maxdepth 1 -type f | sort
printf '\n-- dashboard top types --\n'
sed -n '1,115p' src/components/kakeibo-dashboard.tsx
printf '\n-- integration setup --\n'
sed -n '1,125p' tests/integration/api.integration.test.ts
sed -n '260,380p' postgres/migrations/001_initial_schema.sql
printf '\n-- supabase tail --\n'
tail -n 140 supabase/migrations/202605240001_initial_schema.sql
printf '\n-- db readme --\n'
sed -n '1,100p' src/db/README.md
mkdir -p src/app/api/recurring-expenses/candidates
python3 - <<'PY'
from pathlib import Path

migration = '''create table if not exists recurring_expense_occurrences (\n  id uuid primary key default gen_random_uuid(),\n  recurring_expense_id uuid not null references recurring_expenses(id) on delete cascade,\n  year integer not null,\n  month integer not null,\n  status text not null,\n  expense_id uuid references expenses(id) on delete set null,\n  amount integer,\n  created_at timestamptz not null default now(),\n  updated_at timestamptz not null default now(),\n  unique (recurring_expense_id, year, month),\n  constraint recurring_expense_occurrences_month_range check (month between 1 and 12),\n  constraint recurring_expense_occurrences_status_valid check (status in ('confirmed', 'skipped')),\n  constraint recurring_expense_occurrences_amount_positive check (amount is null or amount > 0)\n);\n\ncreate index if not exists recurring_expense_occurrences_recurring_id_idx\n  on recurring_expense_occurrences(recurring_expense_id, year, month);\n'''
Path('postgres/migrations/005_add_recurring_expense_occurrences.sql').write_text(migration)
Path('supabase/migrations/202606020001_add_recurring_expense_occurrences.sql').write_text(migration.replace('recurring_expense_occurrences', 'public.recurring_expense_occurrences').replace('references recurring_expenses', 'references public.recurring_expenses').replace('references expenses', 'references public.expenses') + '''\nalter table public.recurring_expense_occurrences enable row level security;\n\ncreate policy "members can manage recurring expense occurrences"\non public.recurring_expense_occurrences\nfor all\nusing (\n  exists (\n    select 1 from public.recurring_expenses re\n    where re.id = recurring_expense_id\n      and public.is_household_member(re.household_id)\n  )\n)\nwith check (\n  exists (\n    select 1 from public.recurring_expenses re\n    where re.id = recurring_expense_id\n      and public.is_household_member(re.household_id)\n  )\n);\n''')

prepare = Path('postgres/migrations/002_prepare_local_environments.sh')
text = prepare.read_text()
text = text.replace('settlement_events_migration_file="/docker-entrypoint-initdb.d/004_add_settlement_period_events.sql"', 'settlement_events_migration_file="/docker-entrypoint-initdb.d/004_add_settlement_period_events.sql"\nrecurring_occurrences_migration_file="/docker-entrypoint-initdb.d/005_add_recurring_expense_occurrences.sql"')
text = text.replace('  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$settlement_events_migration_file"', '  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$settlement_events_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$recurring_occurrences_migration_file"')
prepare.write_text(text)

Path('src/app/api/recurring-expenses/candidates/route.ts').write_text('''import { NextResponse } from "next/server.js";\nimport { z } from "zod";\nimport { db } from "../../../../lib/db.ts";\n\nexport const runtime = "nodejs";\nexport const dynamic = "force-dynamic";\n\nconst periodSchema = z.object({\n  year: z.number().int().min(2000).max(9999),\n  month: z.number().int().min(1).max(12),\n});\nconst querySchema = z.object({\n  year: z.string().transform(Number).pipe(z.number().int().min(2000).max(9999)),\n  month: z.string().transform(Number).pipe(z.number().int().min(1).max(12)),\n});\nconst actionSchema = periodSchema.extend({\n  recurringExpenseId: z.string().uuid(),\n  action: z.enum(["confirm", "skip"]),\n  amount: z.number().int().positive().optional(),\n});\n\nconst householdQuery = `select id from households order by created_at asc limit 1`;\nconst candidatesQuery = `\n  select re.id::text, c.name as title, c.name as category, re.amount, re.day_of_month, re.scope::text,\n    coalesce(reo.status, 'pending') as status, reo.expense_id::text,\n    greatest(make_date($2, $3, 1), least(\n      (make_date($2, $3, 1) + interval '1 month - 1 day')::date,\n      make_date($2, $3, 1) + (re.day_of_month - 1)\n    ))::text as due_date\n  from recurring_expenses re\n  join categories c on c.id = re.category_id\n  left join recurring_expense_occurrences reo\n    on reo.recurring_expense_id = re.id and reo.year = $2 and reo.month = $3\n  where re.household_id = $1 and re.active\n  order by re.day_of_month asc, c.name asc\n`;\nconst recurringQuery = `\n  select re.id, re.household_id, re.category_id, re.payment_source_id, re.paid_by_member_id,\n    re.amount, re.day_of_month, re.scope::text, re.split_type::text, c.name as title\n  from recurring_expenses re join categories c on c.id = re.category_id\n  where re.id = $1 and re.household_id = $2 and re.active\n  for update\n`;\nconst periodLockedQuery = `select exists (select 1 from settlement_periods where household_id = $1 and year = $2 and month = $3 and status = 'settled') as locked`;\nconst insertExpenseQuery = `\n  insert into expenses (household_id, paid_by_member_id, payment_source_id, category_id, amount, expense_date, scope, memo)\n  values ($1, $2, $3, $4, $5, least(make_date($6, $7, $8), (make_date($6, $7, 1) + interval '1 month - 1 day')::date), $9, $10)\n  returning id\n`;\nconst insertOccurrenceQuery = `\n  insert into recurring_expense_occurrences (recurring_expense_id, year, month, status, expense_id, amount)\n  values ($1, $2, $3, $4, $5, $6)\n  on conflict (recurring_expense_id, year, month) do nothing\n  returning id\n`;\nconst membersQuery = `select id, default_share_rate::float8 from household_members where household_id = $1 order by role asc, created_at asc`;\nconst insertSplitQuery = `insert into expense_splits (expense_id, member_id, rate) values ($1, $2, $3)`;\n\ntype Household = { id: string };\ntype Recurring = { id: string; household_id: string; category_id: string; payment_source_id: string; paid_by_member_id: string | null; amount: number; day_of_month: number; scope: string; split_type: string; title: string };\n\nfunction ratesFor(splitType: string, members: { id: string; default_share_rate: number | null }[]) {\n  if (splitType === "income_ratio" && members.every((member) => member.default_share_rate != null)) {\n    return members.map((member) => ({ id: member.id, rate: member.default_share_rate as number }));\n  }\n  return members.map((member) => ({ id: member.id, rate: 1 / members.length }));\n}\n\nexport async function GET(request: Request) {\n  try {\n    const url = new URL(request.url);\n    const result = querySchema.safeParse({ year: url.searchParams.get("year") ?? "", month: url.searchParams.get("month") ?? "" });\n    if (!result.success) return NextResponse.json({ error: "invalid_period" }, { status: 400 });\n    const pool = db();\n    const household = (await pool.query<Household>(householdQuery)).rows[0];\n    if (!household) return NextResponse.json({ candidates: [] });\n    const candidates = (await pool.query(candidatesQuery, [household.id, result.data.year, result.data.month])).rows;\n    return NextResponse.json({ candidates });\n  } catch (error) {\n    console.error("Failed to load recurring expense candidates", error);\n    return NextResponse.json({ error: "recurring_candidates_failed" }, { status: 500 });\n  }\n}\n\nexport async function POST(request: Request) {\n  const pool = db();\n  const client = await pool.connect();\n  try {\n    const result = actionSchema.safeParse(await request.json());\n    if (!result.success) return NextResponse.json({ error: "invalid_recurring_action" }, { status: 400 });\n    const { recurringExpenseId, year, month, action, amount } = result.data;\n    await client.query("begin");\n    const household = (await client.query<Household>(householdQuery)).rows[0];\n    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }\n    const locked = (await client.query<{ locked: boolean }>(periodLockedQuery, [household.id, year, month])).rows[0]?.locked;\n    if (locked) { await client.query("rollback"); return NextResponse.json({ error: "settled_period_locked" }, { status: 409 }); }\n    const recurring = (await client.query<Recurring>(recurringQuery, [recurringExpenseId, household.id])).rows[0];\n    if (!recurring) { await client.query("rollback"); return NextResponse.json({ error: "recurring_expense_not_found" }, { status: 404 }); }\n    const candidateAmount = amount ?? recurring.amount;\n    let expenseId: string | null = null;\n    if (action === "confirm") {\n      expenseId = (await client.query<{ id: string }>(insertExpenseQuery, [recurring.household_id, recurring.paid_by_member_id, recurring.payment_source_id, recurring.category_id, candidateAmount, year, month, recurring.day_of_month, recurring.scope, `${recurring.title} 定期支出`])).rows[0]?.id ?? null;\n      if (expenseId && recurring.scope === "shared") {\n        const members = (await client.query<{ id: string; default_share_rate: number | null }>(membersQuery, [household.id])).rows;\n        for (const member of ratesFor(recurring.split_type, members)) await client.query(insertSplitQuery, [expenseId, member.id, member.rate]);\n      }\n    }\n    const occurrence = (await client.query(insertOccurrenceQuery, [recurring.id, year, month, action === "confirm" ? "confirmed" : "skipped", expenseId, candidateAmount])).rows[0];\n    if (!occurrence) { await client.query("rollback"); return NextResponse.json({ error: "recurring_expense_already_handled" }, { status: 409 }); }\n    await client.query("commit");\n    console.info("Handled recurring expense candidate", { recurringExpenseId, year, month, action });\n    return NextResponse.json({ status: action === "confirm" ? "confirmed" : "skipped", expenseId }, { status: 201 });\n  } catch (error) {\n    await client.query("rollback");\n    console.error("Failed to handle recurring expense candidate", error);\n    return NextResponse.json({ error: "recurring_candidate_action_failed" }, { status: 500 });\n  } finally { client.release(); }\n}\n''')
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts
sed -n '1,120p' postgres/migrations/002_prepare_local_environments.sh
printf '\n-- status --\n'
git status --short --branch
sed -n '1,180p' supabase/migrations/202606020001_add_recurring_expense_occurrences.sql
printf '\n-- api --\n'
sed -n '1,280p' src/app/api/recurring-expenses/candidates/route.ts
grep -n "analyticsPanel\|homeView\|useState<Analytics\|useEffect" src/components/kakeibo-dashboard.tsx | head -n 120
printf '\n-- css tail --\n'
tail -n 180 src/app/globals.css
python3 - <<'PY'
from pathlib import Path
path = Path('supabase/migrations/202606020001_add_recurring_expense_occurrences.sql')
text = path.read_text().replace('constraint public.recurring_expense_occurrences_', 'constraint recurring_expense_occurrences_')
path.write_text(text)
path = Path('src/app/api/recurring-expenses/candidates/route.ts')
text = path.read_text().replace("least(make_date($6, $7, $8), (make_date($6, $7, 1) + interval '1 month - 1 day')::date)", "least(make_date($6, $7, 1) + ($8 - 1), (make_date($6, $7, 1) + interval '1 month - 1 day')::date)")
path.write_text(text)
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts
grep -n "constraint\|values (\$1" supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts
python3 - <<'PY'
from pathlib import Path
path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
anchor = '''type AnalyticsResponse = {\n  monthly: { period: string; label: string; total: number }[];\n  categories: { category: string; total: number }[];\n  categoryOptions: string[];\n};\n'''
addition = '''\ntype RecurringCandidate = {\n  id: string;\n  title: string;\n  category: string;\n  amount: number;\n  day_of_month: number;\n  scope: ExpenseScope;\n  status: "pending" | "confirmed" | "skipped";\n  due_date: string;\n};\n'''
if anchor not in text: raise SystemExit('analytics type anchor missing')
text = text.replace(anchor, anchor + addition)
anchor = '''  const [analyticsError, setAnalyticsError] = useState<string | null>(null);\n  const composerInputRef = useRef<HTMLInputElement>(null);\n'''
addition = '''  const [recurringCandidates, setRecurringCandidates] = useState<RecurringCandidate[]>([]);\n  const [isRecurringLoading, setIsRecurringLoading] = useState(true);\n  const [recurringError, setRecurringError] = useState<string | null>(null);\n  const [pendingRecurringId, setPendingRecurringId] = useState<string | null>(null);\n  const [recurringRefresh, setRecurringRefresh] = useState(0);\n'''
if anchor not in text: raise SystemExit('state anchor missing')
text = text.replace(anchor, '  const [analyticsError, setAnalyticsError] = useState<string | null>(null);\n' + addition + '  const composerInputRef = useRef<HTMLInputElement>(null);\n')
text = text.replace('  }, [selectedPeriod]);\n\n  const stats = summaryFor(expenses);', '  }, [selectedPeriod, recurringRefresh]);\n\n  const stats = summaryFor(expenses);', 1)
anchor = '''  useEffect(() => {\n    if (didHandleQuickCompose.current || isComposerDisabled) return;\n'''
addition = '''  useEffect(() => {\n    let active = true;\n    setIsRecurringLoading(true);\n    setRecurringError(null);\n    void fetch(`/api/recurring-expenses/candidates?year=${selectedPeriod.year}&month=${selectedPeriod.month}`)\n      .then((response) => {\n        if (!response.ok) throw new Error("recurring_candidates_load_failed");\n        return response.json() as Promise<{ candidates: RecurringCandidate[] }>;\n      })\n      .then((data) => {\n        if (active) setRecurringCandidates(data.candidates);\n      })\n      .catch(() => {\n        if (active) {\n          setRecurringCandidates([]);\n          setRecurringError("定期支出を読み込めませんでした。");\n        }\n      })\n      .finally(() => {\n        if (active) setIsRecurringLoading(false);\n      });\n    return () => { active = false; };\n  }, [selectedPeriod, recurringRefresh]);\n\n  const handleRecurringCandidate = async (candidate: RecurringCandidate, action: "confirm" | "skip") => {\n    if (pendingRecurringId || candidate.status !== "pending") return;\n    setPendingRecurringId(candidate.id);\n    setRecurringError(null);\n    try {\n      const response = await fetch("/api/recurring-expenses/candidates", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ recurringExpenseId: candidate.id, year: selectedPeriod.year, month: selectedPeriod.month, action }),\n      });\n      if (!response.ok) throw new Error("recurring_candidate_action_failed");\n      setRecurringRefresh((current) => current + 1);\n    } catch {\n      setRecurringError("定期支出を更新できませんでした。");\n    } finally {\n      setPendingRecurringId(null);\n    }\n  };\n\n'''
if anchor not in text: raise SystemExit('quick effect anchor missing')
text = text.replace(anchor, addition + anchor)
anchor = '''  const homeView = (\n    <div className="layout-grid">\n      <section>\n        {analyticsPanel}\n'''
addition = '''  const recurringPanel = (\n    <section className="section recurring-panel" aria-label="今月の定期支出">\n      <div className="section__header">\n        <div>\n          <h2 className="section__title">今月の定期支出</h2>\n          <p className="section__meta">引き落とし予定を確認して支出へ反映する</p>\n        </div>\n      </div>\n      {isRecurringLoading ? <p className="recurring-empty">読み込み中...</p> : recurringError ? <p className="recurring-empty">{recurringError}</p> : recurringCandidates.length === 0 ? <p className="recurring-empty">登録された定期支出はありません。</p> : (\n        <div className="recurring-list">\n          {recurringCandidates.map((candidate) => {\n            const daysUntilDue = Math.ceil((new Date(`${candidate.due_date}T00:00:00`).getTime() - new Date().setHours(0, 0, 0, 0)) / 86400000);\n            return (\n              <article key={candidate.id} className="recurring-card">\n                <div>\n                  <strong>{candidate.title}</strong>\n                  <p>{candidate.due_date} ・ {daysUntilDue >= 0 ? `あと${daysUntilDue}日` : `${Math.abs(daysUntilDue)}日経過`}</p>\n                </div>\n                <div className="recurring-card__amount">{formatMoney(candidate.amount)}</div>\n                <div className="recurring-card__actions">\n                  {candidate.status === "pending" ? (\n                    <>\n                      <button className="chip chip--active" type="button" disabled={pendingRecurringId === candidate.id || isPeriodLocked} onClick={() => void handleRecurringCandidate(candidate, "confirm")}>確定</button>\n                      <button className="chip" type="button" disabled={pendingRecurringId === candidate.id || isPeriodLocked} onClick={() => void handleRecurringCandidate(candidate, "skip")}>スキップ</button>\n                    </>\n                  ) : <span className="pill pill--muted">{candidate.status === "confirmed" ? "支出へ反映済み" : "今月はスキップ"}</span>}\n                </div>\n              </article>\n            );\n          })}\n        </div>\n      )}\n    </section>\n  );\n\n'''
if anchor not in text: raise SystemExit('home panel anchor missing')
text = text.replace(anchor, addition + anchor.replace('{analyticsPanel}\n', '{analyticsPanel}\n        {recurringPanel}\n'))
path.write_text(text)

css = Path('src/app/globals.css')
css.write_text(css.read_text() + '''\n.recurring-panel {\n  padding: 14px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.recurring-list {\n  display: grid;\n  gap: 8px;\n}\n\n.recurring-card {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto;\n  gap: 8px 12px;\n  padding: 12px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: rgba(255, 255, 255, 0.64);\n}\n\n.recurring-card strong {\n  font-size: 14px;\n}\n\n.recurring-card p,\n.recurring-empty {\n  margin: 4px 0 0;\n  color: var(--muted);\n  font-size: 12px;\n}\n\n.recurring-card__amount {\n  font-weight: 700;\n}\n\n.recurring-card__actions {\n  display: flex;\n  grid-column: 1 / -1;\n  flex-wrap: wrap;\n  gap: 8px;\n}\n''')
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css
grep -n "recurringPanel\|recurringRefresh\|handleRecurringCandidate" src/components/kakeibo-dashboard.tsx
git status --short --branch
python3 - <<'PY'
from pathlib import Path
seed = Path('postgres/migrations/003_seed_local_data.sql')
text = seed.read_text()
anchor = '''on conflict (id) do update\nset\n  category_id = excluded.category_id,\n  payment_source_id = excluded.payment_source_id,\n  paid_by_member_id = excluded.paid_by_member_id,\n  amount = excluded.amount,\n  day_of_month = excluded.day_of_month,\n  scope = excluded.scope,\n  split_type = excluded.split_type,\n  active = excluded.active;\n\ninsert into settlement_periods'''
replace = '''on conflict (id) do update\nset\n  category_id = excluded.category_id,\n  payment_source_id = excluded.payment_source_id,\n  paid_by_member_id = excluded.paid_by_member_id,\n  amount = excluded.amount,\n  day_of_month = excluded.day_of_month,\n  scope = excluded.scope,\n  split_type = excluded.split_type,\n  active = excluded.active;\n\ndelete from recurring_expense_occurrences\nwhere year = extract(year from current_date)::int\n  and month = extract(month from current_date)::int;\n\ninsert into settlement_periods'''
if anchor not in text: raise SystemExit('seed recurring anchor missing')
seed.write_text(text.replace(anchor, replace))

path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
text = text.replace('  const testPeriod = { year: 2098, month: 12 };', '  const testPeriod = { year: 2098, month: 12 };\n  const recurringTestPeriod = { year: 2097, month: 9 };')
text = text.replace('''    await pool.query("delete from settlement_periods where year = $1 and month = $2", [testPeriod.year, testPeriod.month]);\n    await pool.end();''', '''    const recurringExpenseIds = (await pool.query<{ expense_id: string }>("select expense_id::text from recurring_expense_occurrences where year = $1 and month = $2 and expense_id is not null", [recurringTestPeriod.year, recurringTestPeriod.month])).rows.map((row) => row.expense_id);\n    await pool.query("delete from recurring_expense_occurrences where year = $1 and month = $2", [recurringTestPeriod.year, recurringTestPeriod.month]);\n    if (recurringExpenseIds.length > 0) await pool.query("delete from expenses where id = any($1::uuid[])", [recurringExpenseIds]);\n    await pool.query("delete from settlement_periods where year = $1 and month = $2", [testPeriod.year, testPeriod.month]);\n    await pool.end();''')
anchor = '''  it("rejects malformed expense ids before querying PostgreSQL", async () => {\n'''
addition = '''  it("confirms a monthly recurring expense candidate only once", async () => {\n    const { GET, POST } = await import("../../src/app/api/recurring-expenses/candidates/route.ts");\n    const list = await GET(new Request(`http://localhost/api/recurring-expenses/candidates?year=${recurringTestPeriod.year}&month=${recurringTestPeriod.month}`));\n    assert.equal(list.status, 200);\n    const candidate = (await list.json()).candidates[0];\n    assert.equal(candidate.title, "サブスク");\n    assert.equal(candidate.status, "pending");\n\n    const requestBody = { recurringExpenseId: candidate.id, ...recurringTestPeriod, action: "confirm" };\n    const confirmed = await POST(new Request("http://localhost/api/recurring-expenses/candidates", {\n      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(requestBody),\n    }));\n    assert.equal(confirmed.status, 201);\n    const confirmedBody = await confirmed.json();\n    assert.equal((await pool.query("select id from expenses where id = $1", [confirmedBody.expenseId])).rowCount, 1);\n\n    const duplicate = await POST(new Request("http://localhost/api/recurring-expenses/candidates", {\n      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(requestBody),\n    }));\n    assert.equal(duplicate.status, 409);\n  });\n\n'''
if anchor not in text: raise SystemExit('integration recurring anchor missing')
path.write_text(text.replace(anchor, addition + anchor))

path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
anchor = '''test("月を切り替えると過去月では支出追加を無効化する", async ({ page }) => {\n'''
addition = '''test("定期支出候補を今月の支出へ確定する", async ({ page }) => {\n  await page.goto("/");\n\n  const panel = page.getByLabel("今月の定期支出");\n  await expect(panel.getByText("サブスク", { exact: true })).toBeVisible();\n  await Promise.all([\n    page.waitForResponse((response) => response.url().endsWith("/api/recurring-expenses/candidates") && response.request().method() === "POST" && response.ok()),\n    panel.getByRole("button", { name: "確定", exact: true }).click(),\n  ]);\n  await expect(panel.getByText("支出へ反映済み", { exact: true })).toBeVisible();\n});\n\n'''
if anchor not in text: raise SystemExit('e2e recurring anchor missing')
path.write_text(text.replace(anchor, addition + anchor))
PY

git diff --check -- postgres/migrations/003_seed_local_data.sql tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts

docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
ps -eo pid,ppid,etime,cmd | grep '/home/haya-ryoo/apps/kakeibo' | grep -E 'next|playwright|npm' | grep -v grep || true
ss -ltnp | grep -E ':(3021|3022)\b' || true
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '定期支出候補を今月の支出へ確定する'
python3 - <<'PY'
from pathlib import Path
path = Path('src/app/api/recurring-expenses/candidates/route.ts')
text = path.read_text()
old = '''  select re.id::text, c.name as title, c.name as category, re.amount, re.day_of_month, re.scope::text,
    coalesce(reo.status, 'pending') as status, reo.expense_id::text,
'''
new = '''  select re.id::text, c.name as title, c.name as category, re.amount, re.day_of_month, re.scope::text,
    coalesce(reo.status, 'pending') as status, reo.expense_id::text,
    (select previous.amount from recurring_expense_occurrences previous
      where previous.recurring_expense_id = re.id and previous.status = 'confirmed'
        and (previous.year, previous.month) < ($2, $3)
      order by previous.year desc, previous.month desc limit 1) as previous_amount,
'''
if old not in text: raise SystemExit('candidate query anchor missing')
path.write_text(text.replace(old, new))

path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('''  status: "pending" | "confirmed" | "skipped";
  due_date: string;
};''', '''  status: "pending" | "confirmed" | "skipped";
  due_date: string;
  previous_amount: number | null;
};''')
text = text.replace('''body: JSON.stringify({ recurringExpenseId: candidate.id, year: selectedPeriod.year, month: selectedPeriod.month, action }),''', '''body: JSON.stringify({ recurringExpenseId: candidate.id, year: selectedPeriod.year, month: selectedPeriod.month, action, amount: candidate.amount }),''')
old = '''                <div className="recurring-card__amount">{formatMoney(candidate.amount)}</div>
                <div className="recurring-card__actions">
'''
new = '''                <label className="recurring-card__amount">
                  <span>金額</span>
                  <input aria-label={`${candidate.title}の金額`} inputMode="numeric" value={candidate.amount} disabled={candidate.status !== "pending"} onChange={(event) => {
                    const amount = Number(event.target.value);
                    if (Number.isInteger(amount) && amount > 0) setRecurringCandidates((current) => current.map((item) => item.id === candidate.id ? { ...item, amount } : item));
                  }} />
                </label>
                {candidate.previous_amount != null ? <p className="recurring-card__difference">前回比 {candidate.amount - candidate.previous_amount >= 0 ? "+" : ""}{formatMoney(candidate.amount - candidate.previous_amount)}</p> : null}
                <div className="recurring-card__actions">
'''
if old not in text: raise SystemExit('recurring card amount anchor missing')
path.write_text(text.replace(old, new))

css = Path('src/app/globals.css')
css.write_text(css.read_text() + '''\n.recurring-card__amount {\n  display: grid;\n  gap: 3px;\n  color: var(--muted);\n  font-size: 11px;\n}\n\n.recurring-card__amount input {\n  width: 104px;\n  min-height: 36px;\n  padding: 0 8px;\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fff;\n  color: var(--text);\n  font-weight: 700;\n  text-align: right;\n}\n\n.recurring-card__difference {\n  grid-column: 1 / -1;\n}\n''')

readme = Path('src/db/README.md')
text = readme.read_text()
text = text.replace('- `recurring_expenses`\n', '- `recurring_expenses`\n- `recurring_expense_occurrences`\n')
readme.write_text(text)
PY

git diff --check -- src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx src/app/globals.css src/db/README.md
grep -n "previous_amount\|recurring-card__amount\|recurring_expense_occurrences" src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx src/db/README.md
python3 - <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
text = text.replace('''    const requestBody = { recurringExpenseId: candidate.id, ...recurringTestPeriod, action: "confirm" };''', '''    const requestBody = { recurringExpenseId: candidate.id, ...recurringTestPeriod, action: "confirm", amount: 1590 };''')
text = text.replace('''    assert.equal((await pool.query("select id from expenses where id = $1", [confirmedBody.expenseId])).rowCount, 1);

    const duplicate''', '''    assert.deepEqual((await pool.query<{ amount: number }>("select amount from expenses where id = $1", [confirmedBody.expenseId])).rows, [{ amount: 1590 }]);
    const nextMonth = await GET(new Request(`http://localhost/api/recurring-expenses/candidates?year=${recurringTestPeriod.year}&month=${recurringTestPeriod.month + 1}`));
    assert.equal((await nextMonth.json()).candidates[0].previous_amount, 1590);

    const duplicate''')
path.write_text(text)
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx src/app/globals.css src/db/README.md tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
python3 - <<'PY'
from pathlib import Path
path = Path('postgres/migrations/003_seed_local_data.sql')
text = path.read_text()
old = '''delete from recurring_expense_occurrences
where year = extract(year from current_date)::int
  and month = extract(month from current_date)::int;
'''
new = '''delete from expenses
where id in (
  select expense_id
  from recurring_expense_occurrences
  where year = extract(year from current_date)::int
    and month = extract(month from current_date)::int
    and expense_id is not null
);

delete from recurring_expense_occurrences
where year = extract(year from current_date)::int
  and month = extract(month from current_date)::int;
'''
if old not in text: raise SystemExit('seed cleanup anchor missing')
path.write_text(text.replace(old, new))
PY

git diff --check -- postgres/migrations/003_seed_local_data.sql
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
python3 - <<'PY'
from pathlib import Path
path = Path('postgres/migrations/003_seed_local_data.sql')
text = path.read_text()
anchor = '''delete from recurring_expense_occurrences
where year = extract(year from current_date)::int
  and month = extract(month from current_date)::int;

insert into settlement_periods'''
replacement = '''delete from recurring_expense_occurrences
where year = extract(year from current_date)::int
  and month = extract(month from current_date)::int;

delete from expenses e
where e.expense_date >= date_trunc('month', current_date)::date
  and e.expense_date < (date_trunc('month', current_date) + interval '1 month')::date
  and exists (
    select 1
    from recurring_expenses re
    join categories c on c.id = re.category_id
    where re.category_id = e.category_id
      and e.memo = c.name || ' 定期支出'
  );

insert into settlement_periods'''
if anchor not in text: raise SystemExit('orphan cleanup anchor missing')
path.write_text(text.replace(anchor, replacement))
PY

git diff --check -- postgres/migrations/003_seed_local_data.sql
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx src/app/globals.css src/db/README.md tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git status --short --branch
git status --short --branch
git diff --stat -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx src/db/README.md tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git add postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx src/db/README.md tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git diff --cached --check
git diff --cached --stat
git status --short --branch
git status --short --branch
git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/005_add_recurring_expense_occurrences.sql supabase/migrations/202606020001_add_recurring_expense_occurrences.sql src/app/api/recurring-expenses/candidates/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx src/db/README.md tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git commit -m 'feat: 定期支出の月次候補を追加する' -m '- 定期支出の月次処理記録を追加し、二重登録を防止する
- 当月候補の確定、スキップ、金額修正、前回差分表示を追加する
- 統合テストとPlaywrightで定期支出の確定導線を確認する'
git status --short --branch
git log -2 --oneline
python3 - <<'PY'
from pathlib import Path
migration = "alter table recurring_expenses add column if not exists notification_enabled boolean not null default true;\n"
Path('postgres/migrations/006_add_recurring_expense_notification.sql').write_text(migration)
Path('supabase/migrations/202606020002_add_recurring_expense_notification.sql').write_text(migration.replace('alter table recurring_expenses', 'alter table public.recurring_expenses'))

prepare = Path('postgres/migrations/002_prepare_local_environments.sh')
text = prepare.read_text()
text = text.replace('recurring_occurrences_migration_file="/docker-entrypoint-initdb.d/005_add_recurring_expense_occurrences.sql"', 'recurring_occurrences_migration_file="/docker-entrypoint-initdb.d/005_add_recurring_expense_occurrences.sql"\nrecurring_notification_migration_file="/docker-entrypoint-initdb.d/006_add_recurring_expense_notification.sql"')
text = text.replace('''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$recurring_occurrences_migration_file"''', '''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$recurring_occurrences_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$recurring_notification_migration_file"''')
prepare.write_text(text)

api = Path('src/app/api/recurring-expenses/candidates/route.ts')
text = api.read_text()
text = text.replace('''const actionSchema = periodSchema.extend({\n  recurringExpenseId: z.string().uuid(),\n  action: z.enum(["confirm", "skip"]),\n  amount: z.number().int().positive().optional(),\n});''', '''const actionSchema = periodSchema.extend({\n  recurringExpenseId: z.string().uuid(),\n  action: z.enum(["confirm", "skip"]),\n  amount: z.number().int().positive().optional(),\n});\nconst notificationSchema = z.object({\n  recurringExpenseId: z.string().uuid(),\n  notificationEnabled: z.boolean(),\n});''')
text = text.replace('''  select re.id::text, c.name as title, c.name as category, re.amount, re.day_of_month, re.scope::text,''', '''  select re.id::text, c.name as title, c.name as category, re.amount, re.day_of_month, re.scope::text, re.notification_enabled,''')
text += '''\nexport async function PATCH(request: Request) {\n  try {\n    const result = notificationSchema.safeParse(await request.json());\n    if (!result.success) return NextResponse.json({ error: "invalid_notification_setting" }, { status: 400 });\n    const pool = db();\n    const household = (await pool.query<Household>(householdQuery)).rows[0];\n    if (!household) return NextResponse.json({ error: "household_not_found" }, { status: 404 });\n    const recurring = (await pool.query<{ id: string }>(\n      "update recurring_expenses set notification_enabled = $1 where id = $2 and household_id = $3 returning id",\n      [result.data.notificationEnabled, result.data.recurringExpenseId, household.id],\n    )).rows[0];\n    if (!recurring) return NextResponse.json({ error: "recurring_expense_not_found" }, { status: 404 });\n    console.info("Updated recurring expense notification", result.data);\n    return NextResponse.json({ notificationEnabled: result.data.notificationEnabled });\n  } catch (error) {\n    console.error("Failed to update recurring expense notification", error);\n    return NextResponse.json({ error: "recurring_notification_update_failed" }, { status: 500 });\n  }\n}\n'''
api.write_text(text)

ui = Path('src/components/kakeibo-dashboard.tsx')
text = ui.read_text()
text = text.replace('''  previous_amount: number | null;\n};''', '''  previous_amount: number | null;\n  notification_enabled: boolean;\n};''')
anchor = '''  const handleRecurringCandidate = async (candidate: RecurringCandidate, action: "confirm" | "skip") => {'''
addition = '''  const toggleRecurringNotification = async (candidate: RecurringCandidate) => {\n    setRecurringError(null);\n    try {\n      const response = await fetch("/api/recurring-expenses/candidates", {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ recurringExpenseId: candidate.id, notificationEnabled: !candidate.notification_enabled }),\n      });\n      if (!response.ok) throw new Error("recurring_notification_update_failed");\n      setRecurringCandidates((current) => current.map((item) => item.id === candidate.id ? { ...item, notification_enabled: !item.notification_enabled } : item));\n    } catch {\n      setRecurringError("通知設定を更新できませんでした。");\n    }\n  };\n\n'''
if anchor not in text: raise SystemExit('handler anchor missing')
text = text.replace(anchor, addition + anchor)
old = '''                <div className="recurring-card__actions">\n                  {candidate.status === "pending" ? ('''
new = '''                <div className="recurring-card__actions">\n                  <button className="chip" type="button" aria-pressed={candidate.notification_enabled} onClick={() => void toggleRecurringNotification(candidate)}>通知 {candidate.notification_enabled ? "ON" : "OFF"}</button>\n                  {candidate.status === "pending" ? ('''
if old not in text: raise SystemExit('actions anchor missing')
ui.write_text(text.replace(old, new))

integration = Path('tests/integration/api.integration.test.ts')
text = integration.read_text()
anchor = '''  it("rejects malformed expense ids before querying PostgreSQL", async () => {\n'''
addition = '''  it("updates recurring expense notification settings", async () => {\n    const { GET, PATCH } = await import("../../src/app/api/recurring-expenses/candidates/route.ts");\n    const recurringExpenseId = "eeeeeeee-eeee-4eee-8eee-eeeeeeeeeee1";\n    const updated = await PATCH(new Request("http://localhost/api/recurring-expenses/candidates", {\n      method: "PATCH", headers: { "Content-Type": "application/json" },\n      body: JSON.stringify({ recurringExpenseId, notificationEnabled: false }),\n    }));\n    assert.equal(updated.status, 200);\n    const list = await GET(new Request(`http://localhost/api/recurring-expenses/candidates?year=${recurringTestPeriod.year}&month=${recurringTestPeriod.month}`));\n    assert.equal((await list.json()).candidates[0].notification_enabled, false);\n    await pool.query("update recurring_expenses set notification_enabled = true where id = $1", [recurringExpenseId]);\n  });\n\n'''
if anchor not in text: raise SystemExit('test anchor missing')
integration.write_text(text.replace(anchor, addition + anchor))
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/006_add_recurring_expense_notification.sql supabase/migrations/202606020002_add_recurring_expense_notification.sql src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts

docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
python3 - <<'PY'
from pathlib import Path
path = Path('tests/e2e/kakeibo.spec.ts')
text = path.read_text()
old = '''  await expect(panel.getByText("サブスク", { exact: true })).toBeVisible();
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/recurring-expenses/candidates") && response.request().method() === "POST" && response.ok()),
'''
new = '''  await expect(panel.getByText("サブスク", { exact: true })).toBeVisible();
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/recurring-expenses/candidates") && response.request().method() === "PATCH" && response.ok()),
    panel.getByRole("button", { name: "通知 ON", exact: true }).click(),
  ]);
  await expect(panel.getByRole("button", { name: "通知 OFF", exact: true })).toBeVisible();
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/recurring-expenses/candidates") && response.request().method() === "POST" && response.ok()),
'''
if old not in text: raise SystemExit('recurring e2e anchor missing')
path.write_text(text.replace(old, new))
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/006_add_recurring_expense_notification.sql supabase/migrations/202606020002_add_recurring_expense_notification.sql src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
npm run build
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '定期支出候補を今月の支出へ確定する'
sed -n '1,220p' test-results/kakeibo-定期支出候補を今月の支出へ確定する-chromium/error-context.md 2>/dev/null || true
docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select column_name from information_schema.columns where table_name = 'recurring_expenses' order by ordinal_position; select id, notification_enabled from recurring_expenses;"
git add postgres/migrations/002_prepare_local_environments.sh postgres/migrations/006_add_recurring_expense_notification.sql supabase/migrations/202606020002_add_recurring_expense_notification.sql src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git diff --cached --check
git diff --cached --stat
git status --short --branch
git commit -m 'feat: 定期支出の通知設定を追加する' -m '- 定期支出テンプレートへ通知ON/OFF設定を追加する
- 候補画面から通知設定を切り替えられるようにする
- 統合テストとPlaywrightで通知切替を確認する'
git status --short --branch
git log -3 --oneline
mkdir -p src/app/api/recurring-expenses
python3 - <<'PY'
from pathlib import Path
migration = "alter table recurring_expenses add column if not exists name text;\nupdate recurring_expenses re set name = c.name from categories c where c.id = re.category_id and (re.name is null or trim(re.name) = '');\nalter table recurring_expenses alter column name set not null;\n"
Path('postgres/migrations/007_add_recurring_expense_name.sql').write_text(migration)
Path('supabase/migrations/202606020003_add_recurring_expense_name.sql').write_text(migration.replace('alter table recurring_expenses', 'alter table public.recurring_expenses').replace('update recurring_expenses', 'update public.recurring_expenses').replace('from categories', 'from public.categories'))
prepare=Path('postgres/migrations/002_prepare_local_environments.sh'); text=prepare.read_text()
text=text.replace('recurring_notification_migration_file="/docker-entrypoint-initdb.d/006_add_recurring_expense_notification.sql"','recurring_notification_migration_file="/docker-entrypoint-initdb.d/006_add_recurring_expense_notification.sql"\nrecurring_name_migration_file="/docker-entrypoint-initdb.d/007_add_recurring_expense_name.sql"')
text=text.replace('''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$recurring_notification_migration_file"''','''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$recurring_notification_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$recurring_name_migration_file"''')
prepare.write_text(text)

seed=Path('postgres/migrations/003_seed_local_data.sql'); text=seed.read_text()
text=text.replace('insert into recurring_expenses (id, household_id, category_id, payment_source_id, paid_by_member_id, amount, day_of_month, scope, split_type, active)', 'insert into recurring_expenses (id, household_id, category_id, payment_source_id, paid_by_member_id, name, amount, day_of_month, scope, split_type, active)')
text=text.replace("    'aaaaaaaa-1111-4111-8111-aaaaaaaa1111',\n    1490,", "    'aaaaaaaa-1111-4111-8111-aaaaaaaa1111',\n    '動画サブスク',\n    1490,")
text=text.replace('''  paid_by_member_id = excluded.paid_by_member_id,
  amount = excluded.amount,''','''  paid_by_member_id = excluded.paid_by_member_id,
  name = excluded.name,
  amount = excluded.amount,''',1)
text=text.replace("and e.memo = c.name || ' 定期支出'", "and e.memo = re.name || ' 定期支出'")
seed.write_text(text)

api=Path('src/app/api/recurring-expenses/candidates/route.ts'); text=api.read_text()
text=text.replace('c.name as title, c.name as category', 're.name as title, c.name as category')
text=text.replace('re.split_type::text, c.name as title', 're.split_type::text, re.name as title')
api.write_text(text)

Path('src/app/api/recurring-expenses/route.ts').write_text('''import { NextResponse } from "next/server.js";\nimport { z } from "zod";\nimport { db } from "../../../lib/db.ts";\n\nexport const runtime = "nodejs";\nexport const dynamic = "force-dynamic";\nconst schema = z.object({ name: z.string().trim().min(1).max(100), category: z.string().trim().min(1).max(50), paidBy: z.enum(["a", "b"]), amount: z.number().int().positive(), dayOfMonth: z.number().int().min(1).max(31), scope: z.enum(["shared", "personal", "review"]), notificationEnabled: z.boolean().default(true) });\nconst householdQuery = `select id from households order by created_at asc limit 1`;\nconst membersQuery = `select id from household_members where household_id = $1 order by role asc, created_at asc`;\nconst categoryQuery = `insert into categories (household_id, name, kind, default_expense_scope, default_split_type) values ($1, $2, 'other', $3, 'equal') on conflict (household_id, name) do update set name = excluded.name returning id`;\nconst paymentQuery = `select id from payment_sources where household_id = $1 and owner_member_id = $2 and type = 'member_personal' order by created_at asc limit 1`;\nconst insertQuery = `insert into recurring_expenses (household_id, category_id, payment_source_id, paid_by_member_id, name, amount, day_of_month, scope, split_type, notification_enabled) values ($1,$2,$3,$4,$5,$6,$7,$8,'equal',$9) returning id::text`;\nexport async function POST(request: Request) {\n  const pool = db(); const client = await pool.connect();\n  try {\n    const parsed = schema.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ error: "invalid_recurring_expense" }, { status: 400 });\n    await client.query("begin"); const household=(await client.query<{id:string}>(householdQuery)).rows[0]; if(!household){await client.query("rollback");return NextResponse.json({error:"household_not_found"},{status:404});}\n    const members=(await client.query<{id:string}>(membersQuery,[household.id])).rows; const payer=parsed.data.paidBy==="a"?members[0]:members[1]; if(!payer){await client.query("rollback");return NextResponse.json({error:"payer_not_found"},{status:400});}\n    const category=(await client.query<{id:string}>(categoryQuery,[household.id,parsed.data.category,parsed.data.scope])).rows[0]; const payment=(await client.query<{id:string}>(paymentQuery,[household.id,payer.id])).rows[0]; if(!category||!payment){await client.query("rollback");return NextResponse.json({error:"recurring_source_not_found"},{status:400});}\n    const recurring=(await client.query<{id:string}>(insertQuery,[household.id,category.id,payment.id,payer.id,parsed.data.name,parsed.data.amount,parsed.data.dayOfMonth,parsed.data.scope,parsed.data.notificationEnabled])).rows[0]; await client.query("commit"); console.info("Created recurring expense",{id:recurring.id}); return NextResponse.json({id:recurring.id},{status:201});\n  } catch(error){await client.query("rollback");console.error("Failed to create recurring expense",error);return NextResponse.json({error:"recurring_expense_create_failed"},{status:500});} finally {client.release();}\n}\n''')
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/007_add_recurring_expense_name.sql supabase/migrations/202606020003_add_recurring_expense_name.sql src/app/api/recurring-expenses/candidates/route.ts src/app/api/recurring-expenses/route.ts
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
python3 - <<'PY'
from pathlib import Path
ui=Path('src/components/kakeibo-dashboard.tsx'); text=ui.read_text()
anchor='''  const [recurringRefresh, setRecurringRefresh] = useState(0);\n  const composerInputRef'''
addition='''  const [recurringDraft, setRecurringDraft] = useState({ name: "", category: "", paidBy: "a" as MemberId, amount: "", dayOfMonth: "10", scope: "shared" as ExpenseScope, notificationEnabled: true });\n'''
if anchor not in text: raise SystemExit('draft state anchor missing')
text=text.replace(anchor,'  const [recurringRefresh, setRecurringRefresh] = useState(0);\n'+addition+'  const composerInputRef')
anchor='''  const toggleRecurringNotification = async (candidate: RecurringCandidate) => {'''
addition='''  const createRecurringExpense = async () => {\n    const amount = Number(recurringDraft.amount);\n    const dayOfMonth = Number(recurringDraft.dayOfMonth);\n    if (!recurringDraft.name.trim() || !recurringDraft.category.trim() || !Number.isInteger(amount) || amount <= 0 || !Number.isInteger(dayOfMonth) || dayOfMonth < 1 || dayOfMonth > 31) {\n      setRecurringError("定期支出の入力内容を確認してください。");\n      return;\n    }\n    setRecurringError(null);\n    try {\n      const response = await fetch("/api/recurring-expenses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...recurringDraft, amount, dayOfMonth }) });\n      if (!response.ok) throw new Error("recurring_expense_create_failed");\n      setRecurringDraft({ name: "", category: "", paidBy: "a", amount: "", dayOfMonth: "10", scope: "shared", notificationEnabled: true });\n      setRecurringRefresh((current) => current + 1);\n    } catch { setRecurringError("定期支出を登録できませんでした。"); }\n  };\n\n'''
if anchor not in text: raise SystemExit('handler anchor missing')
text=text.replace(anchor,addition+anchor)
anchor='''      <div className="settings-grid">\n        <article className="panel settings-panel">'''
new='''      <div className="settings-grid">\n        <article className="panel settings-panel recurring-settings">\n          <h3>定期支出を登録</h3>\n          <div className="recurring-settings__grid">\n            <input aria-label="定期支出名" placeholder="例: 動画サブスク" value={recurringDraft.name} onChange={(event) => setRecurringDraft({ ...recurringDraft, name: event.target.value })} />\n            <input aria-label="定期支出カテゴリ" placeholder="例: サブスク" value={recurringDraft.category} onChange={(event) => setRecurringDraft({ ...recurringDraft, category: event.target.value })} />\n            <input aria-label="定期支出金額" inputMode="numeric" placeholder="金額" value={recurringDraft.amount} onChange={(event) => setRecurringDraft({ ...recurringDraft, amount: event.target.value })} />\n            <input aria-label="定期支出予定日" inputMode="numeric" placeholder="日" value={recurringDraft.dayOfMonth} onChange={(event) => setRecurringDraft({ ...recurringDraft, dayOfMonth: event.target.value })} />\n            <select aria-label="定期支出支払者" value={recurringDraft.paidBy} onChange={(event) => setRecurringDraft({ ...recurringDraft, paidBy: event.target.value as MemberId })}>{members.map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}</select>\n            <select aria-label="定期支出分類" value={recurringDraft.scope} onChange={(event) => setRecurringDraft({ ...recurringDraft, scope: event.target.value as ExpenseScope })}><option value="shared">共通費</option><option value="personal">個人費</option><option value="review">要確認</option></select>\n            <button className="chip chip--active" type="button" onClick={() => void createRecurringExpense()}>定期支出を追加</button>\n          </div>\n        </article>\n        <article className="panel settings-panel">'''
if anchor not in text: raise SystemExit('settings grid anchor missing')
text=text.replace(anchor,new)
ui.write_text(text)

css=Path('src/app/globals.css'); css.write_text(css.read_text()+'''\n.recurring-settings { grid-column: 1 / -1; }\n.recurring-settings__grid { display: grid; gap: 8px; }\n.recurring-settings__grid input, .recurring-settings__grid select { min-height: 40px; padding: 0 10px; border: 1px solid var(--border); border-radius: 8px; background: #fff; color: var(--text); }\n@media (min-width: 720px) { .recurring-settings__grid { grid-template-columns: repeat(3, minmax(0, 1fr)); } }\n''')

integration=Path('tests/integration/api.integration.test.ts'); text=integration.read_text()
text=text.replace('''    await pool.query("delete from recurring_expense_occurrences where year = $1 and month = $2", [recurringTestPeriod.year, recurringTestPeriod.month]);''','''    await pool.query("delete from recurring_expense_occurrences where year = $1 and month = $2", [recurringTestPeriod.year, recurringTestPeriod.month]);\n    await pool.query("delete from recurring_expenses where name = '統合テスト定期支出'");''')
anchor='''  it("updates recurring expense notification settings", async () => {'''
addition='''  it("creates a recurring expense template", async () => {\n    const { POST } = await import("../../src/app/api/recurring-expenses/route.ts");\n    const response = await POST(new Request("http://localhost/api/recurring-expenses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: "統合テスト定期支出", category: "通信費", paidBy: "a", amount: 3980, dayOfMonth: 20, scope: "shared", notificationEnabled: true }) }));\n    assert.equal(response.status, 201);\n    assert.deepEqual((await pool.query<{ name:string; amount:number }>("select name, amount from recurring_expenses where name = '統合テスト定期支出'")).rows, [{ name: "統合テスト定期支出", amount: 3980 }]);\n  });\n\n'''
if anchor not in text: raise SystemExit('integration add anchor missing')
integration.write_text(text.replace(anchor,addition+anchor))
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/integration/api.integration.test.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
python3 - <<'PY'
from pathlib import Path
path=Path('tests/integration/api.integration.test.ts'); text=path.read_text().replace('assert.equal(candidate.title, "サブスク");','assert.equal(candidate.title, "動画サブスク");'); path.write_text(text)
path=Path('tests/e2e/kakeibo.spec.ts'); text=path.read_text().replace('panel.getByText("サブスク", { exact: true })','panel.getByText("動画サブスク", { exact: true })'); path.write_text(text)
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/007_add_recurring_expense_name.sql supabase/migrations/202606020003_add_recurring_expense_name.sql src/app/api/recurring-expenses/route.ts src/app/api/recurring-expenses/candidates/route.ts src/components/kakeibo-dashboard.tsx src/app/globals.css tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm test
npm run build
git add postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/007_add_recurring_expense_name.sql supabase/migrations/202606020003_add_recurring_expense_name.sql src/app/api/recurring-expenses/route.ts src/app/api/recurring-expenses/candidates/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts
git diff --cached --check
git diff --cached --stat
git status --short --branch
git commit -m 'feat: 定期支出テンプレート登録を追加する' -m '- 定期支出テンプレートへ名称を追加する
- 設定画面から名称、カテゴリ、支払者、金額、予定日、分類を登録する
- 統合テストでテンプレート作成を確認する'
git status --short --branch
git log -4 --oneline
git push origin main
gh issue close 12 --comment 'MVP実装済みです。定期支出テンプレート登録、当月候補生成、金額修正、前回差分、確定・スキップ、二重登録防止、予定日までの日数表示、通知ON/OFF設定を追加しました。commits: bcc4e43, 525cd32, db15021'
gh issue view 12 --json number,state,url
git status --short --branch
find . -path './node_modules' -prune -o -path './.git' -prune -o -type f -print | sort | grep -E 'README|SPEC|docs|Docs|\.md$'
git status --short --branch
git diff -- README.md package.json
git log --oneline --max-count=15
sed -n '1,280p' Docs/product-spec.md
printf '\n-- screens --\n'
sed -n '1,260p' Docs/screens.md
sed -n '1,300p' Docs/data-model.md
printf '\n-- api security --\n'
sed -n '1,320p' Docs/api-security-spec.md
sed -n '1,240p' Docs/mvp-policy.md
printf '\n-- architecture --\n'
sed -n '1,240p' Docs/architecture.md
printf '\n-- settlement --\n'
sed -n '1,260p' Docs/settlement-logic.md
python3 - <<'PY'
from pathlib import Path

product = Path('Docs/product-spec.md')
text = product.read_text()
text += '''\n## 実装済みMVP機能（2026-06-02）\n\n### 軽量な支出入力\n\n- 画面下部の入力欄から `店舗名 金額` 形式で登録する\n- 入力欄は通常時に畳み、フォーカス時だけ候補や補助操作を展開する\n- `仮登録` は入力内容を `要確認` として保存する\n- PWAショートカット `/?compose=quick` から入力欄へ直接フォーカスできる\n- 過去履歴から店舗名、カテゴリ、区分、直近金額を候補表示する\n\n### 支出整理と月次精算\n\n- 支出は `共通費`、`個人費`、`要確認` に分類する\n- 共通費の負担割合は `5:5`、`6:4`、`4:6`、`7:3`、`3:7` から選択する\n- 支出のタイトル、金額、日付、支払者、カテゴリ、メモを編集できる\n- 支出を削除できる\n- 精算済み月は支出の追加、編集、削除を禁止する\n- 精算取り消し時は理由と履歴を残し、再編集を許可する\n\n### 使用料グラフ\n\n- 選択月を含む直近6か月の支出推移を棒グラフで表示する\n- 選択月のカテゴリ別内訳を表示する\n- 区分、支払者、カテゴリで絞り込める\n- ローディング、エラー、空状態を表示する\n\n### 定期支出\n\n- 設定画面から名称、カテゴリ、支払者、標準金額、予定日、区分を登録する\n- 当月候補をホーム画面に表示する\n- 候補金額を修正して確定、または当月分をスキップする\n- 月ごとの処理記録により二重登録を防止する\n- 過去の確定額があれば前回差分を表示する\n- 引き落とし予定日までの日数を表示する\n- テンプレート単位で通知ON/OFFを保存する\n\n## MVPの未実装境界\n\n- 認証、世帯招待、世帯単位のアクセス制御は未実装\n- 通知ON/OFFは保存できるが、Push通知やメール配信は未実装\n- レシートOCR、明細分割、外部金融連携は未実装\n- 共通口座への入金管理は未実装\n'''
product.write_text(text)

screens = Path('Docs/screens.md')
text = screens.read_text()
text += '''\n## 現在の画面構成（2026-06-02）\n\n### 共通ヘッダー\n\n- 表示月の前月・翌月切替\n- レシートOCR用の将来導線\n- 今月の精算結果、共有費、要確認額\n\n### ホームタブ\n\n表示項目:\n\n- 使用料グラフ: 直近6か月、カテゴリ内訳、区分・支払者・カテゴリフィルタ\n- 今月の定期支出: 予定日、予定日までの日数、金額、前回差分、通知ON/OFF、確定、スキップ\n- 今月の流れ: 支出タイムライン\n- 要確認: 未整理支出と分類操作\n\n### 精算タブ\n\n- 月内支出の区分変更と負担割合変更\n- 要確認が0件の場合のみ精算確定\n- 精算済み月の取り消し\n- 確定・取り消し履歴\n\n### 履歴タブ\n\n- 支出の区分、負担割合確認\n- タイトル、金額、日付、支払者、カテゴリ、メモ編集\n- 支出削除\n\n### 設定タブ\n\n- 入力する人\n- 共通費の初期割合\n- 定期支出テンプレート登録\n\n### 固定composer\n\n- `店舗名 金額` 形式の入力\n- 店舗履歴候補\n- クイック入力\n- `仮登録` による要確認保存\n- PWAショートカット `/?compose=quick` からの直接フォーカス\n'''
screens.write_text(text)

model = Path('Docs/data-model.md')
text = model.read_text()
text = text.replace('''paid_by_member_id\namount\nday_of_month\nscope\nsplit_type\nactive''', '''paid_by_member_id\nname\namount\nday_of_month\nscope\nsplit_type\nactive\nnotification_enabled''')
text = text.replace('''## settlement_periods\n''', '''## recurring_expense_occurrences\n\n定期支出テンプレートの月ごとの処理状態。候補再取得時の二重登録を防止する。\n\n```txt\nid\nrecurring_expense_id\nyear\nmonth\nstatus\nexpense_id\namount\ncreated_at\nupdated_at\n```\n\nstatus:\n\n- confirmed\n- skipped\n\n## settlement_periods\n''')
text += '''\n## 追加マイグレーション\n\n- `202606010001_add_settlement_period_events.sql`: 精算確定・取り消し履歴\n- `202606020001_add_recurring_expense_occurrences.sql`: 定期支出の月次処理記録\n- `202606020002_add_recurring_expense_notification.sql`: 定期支出の通知ON/OFF\n- `202606020003_add_recurring_expense_name.sql`: 定期支出テンプレート名称\n'''
model.write_text(text)

api = Path('Docs/api-security-spec.md')
text = api.read_text()
text = text.replace('`"equal"` / `"a70"` / `"b70"`', '`"equal"` / `"a60"` / `"b60"` / `"a70"` / `"b70"`')
text += '''\n---\n\n## 5. 追加API（2026-06-02）\n\n### 5.1. 店舗候補 (`GET /api/expenses/suggestions`)\n\n過去の支出履歴から、入力途中の店舗名に合う候補を最大6件返す。店舗ごとによく使うカテゴリ、区分、直近金額を返す。\n\n| パラメータ | 型 | 必須 | ルール |\n|---|---|---|---|\n| `q` | string | 任意 | 最大100文字 |\n\n### 5.2. 使用料グラフ (`GET /api/analytics`)\n\n選択月を含む直近6か月の支出推移、選択月のカテゴリ内訳、カテゴリ候補を返す。\n\n| パラメータ | 型 | 必須 | ルール |\n|---|---|---|---|\n| `year` | string | 必須 | 2000〜9999 |\n| `month` | string | 必須 | 1〜12 |\n| `scope` | string | 任意 | `all` / `shared` / `personal` / `review` |\n| `paidBy` | string | 任意 | `all` / `a` / `b` |\n| `category` | string | 任意 | 最大50文字 |\n\n### 5.3. 定期支出候補 (`GET /api/recurring-expenses/candidates`)\n\n指定月の有効な定期支出テンプレートと処理状態を返す。予定日は月末を超えないよう補正する。\n\n### 5.4. 定期支出候補の処理 (`POST /api/recurring-expenses/candidates`)\n\n`confirm` または `skip` をトランザクション内で処理する。`confirm` は支出と負担割合を作成し、月次処理記録を保存する。`recurring_expense_id + year + month` の一意制約により二重登録を拒否する。\n\n### 5.5. 定期支出の通知設定 (`PATCH /api/recurring-expenses/candidates`)\n\nテンプレート単位の `notificationEnabled` を更新する。通知配信基盤自体は未実装。\n\n### 5.6. 定期支出テンプレート登録 (`POST /api/recurring-expenses`)\n\n名称、カテゴリ、支払者、標準金額、予定日、区分、通知ON/OFFを登録する。カテゴリが未登録の場合は世帯内カテゴリを作成する。\n\n### 5.7. 精算取り消し (`POST /api/settlements/reopen`)\n\n精算済み月を明示的に開き直す。理由を必須とし、取り消し履歴を保存する。開き直し後は支出の追加、編集、削除を再び許可する。\n\n## 6. 精算済み月の保護\n\n- 精算済み月では支出の追加、編集、削除を拒否する\n- 定期支出候補の確定・スキップも拒否する\n- 再編集には `POST /api/settlements/reopen` による明示的な取り消しが必要\n'''
api.write_text(text)
PY

git diff --check -- Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
git diff --stat -- Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
git status --short --branch
grep -R "a60\|b60\|settlements/reopen\|recurring_expense_occurrences\|api/analytics\|expenses/suggestions\|compose=quick" -n Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
git diff -- Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
python3 - <<'PY'
from pathlib import Path
path = Path('Docs/data-model.md')
text = path.read_text()
anchor = '''## settlements\n\n精算結果。\n'''
addition = '''## settlement_period_events\n\n精算期間の確定・取り消し履歴。取り消し時は理由を必須とする。\n\n```txt\nid\nperiod_id\naction\nreason\nacted_by_member_id\nsettlement_snapshot\ncreated_at\n```\n\naction:\n\n- settled\n- reopened\n\n'''
if anchor not in text:
    raise SystemExit('settlements anchor not found')
path.write_text(text.replace(anchor, addition + anchor))
PY

git diff --check -- Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
git diff --stat -- Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
git status --short --branch
git add Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
git diff --cached --check
git diff --cached --stat
git status --short --branch
git commit -m 'docs: 実装済みMVP仕様を整理する' -m '- 支出入力、精算、グラフ、定期支出の現行仕様を追記する
- 追加テーブルとmigrationをデータモデルへ反映する
- 店舗候補、分析、定期支出、精算取り消しAPIを整理する'
git push origin main
git status --short --branch
git log -2 --oneline
git status --short --branch && printf '\n-- files --\n' && find . -maxdepth 3 -type f | sed 's#^./##' | sort | sed -n '1,220p'
gh issue list -R hayaryoo519/kakeibo --state open --limit 100
gh issue view 15 -R hayaryoo519/kakeibo --json number,title,state,body,url && printf '\n-- source files --\n' && find app lib components tests -type f 2>/dev/null | sort && printf '\n-- package --\n' && sed -n '1,220p' package.json
gh api repos/team-mirai/marumie --jq '{html_url,default_branch,license:.license.spdx_id,description}' && printf '\n-- root --\n' && gh api repos/team-mirai/marumie/contents --jq '.[] | [.type,.name,.path] | @tsv' && printf '\n-- license candidates --\n' && gh api repos/team-mirai/marumie/git/trees/main?recursive=1 --jq '.tree[].path' | grep -Ei '(^|/)(license|copying|notice)(\.|$)|chart|graph|dashboard' | sed -n '1,240p'
printf '%s\n' '-- dirs --' && find . -path './node_modules' -prune -o -path './.next' -prune -o -path './.git' -prune -o -type f -print | sort | sed -n '1,260p' && printf '\n-- analytics refs --\n' && grep -R "analytics\|月別\|カテゴリ別\|categoryTotals\|monthly" -n --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git . | sed -n '1,260p'
printf '%s\n' '-- LICENSE --' && gh api repos/team-mirai/marumie/contents/LICENSE?ref=develop --jq '.content' | base64 -d && printf '\n-- charts package refs --\n' && gh api repos/team-mirai/marumie/contents/webapp/package.json?ref=develop --jq '.content' | base64 -d | grep -n "recharts\|d3\|chart\|dependencies" && printf '\n-- MonthlyChart --\n' && gh api repos/team-mirai/marumie/contents/webapp/src/client/components/top-page/features/charts/MonthlyChart.tsx?ref=develop --jq '.content' | base64 -d && printf '\n-- BalanceSheetChart --\n' && gh api repos/team-mirai/marumie/contents/webapp/src/client/components/top-page/features/charts/BalanceSheetChart.tsx?ref=develop --jq '.content' | base64 -d
sed -n '1,280p' src/components/kakeibo-dashboard.tsx && printf '\n-- middle --\n' && sed -n '280,620p' src/components/kakeibo-dashboard.tsx && printf '\n-- lower --\n' && sed -n '620,980p' src/components/kakeibo-dashboard.tsx
sed -n '1,220p' src/app/api/analytics/route.ts && printf '\n-- analytics css --\n' && sed -n '650,880p' src/app/globals.css && printf '\n-- dashboard css nav refs --\n' && grep -n "tab\|nav\|header\|layout-grid\|section__" src/app/globals.css | sed -n '1,240p' && printf '\n-- e2e --\n' && sed -n '1,180p' tests/e2e/kakeibo.spec.ts && printf '\n-- integration analytics --\n' && sed -n '70,130p' tests/integration/api.integration.test.ts
printf '%s\n' '-- SankeyChart --' && gh api repos/team-mirai/marumie/contents/webapp/src/client/components/top-page/features/charts/SankeyChart.tsx?ref=develop --jq '.content' | base64 -d | sed -n '1,280p' && printf '\n-- README license refs --\n' && gh api repos/team-mirai/marumie/contents/README.md?ref=develop --jq '.content' | base64 -d | grep -n -i "license\|AGPL"
sed -n '900,1160p' src/components/kakeibo-dashboard.tsx && printf '\n-- css beginning and nav --\n' && sed -n '1,140p' src/app/globals.css && sed -n '470,570p' src/app/globals.css && printf '\n-- docs analysis refs --\n' && grep -n "分析\|使用料グラフ\|画面構成\|ホーム" Docs/product-spec.md Docs/screens.md | sed -n '1,220p'
python3 - <<'PY'
from pathlib import Path
p = Path('src/components/kakeibo-dashboard.tsx')
s = p.read_text()
def replace(old, new):
    global s
    if old not in s:
        raise SystemExit(f'anchor missing: {old[:120]!r}')
    s = s.replace(old, new, 1)
replace('import { Camera, Check, ChevronLeft, ChevronRight, Home, List, Pencil, Plus, ReceiptText, Save, Send, Settings, Trash2, Wallet, X } from "lucide-react";', 'import { Camera, ChartNoAxesCombined, Check, ChevronLeft, ChevronRight, Home, List, Pencil, Plus, ReceiptText, Save, Send, Settings, Trash2, Wallet, X } from "lucide-react";')
replace('  { id: "home", label: "ホーム", icon: Home },\n  { id: "settlement", label: "精算", icon: Wallet },', '  { id: "home", label: "ホーム", icon: Home },\n  { id: "analytics", label: "分析", icon: ChartNoAxesCombined },\n  { id: "settlement", label: "精算", icon: Wallet },')
replace('  const categoryTotal = analytics?.categories.reduce((total, item) => total + item.total, 0) ?? 0;\n', '  const categoryTotal = analytics?.categories.reduce((total, item) => total + item.total, 0) ?? 0;\n  const sharedPayerMax = Math.max(stats.paidA, stats.paidB, 1);\n')
anchor = '''  const homeView = (\n'''
addition = '''  const analyticsView = (\n    <section className="analysis-view tab-view" aria-label="分析ページ">\n      <div className="section__header">\n        <div>\n          <h2 className="section__title">分析（仮）</h2>\n          <p className="section__meta">支出の傾向と立替状況をまとめて見る</p>\n        </div>\n      </div>\n\n      <div className="metric-grid analysis-metrics">\n        <div className="metric-card">\n          <span>支出合計</span>\n          <strong>{formatMoney(categoryTotal)}</strong>\n        </div>\n        <div className="metric-card">\n          <span>共通費</span>\n          <strong>{formatMoney(stats.sharedTotal)}</strong>\n        </div>\n        <div className="metric-card">\n          <span>要確認</span>\n          <strong>{formatMoney(stats.reviewTotal)}</strong>\n        </div>\n      </div>\n\n      {analyticsPanel}\n\n      <div className="analytics-detail-grid">\n        <article className="panel analytics-detail-card" aria-label="カテゴリ別支出構成">\n          <div className="section__header">\n            <div>\n              <h3 className="section__title">支出の流れ</h3>\n              <p className="section__meta">合計からカテゴリへの配分を見る</p>\n            </div>\n          </div>\n          <div className="expense-flow">\n            <div className="expense-flow__total">\n              <span>支出合計</span>\n              <strong>{formatMoney(categoryTotal)}</strong>\n            </div>\n            <div className="expense-flow__categories">\n              {(analytics?.categories ?? []).map((item) => {\n                const percentage = categoryTotal ? item.total / categoryTotal * 100 : 0;\n                return (\n                  <div key={item.category} className="expense-flow__category">\n                    <div>\n                      <span>{item.category}</span>\n                      <strong>{formatMoney(item.total)}</strong>\n                    </div>\n                    <div className="expense-flow__track"><span style={{ width: `${percentage}%` }} /></div>\n                    <small>{Math.round(percentage)}%</small>\n                  </div>\n                );\n              })}\n            </div>\n          </div>\n        </article>\n\n        <article className="panel analytics-detail-card" aria-label="支払者別立替比較">\n          <div className="section__header">\n            <div>\n              <h3 className="section__title">共通費の立替</h3>\n              <p className="section__meta">誰がどれだけ先に払っているかを見る</p>\n            </div>\n          </div>\n          <div className="payer-chart">\n            {members.map((member) => {\n              const amount = member.id === "a" ? stats.paidA : stats.paidB;\n              return (\n                <div key={member.id} className="payer-chart__item">\n                  <div><span>{member.name}</span><strong>{formatMoney(amount)}</strong></div>\n                  <div className="payer-chart__track"><span style={{ width: `${amount / sharedPayerMax * 100}%` }} /></div>\n                </div>\n              );\n            })}\n          </div>\n        </article>\n      </div>\n    </section>\n  );\n\n'''
replace(anchor, addition + anchor)
replace('  const currentView = activeTab === "settlement" ? settlementView : activeTab === "history" ? historyView : activeTab === "settings" ? settingsView : homeView;', '  const currentView = activeTab === "analytics" ? analyticsView : activeTab === "settlement" ? settlementView : activeTab === "history" ? historyView : activeTab === "settings" ? settingsView : homeView;')
p.write_text(s)
PY
python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
s = p.read_text()
s = s.replace('  grid-template-columns: repeat(4, 1fr);', '  grid-template-columns: repeat(5, 1fr);', 1)
addition = '''\n.analysis-view {\n  display: grid;\n  gap: 16px;\n}\n\n.analysis-metrics {\n  margin-top: 0;\n}\n\n.analytics-detail-grid {\n  display: grid;\n  gap: 16px;\n}\n\n.analytics-detail-card {\n  padding: 14px;\n}\n\n.expense-flow {\n  display: grid;\n  gap: 14px;\n}\n\n.expense-flow__total {\n  display: grid;\n  gap: 4px;\n  padding: 14px;\n  border-radius: var(--radius);\n  background: var(--primary);\n  color: #fff;\n}\n\n.expense-flow__total span,\n.expense-flow__category small {\n  font-size: 12px;\n}\n\n.expense-flow__categories,\n.payer-chart {\n  display: grid;\n  gap: 12px;\n}\n\n.expense-flow__category > div:first-child,\n.payer-chart__item > div:first-child {\n  display: flex;\n  justify-content: space-between;\n  gap: 8px;\n  font-size: 13px;\n}\n\n.expense-flow__track,\n.payer-chart__track {\n  height: 10px;\n  margin-top: 6px;\n  overflow: hidden;\n  border-radius: 999px;\n  background: rgba(15, 118, 110, 0.08);\n}\n\n.expense-flow__track span,\n.payer-chart__track span {\n  display: block;\n  height: 100%;\n  border-radius: inherit;\n  background: var(--primary);\n}\n\n.expense-flow__category small {\n  display: block;\n  margin-top: 4px;\n  color: var(--muted);\n  text-align: right;\n}\n\n@media (min-width: 720px) {\n  .analytics-detail-grid {\n    grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);\n  }\n}\n'''
s += addition
p.write_text(s)
PY
python3 - <<'PY'
from pathlib import Path
p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
anchor = '''test("定期支出候補を今月の支出へ確定する", async ({ page }) => {\n'''
addition = '''test("分析ページで支出の流れと立替比較を表示する", async ({ page }) => {\n  await page.goto("/");\n\n  await page.getByRole("button", { name: "分析", exact: true }).click();\n  await expect(page.getByRole("heading", { name: "分析（仮）" })).toBeVisible();\n  await expect(page.getByLabel("カテゴリ別支出構成")).toContainText("家賃");\n  await expect(page.getByLabel("支払者別立替比較")).toContainText("葵");\n  await expect(page.getByLabel("支払者別立替比較")).toContainText("蓮");\n});\n\n'''
if anchor not in s:
    raise SystemExit('e2e anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)
PY
python3 - <<'PY'
from pathlib import Path
p = Path('Docs/product-spec.md')
s = p.read_text()
anchor = '- ローディング、エラー、空状態を表示する\n'
addition = '- `分析（仮）` タブで支出合計、共通費、要確認額をまとめて表示する\n- `分析（仮）` タブでカテゴリ別の支出構成と、支払者別の共通費立替額を比較表示する\n'
if anchor not in s:
    raise SystemExit('product spec anchor missing')
s = s.replace(anchor, anchor + addition, 1)
p.write_text(s)

p = Path('Docs/screens.md')
s = p.read_text()
anchor = '''### 精算タブ\n'''
addition = '''### 分析（仮）タブ\n\n- KPI: 支出合計、共通費、要確認額\n- 使用料グラフ: 直近6か月、カテゴリ内訳、区分・支払者・カテゴリフィルタ\n- 支出の流れ: 支出合計からカテゴリ別内訳への配分\n- 共通費の立替: 支払者別の立替額比較\n\n'''
if anchor not in s:
    raise SystemExit('screens anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md
git diff --stat -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '分析ページで支出の流れと立替比較を表示する'
sed -n '1,180p' test-results/kakeibo-分析ページで支出の流れと立替比較を表示する-chromium/error-context.md && printf '\n-- seed dates --\n' && grep -R "expense_date\|current_date\|now()" -n postgres/migrations/003_seed_local_data.sql supabase/migrations/202605240001_initial_schema.sql | sed -n '1,220p'
git status --short --branch && printf '\n-- test artifacts --\n' && find test-results -maxdepth 2 -type f -print | sort
sed -n '1,220p' playwright.config.ts && printf '\n-- db env --\n' && sed -n '1,180p' src/lib/db.ts && printf '\n-- compose --\n' && sed -n '1,220p' docker-compose.yml && printf '\n-- env keys --\n' && sed -n '1,120p' .env.example
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test node --test --experimental-strip-types tests/integration/api.integration.test.ts
ss -ltnp | grep -E ':3021|:3022|:3000' || true
python3 - <<'PY'
from pathlib import Path
p = Path('src/components/kakeibo-dashboard.tsx')
s = p.read_text()
old = '''            <div className="expense-flow__categories">\n              {(analytics?.categories ?? []).map((item) => {\n                const percentage = categoryTotal ? item.total / categoryTotal * 100 : 0;\n                return (\n                  <div key={item.category} className="expense-flow__category">\n                    <div>\n                      <span>{item.category}</span>\n                      <strong>{formatMoney(item.total)}</strong>\n                    </div>\n                    <div className="expense-flow__track"><span style={{ width: `${percentage}%` }} /></div>\n                    <small>{Math.round(percentage)}%</small>\n                  </div>\n                );\n              })}\n            </div>\n'''
new = '''            {isAnalyticsLoading ? <p className="analytics-empty">読み込み中...</p> : analyticsError ? <p className="analytics-empty">{analyticsError}</p> : (analytics?.categories.length ?? 0) === 0 ? <p className="analytics-empty">この条件の支出はまだありません。</p> : (\n              <div className="expense-flow__categories">\n                {analytics?.categories.map((item) => {\n                  const percentage = categoryTotal ? item.total / categoryTotal * 100 : 0;\n                  return (\n                    <div key={item.category} className="expense-flow__category">\n                      <div>\n                        <span>{item.category}</span>\n                        <strong>{formatMoney(item.total)}</strong>\n                      </div>\n                      <div className="expense-flow__track"><span style={{ width: `${percentage}%` }} /></div>\n                      <small>{Math.round(percentage)}%</small>\n                    </div>\n                  );\n                })}\n              </div>\n            )}\n'''
if old not in s:
    raise SystemExit('analytics flow anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '月別とカテゴリ別の使用料グラフ|分析ページで支出の流れと立替比較'
git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md && printf '\n-- stat --\n' && git diff --stat -- src/components/kakeibo-dashboard.tsx src/app/globals.css tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md && printf '\n-- status --\n' && git status --short --branch
git add Docs/product-spec.md Docs/screens.md src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && printf '\n-- status --\n' && git status --short --branch && git commit -m 'feat: 分析ページの仮画面を追加する' -m '- 支出合計とカテゴリ構成を可視化する
- 共通費の支払者別立替額を比較表示する
- 分析導線のスモークテストと仕様書を更新する' && printf '\n-- latest --\n' && git log -2 --oneline && printf '\n-- final status --\n' && git status --short --branch
gh issue view 14 -R hayaryoo519/kakeibo --json number,title,state,body,url && printf '\n-- design docs --\n' && sed -n '1,240p' Docs/design-system.md && printf '\n-- current screens --\n' && sed -n '118,190p' Docs/screens.md
sed -n '1,300p' src/app/api/recurring-expenses/route.ts && printf '\n-- candidates --\n' && sed -n '1,320p' src/app/api/recurring-expenses/candidates/route.ts && printf '\n-- schema --\n' && sed -n '1,340p' src/db/schema.sql
sed -n '1,360p' tests/integration/api.integration.test.ts && printf '\n-- seed tail --\n' && sed -n '180,310p' postgres/migrations/003_seed_local_data.sql && printf '\n-- migrations --\n' && ls -1 postgres/migrations supabase/migrations
grep -n "analyticsView\|homeView\|settingsView\|recurringDraft\|currentView\|tabbar" src/components/kakeibo-dashboard.tsx src/app/globals.css && printf '\n-- dashboard relevant --\n' && sed -n '760,1120p' src/components/kakeibo-dashboard.tsx && printf '\n-- docs tails --\n' && tail -n 110 Docs/product-spec.md && printf '\n-- api docs tail --\n' && tail -n 120 Docs/api-security-spec.md && printf '\n-- data docs tail --\n' && tail -n 110 Docs/data-model.md
sed -n '1,260p' postgres/migrations/002_prepare_local_environments.sh && printf '\n-- migration styles --\n' && for f in postgres/migrations/004_add_settlement_period_events.sql postgres/migrations/005_add_recurring_expense_occurrences.sql postgres/migrations/006_add_recurring_expense_notification.sql postgres/migrations/007_add_recurring_expense_name.sql; do printf '\n-- %s --\n' "$f"; sed -n '1,220p' "$f"; done && printf '\n-- e2e tail --\n' && sed -n '1,160p' tests/e2e/kakeibo.spec.ts
mkdir -p src/app/api/savings-goals
python3 - <<'PY'
from pathlib import Path

Path('postgres/migrations/008_add_savings_goals.sql').write_text('''create table if not exists savings_goals (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households(id) on delete cascade,
  name text not null,
  target_amount integer not null,
  current_amount integer not null default 0,
  target_date date,
  featured boolean not null default false,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint savings_goals_name_required check (nullif(trim(name), '') is not null),
  constraint savings_goals_target_amount_positive check (target_amount > 0),
  constraint savings_goals_current_amount_non_negative check (current_amount >= 0)
);

create index if not exists savings_goals_household_id_idx
  on savings_goals(household_id, featured desc, target_date asc nulls last, created_at asc);
''')
Path('supabase/migrations/202606020004_add_savings_goals.sql').write_text(Path('postgres/migrations/008_add_savings_goals.sql').read_text())

p = Path('postgres/migrations/002_prepare_local_environments.sh')
s = p.read_text()
s = s.replace('recurring_name_migration_file="/docker-entrypoint-initdb.d/007_add_recurring_expense_name.sql"\n', 'recurring_name_migration_file="/docker-entrypoint-initdb.d/007_add_recurring_expense_name.sql"\nsavings_goals_migration_file="/docker-entrypoint-initdb.d/008_add_savings_goals.sql"\n')
s = s.replace('  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$recurring_name_migration_file"\n', '  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$recurring_name_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$savings_goals_migration_file"\n')
p.write_text(s)

p = Path('postgres/migrations/003_seed_local_data.sql')
s = p.read_text()
addition = '''\n\ninsert into savings_goals (id, household_id, name, target_amount, current_amount, target_date, featured)\nvalues (\n  '99999999-9999-4999-8999-999999999991',\n  'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',\n  '沖縄旅行',\n  200000,\n  116000,\n  (date_trunc('month', current_date) + interval '4 months - 1 day')::date,\n  true\n)\non conflict (id) do update\nset\n  name = excluded.name,\n  target_amount = excluded.target_amount,\n  current_amount = excluded.current_amount,\n  target_date = excluded.target_date,\n  featured = excluded.featured;\n'''
if '99999999-9999-4999-8999-999999999991' not in s:
    s += addition
p.write_text(s)

Path('src/app/api/savings-goals/route.ts').write_text('''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const createSchema = z.object({
  name: z.string().trim().min(1).max(100),
  targetAmount: z.number().int().positive(),
  targetDate: z.string().date().nullable().optional(),
  featured: z.boolean().default(false),
});
const contributionSchema = z.object({ savingsGoalId: z.string().uuid(), contributionAmount: z.number().int().positive() });
const householdQuery = `select id from households order by created_at asc limit 1`;
const goalsQuery = `select id::text, name, target_amount, current_amount, target_date::text, featured from savings_goals where household_id = $1 order by featured desc, target_date asc nulls last, created_at asc`;

export async function GET() {
  try {
    const pool = db();
    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) return NextResponse.json({ goals: [] });
    return NextResponse.json({ goals: (await pool.query(goalsQuery, [household.id])).rows });
  } catch (error) {
    console.error("Failed to load savings goals", error);
    return NextResponse.json({ error: "savings_goals_failed" }, { status: 500 });
  }
}

export async function POST(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = createSchema.safeParse(await request.json());
    if (!parsed.success) return NextResponse.json({ error: "invalid_savings_goal" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    if (parsed.data.featured) await client.query("update savings_goals set featured = false where household_id = $1", [household.id]);
    const goal = (await client.query<{ id: string }>(`insert into savings_goals (household_id, name, target_amount, target_date, featured) values ($1, $2, $3, $4, $5) returning id::text`, [household.id, parsed.data.name, parsed.data.targetAmount, parsed.data.targetDate ?? null, parsed.data.featured])).rows[0];
    await client.query("commit");
    console.info("Created savings goal", { id: goal.id });
    return NextResponse.json({ id: goal.id }, { status: 201 });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to create savings goal", error);
    return NextResponse.json({ error: "savings_goal_create_failed" }, { status: 500 });
  } finally { client.release(); }
}

export async function PATCH(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = contributionSchema.safeParse(await request.json());
    if (!parsed.success) return NextResponse.json({ error: "invalid_savings_contribution" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    const goal = (await client.query<{ current_amount: number }>(`update savings_goals set current_amount = current_amount + $1, updated_at = now() where id = $2 and household_id = $3 returning current_amount`, [parsed.data.contributionAmount, parsed.data.savingsGoalId, household.id])).rows[0];
    if (!goal) { await client.query("rollback"); return NextResponse.json({ error: "savings_goal_not_found" }, { status: 404 }); }
    await client.query("commit");
    console.info("Added savings contribution", { savingsGoalId: parsed.data.savingsGoalId, contributionAmount: parsed.data.contributionAmount });
    return NextResponse.json({ currentAmount: goal.current_amount });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to add savings contribution", error);
    return NextResponse.json({ error: "savings_contribution_failed" }, { status: 500 });
  } finally { client.release(); }
}
''')
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/008_add_savings_goals.sql supabase/migrations/202606020004_add_savings_goals.sql src/app/api/savings-goals/route.ts
python3 - <<'PY'
from pathlib import Path
p = Path('src/components/kakeibo-dashboard.tsx')
s = p.read_text()
def replace(old, new):
    global s
    if old not in s:
        raise SystemExit(f'anchor missing: {old[:140]!r}')
    s = s.replace(old, new, 1)
replace('''type RecurringCandidate = {\n  id: string;\n  title: string;\n  category: string;\n  amount: number;\n  day_of_month: number;\n  scope: ExpenseScope;\n  status: "pending" | "confirmed" | "skipped";\n  due_date: string;\n  previous_amount: number | null;\n  notification_enabled: boolean;\n};\n''', '''type RecurringCandidate = {\n  id: string;\n  title: string;\n  category: string;\n  amount: number;\n  day_of_month: number;\n  scope: ExpenseScope;\n  status: "pending" | "confirmed" | "skipped";\n  due_date: string;\n  previous_amount: number | null;\n  notification_enabled: boolean;\n};\n\ntype SavingsGoal = {\n  id: string;\n  name: string;\n  target_amount: number;\n  current_amount: number;\n  target_date: string | null;\n  featured: boolean;\n};\n''')
replace('''  const [recurringDraft, setRecurringDraft] = useState({ name: "", category: "", paidBy: "a" as MemberId, amount: "", dayOfMonth: "10", scope: "shared" as ExpenseScope, notificationEnabled: true });\n  const composerInputRef''', '''  const [recurringDraft, setRecurringDraft] = useState({ name: "", category: "", paidBy: "a" as MemberId, amount: "", dayOfMonth: "10", scope: "shared" as ExpenseScope, notificationEnabled: true });\n  const [savingsGoals, setSavingsGoals] = useState<SavingsGoal[]>([]);\n  const [isSavingsGoalsLoading, setIsSavingsGoalsLoading] = useState(true);\n  const [savingsGoalError, setSavingsGoalError] = useState<string | null>(null);\n  const [savingsGoalRefresh, setSavingsGoalRefresh] = useState(0);\n  const [savingsGoalDraft, setSavingsGoalDraft] = useState({ name: "", targetAmount: "", targetDate: "", featured: false });\n  const [savingsContributions, setSavingsContributions] = useState<Record<string, string>>({});\n  const composerInputRef''')
anchor = '''  const createRecurringExpense = async () => {\n'''
addition = '''  useEffect(() => {\n    let active = true;\n    setIsSavingsGoalsLoading(true);\n    setSavingsGoalError(null);\n    void fetch("/api/savings-goals")\n      .then((response) => {\n        if (!response.ok) throw new Error("savings_goals_load_failed");\n        return response.json() as Promise<{ goals: SavingsGoal[] }>;\n      })\n      .then((data) => { if (active) setSavingsGoals(data.goals); })\n      .catch(() => {\n        if (active) {\n          setSavingsGoals([]);\n          setSavingsGoalError("貯金目標を読み込めませんでした。");\n        }\n      })\n      .finally(() => { if (active) setIsSavingsGoalsLoading(false); });\n    return () => { active = false; };\n  }, [savingsGoalRefresh]);\n\n  const createSavingsGoal = async () => {\n    const targetAmount = Number(savingsGoalDraft.targetAmount);\n    if (!savingsGoalDraft.name.trim() || !Number.isInteger(targetAmount) || targetAmount <= 0) {\n      setSavingsGoalError("貯金目標の名称と金額を確認してください。");\n      return;\n    }\n    setSavingsGoalError(null);\n    try {\n      const response = await fetch("/api/savings-goals", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ ...savingsGoalDraft, targetAmount, targetDate: savingsGoalDraft.targetDate || null }),\n      });\n      if (!response.ok) throw new Error("savings_goal_create_failed");\n      setSavingsGoalDraft({ name: "", targetAmount: "", targetDate: "", featured: false });\n      setSavingsGoalRefresh((current) => current + 1);\n    } catch { setSavingsGoalError("貯金目標を登録できませんでした。"); }\n  };\n\n  const addSavingsContribution = async (goal: SavingsGoal) => {\n    const contributionAmount = Number(savingsContributions[goal.id]);\n    if (!Number.isInteger(contributionAmount) || contributionAmount <= 0) {\n      setSavingsGoalError("積立額を確認してください。");\n      return;\n    }\n    setSavingsGoalError(null);\n    try {\n      const response = await fetch("/api/savings-goals", {\n        method: "PATCH",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ savingsGoalId: goal.id, contributionAmount }),\n      });\n      if (!response.ok) throw new Error("savings_contribution_failed");\n      const data = (await response.json()) as { currentAmount: number };\n      setSavingsGoals((current) => current.map((item) => item.id === goal.id ? { ...item, current_amount: data.currentAmount } : item));\n      setSavingsContributions((current) => ({ ...current, [goal.id]: "" }));\n    } catch { setSavingsGoalError("積立額を反映できませんでした。"); }\n  };\n\n'''
replace(anchor, addition + anchor)
anchor = '''  const analyticsView = (\n'''
addition = '''  const featuredSavingsGoal = savingsGoals.find((goal) => goal.featured) ?? savingsGoals[0];\n  const savingsGoalCard = (goal: SavingsGoal, compact = false) => {\n    const percentage = Math.min(goal.current_amount / goal.target_amount * 100, 100);\n    const remaining = Math.max(goal.target_amount - goal.current_amount, 0);\n    return (\n      <article key={goal.id} className={compact ? "savings-goal-card savings-goal-card--compact" : "savings-goal-card"}>\n        <div className="savings-goal-card__header">\n          <div>\n            <span>{compact ? "次の楽しみ" : goal.featured ? "最優先の目標" : "貯金目標"}</span>\n            <h3>{goal.name}</h3>\n          </div>\n          <strong>{Math.round(percentage)}%</strong>\n        </div>\n        <div className="savings-goal-card__amount"><strong>{formatMoney(goal.current_amount)}</strong><span> / {formatMoney(goal.target_amount)}</span></div>\n        <div className="savings-goal-card__track"><span style={{ width: `${percentage}%` }} /></div>\n        <div className="savings-goal-card__footer">\n          <span>{remaining > 0 ? `あと ${formatMoney(remaining)}` : "目標達成"}</span>\n          {goal.target_date ? <span>{goal.target_date} まで</span> : null}\n        </div>\n        {!compact ? (\n          <div className="savings-goal-card__actions">\n            <input aria-label={`${goal.name}の積立額`} inputMode="numeric" placeholder="積立額" value={savingsContributions[goal.id] ?? ""} onChange={(event) => setSavingsContributions((current) => ({ ...current, [goal.id]: event.target.value }))} />\n            <button className="chip chip--active" type="button" onClick={() => void addSavingsContribution(goal)}>積み立てる</button>\n          </div>\n        ) : null}\n      </article>\n    );\n  };\n\n  const savingsGoalsPanel = (\n    <section className="section savings-goals-panel" aria-label="貯金目標">\n      <div className="section__header">\n        <div>\n          <h2 className="section__title">目標と予算</h2>\n          <p className="section__meta">楽しみのために、少しずつ積み立てる</p>\n        </div>\n      </div>\n      {isSavingsGoalsLoading ? <p className="analytics-empty">読み込み中...</p> : savingsGoalError ? <p className="analytics-empty">{savingsGoalError}</p> : savingsGoals.length === 0 ? <p className="analytics-empty">貯金目標はまだありません。設定から追加できます。</p> : <div className="savings-goals-list">{savingsGoals.map((goal) => savingsGoalCard(goal))}</div>}\n    </section>\n  );\n\n'''
replace(anchor, addition + anchor)
replace('''      <div className="analytics-detail-grid">\n''', '''      {savingsGoalsPanel}\n\n      <div className="analytics-detail-grid">\n''')
replace('''      <section>\n        {analyticsPanel}\n''', '''      <section>\n        {featuredSavingsGoal ? <section className="section featured-savings-goal" aria-label="最優先の貯金目標">{savingsGoalCard(featuredSavingsGoal, true)}</section> : null}\n        {analyticsPanel}\n''')
replace('''      <div className="settings-grid">\n        <article className="panel settings-panel recurring-settings">\n''', '''      <div className="settings-grid">\n        <article className="panel settings-panel savings-goal-settings">\n          <h3>貯金目標を登録</h3>\n          <div className="savings-goal-settings__grid">\n            <input aria-label="貯金目標名" placeholder="例: 沖縄旅行" value={savingsGoalDraft.name} onChange={(event) => setSavingsGoalDraft({ ...savingsGoalDraft, name: event.target.value })} />\n            <input aria-label="貯金目標金額" inputMode="numeric" placeholder="目標金額" value={savingsGoalDraft.targetAmount} onChange={(event) => setSavingsGoalDraft({ ...savingsGoalDraft, targetAmount: event.target.value })} />\n            <input aria-label="貯金目標期限" type="date" value={savingsGoalDraft.targetDate} onChange={(event) => setSavingsGoalDraft({ ...savingsGoalDraft, targetDate: event.target.value })} />\n            <label className="savings-goal-settings__featured"><input type="checkbox" checked={savingsGoalDraft.featured} onChange={(event) => setSavingsGoalDraft({ ...savingsGoalDraft, featured: event.target.checked })} />ホームに表示</label>\n            <button className="chip chip--active" type="button" onClick={() => void createSavingsGoal()}>貯金目標を追加</button>\n          </div>\n          {savingsGoalError ? <p className="recurring-empty">{savingsGoalError}</p> : null}\n        </article>\n        <article className="panel settings-panel recurring-settings">\n''')
p.write_text(s)
PY

python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
s = p.read_text()
s += '''\n.savings-goals-panel,\n.featured-savings-goal {\n  padding: 14px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.savings-goals-list {\n  display: grid;\n  gap: 10px;\n}\n\n.savings-goal-card {\n  padding: 14px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: rgba(255, 255, 255, 0.68);\n}\n\n.savings-goal-card--compact {\n  border-color: rgba(15, 118, 110, 0.24);\n  background: linear-gradient(135deg, var(--primary-ghost), #fff);\n}\n\n.savings-goal-card__header,\n.savings-goal-card__footer,\n.savings-goal-card__actions {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n}\n\n.savings-goal-card__header span,\n.savings-goal-card__footer {\n  color: var(--muted);\n  font-size: 12px;\n}\n\n.savings-goal-card__header h3 {\n  margin: 3px 0 0;\n  font-size: 16px;\n}\n\n.savings-goal-card__header > strong {\n  color: var(--primary);\n  font-size: 20px;\n}\n\n.savings-goal-card__amount {\n  margin-top: 12px;\n  color: var(--muted);\n  font-size: 13px;\n}\n\n.savings-goal-card__amount strong {\n  color: var(--text);\n  font-size: 20px;\n}\n\n.savings-goal-card__track {\n  height: 12px;\n  margin-top: 10px;\n  overflow: hidden;\n  border-radius: 999px;\n  background: rgba(15, 118, 110, 0.1);\n}\n\n.savings-goal-card__track span {\n  display: block;\n  height: 100%;\n  border-radius: inherit;\n  background: var(--primary);\n}\n\n.savings-goal-card__footer {\n  margin-top: 8px;\n}\n\n.savings-goal-card__actions {\n  justify-content: flex-start;\n  margin-top: 12px;\n}\n\n.savings-goal-card__actions input,\n.savings-goal-settings__grid input {\n  min-height: 40px;\n  min-width: 0;\n  padding: 0 10px;\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fff;\n}\n\n.savings-goal-card__actions input {\n  width: 120px;\n}\n\n.savings-goal-settings__grid {\n  display: grid;\n  gap: 8px;\n}\n\n.savings-goal-settings__featured {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  color: var(--muted);\n  font-size: 13px;\n}\n'''
p.write_text(s)
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css
python3 - <<'PY'
from pathlib import Path
p = Path('tests/integration/api.integration.test.ts')
s = p.read_text()
s = s.replace('''    await pool.query("delete from recurring_expenses where name = '統合テスト定期支出'");\n''', '''    await pool.query("delete from recurring_expenses where name = '統合テスト定期支出'");\n    await pool.query("delete from savings_goals where name = '統合テスト貯金目標'");\n    await pool.query("update savings_goals set current_amount = 116000 where id = '99999999-9999-4999-8999-999999999991'");\n''')
anchor = '''  it("updates recurring expense notification settings", async () => {\n'''
addition = '''  it("creates savings goals and adds contributions", async () => {\n    const { GET, PATCH, POST } = await import("../../src/app/api/savings-goals/route.ts");\n    const created = await POST(new Request("http://localhost/api/savings-goals", {\n      method: "POST", headers: { "Content-Type": "application/json" },\n      body: JSON.stringify({ name: "統合テスト貯金目標", targetAmount: 50000, targetDate: "2098-12-31", featured: false }),\n    }));\n    assert.equal(created.status, 201);\n    const savingsGoalId = (await created.json()).id;\n\n    const contributed = await PATCH(new Request("http://localhost/api/savings-goals", {\n      method: "PATCH", headers: { "Content-Type": "application/json" },\n      body: JSON.stringify({ savingsGoalId, contributionAmount: 3000 }),\n    }));\n    assert.equal(contributed.status, 200);\n    assert.deepEqual(await contributed.json(), { currentAmount: 3000 });\n\n    const list = await GET();\n    assert.equal(list.status, 200);\n    assert.ok((await list.json()).goals.some((goal: { name: string; current_amount: number }) => goal.name === "統合テスト貯金目標" && goal.current_amount === 3000));\n  });\n\n'''
if anchor not in s: raise SystemExit('integration anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)

p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
anchor = '''test("定期支出候補を今月の支出へ確定する", async ({ page }) => {\n'''
addition = '''test("ホームと分析ページで貯金目標を確認して積み立てる", async ({ page }) => {\n  await page.goto("/");\n\n  const featuredGoal = page.getByLabel("最優先の貯金目標");\n  await expect(featuredGoal).toContainText("沖縄旅行");\n  await expect(featuredGoal).toContainText("58%");\n\n  await page.getByRole("button", { name: "分析", exact: true }).click();\n  const goalsPanel = page.getByLabel("貯金目標");\n  await goalsPanel.getByLabel("沖縄旅行の積立額").fill("4000");\n  await Promise.all([\n    page.waitForResponse((response) => response.url().endsWith("/api/savings-goals") && response.request().method() === "PATCH" && response.ok()),\n    goalsPanel.getByRole("button", { name: "積み立てる", exact: true }).click(),\n  ]);\n  await expect(goalsPanel).toContainText("¥120,000");\n});\n\n'''
if anchor not in s: raise SystemExit('e2e anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)
PY

python3 - <<'PY'
from pathlib import Path
p = Path('Docs/product-spec.md')
s = p.read_text()
anchor = '''### 定期支出\n'''
addition = '''### 貯金目標\n\n- 設定画面から名称、目標額、期限、ホーム表示の有無を登録する\n- ホーム画面には最優先の目標を1件だけ表示する\n- 分析画面の `目標と予算` セクションで全目標の進捗を表示する\n- 各目標へ積立額を追加し、達成率と残額を更新する\n\n'''
if anchor not in s: raise SystemExit('product anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)

p = Path('Docs/screens.md')
s = p.read_text()
s = s.replace('- 使用料グラフ: 直近6か月、カテゴリ内訳、区分・支払者・カテゴリフィルタ\n- 今月の定期支出:', '- 最優先の貯金目標: 目標名、達成率、積立額、残額、期限\n- 使用料グラフ: 直近6か月、カテゴリ内訳、区分・支払者・カテゴリフィルタ\n- 今月の定期支出:', 1)
s = s.replace('- 共通費の立替: 支払者別の立替額比較\n', '- 共通費の立替: 支払者別の立替額比較\n- 目標と予算: 貯金目標一覧、達成率、残額、期限、積立操作\n', 1)
s = s.replace('- 定期支出テンプレート登録\n', '- 定期支出テンプレート登録\n- 貯金目標登録: 名称、目標額、期限、ホーム表示\n', 1)
p.write_text(s)

p = Path('Docs/data-model.md')
s = p.read_text()
anchor = '''## settlement_periods\n'''
addition = '''## savings_goals\n\n貯金目標。ホームには `featured = true` の目標を優先して1件表示する。\n\n```txt\nid\nhousehold_id\nname\ntarget_amount\ncurrent_amount\ntarget_date\nfeatured\ncreated_at\nupdated_at\n```\n\n'''
if anchor not in s: raise SystemExit('data anchor missing')
s = s.replace(anchor, addition + anchor, 1)
s = s.replace('- `202606020003_add_recurring_expense_name.sql`: 定期支出テンプレート名称\n', '- `202606020003_add_recurring_expense_name.sql`: 定期支出テンプレート名称\n- `202606020004_add_savings_goals.sql`: 貯金目標\n', 1)
p.write_text(s)

p = Path('Docs/api-security-spec.md')
s = p.read_text()
anchor = '''### 5.7. 精算取り消し (`POST /api/settlements/reopen`)\n'''
addition = '''### 5.7. 貯金目標 (`GET /api/savings-goals`)\n\n世帯の貯金目標をホーム表示優先、期限順で返す。\n\n### 5.8. 貯金目標登録 (`POST /api/savings-goals`)\n\n名称、目標額、期限、ホーム表示の有無を登録する。ホーム表示を有効にした場合は、同じ世帯の既存目標を解除する。\n\n### 5.9. 貯金目標への積立 (`PATCH /api/savings-goals`)\n\n`contributionAmount` をトランザクション内で現在額へ加算する。\n\n'''
if anchor not in s: raise SystemExit('api anchor missing')
s = s.replace(anchor, addition + '### 5.10. 精算取り消し (`POST /api/settlements/reopen`)\n', 1)
p.write_text(s)
PY

git diff --check -- tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep 'ホームと分析ページで貯金目標を確認して積み立てる'
sed -n '1,180p' test-results/kakeibo-ホームと分析ページで貯金目標を確認して積み立てる-chromium/error-context.md
docker compose exec -T db psql -U kakeibo -d kakeibo_test -c "select id,name,target_amount,current_amount,target_date,featured from savings_goals order by created_at;" && printf '\n-- production --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_production -c "select id,name,target_amount,current_amount,target_date,featured from savings_goals order by created_at;"
ss -ltnp | grep -E ':3022|:3000|:3021' || true
npm run dev -- --hostname 127.0.0.1 --port 3023
curl -fsS --max-time 10 http://127.0.0.1:3023/api/savings-goals && printf '\n-- page --\n' && curl -fsS --max-time 10 http://127.0.0.1:3023/ | sed -n '1,8p'
git status --short --branch && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3021|:3022|:3023|:3000' || true
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/008_add_savings_goals.sql src/app/api/savings-goals/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx supabase/migrations/202606020004_add_savings_goals.sql tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run dev -- --hostname 127.0.0.1 --port 3023
curl -fsS --max-time 10 http://127.0.0.1:3023/api/savings-goals && printf '\n-- dashboard --\n' && curl -fsS --max-time 10 'http://127.0.0.1:3023/api/dashboard?year=2026&month=6' | sed -n '1,4p'
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  page.on('console', msg => console.log('console:', msg.type(), msg.text()));
  page.on('pageerror', error => console.log('pageerror:', error.message));
  page.on('response', response => { if (response.url().includes('/api/')) console.log('response:', response.status(), response.url()); });
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded' });
  await page.waitForTimeout(5000);
  console.log('featured:', await page.getByLabel('最優先の貯金目標').count());
  console.log('body:', (await page.locator('body').innerText()).slice(0, 1200));
  await browser.close();
})();
JS
python3 - <<'PY'
from pathlib import Path
p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
old = '''test("ホームと分析ページで貯金目標を確認して積み立てる", async ({ page }) => {\n  await page.goto("/");\n'''
new = '''test("ホームと分析ページで貯金目標を確認して積み立てる", async ({ page }) => {\n  const savingsGoalsResponse = await page.request.get("/api/savings-goals");\n  expect(savingsGoalsResponse.ok()).toBeTruthy();\n  await page.goto("/");\n'''
if old not in s:
    raise SystemExit('savings goal e2e anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- tests/e2e/kakeibo.spec.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep 'ホームと分析ページで貯金目標を確認して積み立てる'
ss -ltnp | grep -E ':3022|:3023|:3000' || true
ps -eo pid,ppid,stat,etime,cmd | grep -E 'playwright|chrome|chromium|next dev|next-server' | grep -v grep | sed -n '1,240p'
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const started = Date.now();
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  console.log(`browser page ready in ${Date.now() - started}ms`);
  await browser.close();
})();
JS
git diff --check && printf '\n-- stat --\n' && git diff --stat && printf '\n-- status --\n' && git status --short --branch
docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select name || ':' || current_amount from savings_goals where id = '99999999-9999-4999-8999-999999999991';" && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/008_add_savings_goals.sql src/app/api/savings-goals/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx supabase/migrations/202606020004_add_savings_goals.sql tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && printf '\n-- feature stat --\n' && git diff --stat -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/008_add_savings_goals.sql src/app/api/savings-goals/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx supabase/migrations/202606020004_add_savings_goals.sql tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && printf '\n-- status --\n' && git status --short --branch
python3 - <<'PY'
from pathlib import Path
p = Path('src/app/api/savings-goals/route.ts')
s = p.read_text()
s = s.replace('const parsed = createSchema.safeParse(await request.json());', 'const parsed = createSchema.safeParse(await request.json().catch(() => null));', 1)
s = s.replace('const parsed = contributionSchema.safeParse(await request.json());', 'const parsed = contributionSchema.safeParse(await request.json().catch(() => null));', 1)
p.write_text(s)

p = Path('tests/integration/api.integration.test.ts')
s = p.read_text()
old = '''    const list = await GET();\n    assert.equal(list.status, 200);\n    assert.ok((await list.json()).goals.some((goal: { name: string; current_amount: number }) => goal.name === "統合テスト貯金目標" && goal.current_amount === 3000));\n  });\n'''
new = '''    const list = await GET();\n    assert.equal(list.status, 200);\n    assert.ok((await list.json()).goals.some((goal: { name: string; current_amount: number }) => goal.name === "統合テスト貯金目標" && goal.current_amount === 3000));\n\n    const invalid = await POST(new Request("http://localhost/api/savings-goals", {\n      method: "POST", headers: { "Content-Type": "application/json" }, body: "{",\n    }));\n    assert.equal(invalid.status, 400);\n  });\n'''
if old not in s:
    raise SystemExit('integration savings anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- src/app/api/savings-goals/route.ts tests/integration/api.integration.test.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test node --test --experimental-strip-types tests/integration/api.integration.test.ts
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && sed -n '/[[:blank:]]$/=' postgres/migrations/008_add_savings_goals.sql src/app/api/savings-goals/route.ts supabase/migrations/202606020004_add_savings_goals.sql && printf '\n-- status --\n' && git status --short --branch
gh issue list -R hayaryoo519/kakeibo --state open --limit 100 && printf '\n-- status --\n' && git status --short --branch
git add Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/008_add_savings_goals.sql src/app/api/savings-goals/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx supabase/migrations/202606020004_add_savings_goals.sql tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && git commit -m 'feat: 貯金目標の進捗管理を追加する' -m '- ホームと分析画面に貯金目標カードを追加する
- 貯金目標の登録と積立APIを追加する
- migration、seed、仕様書、主要導線テストを更新する' && printf '\n-- latest --\n' && git log -3 --oneline && printf '\n-- status --\n' && git status --short --branch
sed -n '1,220p' src/app/api/savings-goals/route.ts && printf '\n-- analytics query --\n' && sed -n '1,180p' src/app/api/analytics/route.ts && printf '\n-- goal UI refs --\n' && grep -n "SavingsGoal\|savingsGoal\|目標と予算\|settings-grid" src/components/kakeibo-dashboard.tsx | sed -n '1,240p'
sed -n '1,180p' postgres/migrations/002_prepare_local_environments.sh && printf '\n-- seed savings tail --\n' && tail -n 60 postgres/migrations/003_seed_local_data.sql && printf '\n-- integration savings --\n' && grep -n "savings" tests/integration/api.integration.test.ts && printf '\n-- e2e savings --\n' && grep -n "貯金" tests/e2e/kakeibo.spec.ts
mkdir -p src/app/api/budget-goals
python3 - <<'PY'
from pathlib import Path
sql = '''create table if not exists budget_goals (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households(id) on delete cascade,
  category_id uuid not null references categories(id) on delete cascade,
  monthly_limit integer not null,
  active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (household_id, category_id),
  constraint budget_goals_monthly_limit_positive check (monthly_limit > 0)
);

create index if not exists budget_goals_household_id_idx
  on budget_goals(household_id, active, created_at asc);
'''
Path('postgres/migrations/009_add_budget_goals.sql').write_text(sql)
Path('supabase/migrations/202606020005_add_budget_goals.sql').write_text(sql)

p = Path('postgres/migrations/002_prepare_local_environments.sh')
s = p.read_text()
s = s.replace('savings_goals_migration_file="/docker-entrypoint-initdb.d/008_add_savings_goals.sql"\n', 'savings_goals_migration_file="/docker-entrypoint-initdb.d/008_add_savings_goals.sql"\nbudget_goals_migration_file="/docker-entrypoint-initdb.d/009_add_budget_goals.sql"\n')
s = s.replace('  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$savings_goals_migration_file"\n', '  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$savings_goals_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$budget_goals_migration_file"\n')
p.write_text(s)

p = Path('postgres/migrations/003_seed_local_data.sql')
s = p.read_text()
addition = '''\n\ninsert into budget_goals (id, household_id, category_id, monthly_limit, active)\nvalues (\n  '99999999-9999-4999-8999-999999999992',\n  'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',\n  'dddddddd-dddd-4ddd-8ddd-ddddddddddd4',\n  20000,\n  true\n)\non conflict (id) do update\nset\n  category_id = excluded.category_id,\n  monthly_limit = excluded.monthly_limit,\n  active = excluded.active;\n'''
if '99999999-9999-4999-8999-999999999992' not in s:
    s += addition
p.write_text(s)

Path('src/app/api/budget-goals/route.ts').write_text('''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const querySchema = z.object({
  year: z.string().transform(Number).pipe(z.number().int().min(2000).max(9999)),
  month: z.string().transform(Number).pipe(z.number().int().min(1).max(12)),
});
const createSchema = z.object({ category: z.string().trim().min(1).max(50), monthlyLimit: z.number().int().positive() });
const householdQuery = `select id from households order by created_at asc limit 1`;
const goalsQuery = `
  select bg.id::text, c.name as category, bg.monthly_limit,
    coalesce(sum(e.amount), 0)::int as spent_amount,
    greatest(bg.monthly_limit - coalesce(sum(e.amount), 0), 0)::int as remaining_amount
  from budget_goals bg
  join categories c on c.id = bg.category_id
  left join expenses e on e.household_id = bg.household_id and e.category_id = bg.category_id
    and e.expense_date >= make_date($2, $3, 1)
    and e.expense_date < make_date($2, $3, 1) + interval '1 month'
  where bg.household_id = $1 and bg.active
  group by bg.id, c.name, bg.monthly_limit, bg.created_at
  order by bg.created_at asc
`;

export async function GET(request: Request) {
  try {
    const url = new URL(request.url);
    const parsed = querySchema.safeParse({ year: url.searchParams.get("year") ?? "", month: url.searchParams.get("month") ?? "" });
    if (!parsed.success) return NextResponse.json({ error: "invalid_period" }, { status: 400 });
    const pool = db();
    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) return NextResponse.json({ goals: [] });
    return NextResponse.json({ goals: (await pool.query(goalsQuery, [household.id, parsed.data.year, parsed.data.month])).rows });
  } catch (error) {
    console.error("Failed to load budget goals", error);
    return NextResponse.json({ error: "budget_goals_failed" }, { status: 500 });
  }
}

export async function POST(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = createSchema.safeParse(await request.json().catch(() => null));
    if (!parsed.success) return NextResponse.json({ error: "invalid_budget_goal" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    const category = (await client.query<{ id: string }>(`insert into categories (household_id, name, kind, default_expense_scope, default_split_type) values ($1, $2, 'other', 'shared', 'equal') on conflict (household_id, name) do update set name = excluded.name returning id`, [household.id, parsed.data.category])).rows[0];
    const goal = (await client.query<{ id: string }>(`insert into budget_goals (household_id, category_id, monthly_limit) values ($1, $2, $3) on conflict (household_id, category_id) do update set monthly_limit = excluded.monthly_limit, active = true, updated_at = now() returning id::text`, [household.id, category.id, parsed.data.monthlyLimit])).rows[0];
    await client.query("commit");
    console.info("Created budget goal", { id: goal.id });
    return NextResponse.json({ id: goal.id }, { status: 201 });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to create budget goal", error);
    return NextResponse.json({ error: "budget_goal_create_failed" }, { status: 500 });
  } finally { client.release(); }
}
''')
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/api/budget-goals/route.ts
python3 - <<'PY'
from pathlib import Path
p = Path('src/components/kakeibo-dashboard.tsx')
s = p.read_text()
def replace(old, new):
    global s
    if old not in s:
        raise SystemExit(f'anchor missing: {old[:140]!r}')
    s = s.replace(old, new, 1)
replace('''type SavingsGoal = {\n  id: string;\n  name: string;\n  target_amount: number;\n  current_amount: number;\n  target_date: string | null;\n  featured: boolean;\n};\n''', '''type SavingsGoal = {\n  id: string;\n  name: string;\n  target_amount: number;\n  current_amount: number;\n  target_date: string | null;\n  featured: boolean;\n};\n\ntype BudgetGoal = {\n  id: string;\n  category: string;\n  monthly_limit: number;\n  spent_amount: number;\n  remaining_amount: number;\n};\n''')
replace('''  const [savingsContributions, setSavingsContributions] = useState<Record<string, string>>({});\n  const composerInputRef''', '''  const [savingsContributions, setSavingsContributions] = useState<Record<string, string>>({});\n  const [budgetGoals, setBudgetGoals] = useState<BudgetGoal[]>([]);\n  const [isBudgetGoalsLoading, setIsBudgetGoalsLoading] = useState(true);\n  const [budgetGoalError, setBudgetGoalError] = useState<string | null>(null);\n  const [budgetGoalRefresh, setBudgetGoalRefresh] = useState(0);\n  const [budgetGoalDraft, setBudgetGoalDraft] = useState({ category: "", monthlyLimit: "" });\n  const composerInputRef''')
anchor = '''  const createSavingsGoal = async () => {\n'''
addition = '''  useEffect(() => {\n    let active = true;\n    setIsBudgetGoalsLoading(true);\n    setBudgetGoalError(null);\n    void fetch(`/api/budget-goals?year=${selectedPeriod.year}&month=${selectedPeriod.month}`)\n      .then((response) => { if (!response.ok) throw new Error("budget_goals_load_failed"); return response.json() as Promise<{ goals: BudgetGoal[] }>; })\n      .then((data) => { if (active) setBudgetGoals(data.goals); })\n      .catch(() => { if (active) { setBudgetGoals([]); setBudgetGoalError("節約目標を読み込めませんでした。"); } })\n      .finally(() => { if (active) setIsBudgetGoalsLoading(false); });\n    return () => { active = false; };\n  }, [selectedPeriod, budgetGoalRefresh, expenses]);\n\n  const createBudgetGoal = async () => {\n    const monthlyLimit = Number(budgetGoalDraft.monthlyLimit);\n    if (!budgetGoalDraft.category.trim() || !Number.isInteger(monthlyLimit) || monthlyLimit <= 0) {\n      setBudgetGoalError("カテゴリと月上限を確認してください。");\n      return;\n    }\n    setBudgetGoalError(null);\n    try {\n      const response = await fetch("/api/budget-goals", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...budgetGoalDraft, monthlyLimit }) });\n      if (!response.ok) throw new Error("budget_goal_create_failed");\n      setBudgetGoalDraft({ category: "", monthlyLimit: "" });\n      setBudgetGoalRefresh((current) => current + 1);\n    } catch { setBudgetGoalError("節約目標を登録できませんでした。"); }\n  };\n\n'''
replace(anchor, addition + anchor)
anchor = '''  const savingsGoalsPanel = (\n'''
addition = '''  const budgetGoalsPanel = (\n    <section className="section budget-goals-panel" aria-label="節約目標">\n      <div className="section__header">\n        <div>\n          <h2 className="section__title">今月の節約目標</h2>\n          <p className="section__meta">使いすぎる前に、残りの余白を見る</p>\n        </div>\n      </div>\n      {isBudgetGoalsLoading ? <p className="analytics-empty">読み込み中...</p> : budgetGoalError ? <p className="analytics-empty">{budgetGoalError}</p> : budgetGoals.length === 0 ? <p className="analytics-empty">節約目標はまだありません。設定から追加できます。</p> : (\n        <div className="budget-goals-list">\n          {budgetGoals.map((goal) => {\n            const percentage = Math.min(goal.spent_amount / goal.monthly_limit * 100, 100);\n            const isOver = goal.spent_amount > goal.monthly_limit;\n            return (\n              <article key={goal.id} className={isOver ? "budget-goal-card budget-goal-card--over" : "budget-goal-card"}>\n                <div className="budget-goal-card__header"><div><span>{goal.category}</span><strong>{isOver ? `${formatMoney(goal.spent_amount - goal.monthly_limit)} 超過` : `残り ${formatMoney(goal.remaining_amount)}`}</strong></div><small>{formatMoney(goal.spent_amount)} / {formatMoney(goal.monthly_limit)}</small></div>\n                <div className="budget-goal-card__track"><span style={{ width: `${percentage}%` }} /></div>\n              </article>\n            );\n          })}\n        </div>\n      )}\n    </section>\n  );\n\n'''
replace(anchor, addition + anchor)
replace('''      {savingsGoalsPanel}\n\n      <div className="analytics-detail-grid">\n''', '''      {savingsGoalsPanel}\n      {budgetGoalsPanel}\n\n      <div className="analytics-detail-grid">\n''')
replace('''        <article className="panel settings-panel savings-goal-settings">\n''', '''        <article className="panel settings-panel budget-goal-settings">\n          <h3>節約目標を登録</h3>\n          <div className="budget-goal-settings__grid">\n            <input aria-label="節約目標カテゴリ" placeholder="例: 外食" value={budgetGoalDraft.category} onChange={(event) => setBudgetGoalDraft({ ...budgetGoalDraft, category: event.target.value })} />\n            <input aria-label="節約目標月上限" inputMode="numeric" placeholder="月上限" value={budgetGoalDraft.monthlyLimit} onChange={(event) => setBudgetGoalDraft({ ...budgetGoalDraft, monthlyLimit: event.target.value })} />\n            <button className="chip chip--active" type="button" onClick={() => void createBudgetGoal()}>節約目標を追加</button>\n          </div>\n          {budgetGoalError ? <p className="recurring-empty">{budgetGoalError}</p> : null}\n        </article>\n        <article className="panel settings-panel savings-goal-settings">\n''')
p.write_text(s)
PY

python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
s = p.read_text()
s += '''\n.budget-goals-panel {\n  padding: 14px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.budget-goals-list,\n.budget-goal-settings__grid {\n  display: grid;\n  gap: 8px;\n}\n\n.budget-goal-card {\n  padding: 12px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: rgba(255, 255, 255, 0.68);\n}\n\n.budget-goal-card__header {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 8px;\n}\n\n.budget-goal-card__header div {\n  display: grid;\n  gap: 3px;\n}\n\n.budget-goal-card__header span,\n.budget-goal-card__header small {\n  color: var(--muted);\n  font-size: 12px;\n}\n\n.budget-goal-card__header strong {\n  font-size: 15px;\n}\n\n.budget-goal-card__track {\n  height: 10px;\n  margin-top: 10px;\n  overflow: hidden;\n  border-radius: 999px;\n  background: rgba(15, 118, 110, 0.1);\n}\n\n.budget-goal-card__track span {\n  display: block;\n  height: 100%;\n  border-radius: inherit;\n  background: var(--primary);\n}\n\n.budget-goal-card--over .budget-goal-card__track span {\n  background: var(--review);\n}\n\n.budget-goal-settings__grid input {\n  min-height: 40px;\n  min-width: 0;\n  padding: 0 10px;\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fff;\n}\n'''
p.write_text(s)
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css
python3 - <<'PY'
from pathlib import Path
p = Path('tests/integration/api.integration.test.ts')
s = p.read_text()
s = s.replace('''    await pool.query("delete from savings_goals where name = '統合テスト貯金目標'");\n''', '''    await pool.query("delete from savings_goals where name = '統合テスト貯金目標'");\n    await pool.query("delete from budget_goals where category_id in (select id from categories where name = '統合テスト節約')");\n''')
anchor = '''  it("updates recurring expense notification settings", async () => {\n'''
addition = '''  it("creates budget goals and calculates remaining amounts", async () => {\n    const { GET, POST } = await import("../../src/app/api/budget-goals/route.ts");\n    const created = await POST(new Request("http://localhost/api/budget-goals", {\n      method: "POST", headers: { "Content-Type": "application/json" },\n      body: JSON.stringify({ category: "統合テスト節約", monthlyLimit: 12000 }),\n    }));\n    assert.equal(created.status, 201);\n\n    const now = new Date();\n    const list = await GET(new Request(`http://localhost/api/budget-goals?year=${now.getFullYear()}&month=${now.getMonth() + 1}`));\n    assert.equal(list.status, 200);\n    assert.ok((await list.json()).goals.some((goal: { category: string; monthly_limit: number; spent_amount: number; remaining_amount: number }) => goal.category === "統合テスト節約" && goal.monthly_limit === 12000 && goal.spent_amount === 0 && goal.remaining_amount === 12000));\n\n    const invalid = await POST(new Request("http://localhost/api/budget-goals", {\n      method: "POST", headers: { "Content-Type": "application/json" }, body: "{",\n    }));\n    assert.equal(invalid.status, 400);\n  });\n\n'''
if anchor not in s: raise SystemExit('integration anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)

p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
anchor = '''test("定期支出候補を今月の支出へ確定する", async ({ page }) => {\n'''
addition = '''test("分析ページで節約目標の残額を確認する", async ({ page }) => {\n  const now = new Date();\n  const budgetGoalsResponse = await page.request.get(`/api/budget-goals?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(budgetGoalsResponse.ok()).toBeTruthy();\n  await page.goto("/");\n\n  await page.getByRole("button", { name: "分析", exact: true }).click();\n  const goalsPanel = page.getByLabel("節約目標");\n  await expect(goalsPanel).toContainText("外食");\n  await expect(goalsPanel).toContainText("残り ¥13,800");\n});\n\n'''
if anchor not in s: raise SystemExit('e2e anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)
PY

python3 - <<'PY'
from pathlib import Path
p = Path('Docs/product-spec.md')
s = p.read_text()
anchor = '''### 定期支出\n'''
addition = '''### 節約目標\n\n- 設定画面からカテゴリと月上限を登録する\n- 分析画面の `今月の節約目標` セクションで、カテゴリ別の利用額と残額を表示する\n- 月上限を超えた場合は超過額を表示する\n\n'''
if anchor not in s: raise SystemExit('product anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)

p = Path('Docs/screens.md')
s = p.read_text()
s = s.replace('- 目標と予算: 貯金目標一覧、達成率、残額、期限、積立操作\n', '- 目標と予算: 貯金目標一覧、達成率、残額、期限、積立操作\n- 今月の節約目標: カテゴリ別の月上限、利用額、残額、超過額\n', 1)
s = s.replace('- 貯金目標登録: 名称、目標額、期限、ホーム表示\n', '- 貯金目標登録: 名称、目標額、期限、ホーム表示\n- 節約目標登録: カテゴリ、月上限\n', 1)
p.write_text(s)

p = Path('Docs/data-model.md')
s = p.read_text()
anchor = '''## settlement_periods\n'''
addition = '''## budget_goals\n\nカテゴリ別の月次節約目標。利用額は `expenses` を指定月で集計して算出する。\n\n```txt\nid\nhousehold_id\ncategory_id\nmonthly_limit\nactive\ncreated_at\nupdated_at\n```\n\n'''
if anchor not in s: raise SystemExit('data anchor missing')
s = s.replace(anchor, addition + anchor, 1)
s = s.replace('- `202606020004_add_savings_goals.sql`: 貯金目標\n', '- `202606020004_add_savings_goals.sql`: 貯金目標\n- `202606020005_add_budget_goals.sql`: カテゴリ別の月次節約目標\n', 1)
p.write_text(s)

p = Path('Docs/api-security-spec.md')
s = p.read_text()
anchor = '''### 5.10. 精算取り消し (`POST /api/settlements/reopen`)\n'''
addition = '''### 5.10. 節約目標 (`GET /api/budget-goals`)\n\n指定月のカテゴリ別節約目標、利用額、残額を返す。\n\n### 5.11. 節約目標登録 (`POST /api/budget-goals`)\n\nカテゴリと月上限を登録する。同じカテゴリが存在する場合は月上限を更新する。\n\n'''
if anchor not in s: raise SystemExit('api anchor missing')
s = s.replace(anchor, addition + '### 5.12. 精算取り消し (`POST /api/settlements/reopen`)\n', 1)
p.write_text(s)
PY

git diff --check -- tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
grep -n "外食\|dddddddd" postgres/migrations/003_seed_local_data.sql | sed -n '1,180p' && printf '\n-- categories --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_production -c "select id,name from categories order by created_at;"
python3 - <<'PY'
from pathlib import Path
p = Path('postgres/migrations/003_seed_local_data.sql')
s = p.read_text()
old = "  'dddddddd-dddd-4ddd-8ddd-ddddddddddd4',\n  20000,\n  true\n)\non conflict (id) do update\nset\n  category_id = excluded.category_id,\n  monthly_limit = excluded.monthly_limit,\n  active = excluded.active;\n"
new = "  'cccccccc-cccc-4ccc-8ccc-ccccccccccc4',\n  20000,\n  true\n)\non conflict (id) do update\nset\n  category_id = excluded.category_id,\n  monthly_limit = excluded.monthly_limit,\n  active = excluded.active;\n"
if old not in s:
    raise SystemExit('budget seed anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- postgres/migrations/003_seed_local_data.sql
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '分析ページで節約目標の残額を確認する'
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && sed -n '/[[:blank:]]$/=' postgres/migrations/009_add_budget_goals.sql src/app/api/budget-goals/route.ts supabase/migrations/202606020005_add_budget_goals.sql && printf '\n-- feature stat --\n' && git diff --stat -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && printf '\n-- status --\n' && git status --short --branch
docker compose exec -T db psql -U kakeibo -d kakeibo_test -tAc "select c.name || ':' || bg.monthly_limit from budget_goals bg join categories c on c.id = bg.category_id where bg.id = '99999999-9999-4999-8999-999999999992';" && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true
git add Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/009_add_budget_goals.sql src/app/api/budget-goals/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx supabase/migrations/202606020005_add_budget_goals.sql tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && printf '\n-- status before commit --\n' && git status --short --branch && git commit -m 'feat: カテゴリ別の節約目標を追加する' -m '- 分析画面に月上限、利用額、残額を表示する
- 節約目標の登録APIと設定フォームを追加する
- migration、seed、仕様書、主要導線テストを更新する' && printf '\n-- latest --\n' && git log -3 --oneline && printf '\n-- final status --\n' && git status --short --branch
git push origin main && printf '\n-- latest --\n' && git log -4 --oneline && printf '\n-- status --\n' && git status --short --branch && printf '\n-- issue 13 --\n' && gh issue view 13 -R hayaryoo519/kakeibo --json number,title,state,body,url
grep -R "shared_wallet_contributions\|payment_sources\|給料\|共通口座\|共通財布" -n postgres/migrations supabase/migrations src Docs tests --exclude-dir=node_modules | sed -n '1,280p'
sed -n '1,290p' supabase/migrations/202605240001_initial_schema.sql && printf '\n-- seed wallets --\n' && sed -n '20,90p' postgres/migrations/003_seed_local_data.sql && sed -n '240,290p' postgres/migrations/003_seed_local_data.sql && printf '\n-- UI analysis/settings refs --\n' && sed -n '940,1100p' src/components/kakeibo-dashboard.tsx && sed -n '1260,1350p' src/components/kakeibo-dashboard.tsx
git status --short --branch && printf '\n-- activity log --\n' && git status --short Docs/activity_logs/2026-06-02.md
mkdir -p src/app/api/shared-wallet-contributions
python3 - <<'PY'
from pathlib import Path
Path('src/app/api/shared-wallet-contributions/route.ts').write_text('''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const querySchema = z.object({
  year: z.string().transform(Number).pipe(z.number().int().min(2000).max(9999)),
  month: z.string().transform(Number).pipe(z.number().int().min(1).max(12)),
});
const householdQuery = `select id from households order by created_at asc limit 1`;
const contributionsQuery = `
  select hm.display_name as member_name, coalesce(sum(swc.amount), 0)::int as amount
  from household_members hm
  left join shared_wallet_contributions swc
    on swc.household_id = hm.household_id and swc.member_id = hm.id
    and swc.contributed_at >= make_date($2, $3, 1)
    and swc.contributed_at < make_date($2, $3, 1) + interval '1 month'
  where hm.household_id = $1
  group by hm.id, hm.display_name, hm.role, hm.created_at
  order by hm.role asc, hm.created_at asc
`;

export async function GET(request: Request) {
  try {
    const url = new URL(request.url);
    const parsed = querySchema.safeParse({ year: url.searchParams.get("year") ?? "", month: url.searchParams.get("month") ?? "" });
    if (!parsed.success) return NextResponse.json({ error: "invalid_period" }, { status: 400 });
    const pool = db();
    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) return NextResponse.json({ total: 0, contributions: [] });
    const contributions = (await pool.query<{ member_name: string; amount: number }>(contributionsQuery, [household.id, parsed.data.year, parsed.data.month])).rows;
    return NextResponse.json({ total: contributions.reduce((total, item) => total + item.amount, 0), contributions });
  } catch (error) {
    console.error("Failed to load shared wallet contributions", error);
    return NextResponse.json({ error: "shared_wallet_contributions_failed" }, { status: 500 });
  }
}
''')

p = Path('src/components/kakeibo-dashboard.tsx')
s = p.read_text()
def replace(old, new):
    global s
    if old not in s:
        raise SystemExit(f'anchor missing: {old[:140]!r}')
    s = s.replace(old, new, 1)
replace('''type BudgetGoal = {\n  id: string;\n  category: string;\n  monthly_limit: number;\n  spent_amount: number;\n  remaining_amount: number;\n};\n''', '''type BudgetGoal = {\n  id: string;\n  category: string;\n  monthly_limit: number;\n  spent_amount: number;\n  remaining_amount: number;\n};\n\ntype SharedWalletContributions = {\n  total: number;\n  contributions: { member_name: string; amount: number }[];\n};\n''')
replace('''  const [budgetGoalDraft, setBudgetGoalDraft] = useState({ category: "", monthlyLimit: "" });\n  const composerInputRef''', '''  const [budgetGoalDraft, setBudgetGoalDraft] = useState({ category: "", monthlyLimit: "" });\n  const [sharedWalletContributions, setSharedWalletContributions] = useState<SharedWalletContributions | null>(null);\n  const [isSharedWalletLoading, setIsSharedWalletLoading] = useState(true);\n  const [sharedWalletError, setSharedWalletError] = useState<string | null>(null);\n  const composerInputRef''')
anchor = '''  const createBudgetGoal = async () => {\n'''
addition = '''  useEffect(() => {\n    let active = true;\n    setIsSharedWalletLoading(true);\n    setSharedWalletError(null);\n    void fetch(`/api/shared-wallet-contributions?year=${selectedPeriod.year}&month=${selectedPeriod.month}`)\n      .then((response) => { if (!response.ok) throw new Error("shared_wallet_contributions_load_failed"); return response.json() as Promise<SharedWalletContributions>; })\n      .then((data) => { if (active) setSharedWalletContributions(data); })\n      .catch(() => { if (active) { setSharedWalletContributions(null); setSharedWalletError("共通口座への入金を読み込めませんでした。"); } })\n      .finally(() => { if (active) setIsSharedWalletLoading(false); });\n    return () => { active = false; };\n  }, [selectedPeriod]);\n\n'''
replace(anchor, addition + anchor)
anchor = '''  const savingsGoalsPanel = (\n'''
addition = '''  const sharedWalletPanel = (\n    <section className="section shared-wallet-panel" aria-label="共通口座への入金">\n      <div className="section__header">\n        <div>\n          <h2 className="section__title">共通口座への入金</h2>\n          <p className="section__meta">今月の生活費として入れた金額を確認する</p>\n        </div>\n        {sharedWalletContributions ? <strong>{formatMoney(sharedWalletContributions.total)}</strong> : null}\n      </div>\n      {isSharedWalletLoading ? <p className="analytics-empty">読み込み中...</p> : sharedWalletError ? <p className="analytics-empty">{sharedWalletError}</p> : (sharedWalletContributions?.contributions.length ?? 0) === 0 ? <p className="analytics-empty">今月の入金はまだありません。</p> : (\n        <div className="shared-wallet-list">\n          {sharedWalletContributions?.contributions.map((contribution) => (\n            <div key={contribution.member_name} className="shared-wallet-item"><span>{contribution.member_name}</span><strong>{formatMoney(contribution.amount)}</strong></div>\n          ))}\n        </div>\n      )}\n      <p className="shared-wallet-panel__note">収入額は表示せず、共通口座への入金額だけを共有します。</p>\n    </section>\n  );\n\n'''
replace(anchor, addition + anchor)
replace('''      {savingsGoalsPanel}\n      {budgetGoalsPanel}\n\n      <div className="analytics-detail-grid">\n''', '''      {savingsGoalsPanel}\n      {budgetGoalsPanel}\n      {sharedWalletPanel}\n\n      <div className="analytics-detail-grid">\n''')
p.write_text(s)
PY

python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
s = p.read_text()
s += '''\n.shared-wallet-panel {\n  padding: 14px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.shared-wallet-list {\n  display: grid;\n  gap: 8px;\n}\n\n.shared-wallet-item {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 8px;\n  padding: 12px;\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  background: rgba(255, 255, 255, 0.68);\n}\n\n.shared-wallet-item span {\n  color: var(--muted);\n  font-size: 13px;\n}\n\n.shared-wallet-panel__note {\n  margin: 10px 0 0;\n  color: var(--muted);\n  font-size: 12px;\n}\n'''
p.write_text(s)
PY

git diff --check -- src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx src/app/globals.css
python3 - <<'PY'
from pathlib import Path
p = Path('tests/integration/api.integration.test.ts')
s = p.read_text()
anchor = '''  it("updates recurring expense notification settings", async () => {\n'''
addition = '''  it("lists monthly shared wallet contributions without exposing income", async () => {\n    const { GET } = await import("../../src/app/api/shared-wallet-contributions/route.ts");\n    const now = new Date();\n    const response = await GET(new Request(`http://localhost/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`));\n    assert.equal(response.status, 200);\n    assert.deepEqual(await response.json(), {\n      total: 55000,\n      contributions: [\n        { member_name: "葵", amount: 30000 },\n        { member_name: "蓮", amount: 25000 },\n      ],\n    });\n\n    const invalid = await GET(new Request("http://localhost/api/shared-wallet-contributions?year=2026&month=13"));\n    assert.equal(invalid.status, 400);\n  });\n\n'''
if anchor not in s: raise SystemExit('integration anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)

p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
anchor = '''test("定期支出候補を今月の支出へ確定する", async ({ page }) => {\n'''
addition = '''test("分析ページで共通口座への入金状況を確認する", async ({ page }) => {\n  const now = new Date();\n  const contributionsResponse = await page.request.get(`/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(contributionsResponse.ok()).toBeTruthy();\n  await page.goto("/");\n\n  await page.getByRole("button", { name: "分析", exact: true }).click();\n  const panel = page.getByLabel("共通口座への入金");\n  await expect(panel).toContainText("¥55,000");\n  await expect(panel).toContainText("葵");\n  await expect(panel).toContainText("¥30,000");\n  await expect(panel).toContainText("蓮");\n  await expect(panel).toContainText("¥25,000");\n});\n\n'''
if anchor not in s: raise SystemExit('e2e anchor missing')
s = s.replace(anchor, addition + anchor, 1)
p.write_text(s)
PY

python3 - <<'PY'
from pathlib import Path
p = Path('Docs/product-spec.md')
s = p.read_text()
anchor = '''### 定期支出\n'''
addition = '''### 共通口座への入金状況\n\n- 分析画面で選択月の共通口座への入金合計を表示する\n- メンバーごとの入金額を一覧表示する\n- 収入額は共有画面へ表示せず、入金額だけを表示する\n- 給料日通知、収入比率による提案額、入金予定と完了状態の操作は未実装\n\n'''
if anchor not in s: raise SystemExit('product anchor missing')
s = s.replace(anchor, addition + anchor, 1)
s = s.replace('- 共通口座への入金管理は未実装\n', '- 共通口座への入金状況は表示できるが、提案額と入金操作は未実装\n', 1)
p.write_text(s)

p = Path('Docs/screens.md')
s = p.read_text()
s = s.replace('- 今月の節約目標: カテゴリ別の月上限、利用額、残額、超過額\n', '- 今月の節約目標: カテゴリ別の月上限、利用額、残額、超過額\n- 共通口座への入金: 当月合計、メンバー別の入金額、収入額を非表示にする注記\n', 1)
p.write_text(s)

p = Path('Docs/data-model.md')
s = p.read_text()
s = s.replace('## shared_wallet_contributions\n\n将来拡張。共通財布・共通カードへの入金を管理する。', '## shared_wallet_contributions\n\n共通財布・共通カードへの入金を管理する。当月合計とメンバー別の入金額表示に使用する。', 1)
p.write_text(s)

p = Path('Docs/api-security-spec.md')
s = p.read_text()
anchor = '''### 5.12. 精算取り消し (`POST /api/settlements/reopen`)\n'''
addition = '''### 5.12. 共通口座への入金状況 (`GET /api/shared-wallet-contributions`)\n\n指定月の共通口座への入金合計とメンバー別入金額を返す。収入額は返さない。\n\n| パラメータ | 型 | 必須 | ルール |\n|---|---|---|---|\n| `year` | string | 必須 | 2000〜9999 |\n| `month` | string | 必須 | 1〜12 |\n\n'''
if anchor not in s: raise SystemExit('api anchor missing')
s = s.replace(anchor, addition + '### 5.13. 精算取り消し (`POST /api/settlements/reopen`)\n', 1)
p.write_text(s)
PY

git diff --check -- src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx src/app/globals.css tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '分析ページで共通口座への入金状況を確認する'
sed -n '1,180p' test-results/kakeibo-分析ページで共通口座への入金状況を確認する-chromium/error-context.md && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true
python3 - <<'PY'
from pathlib import Path
p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
old = '''  const contributionsResponse = await page.request.get(`/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(contributionsResponse.ok()).toBeTruthy();\n  await page.goto("/");\n\n  await page.getByRole("button", { name: "分析", exact: true }).click();\n'''
new = '''  const contributionsResponse = await page.request.get(`/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(contributionsResponse.ok()).toBeTruthy();\n  const dashboardResponse = await page.request.get(`/api/dashboard?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(dashboardResponse.ok()).toBeTruthy();\n  await page.goto("/");\n  await expect(page.getByLabel("支出を入力")).toBeEnabled();\n\n  await page.getByRole("button", { name: "分析", exact: true }).click();\n'''
if old not in s:
    raise SystemExit('shared wallet e2e anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- tests/e2e/kakeibo.spec.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '分析ページで共通口座への入金状況を確認する'
python3 - <<'PY'
from pathlib import Path
p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
old = '''  const dashboardResponse = await page.request.get(`/api/dashboard?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(dashboardResponse.ok()).toBeTruthy();\n  await page.goto("/");\n  await expect(page.getByLabel("支出を入力")).toBeEnabled();\n'''
new = '''  const dashboardResponse = await page.request.get(`/api/dashboard?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(dashboardResponse.ok()).toBeTruthy();\n  await page.goto("/", { waitUntil: "domcontentloaded" });\n  await expect(page.getByLabel("支出を入力")).toBeEnabled();\n'''
if old not in s:
    raise SystemExit('shared wallet navigation anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- tests/e2e/kakeibo.spec.ts
env DATABASE_URL_TEST=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npx playwright test --grep '分析ページで共通口座への入金状況を確認する'
python3 - <<'PY'
from pathlib import Path
p = Path('tests/e2e/kakeibo.spec.ts')
s = p.read_text()
old = '''  const contributionsResponse = await page.request.get(`/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(contributionsResponse.ok()).toBeTruthy();\n  const dashboardResponse = await page.request.get(`/api/dashboard?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(dashboardResponse.ok()).toBeTruthy();\n  await page.goto("/", { waitUntil: "domcontentloaded" });\n  await expect(page.getByLabel("支出を入力")).toBeEnabled();\n'''
new = '''  const contributionsResponse = await page.request.get(`/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`);\n  expect(contributionsResponse.ok()).toBeTruthy();\n  await page.goto("/");\n'''
if old not in s:
    raise SystemExit('shared wallet e2e reset anchor missing')
p.write_text(s.replace(old, new, 1))
PY

git diff --check -- tests/e2e/kakeibo.spec.ts
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run start -- --hostname 127.0.0.1 --port 3023
env DATABASE_URL=postgresql://kakeibo:kakeibo_password@localhost:5433/kakeibo_test npm run start -- --hostname 127.0.0.1 --port 3023
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  page.on('pageerror', error => console.log('pageerror:', error.message));
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '分析', exact: true }).click();
  const panel = page.getByLabel('共通口座への入金');
  await panel.waitFor({ state: 'visible' });
  const text = await panel.innerText();
  for (const expected of ['¥55,000', '葵', '¥30,000', '蓮', '¥25,000']) {
    if (!text.includes(expected)) throw new Error(`missing ${expected}: ${text}`);
  }
  console.log(text);
  await browser.close();
})();
JS
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && sed -n '/[[:blank:]]$/=' src/app/api/shared-wallet-contributions/route.ts && printf '\n-- stat --\n' && git diff --stat -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && printf '\n-- status --\n' && git status --short --branch
ss -ltnp | grep -E ':3022|:3023' || true
git add Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md src/app/api/shared-wallet-contributions/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --check && printf '\n-- staged --\n' && git diff --cached --stat && git commit -m 'feat: 共通口座への入金状況を表示する' -m '- 選択月の入金合計とメンバー別入金額を返すAPIを追加する
- 分析画面に共通口座への入金状況を表示する
- 収入額を共有画面へ出さない仕様とテストを追加する' && printf '\n-- latest --\n' && git log -3 --oneline && printf '\n-- status --\n' && git status --short --branch
sed -n '1,220p' src/app/api/shared-wallet-contributions/route.ts && printf '\n-- members schema --\n' && sed -n '40,82p' supabase/migrations/202605240001_initial_schema.sql && printf '\n-- prepare --\n' && sed -n '1,120p' postgres/migrations/002_prepare_local_environments.sh && printf '\n-- shared UI refs --\n' && grep -n "sharedWallet\|共通口座" src/components/kakeibo-dashboard.tsx Docs/product-spec.md Docs/screens.md Docs/api-security-spec.md
sed -n '1,230p' tests/integration/api.integration.test.ts && printf '\n-- e2e relevant --\n' && sed -n '118,172p' tests/e2e/kakeibo.spec.ts && printf '\n-- seed tail --\n' && tail -n 95 postgres/migrations/003_seed_local_data.sql
python3 - <<'PY'
from pathlib import Path
sql = '''create table if not exists shared_wallet_monthly_plans (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households(id) on delete cascade,
  year integer not null,
  month integer not null,
  target_amount integer not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (household_id, year, month),
  constraint shared_wallet_monthly_plans_month_range check (month between 1 and 12),
  constraint shared_wallet_monthly_plans_target_positive check (target_amount > 0)
);

create index if not exists shared_wallet_monthly_plans_household_id_idx
  on shared_wallet_monthly_plans(household_id, year, month);
'''
Path('postgres/migrations/010_add_shared_wallet_monthly_plans.sql').write_text(sql)
Path('supabase/migrations/202606020006_add_shared_wallet_monthly_plans.sql').write_text(sql)

p=Path('postgres/migrations/002_prepare_local_environments.sh')
s=p.read_text()
s=s.replace('budget_goals_migration_file="/docker-entrypoint-initdb.d/009_add_budget_goals.sql"\n','budget_goals_migration_file="/docker-entrypoint-initdb.d/009_add_budget_goals.sql"\nshared_wallet_plans_migration_file="/docker-entrypoint-initdb.d/010_add_shared_wallet_monthly_plans.sql"\n')
s=s.replace('  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$budget_goals_migration_file"\n','  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$budget_goals_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$shared_wallet_plans_migration_file"\n')
p.write_text(s)

p=Path('postgres/migrations/003_seed_local_data.sql')
s=p.read_text()
addition='''\n\ninsert into shared_wallet_monthly_plans (id, household_id, year, month, target_amount)\nvalues (\n  '99999999-9999-4999-8999-999999999993',\n  'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',\n  extract(year from current_date)::int,\n  extract(month from current_date)::int,\n  100000\n)\non conflict (household_id, year, month) do update\nset target_amount = excluded.target_amount;\n'''
if '99999999-9999-4999-8999-999999999993' not in s: s += addition
p.write_text(s)

p=Path('src/app/api/shared-wallet-contributions/route.ts')
s=p.read_text()
s=s.replace('''const householdQuery = `select id from households order by created_at asc limit 1`;\nconst contributionsQuery = `\n  select hm.display_name as member_name, coalesce(sum(swc.amount), 0)::int as amount\n''','''const planSchema = z.object({ year: z.number().int().min(2000).max(9999), month: z.number().int().min(1).max(12), targetAmount: z.number().int().positive() });\nconst householdQuery = `select id from households order by created_at asc limit 1`;\nconst planQuery = `select target_amount from shared_wallet_monthly_plans where household_id = $1 and year = $2 and month = $3`;\nconst contributionsQuery = `\n  select hm.display_name as member_name, hm.default_share_rate::float8 as share_rate, coalesce(sum(swc.amount), 0)::int as amount\n''')
s=s.replace('''    const contributions = (await pool.query<{ member_name: string; amount: number }>(contributionsQuery, [household.id, parsed.data.year, parsed.data.month])).rows;\n    return NextResponse.json({ total: contributions.reduce((total, item) => total + item.amount, 0), contributions });\n''','''    const contributions = (await pool.query<{ member_name: string; share_rate: number | null; amount: number }>(contributionsQuery, [household.id, parsed.data.year, parsed.data.month])).rows;\n    const targetAmount = (await pool.query<{ target_amount: number }>(planQuery, [household.id, parsed.data.year, parsed.data.month])).rows[0]?.target_amount ?? null;\n    const totalShareRate = contributions.reduce((total, item) => total + (item.share_rate ?? 0), 0);\n    const contributionTotal = contributions.reduce((total, item) => total + item.amount, 0);\n    return NextResponse.json({\n      total: contributionTotal,\n      target_amount: targetAmount,\n      remaining_amount: targetAmount == null ? null : Math.max(targetAmount - contributionTotal, 0),\n      contributions: contributions.map((item) => {\n        const shareRate = totalShareRate > 0 ? (item.share_rate ?? 0) / totalShareRate : 1 / contributions.length;\n        const proposedAmount = targetAmount == null ? null : Math.round(targetAmount * shareRate);\n        return { member_name: item.member_name, amount: item.amount, share_rate: shareRate, proposed_amount: proposedAmount, remaining_amount: proposedAmount == null ? null : Math.max(proposedAmount - item.amount, 0) };\n      }),\n    });\n''')
s += '''\nexport async function POST(request: Request) {\n  const pool = db();\n  const client = await pool.connect();\n  try {\n    const parsed = planSchema.safeParse(await request.json().catch(() => null));\n    if (!parsed.success) return NextResponse.json({ error: "invalid_shared_wallet_plan" }, { status: 400 });\n    await client.query("begin");\n    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];\n    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }\n    await client.query(`insert into shared_wallet_monthly_plans (household_id, year, month, target_amount) values ($1, $2, $3, $4) on conflict (household_id, year, month) do update set target_amount = excluded.target_amount, updated_at = now()`, [household.id, parsed.data.year, parsed.data.month, parsed.data.targetAmount]);\n    await client.query("commit");\n    console.info("Updated shared wallet monthly plan", parsed.data);\n    return NextResponse.json({ targetAmount: parsed.data.targetAmount });\n  } catch (error) {\n    await client.query("rollback");\n    console.error("Failed to update shared wallet monthly plan", error);\n    return NextResponse.json({ error: "shared_wallet_plan_failed" }, { status: 500 });\n  } finally { client.release(); }\n}\n'''
p.write_text(s)
PY

git diff --check -- postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/api/shared-wallet-contributions/route.ts
python3 - <<'PY'
from pathlib import Path
p=Path('src/components/kakeibo-dashboard.tsx')
s=p.read_text()
def replace(old,new):
 global s
 if old not in s: raise SystemExit(f'anchor missing: {old[:140]!r}')
 s=s.replace(old,new,1)
replace('''type SharedWalletContributions = {\n  total: number;\n  contributions: { member_name: string; amount: number }[];\n};\n''','''type SharedWalletContributions = {\n  total: number;\n  target_amount: number | null;\n  remaining_amount: number | null;\n  contributions: { member_name: string; amount: number; share_rate: number; proposed_amount: number | null; remaining_amount: number | null }[];\n};\n''')
replace('''  const [sharedWalletError, setSharedWalletError] = useState<string | null>(null);\n  const composerInputRef''','''  const [sharedWalletError, setSharedWalletError] = useState<string | null>(null);\n  const [sharedWalletPlanDraft, setSharedWalletPlanDraft] = useState("");\n  const composerInputRef''')
anchor='''  const createBudgetGoal = async () => {\n'''
addition='''  const saveSharedWalletPlan = async () => {\n    const targetAmount = Number(sharedWalletPlanDraft);\n    if (!Number.isInteger(targetAmount) || targetAmount <= 0) { setSharedWalletError("共通口座の目標額を確認してください。"); return; }\n    setSharedWalletError(null);\n    try {\n      const response = await fetch("/api/shared-wallet-contributions", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...selectedPeriod, targetAmount }) });\n      if (!response.ok) throw new Error("shared_wallet_plan_failed");\n      setSharedWalletPlanDraft("");\n      const refreshed = await fetch(`/api/shared-wallet-contributions?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);\n      if (!refreshed.ok) throw new Error("shared_wallet_refresh_failed");\n      setSharedWalletContributions(await refreshed.json() as SharedWalletContributions);\n    } catch { setSharedWalletError("共通口座の目標額を更新できませんでした。"); }\n  };\n\n'''
replace(anchor,addition+anchor)
old='''        {sharedWalletContributions ? <strong>{formatMoney(sharedWalletContributions.total)}</strong> : null}\n      </div>\n      {isSharedWalletLoading ? <p className="analytics-empty">読み込み中...</p> : sharedWalletError ? <p className="analytics-empty">{sharedWalletError}</p> : (sharedWalletContributions?.contributions.length ?? 0) === 0 ? <p className="analytics-empty">今月の入金はまだありません。</p> : (\n        <div className="shared-wallet-list">\n          {sharedWalletContributions?.contributions.map((contribution) => (\n            <div key={contribution.member_name} className="shared-wallet-item"><span>{contribution.member_name}</span><strong>{formatMoney(contribution.amount)}</strong></div>\n          ))}\n        </div>\n      )}\n'''
new='''        {sharedWalletContributions ? <strong>{formatMoney(sharedWalletContributions.total)}</strong> : null}\n      </div>\n      {isSharedWalletLoading ? <p className="analytics-empty">読み込み中...</p> : sharedWalletError ? <p className="analytics-empty">{sharedWalletError}</p> : (sharedWalletContributions?.contributions.length ?? 0) === 0 ? <p className="analytics-empty">今月の入金はまだありません。</p> : (\n        <>\n          {sharedWalletContributions?.target_amount != null ? <p className="shared-wallet-panel__summary">目標 {formatMoney(sharedWalletContributions.target_amount)} ・ あと {formatMoney(sharedWalletContributions.remaining_amount ?? 0)}</p> : null}\n          <div className="shared-wallet-list">\n            {sharedWalletContributions?.contributions.map((contribution) => (\n              <div key={contribution.member_name} className="shared-wallet-item">\n                <div><span>{contribution.member_name}</span>{contribution.proposed_amount != null ? <small>提案 {formatMoney(contribution.proposed_amount)} ・ あと {formatMoney(contribution.remaining_amount ?? 0)}</small> : null}</div>\n                <strong>{formatMoney(contribution.amount)}</strong>\n              </div>\n            ))}\n          </div>\n        </>\n      )}\n'''
replace(old,new)
replace('''      <div className="settings-grid">\n        <article className="panel settings-panel budget-goal-settings">\n''','''      <div className="settings-grid">\n        <article className="panel settings-panel shared-wallet-settings">\n          <h3>共通口座の入金目標</h3>\n          <div className="shared-wallet-settings__grid">\n            <input aria-label="共通口座の入金目標額" inputMode="numeric" placeholder={sharedWalletContributions?.target_amount ? `現在 ${sharedWalletContributions.target_amount}` : "目標額"} value={sharedWalletPlanDraft} onChange={(event) => setSharedWalletPlanDraft(event.target.value)} />\n            <button className="chip chip--active" type="button" onClick={() => void saveSharedWalletPlan()}>目標額を更新</button>\n          </div>\n        </article>\n        <article className="panel settings-panel budget-goal-settings">\n''')
p.write_text(s)
PY
python3 - <<'PY'
from pathlib import Path
p=Path('src/app/globals.css')
s=p.read_text()
s += '''\n.shared-wallet-panel__summary {\n  margin: 0 0 10px;\n  color: var(--primary);\n  font-size: 13px;\n  font-weight: 700;\n}\n\n.shared-wallet-item div,\n.shared-wallet-item small {\n  display: grid;\n  gap: 3px;\n}\n\n.shared-wallet-item small {\n  color: var(--muted);\n  font-size: 11px;\n}\n\n.shared-wallet-settings__grid {\n  display: grid;\n  gap: 8px;\n}\n\n.shared-wallet-settings__grid input {\n  min-height: 40px;\n  min-width: 0;\n  padding: 0 10px;\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fff;\n}\n'''
p.write_text(s)
PY

git diff --check -- src/components/kakeibo-dashboard.tsx src/app/globals.css
python3 - <<'PY'
from pathlib import Path
p=Path('tests/integration/api.integration.test.ts')
s=p.read_text()
s=s.replace('''    await pool.query("update savings_goals set current_amount = 116000 where id = '99999999-9999-4999-8999-999999999991'");\n''','''    await pool.query("update savings_goals set current_amount = 116000 where id = '99999999-9999-4999-8999-999999999991'");\n    await pool.query("update shared_wallet_monthly_plans set target_amount = 100000 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");\n''')
old='''    const { GET } = await import("../../src/app/api/shared-wallet-contributions/route.ts");\n'''
new='''    const { GET, POST } = await import("../../src/app/api/shared-wallet-contributions/route.ts");\n'''
s=s.replace(old,new,1)
old='''    assert.deepEqual(await response.json(), {\n      total: 55000,\n      contributions: [\n        { member_name: "葵", amount: 30000 },\n        { member_name: "蓮", amount: 25000 },\n      ],\n    });\n\n    const invalid = await GET(new Request("http://localhost/api/shared-wallet-contributions?year=2026&month=13"));\n'''
new='''    assert.deepEqual(await response.json(), {\n      total: 55000,\n      target_amount: 100000,\n      remaining_amount: 45000,\n      contributions: [\n        { member_name: "葵", amount: 30000, share_rate: 0.55, proposed_amount: 55000, remaining_amount: 25000 },\n        { member_name: "蓮", amount: 25000, share_rate: 0.45, proposed_amount: 45000, remaining_amount: 20000 },\n      ],\n    });\n\n    const updated = await POST(new Request("http://localhost/api/shared-wallet-contributions", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ year: now.getFullYear(), month: now.getMonth() + 1, targetAmount: 120000 }) }));\n    assert.equal(updated.status, 200);\n    assert.deepEqual(await updated.json(), { targetAmount: 120000 });\n\n    const invalid = await GET(new Request("http://localhost/api/shared-wallet-contributions?year=2026&month=13"));\n'''
if old not in s: raise SystemExit('integration wallet anchor missing')
s=s.replace(old,new,1)
p.write_text(s)

p=Path('tests/e2e/kakeibo.spec.ts')
s=p.read_text()
s=s.replace('''  await expect(panel).toContainText("¥25,000");\n});\n''','''  await expect(panel).toContainText("¥25,000");\n  await expect(panel).toContainText("目標 ¥100,000");\n  await expect(panel).toContainText("提案 ¥55,000");\n  await expect(panel).toContainText("提案 ¥45,000");\n});\n''',1)
p.write_text(s)
PY
python3 - <<'PY'
from pathlib import Path
p=Path('Docs/product-spec.md'); s=p.read_text()
s=s.replace('- メンバーごとの入金額を一覧表示する\n- 収入額は共有画面へ表示せず、入金額だけを表示する\n- 給料日通知、収入比率による提案額、入金予定と完了状態の操作は未実装\n','- メンバーごとの入金額を一覧表示する\n- 設定画面から月次の入金目標額を登録する\n- 既存の負担率に応じた入金提案額と不足額を表示する\n- 収入額は共有画面へ表示せず、入金額と比率から算出した提案額だけを表示する\n- 給料日通知、収入登録の更新、入金予定と完了状態の操作は未実装\n',1)
s=s.replace('- 共通口座への入金状況は表示できるが、提案額と入金操作は未実装\n','- 共通口座への入金状況と提案額は表示できるが、給料日通知と入金操作は未実装\n',1)
p.write_text(s)
p=Path('Docs/screens.md'); s=p.read_text(); s=s.replace('- 共通口座への入金: 当月合計、メンバー別の入金額、収入額を非表示にする注記\n','- 共通口座への入金: 当月目標、当月合計、メンバー別の入金額、提案額、不足額、収入額を非表示にする注記\n',1); s=s.replace('- 節約目標登録: カテゴリ、月上限\n','- 節約目標登録: カテゴリ、月上限\n- 共通口座の入金目標: 選択月の目標額\n',1); p.write_text(s)
p=Path('Docs/data-model.md'); s=p.read_text(); anchor='''## shared_wallet_contributions\n'''; addition='''## shared_wallet_monthly_plans\n\n月ごとの共通口座への入金目標額。メンバー別の提案額は `household_members.default_share_rate` から算出する。\n\n```txt\nid\nhousehold_id\nyear\nmonth\ntarget_amount\ncreated_at\nupdated_at\n```\n\n''';
if anchor not in s: raise SystemExit('data anchor missing')
s=s.replace(anchor,addition+anchor,1); s=s.replace('- `202606020005_add_budget_goals.sql`: カテゴリ別の月次節約目標\n','- `202606020005_add_budget_goals.sql`: カテゴリ別の月次節約目標\n- `202606020006_add_shared_wallet_monthly_plans.sql`: 共通口座への月次入金目標\n',1); p.write_text(s)
p=Path('Docs/api-security-spec.md'); s=p.read_text(); s=s.replace('指定月の共通口座への入金合計とメンバー別入金額を返す。収入額は返さない。','指定月の共通口座への入金目標、入金合計、メンバー別入金額、負担率から算出した提案額と不足額を返す。収入額は返さない。',1); anchor='''### 5.13. 精算取り消し (`POST /api/settlements/reopen`)\n'''; addition='''### 5.13. 共通口座への月次入金目標 (`POST /api/shared-wallet-contributions`)\n\n指定月の共通口座への入金目標額を登録または更新する。提案額は登録済みの負担率から算出する。\n\n''';
if anchor not in s: raise SystemExit('api anchor missing')
s=s.replace(anchor,addition+'### 5.14. 精算取り消し (`POST /api/settlements/reopen`)\n',1); p.write_text(s)
PY

git diff --check -- tests/integration/api.integration.test.ts tests/e2e/kakeibo.spec.ts Docs/product-spec.md Docs/screens.md Docs/data-model.md Docs/api-security-spec.md
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  page.on('pageerror', error => console.log('pageerror:', error.message));
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '分析', exact: true }).click();
  const panel = page.getByLabel('共通口座への入金');
  await panel.waitFor({ state: 'visible' });
  let text = await panel.innerText();
  for (const expected of ['¥55,000', '目標 ¥100,000', '提案 ¥55,000', '提案 ¥45,000']) {
    if (!text.includes(expected)) throw new Error(`missing ${expected}: ${text}`);
  }
  await page.getByRole('button', { name: '設定', exact: true }).click();
  await page.getByLabel('共通口座の入金目標額').fill('110000');
  await Promise.all([
    page.waitForResponse(response => response.url().endsWith('/api/shared-wallet-contributions') && response.request().method() === 'POST' && response.ok()),
    page.getByRole('button', { name: '目標額を更新', exact: true }).click(),
  ]);
  await page.getByRole('button', { name: '分析', exact: true }).click();
  const updatedPanel = page.getByLabel('共通口座への入金');
  await updatedPanel.waitFor({ state: 'visible' });
  text = await updatedPanel.innerText();
  for (const expected of ['目標 ¥110,000', '提案 ¥60,500', '提案 ¥49,500']) {
    if (!text.includes(expected)) throw new Error(`missing ${expected}: ${text}`);
  }
  console.log(text);
  await browser.close();
})();
JS
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/api/shared-wallet-contributions/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
sed -n '/[[:blank:]]$/=' postgres/migrations/010_add_shared_wallet_monthly_plans.sql supabase/migrations/202606020006_add_shared_wallet_monthly_plans.sql
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select target_amount from shared_wallet_monthly_plans where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int;"
git diff --stat -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/api/shared-wallet-contributions/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git ls-files --others --exclude-standard postgres/migrations/010_add_shared_wallet_monthly_plans.sql supabase/migrations/202606020006_add_shared_wallet_monthly_plans.sql
git diff --check -- Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/app/api/shared-wallet-contributions/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && sed -n '/[[:blank:]]$/=' postgres/migrations/010_add_shared_wallet_monthly_plans.sql supabase/migrations/202606020006_add_shared_wallet_monthly_plans.sql
git add Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/010_add_shared_wallet_monthly_plans.sql supabase/migrations/202606020006_add_shared_wallet_monthly_plans.sql src/app/api/shared-wallet-contributions/route.ts src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --stat && git diff --cached --name-only
git commit -m "feat: 共通口座の月次入金目標を追加する" -m "- 月次の共通口座入金目標を保存するmigrationとAPIを追加
- 負担比率に基づく提案額と不足額を分析画面に表示
- 設定画面、仕様書、integration・E2Eテストを更新" && git status --short --branch && git log --oneline --max-count=3
gh issue list --state open --limit 30 --json number,title,labels,url
rg -n "共通口座|給料日|入金|通知|未実装|TODO" Docs src tests postgres supabase --glob '!**/node_modules/**'
git status --short --branch && git log --oneline --max-count=5
gh issue view 13 --json number,title,body,state,url
grep -RInE "共通口座|給料日|入金|通知|未実装|TODO" Docs src tests postgres supabase --exclude-dir=node_modules
sed -n '220,330p' Docs/data-model.md && sed -n '145,180p' Docs/product-spec.md && sed -n '240,285p' Docs/api-security-spec.md
sed -n '1,280p' src/app/api/shared-wallet-contributions/route.ts && sed -n '300,390p' src/components/kakeibo-dashboard.tsx && sed -n '1020,1080p' src/components/kakeibo-dashboard.tsx
grep -RIn "shared_wallet_contributions" postgres supabase tests src --exclude-dir=node_modules && sed -n '1,240p' postgres/migrations/001_init.sql && sed -n '1,240p' tests/integration/api.integration.test.ts
sed -n '1,220p' postgres/migrations/002_prepare_local_environments.sh && sed -n '1,280p' postgres/migrations/003_seed_local_data.sql && sed -n '120,185p' tests/e2e/kakeibo.spec.ts
sed -n '210,275p' postgres/migrations/001_initial_schema.sql && sed -n '1,130p' src/components/kakeibo-dashboard.tsx && sed -n '1160,1260p' src/app/globals.css && sed -n '1,260p' tests/integration/api.integration.test.ts
ls postgres/migrations supabase/migrations && grep -n "shared-wallet\|shared wallet" tests/integration/api.integration.test.ts && grep -n "shared-wallet" src/app/globals.css
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:100]!r}')
    p.write_text(text.replace(old, new, 1))

migration = '''create table if not exists shared_wallet_member_plans (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households(id) on delete cascade,
  member_id uuid not null,
  year integer not null,
  month integer not null,
  planned_amount integer not null,
  status text not null default 'pending',
  completed_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (household_id, member_id, year, month),
  constraint shared_wallet_member_plans_member_same_household
    foreign key (member_id, household_id)
    references household_members(id, household_id)
    on delete cascade,
  constraint shared_wallet_member_plans_month_range check (month between 1 and 12),
  constraint shared_wallet_member_plans_amount_positive check (planned_amount > 0),
  constraint shared_wallet_member_plans_status_valid check (status in ('pending', 'completed'))
);

create index if not exists shared_wallet_member_plans_household_id_idx
  on shared_wallet_member_plans(household_id, year, month);
'''
Path('postgres/migrations/011_add_shared_wallet_member_plans.sql').write_text(migration)
Path('supabase/migrations/202606020007_add_shared_wallet_member_plans.sql').write_text(migration)

replace('postgres/migrations/002_prepare_local_environments.sh',
'''shared_wallet_plans_migration_file="/docker-entrypoint-initdb.d/010_add_shared_wallet_monthly_plans.sql"
''',
'''shared_wallet_plans_migration_file="/docker-entrypoint-initdb.d/010_add_shared_wallet_monthly_plans.sql"
shared_wallet_member_plans_migration_file="/docker-entrypoint-initdb.d/011_add_shared_wallet_member_plans.sql"
''')
replace('postgres/migrations/002_prepare_local_environments.sh',
'''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$shared_wallet_plans_migration_file"
''',
'''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$shared_wallet_plans_migration_file"
  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$shared_wallet_member_plans_migration_file"
''')

seed = '''
insert into shared_wallet_member_plans (id, household_id, member_id, year, month, planned_amount, status, completed_at)
values
  (
    '99999999-9999-4999-8999-999999999994',
    'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',
    'aaaaaaaa-1111-4111-8111-aaaaaaaa1111',
    extract(year from current_date)::int,
    extract(month from current_date)::int,
    55000,
    'pending',
    null
  ),
  (
    '99999999-9999-4999-8999-999999999995',
    'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',
    'aaaaaaaa-2222-4222-8222-aaaaaaaa2222',
    extract(year from current_date)::int,
    extract(month from current_date)::int,
    45000,
    'pending',
    null
  )
on conflict (household_id, member_id, year, month) do update
set
  planned_amount = excluded.planned_amount,
  status = excluded.status,
  completed_at = excluded.completed_at;
'''
p = Path('postgres/migrations/003_seed_local_data.sql')
text = p.read_text()
if 'insert into shared_wallet_member_plans' in text: raise SystemExit('seed already modified')
text += seed
p.write_text(text)
PY
python3 - <<'PY'
from pathlib import Path
p = Path('src/app/api/shared-wallet-contributions/route.ts')
p.write_text('''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const querySchema = z.object({
  year: z.string().transform(Number).pipe(z.number().int().min(2000).max(9999)),
  month: z.string().transform(Number).pipe(z.number().int().min(1).max(12)),
});
const planSchema = z.object({ year: z.number().int().min(2000).max(9999), month: z.number().int().min(1).max(12), targetAmount: z.number().int().positive() });
const memberPlanSchema = z.object({ memberId: z.string().uuid(), year: z.number().int().min(2000).max(9999), month: z.number().int().min(1).max(12), status: z.enum(["pending", "completed"]) });
const householdQuery = `select id from households order by created_at asc limit 1`;
const planQuery = `select target_amount from shared_wallet_monthly_plans where household_id = $1 and year = $2 and month = $3`;
const contributionsQuery = `
  select hm.id::text as member_id, hm.display_name as member_name, hm.default_share_rate::float8 as share_rate, coalesce(sum(swc.amount), 0)::int as amount, swmp.planned_amount, coalesce(swmp.status, 'pending') as status
  from household_members hm
  left join shared_wallet_contributions swc
    on swc.household_id = hm.household_id and swc.member_id = hm.id
    and swc.contributed_at >= make_date($2, $3, 1)
    and swc.contributed_at < make_date($2, $3, 1) + interval '1 month'
  left join shared_wallet_member_plans swmp
    on swmp.household_id = hm.household_id and swmp.member_id = hm.id
    and swmp.year = $2 and swmp.month = $3
  where hm.household_id = $1
  group by hm.id, hm.display_name, hm.role, hm.created_at, swmp.planned_amount, swmp.status
  order by hm.role asc, hm.created_at asc
`;

export async function GET(request: Request) {
  try {
    const url = new URL(request.url);
    const parsed = querySchema.safeParse({ year: url.searchParams.get("year") ?? "", month: url.searchParams.get("month") ?? "" });
    if (!parsed.success) return NextResponse.json({ error: "invalid_period" }, { status: 400 });
    const pool = db();
    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) return NextResponse.json({ total: 0, contributions: [] });
    const contributions = (await pool.query<{ member_id: string; member_name: string; share_rate: number | null; amount: number; planned_amount: number | null; status: "pending" | "completed" }>(contributionsQuery, [household.id, parsed.data.year, parsed.data.month])).rows;
    const targetAmount = (await pool.query<{ target_amount: number }>(planQuery, [household.id, parsed.data.year, parsed.data.month])).rows[0]?.target_amount ?? null;
    const totalShareRate = contributions.reduce((total, item) => total + (item.share_rate ?? 0), 0);
    const contributionTotal = contributions.reduce((total, item) => total + item.amount, 0);
    return NextResponse.json({
      total: contributionTotal,
      target_amount: targetAmount,
      remaining_amount: targetAmount == null ? null : Math.max(targetAmount - contributionTotal, 0),
      contributions: contributions.map((item) => {
        const shareRate = totalShareRate > 0 ? (item.share_rate ?? 0) / totalShareRate : 1 / contributions.length;
        const proposedAmount = targetAmount == null ? null : Math.round(targetAmount * shareRate);
        const plannedAmount = item.planned_amount ?? proposedAmount;
        return { member_id: item.member_id, member_name: item.member_name, amount: item.amount, share_rate: shareRate, proposed_amount: proposedAmount, planned_amount: plannedAmount, remaining_amount: plannedAmount == null ? null : Math.max(plannedAmount - item.amount, 0), status: item.status };
      }),
    });
  } catch (error) {
    console.error("Failed to load shared wallet contributions", error);
    return NextResponse.json({ error: "shared_wallet_contributions_failed" }, { status: 500 });
  }
}

export async function POST(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = planSchema.safeParse(await request.json().catch(() => null));
    if (!parsed.success) return NextResponse.json({ error: "invalid_shared_wallet_plan" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    await client.query(`insert into shared_wallet_monthly_plans (household_id, year, month, target_amount) values ($1, $2, $3, $4) on conflict (household_id, year, month) do update set target_amount = excluded.target_amount, updated_at = now()`, [household.id, parsed.data.year, parsed.data.month, parsed.data.targetAmount]);
    await client.query("commit");
    console.info("Updated shared wallet monthly plan", parsed.data);
    return NextResponse.json({ targetAmount: parsed.data.targetAmount });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to update shared wallet monthly plan", error);
    return NextResponse.json({ error: "shared_wallet_plan_failed" }, { status: 500 });
  } finally { client.release(); }
}

export async function PATCH(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = memberPlanSchema.safeParse(await request.json().catch(() => null));
    if (!parsed.success) return NextResponse.json({ error: "invalid_shared_wallet_member_plan" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    const member = (await client.query<{ share_rate: number; total_share_rate: number; target_amount: number }>(`
      select hm.default_share_rate::float8 as share_rate, rates.total_share_rate::float8, swmp.target_amount
      from household_members hm
      join shared_wallet_monthly_plans swmp on swmp.household_id = hm.household_id and swmp.year = $3 and swmp.month = $4
      cross join lateral (select coalesce(sum(default_share_rate), 0) as total_share_rate from household_members where household_id = $1) rates
      where hm.household_id = $1 and hm.id = $2
    `, [household.id, parsed.data.memberId, parsed.data.year, parsed.data.month])).rows[0];
    if (!member) { await client.query("rollback"); return NextResponse.json({ error: "shared_wallet_member_plan_not_found" }, { status: 404 }); }
    const plannedAmount = Math.round(member.target_amount * (member.total_share_rate > 0 ? member.share_rate / member.total_share_rate : 0));
    await client.query(`
      insert into shared_wallet_member_plans (household_id, member_id, year, month, planned_amount, status, completed_at)
      values ($1, $2, $3, $4, $5, $6, case when $6 = 'completed' then now() else null end)
      on conflict (household_id, member_id, year, month) do update
      set status = excluded.status, completed_at = excluded.completed_at, updated_at = now()
    `, [household.id, parsed.data.memberId, parsed.data.year, parsed.data.month, plannedAmount, parsed.data.status]);
    await client.query("commit");
    console.info("Updated shared wallet member plan status", parsed.data);
    return NextResponse.json({ memberId: parsed.data.memberId, status: parsed.data.status });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to update shared wallet member plan status", error);
    return NextResponse.json({ error: "shared_wallet_member_plan_failed" }, { status: 500 });
  } finally { client.release(); }
}
''')
PY
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:100]!r}')
    p.write_text(text.replace(old, new, 1))

replace('src/components/kakeibo-dashboard.tsx',
'''  contributions: { member_name: string; amount: number; share_rate: number; proposed_amount: number | null; remaining_amount: number | null }[];
''',
'''  contributions: { member_id: string; member_name: string; amount: number; share_rate: number; proposed_amount: number | null; planned_amount: number | null; remaining_amount: number | null; status: "pending" | "completed" }[];
''')
replace('src/components/kakeibo-dashboard.tsx',
'''  const createBudgetGoal = async () => {
''',
'''  const updateSharedWalletMemberStatus = async (memberId: string, status: "pending" | "completed") => {
    setSharedWalletError(null);
    try {
      const response = await fetch("/api/shared-wallet-contributions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...selectedPeriod, memberId, status }) });
      if (!response.ok) throw new Error("shared_wallet_member_plan_failed");
      const refreshed = await fetch(`/api/shared-wallet-contributions?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);
      if (!refreshed.ok) throw new Error("shared_wallet_refresh_failed");
      setSharedWalletContributions(await refreshed.json() as SharedWalletContributions);
    } catch { setSharedWalletError("共通口座への入金状態を更新できませんでした。"); }
  };

  const createBudgetGoal = async () => {
''')
replace('src/components/kakeibo-dashboard.tsx',
'''                <div><span>{contribution.member_name}</span>{contribution.proposed_amount != null ? <small>提案 {formatMoney(contribution.proposed_amount)} ・ あと {formatMoney(contribution.remaining_amount ?? 0)}</small> : null}</div>
                <strong>{formatMoney(contribution.amount)}</strong>
''',
'''                <div><span>{contribution.member_name}</span>{contribution.planned_amount != null ? <small>予定 {formatMoney(contribution.planned_amount)} ・ あと {formatMoney(contribution.remaining_amount ?? 0)}</small> : null}</div>
                <div className="shared-wallet-item__status"><strong>{formatMoney(contribution.amount)}</strong><button className={contribution.status === "completed" ? "chip chip--active" : "chip"} type="button" onClick={() => void updateSharedWalletMemberStatus(contribution.member_id, contribution.status === "completed" ? "pending" : "completed")}>{contribution.status === "completed" ? "入金済み" : "入金待ち"}</button></div>
''')
replace('src/app/globals.css',
'''.shared-wallet-settings__grid {
''',
'''.shared-wallet-item__status {
  justify-items: end;
}

.shared-wallet-settings__grid {
''')

replace('Docs/product-spec.md',
'''- 既存の負担率に応じた入金提案額と不足額を表示する
- 収入額は共有画面へ表示せず、入金額と比率から算出した提案額だけを表示する
- 給料日通知、収入登録の更新、入金予定と完了状態の操作は未実装
''',
'''- 既存の負担率に応じた入金提案額と不足額を表示する
- メンバーごとの入金予定額と `入金待ち` / `入金済み` を表示し、分析画面から状態を切り替える
- 収入額は共有画面へ表示せず、入金額と比率から算出した提案額だけを表示する
- 給料日通知、収入登録の更新、メンバー別予定額の手動調整は未実装
''')
replace('Docs/product-spec.md',
'''- 共通口座への入金状況と提案額は表示できるが、給料日通知と入金操作は未実装
''',
'''- 共通口座への入金状況、提案額、入金完了状態は管理できるが、給料日通知は未実装
''')
replace('Docs/data-model.md',
'''## shared_wallet_contributions

共通財布・共通カードへの入金を管理する。当月合計とメンバー別の入金額表示に使用する。
''',
'''## shared_wallet_member_plans

月・メンバー単位の共通口座への入金予定額と完了状態。実際の入金履歴とは分けて管理する。

```txt
id
household_id
member_id
year
month
planned_amount
status
completed_at
created_at
updated_at
```

## shared_wallet_contributions

共通財布・共通カードへの入金を管理する。当月合計とメンバー別の入金額表示に使用する。
''')
replace('Docs/data-model.md',
'''- `202606020006_add_shared_wallet_monthly_plans.sql`: 共通口座への月次入金目標
''',
'''- `202606020006_add_shared_wallet_monthly_plans.sql`: 共通口座への月次入金目標
- `202606020007_add_shared_wallet_member_plans.sql`: メンバー別の共通口座入金予定と完了状態
''')
replace('Docs/screens.md',
'''- 共通口座への入金: 当月目標、当月合計、メンバー別の入金額、提案額、不足額、収入額を非表示にする注記
''',
'''- 共通口座への入金: 当月目標、当月合計、メンバー別の入金額、予定額、不足額、入金完了状態、収入額を非表示にする注記
''')
replace('Docs/api-security-spec.md',
'''指定月の共通口座への入金目標、入金合計、メンバー別入金額、負担率から算出した提案額と不足額を返す。収入額は返さない。
''',
'''指定月の共通口座への入金目標、入金合計、メンバー別入金額、負担率から算出した提案額、予定額、不足額、入金完了状態を返す。収入額は返さない。
''')
replace('Docs/api-security-spec.md',
'''### 5.14. 精算取り消し (`POST /api/settlements/reopen`)
''',
'''### 5.14. 共通口座へのメンバー別入金状態 (`PATCH /api/shared-wallet-contributions`)

指定月・メンバーの入金状態を `pending` または `completed` に更新する。初回更新時は負担率から算出した提案額を予定額として保存する。

### 5.15. 精算取り消し (`POST /api/settlements/reopen`)
''')

replace('tests/integration/api.integration.test.ts',
'''    await pool.query("update shared_wallet_monthly_plans set target_amount = 100000 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
''',
'''    await pool.query("update shared_wallet_monthly_plans set target_amount = 100000 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
    await pool.query("update shared_wallet_member_plans set status = 'pending', completed_at = null where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
''')
replace('tests/integration/api.integration.test.ts',
'''    const { GET, POST } = await import("../../src/app/api/shared-wallet-contributions/route.ts");
''',
'''    const { GET, PATCH, POST } = await import("../../src/app/api/shared-wallet-contributions/route.ts");
''')
replace('tests/integration/api.integration.test.ts',
'''        { member_name: "葵", amount: 30000, share_rate: 0.55, proposed_amount: 55000, remaining_amount: 25000 },
        { member_name: "蓮", amount: 25000, share_rate: 0.45, proposed_amount: 45000, remaining_amount: 20000 },
''',
'''        { member_id: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", member_name: "葵", amount: 30000, share_rate: 0.55, proposed_amount: 55000, planned_amount: 55000, remaining_amount: 25000, status: "pending" },
        { member_id: "aaaaaaaa-2222-4222-8222-aaaaaaaa2222", member_name: "蓮", amount: 25000, share_rate: 0.45, proposed_amount: 45000, planned_amount: 45000, remaining_amount: 20000, status: "pending" },
''')
replace('tests/integration/api.integration.test.ts',
'''    assert.deepEqual(await updated.json(), { targetAmount: 120000 });

    const invalid = await GET''',
'''    assert.deepEqual(await updated.json(), { targetAmount: 120000 });

    const completed = await PATCH(new Request("http://localhost/api/shared-wallet-contributions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ year: now.getFullYear(), month: now.getMonth() + 1, memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", status: "completed" }) }));
    assert.equal(completed.status, 200);
    assert.deepEqual(await completed.json(), { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", status: "completed" });

    const invalid = await GET''')
replace('tests/e2e/kakeibo.spec.ts',
'''  await expect(panel).toContainText("提案 ¥55,000");
  await expect(panel).toContainText("提案 ¥45,000");
''',
'''  await expect(panel).toContainText("予定 ¥55,000");
  await expect(panel).toContainText("予定 ¥45,000");
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/shared-wallet-contributions") && response.request().method() === "PATCH" && response.ok()),
    panel.getByRole("button", { name: "入金待ち", exact: true }).first().click(),
  ]);
  await expect(panel.getByRole("button", { name: "入金済み", exact: true })).toBeVisible();
''')
PY
git diff --check && sed -n '/[[:blank:]]$/=' postgres/migrations/011_add_shared_wallet_member_plans.sql supabase/migrations/202606020007_add_shared_wallet_member_plans.sql
sed -n '245,285p' Docs/api-security-spec.md && sed -n '1,240p' src/app/api/shared-wallet-contributions/route.ts && sed -n '1035,1075p' src/components/kakeibo-dashboard.tsx
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '分析', exact: true }).click();
  const panel = page.getByLabel('共通口座への入金');
  await panel.waitFor({ state: 'visible' });
  let text = await panel.innerText();
  for (const expected of ['予定 ¥55,000', '予定 ¥45,000', '入金待ち']) if (!text.includes(expected)) throw new Error(`missing ${expected}: ${text}`);
  const firstPending = panel.getByRole('button', { name: '入金待ち', exact: true }).first();
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/shared-wallet-contributions') && r.request().method() === 'PATCH' && r.ok()),
    firstPending.click(),
  ]);
  await panel.getByRole('button', { name: '入金済み', exact: true }).waitFor();
  const completed = panel.getByRole('button', { name: '入金済み', exact: true });
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/shared-wallet-contributions') && r.request().method() === 'PATCH' && r.ok()),
    completed.click(),
  ]);
  await panel.getByRole('button', { name: '入金待ち', exact: true }).first().waitFor();
  console.log(await panel.innerText());
  await browser.close();
})();
JS
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '分析', exact: true }).click();
  const panel = page.getByLabel('共通口座への入金');
  await panel.getByText('予定 ¥55,000', { exact: false }).waitFor({ timeout: 30000 });
  let text = await panel.innerText();
  for (const expected of ['予定 ¥55,000', '予定 ¥45,000', '入金待ち']) if (!text.includes(expected)) throw new Error(`missing ${expected}: ${text}`);
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/shared-wallet-contributions') && r.request().method() === 'PATCH' && r.ok()),
    panel.getByRole('button', { name: '入金待ち', exact: true }).first().click(),
  ]);
  await panel.getByRole('button', { name: '入金済み', exact: true }).waitFor();
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/shared-wallet-contributions') && r.request().method() === 'PATCH' && r.ok()),
    panel.getByRole('button', { name: '入金済み', exact: true }).click(),
  ]);
  await panel.getByRole('button', { name: '入金待ち', exact: true }).first().waitFor();
  console.log(await panel.innerText());
  await browser.close();
})();
JS
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select hm.display_name || ':' || swmp.status || ':' || swmp.planned_amount from shared_wallet_member_plans swmp join household_members hm on hm.id = swmp.member_id where swmp.household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and swmp.year = extract(year from current_date)::int and swmp.month = extract(month from current_date)::int order by hm.role asc;"
git diff --check && git status --short --branch && git diff --stat
git status --short --branch && git diff --check && git diff --stat
git diff -- src/app/globals.css && printf '\n-- DESIGN --\n' && sed -n '1,120p' DESIGN.md
ps -fp 3043186 && tr '\0' ' ' < /proc/3043186/cmdline && printf '\n'
git diff --name-only && git ls-files --others --exclude-standard
readlink -f /proc/3043186/cwd && ps -fp 3043068
kill 3043068 3043186
git add Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/011_add_shared_wallet_member_plans.sql supabase/migrations/202606020007_add_shared_wallet_member_plans.sql src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git apply --cached <<'PATCH'
diff --git a/src/app/globals.css b/src/app/globals.css
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -1211,6 +1211,10 @@ button {
   font-size: 11px;
 }
 
+.shared-wallet-item__status {
+  justify-items: end;
+}
+
 .shared-wallet-settings__grid {
   display: grid;
   gap: 8px;
PATCH
git diff --cached --check && git diff --cached --stat && printf '\n-- staged --\n' && git diff --cached --name-only && printf '\n-- remaining --\n' && git status --short
git commit -m "feat: 共通口座の入金状態を管理する" -m "- メンバー別の入金予定額と完了状態を保存するmigrationを追加
- 分析画面から入金待ちと入金済みを切り替えられるようにする
- API、仕様書、integration・E2Eテストを更新" && git status --short --branch && git log --oneline --max-count=4
sed -n '1,220p' src/app/api/shared-wallet-contributions/route.ts && sed -n '1350,1405p' src/components/kakeibo-dashboard.tsx && sed -n '60,105p' src/components/kakeibo-dashboard.tsx && sed -n '330,390p' src/components/kakeibo-dashboard.tsx
sed -n '145,180p' Docs/product-spec.md && sed -n '250,285p' Docs/api-security-spec.md && sed -n '190,235p' tests/integration/api.integration.test.ts && sed -n '130,170p' tests/e2e/kakeibo.spec.ts
grep -n "sharedWalletPlanDraft\|useState" src/components/kakeibo-dashboard.tsx | head -n 45
sed -n '190,210p' src/components/kakeibo-dashboard.tsx
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:120]!r}')
    p.write_text(text.replace(old, new, 1))

path = 'src/app/api/shared-wallet-contributions/route.ts'
replace(path,
'''const memberPlanSchema = z.object({ memberId: z.string().uuid(), year: z.number().int().min(2000).max(9999), month: z.number().int().min(1).max(12), status: z.enum(["pending", "completed"]) });
''',
'''const memberPlanSchema = z.object({ memberId: z.string().uuid(), year: z.number().int().min(2000).max(9999), month: z.number().int().min(1).max(12), status: z.enum(["pending", "completed"]).optional(), plannedAmount: z.number().int().positive().optional() }).refine((value) => value.status != null || value.plannedAmount != null);
''')
replace(path,
'''    const member = (await client.query<{ share_rate: number; total_share_rate: number; target_amount: number }>(`
      select hm.default_share_rate::float8 as share_rate, rates.total_share_rate::float8, swmp.target_amount
      from household_members hm
      join shared_wallet_monthly_plans swmp on swmp.household_id = hm.household_id and swmp.year = $3 and swmp.month = $4
      cross join lateral (select coalesce(sum(default_share_rate), 0) as total_share_rate from household_members where household_id = $1) rates
      where hm.household_id = $1 and hm.id = $2
    `, [household.id, parsed.data.memberId, parsed.data.year, parsed.data.month])).rows[0];
    if (!member) { await client.query("rollback"); return NextResponse.json({ error: "shared_wallet_member_plan_not_found" }, { status: 404 }); }
    const plannedAmount = Math.round(member.target_amount * (member.total_share_rate > 0 ? member.share_rate / member.total_share_rate : 0));
    await client.query(`
      insert into shared_wallet_member_plans (household_id, member_id, year, month, planned_amount, status, completed_at)
      values ($1, $2, $3, $4, $5, $6, case when $6 = 'completed' then now() else null end)
      on conflict (household_id, member_id, year, month) do update
      set status = excluded.status, completed_at = excluded.completed_at, updated_at = now()
    `, [household.id, parsed.data.memberId, parsed.data.year, parsed.data.month, plannedAmount, parsed.data.status]);
    await client.query("commit");
    console.info("Updated shared wallet member plan status", parsed.data);
    return NextResponse.json({ memberId: parsed.data.memberId, status: parsed.data.status });
''',
'''    const member = (await client.query<{ share_rate: number; total_share_rate: number; target_amount: number; planned_amount: number | null; status: "pending" | "completed" | null }>(`
      select hm.default_share_rate::float8 as share_rate, rates.total_share_rate::float8, monthly_plan.target_amount, member_plan.planned_amount, member_plan.status
      from household_members hm
      join shared_wallet_monthly_plans monthly_plan on monthly_plan.household_id = hm.household_id and monthly_plan.year = $3 and monthly_plan.month = $4
      cross join lateral (select coalesce(sum(default_share_rate), 0) as total_share_rate from household_members where household_id = $1) rates
      left join shared_wallet_member_plans member_plan on member_plan.household_id = hm.household_id and member_plan.member_id = hm.id and member_plan.year = $3 and member_plan.month = $4
      where hm.household_id = $1 and hm.id = $2
    `, [household.id, parsed.data.memberId, parsed.data.year, parsed.data.month])).rows[0];
    if (!member) { await client.query("rollback"); return NextResponse.json({ error: "shared_wallet_member_plan_not_found" }, { status: 404 }); }
    const proposedAmount = Math.round(member.target_amount * (member.total_share_rate > 0 ? member.share_rate / member.total_share_rate : 0));
    const plannedAmount = parsed.data.plannedAmount ?? member.planned_amount ?? proposedAmount;
    const status = parsed.data.status ?? member.status ?? "pending";
    await client.query(`
      insert into shared_wallet_member_plans (household_id, member_id, year, month, planned_amount, status, completed_at)
      values ($1, $2, $3, $4, $5, $6, case when $6 = 'completed' then now() else null end)
      on conflict (household_id, member_id, year, month) do update
      set planned_amount = excluded.planned_amount, status = excluded.status, completed_at = case when excluded.status = 'completed' then coalesce(shared_wallet_member_plans.completed_at, now()) else null end, updated_at = now()
    `, [household.id, parsed.data.memberId, parsed.data.year, parsed.data.month, plannedAmount, status]);
    await client.query("commit");
    console.info("Updated shared wallet member plan", parsed.data);
    return NextResponse.json({ memberId: parsed.data.memberId, plannedAmount, status });
''')

path = 'src/components/kakeibo-dashboard.tsx'
replace(path,
'''  const [sharedWalletPlanDraft, setSharedWalletPlanDraft] = useState("");
''',
'''  const [sharedWalletPlanDraft, setSharedWalletPlanDraft] = useState("");
  const [sharedWalletMemberPlanDrafts, setSharedWalletMemberPlanDrafts] = useState<Record<string, string>>({});
''')
replace(path,
'''  const createBudgetGoal = async () => {
''',
'''  const updateSharedWalletMemberPlan = async (memberId: string) => {
    const plannedAmount = Number(sharedWalletMemberPlanDrafts[memberId]);
    if (!Number.isInteger(plannedAmount) || plannedAmount <= 0) { setSharedWalletError("共通口座の予定額を確認してください。"); return; }
    setSharedWalletError(null);
    try {
      const response = await fetch("/api/shared-wallet-contributions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...selectedPeriod, memberId, plannedAmount }) });
      if (!response.ok) throw new Error("shared_wallet_member_plan_failed");
      setSharedWalletMemberPlanDrafts((current) => ({ ...current, [memberId]: "" }));
      const refreshed = await fetch(`/api/shared-wallet-contributions?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);
      if (!refreshed.ok) throw new Error("shared_wallet_refresh_failed");
      setSharedWalletContributions(await refreshed.json() as SharedWalletContributions);
    } catch { setSharedWalletError("共通口座の予定額を更新できませんでした。"); }
  };

  const createBudgetGoal = async () => {
''')
replace(path,
'''            <button className="chip chip--active" type="button" onClick={() => void saveSharedWalletPlan()}>目標額を更新</button>
''',
'''            <button className="chip chip--active" type="button" onClick={() => void saveSharedWalletPlan()}>目標額を更新</button>
            {sharedWalletContributions?.contributions.map((contribution) => <div key={contribution.member_id}><input aria-label={`${contribution.member_name}の入金予定額`} inputMode="numeric" placeholder={`${contribution.member_name} 現在 ${contribution.planned_amount ?? 0}`} value={sharedWalletMemberPlanDrafts[contribution.member_id] ?? ""} onChange={(event) => setSharedWalletMemberPlanDrafts((current) => ({ ...current, [contribution.member_id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateSharedWalletMemberPlan(contribution.member_id)}>{contribution.member_name}の予定額を更新</button></div>)}
''')

replace('Docs/product-spec.md',
'''- メンバーごとの入金予定額と `入金待ち` / `入金済み` を表示し、分析画面から状態を切り替える
''',
'''- メンバーごとの入金予定額と `入金待ち` / `入金済み` を表示し、分析画面から状態を切り替える
- 設定画面から選択月のメンバー別予定額を手動調整する
''')
replace('Docs/product-spec.md',
'''- 給料日通知、収入登録の更新、メンバー別予定額の手動調整は未実装
''',
'''- 給料日通知、収入登録の更新は未実装
''')
replace('Docs/screens.md',
'''- 共通口座の入金目標: 選択月の目標額
''',
'''- 共通口座の入金目標: 選択月の目標額、メンバー別予定額
''')
replace('Docs/api-security-spec.md',
'''指定月・メンバーの入金状態を `pending` または `completed` に更新する。初回更新時は負担率から算出した提案額を予定額として保存する。
''',
'''指定月・メンバーの入金状態を `pending` または `completed` に更新する。`plannedAmount` を指定した場合は予定額も更新する。状態切替時は手動調整済みの予定額を保持し、未登録の場合だけ負担率から算出した提案額を保存する。
''')

path = 'tests/integration/api.integration.test.ts'
replace(path,
'''    const completed = await PATCH(new Request("http://localhost/api/shared-wallet-contributions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ year: now.getFullYear(), month: now.getMonth() + 1, memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", status: "completed" }) }));
    assert.equal(completed.status, 200);
    assert.deepEqual(await completed.json(), { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", status: "completed" });
''',
'''    const adjusted = await PATCH(new Request("http://localhost/api/shared-wallet-contributions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ year: now.getFullYear(), month: now.getMonth() + 1, memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", plannedAmount: 60000 }) }));
    assert.equal(adjusted.status, 200);
    assert.deepEqual(await adjusted.json(), { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", plannedAmount: 60000, status: "pending" });

    const completed = await PATCH(new Request("http://localhost/api/shared-wallet-contributions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ year: now.getFullYear(), month: now.getMonth() + 1, memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", status: "completed" }) }));
    assert.equal(completed.status, 200);
    assert.deepEqual(await completed.json(), { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", plannedAmount: 60000, status: "completed" });
''')
replace('tests/e2e/kakeibo.spec.ts',
'''  await page.getByRole("button", { name: "分析", exact: true }).click();
  const panel = page.getByLabel("共通口座への入金");
''',
'''  await page.getByRole("button", { name: "設定", exact: true }).click();
  await page.getByLabel("葵の入金予定額").fill("60000");
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/shared-wallet-contributions") && response.request().method() === "PATCH" && response.ok()),
    page.getByRole("button", { name: "葵の予定額を更新", exact: true }).click(),
  ]);
  await page.getByRole("button", { name: "分析", exact: true }).click();
  const panel = page.getByLabel("共通口座への入金");
''')
replace('tests/e2e/kakeibo.spec.ts',
'''  await expect(panel).toContainText("予定 ¥55,000");
''',
'''  await expect(panel).toContainText("予定 ¥60,000");
''')
PY
python3 - <<'PY'
from pathlib import Path
p = Path('tests/integration/api.integration.test.ts')
text = p.read_text()
old = '''    await pool.query("update shared_wallet_member_plans set status = 'pending', completed_at = null where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
'''
new = '''    await pool.query("update shared_wallet_member_plans set planned_amount = case member_id when 'aaaaaaaa-1111-4111-8111-aaaaaaaa1111' then 55000 when 'aaaaaaaa-2222-4222-8222-aaaaaaaa2222' then 45000 else planned_amount end, status = 'pending', completed_at = null where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
'''
if old not in text: raise SystemExit('missing cleanup pattern')
p.write_text(text.replace(old, new, 1))
PY
git diff --check && git status --short --branch && git diff --stat -- Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
sed -n '85,165p' src/app/api/shared-wallet-contributions/route.ts && sed -n '360,430p' src/components/kakeibo-dashboard.tsx && sed -n '1365,1405p' src/components/kakeibo-dashboard.tsx
sed -n '125,180p' tests/e2e/kakeibo.spec.ts && sed -n '200,240p' tests/integration/api.integration.test.ts
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '設定', exact: true }).click();
  const input = page.getByLabel('葵の入金予定額');
  await input.waitFor({ timeout: 30000 });
  await input.fill('60000');
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/shared-wallet-contributions') && r.request().method() === 'PATCH' && r.ok()),
    page.getByRole('button', { name: '葵の予定額を更新', exact: true }).click(),
  ]);
  await page.getByRole('button', { name: '分析', exact: true }).click();
  const panel = page.getByLabel('共通口座への入金');
  await panel.getByText('予定 ¥60,000', { exact: false }).waitFor({ timeout: 30000 });
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/shared-wallet-contributions') && r.request().method() === 'PATCH' && r.ok()),
    panel.getByRole('button', { name: '入金待ち', exact: true }).first().click(),
  ]);
  await panel.getByRole('button', { name: '入金済み', exact: true }).waitFor();
  const text = await panel.innerText();
  if (!text.includes('予定 ¥60,000')) throw new Error(`adjustment not preserved: ${text}`);
  console.log(text);
  await browser.close();
})();
JS
git diff --check && git status --short --branch && git diff --stat -- Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git status --short --branch && printf '\n-- diff check --\n' && git diff --check && printf '\n-- unstaged names --\n' && git diff --name-only && printf '\n-- untracked --\n' && git ls-files --others --exclude-standard
git diff -- Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts
git diff -- src/app/globals.css && printf '\n-- DESIGN --\n' && sed -n '1,260p' DESIGN.md
git add Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md src/app/api/shared-wallet-contributions/route.ts src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts tests/integration/api.integration.test.ts && git diff --cached --check && git diff --cached --stat && printf '\n-- staged --\n' && git diff --cached --name-only && printf '\n-- remaining --\n' && git status --short
git commit -m "feat: 共通口座の入金予定額を調整できるようにする" -m "- 設定画面からメンバー別の月次入金予定額を更新可能にする
- 入金状態の切替時に手動調整済みの予定額を保持する
- API仕様、integration・E2Eテストを更新" && git status --short --branch && git log --oneline --max-count=4
grep -o 'var(--[a-zA-Z0-9_-]*' src/app/globals.css | sed 's/var(//' | sort -u && printf '\n-- definitions --\n' && grep -o -- '--[a-zA-Z0-9_-]*:' src/app/globals.css | sed 's/:$//' | sort -u
grep -RInE 'linear-gradient|radial-gradient|backdrop-filter:[[:space:]]*blur|box-shadow:[[:space:]]*0[[:space:]]+[4-9][0-9]*px|border-radius:[[:space:]]*(1[0-9]|[2-9][0-9])px' src/app/globals.css DESIGN.md || true
sed -n '/[[:blank:]]$/=' DESIGN.md && printf '\n-- css diff stat --\n' && git diff --stat -- src/app/globals.css && printf '\n-- css diff check --\n' && git diff --check -- src/app/globals.css
grep -nE 'var\(--(danger|review|primary-ghost)\)|border-radius:[[:space:]]*10px|linear-gradient' src/app/globals.css
sed -n '430,465p' src/app/globals.css && sed -n '995,1020p' src/app/globals.css && grep -nE 'var\(--(danger|review|primary-ghost)\)' src/app/globals.css
python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
text = p.read_text()
replacements = {
  '  border-radius: 10px;\n  background: var(--primary-ghost);': '  border-radius: var(--radius);\n  background: var(--hover);',
  '  color: var(--danger);': '  color: var(--danger-text);',
  '  background: linear-gradient(135deg, var(--primary-ghost), #fff);': '  background: var(--surface);',
  '  background: var(--review);': '  background: var(--review-text);',
}
for old, new in replacements.items():
    if old not in text:
        raise SystemExit(f'missing CSS pattern: {old!r}')
    text = text.replace(old, new, 1)
p.write_text(text)

p = Path('DESIGN.md')
text = p.read_text()
p.write_text('\n'.join(line.rstrip() for line in text.splitlines()) + '\n')
PY
printf '%s\n' '-- forbidden patterns --'
grep -nE 'var\(--(danger|review|primary-ghost)\)|linear-gradient|radial-gradient|backdrop-filter:[[:space:]]*blur|border-radius:[[:space:]]*(1[0-9]|[2-9][0-9])px' src/app/globals.css || true
printf '\n%s\n' '-- trailing whitespace --'
sed -n '/[[:blank:]]$/=' DESIGN.md src/app/globals.css
printf '\n%s\n' '-- diff check --'
git diff --check
grep -nE 'background(-color)?:[^;]*(rgba|hsla)|backdrop-filter|box-shadow:[^;]+' src/app/globals.css || true
grep -o 'var(--[a-zA-Z0-9_-]*' src/app/globals.css | sed 's/var(//' | sort -u > /tmp/kakeibo-css-refs && grep -o -- '--[a-zA-Z0-9_-]*:' src/app/globals.css | sed 's/:$//' | sort -u > /tmp/kakeibo-css-defs && comm -23 /tmp/kakeibo-css-refs /tmp/kakeibo-css-defs
git diff --stat -- src/app/globals.css DESIGN.md && printf '\n-- status --\n' && git status --short --branch
sed -n '100,145p' src/app/globals.css && sed -n '725,750p' src/app/globals.css && sed -n '995,1015p' src/app/globals.css && sed -n '1180,1200p' src/app/globals.css
grep -n 'border-radius:' src/app/globals.css
sed -n '640,735p' src/app/globals.css && sed -n '1158,1172p' src/app/globals.css && sed -n '1230,1245p' src/app/globals.css
python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
text = p.read_text()
text = text.replace('  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.74);', '  border-radius: var(--radius);\n  background: var(--surface);', 1)
text = text.replace('  border-radius: 999px;\n  background: var(--primary);', '  border-radius: var(--radius);\n  background: var(--primary);', 1)
text = text.replace('  backdrop-filter: none;\n', '')
text = text.replace('  background: rgba(255, 255, 255, 0.64);', '  background: var(--surface);')
text = text.replace('  background: rgba(255, 255, 255, 0.68);', '  background: var(--surface);')
text = text.replace('  border-radius: 8px;\n  background: #fff;', '  border-radius: var(--radius);\n  background: #fff;')
p.write_text(text)
PY
printf '%s\n' '-- forbidden --'
grep -nE 'background(-color)?:[^;]*(rgba|hsla)|linear-gradient|radial-gradient|backdrop-filter|border-radius:[[:space:]]*(999|[89]|1[0-9]|[2-9][0-9])px' src/app/globals.css || true
printf '\n%s\n' '-- undefined vars --'
grep -o 'var(--[a-zA-Z0-9_-]*' src/app/globals.css | sed 's/var(//' | sort -u > /tmp/kakeibo-css-refs
grep -o -- '--[a-zA-Z0-9_-]*:' src/app/globals.css | sed 's/:$//' | sort -u > /tmp/kakeibo-css-defs
comm -23 /tmp/kakeibo-css-refs /tmp/kakeibo-css-defs
printf '\n%s\n' '-- diff check --'
git diff --check
git diff --stat -- src/app/globals.css DESIGN.md && printf '\n-- status --\n' && git status --short --branch && printf '\n-- remaining rgba --\n' && grep -n 'rgba' src/app/globals.css || true
python3 - <<'PY'
from pathlib import Path
p = Path('src/app/globals.css')
text = p.read_text()
text = text.replace('  border: 1px solid rgba(15, 118, 110, 0.18);', '  border: 1px solid var(--border);')
text = text.replace('  border-color: rgba(15, 118, 110, 0.24);', '  border-color: var(--border);')
p.write_text(text)
PY
git diff --check
printf '\n-- remaining rgba --\n'
grep -n 'rgba' src/app/globals.css || true
printf '\n-- forbidden --\n'
grep -nE 'background(-color)?:[^;]*(rgba|hsla)|linear-gradient|radial-gradient|backdrop-filter|border-radius:[[:space:]]*(999|[89]|1[0-9]|[2-9][0-9])px' src/app/globals.css || true
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage({ viewport: { width: 1280, height: 900 } });
  const pageErrors = [];
  page.on('pageerror', error => pageErrors.push(error.message));
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByText('今月の流れ', { exact: true }).waitFor({ timeout: 30000 });
  for (const tab of ['分析', '精算', '履歴', '設定', 'ホーム']) {
    await page.getByRole('button', { name: tab, exact: true }).click();
  }
  const styles = await page.evaluate(() => {
    const css = [...document.styleSheets].flatMap(sheet => {
      try { return [...sheet.cssRules].map(rule => rule.cssText); } catch { return []; }
    }).join('\n');
    const body = getComputedStyle(document.body);
    return {
      background: body.backgroundColor,
      gradients: /linear-gradient|radial-gradient/.test(css),
      blur: /backdrop-filter:\s*blur/.test(css),
      errors: [],
    };
  });
  if (styles.gradients || styles.blur || pageErrors.length) throw new Error(JSON.stringify({ styles, pageErrors }));
  console.log(JSON.stringify({ ...styles, pageErrors }));
  await browser.close();
})();
JS
git add src/app/globals.css DESIGN.md && git diff --cached --check && git diff --cached --stat && printf '\n-- staged --\n' && git diff --cached --name-only && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true
git commit -m "chore: AI感を抑えたフラットデザインへ整える" -m "- Notion風の静かなフラットUI方針をDESIGN.mdに追加
- グラデーション、半透明、ぼかし、大きな影を除去
- ソリッド背景、小さい角丸、境界線中心のデザイントークンへ統一" && git status --short --branch && git log --oneline --max-count=5
git status --short --branch && git switch -c codex/design-polish && git status --short --branch
sed -n '1,260p' DESIGN.md && printf '\n-- CSS rules --\n' && grep -nE 'border-radius|background:|box-shadow|backdrop-filter|linear-gradient|radial-gradient|rgba|font-family|transition|:hover' src/app/globals.css
sed -n '1,260p' src/app/globals.css && sed -n '260,620p' src/app/globals.css && sed -n '620,980p' src/app/globals.css && sed -n '980,1280p' src/app/globals.css
grep -nE 'className=|style=|<button|<input|<select|<article|<section' src/components/kakeibo-dashboard.tsx | sed -n '1,280p'
find src -maxdepth 4 -type f | sort && printf '\n-- status --\n' && git status --short --branch
nl -ba src/app/globals.css | sed -n '230,275p;590,635p;910,950p;1,35p;300,335p;1160,1210p'
grep -RInE 'common-bg|common-text|personal-bg|personal-text|success-bg|success-text|shadow-subtle|chip--active|chip--review|chip--danger' src --exclude-dir=node_modules && printf '\n-- inline style --\n' && grep -RIn 'style={{' src/components src/app --exclude-dir=node_modules
nl -ba src/components/kakeibo-dashboard.tsx | sed -n '850,885p;1068,1082p;1390,1400p;1498,1520p' && printf '\n-- DESIGN lines --\n' && nl -ba DESIGN.md | sed -n '8,90p'
grep -nE '^\.[^{]+ \{|^\.[^{]+,$|font-size: 13px;|grid-template-columns: minmax\(0, 1\.1fr\)' src/app/globals.css | sed -n '1,260p'
nl -ba src/app/globals.css | sed -n '640,690p;970,990p' && printf '\n-- semantic token refs --\n' && for token in common-bg common-text personal-bg personal-text review-bg review-text success-bg success-text danger-bg danger-text shadow-subtle; do printf '%s ' "$token"; grep -c "var(--$token)" src/app/globals.css; done
nl -ba src/app/globals.css | sed -n '1218,1245p;1070,1100p;295,330p;510,535p' && printf '\n-- git status --\n' && git status --short --branch
sed -n '1,180p' src/app/layout.tsx && printf '\n-- font references --\n' && grep -RInE 'next/font|Inter|font-family|@font-face' src --exclude-dir=node_modules
git branch --show-current && git status --short --branch
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:140]!r}')
    p.write_text(text.replace(old, new, 1))

path = 'DESIGN.md'
replace(path,
'''### 🚨 厳禁ルール (Anti-AI-Aesthetics)
- **グラデーションの禁止**: `linear-gradient` や `radial-gradient` は背景・コンポーネントを問わず一切使用しない。
- **ぼかし・半透明（グラスモルフィズム）の禁止**: `backdrop-filter: blur(...)` は完全禁止。背景色は常に不透明なソリッドカラーとする。
- **大きな影（ドロップシャドウ）の禁止**: `box-shadow` は原則使用しない。領域の区切りはすべて「枠線（ボーダー）」で行う。
- **過剰な角丸の禁止**: ボタンやカードの角丸（Radius）を `8px` や `12px` にしてカジュアルにしすぎない。Notion に近い `4px` 〜 `6px` のシャープな角丸に統一する。
''',
'''### AI感の定義

このプロジェクトでいう「AI感」とは、生成UIで頻出する、意味より装飾が先行した画面表現を指します。個々の装飾が単独で悪いのではなく、情報の優先順位や操作理由を説明できないまま積み重なる状態を避けます。

以下のいずれかに当てはまる場合は、AI感があるものとして修正対象にします。

- **装飾過多**: グラデーション、ぼかし、半透明、大きな影、過剰な角丸を使って見栄えだけを足している。
- **意味のない強調**: 状態や重要度が異なる要素を、すべて同じアクセントカラーや塗りボタンで強調している。
- **カードの乱立**: まとまりを考えず、すべての情報を同じ白いカードと枠線で分割している。
- **余白の不整合**: 同じ階層の要素で余白、入力欄、ボタン配置が揃わず、視線の流れが途切れる。
- **状態の曖昧さ**: hover、focus-visible、disabled、完了、警告などの違いが見た目で判断できない。
- **目的のない英語・コピー**: 画面理解に不要な英語見出し、抽象的なキャッチコピー、説明過多な文言を足している。

### 厳禁ルール (Anti-AI-Aesthetics)
- **グラデーションの禁止**: `linear-gradient` や `radial-gradient` は背景・コンポーネントを問わず一切使用しない。
- **ぼかし・半透明（グラスモルフィズム）の禁止**: `backdrop-filter: blur(...)` は完全禁止。背景色は常に不透明なソリッドカラーとする。
- **大きな影（ドロップシャドウ）の禁止**: `box-shadow` は原則使用しない。領域の区切りはすべて「枠線（ボーダー）」で行う。
- **過剰な角丸の禁止**: ボタンやカードの角丸（Radius）を `8px` や `12px` にしてカジュアルにしすぎない。Notion に近い `4px` 〜 `6px` のシャープな角丸に統一する。

### レビュー時の判定チェックリスト

- その色、枠線、背景は情報の意味を説明できるか。
- 主操作だけが強調され、状態表示は淡いセマンティックカラーに留まっているか。
- 同じ階層の入力欄とボタンが、同じリズムで並んでいるか。
- カードを減らしても情報構造が崩れない箇所に、不要なカードを足していないか。
- キーボード操作時に `focus-visible` が確認できるか。
- hover、disabled、完了、警告を色だけに頼らず文言でも判断できるか。
''')

path = 'src/components/kakeibo-dashboard.tsx'
replace(path,
'''function scopeClass(scope: ExpenseScope, active: boolean) {
  if (active && scope === "review") return "chip chip--review chip--active";
  if (active) return "chip chip--active";
  if (scope === "review") return "chip chip--review";
  return "chip";
}
''',
'''function scopeClass(scope: ExpenseScope, active: boolean) {
  const scopeVariant = scope === "shared" ? "chip--common" : scope === "personal" ? "chip--personal" : "chip--review";
  return active ? `chip ${scopeVariant} chip--selected` : `chip ${scopeVariant}`;
}
''')
replace(path,
'''<button className={contribution.status === "completed" ? "chip chip--active" : "chip"} type="button" onClick={() => void updateSharedWalletMemberStatus(contribution.member_id, contribution.status === "completed" ? "pending" : "completed")}>{contribution.status === "completed" ? "入金済み" : "入金待ち"}</button>''',
'''<button className={contribution.status === "completed" ? "chip chip--success" : "chip"} type="button" onClick={() => void updateSharedWalletMemberStatus(contribution.member_id, contribution.status === "completed" ? "pending" : "completed")}>{contribution.status === "completed" ? "入金済み" : "入金待ち"}</button>''')
replace(path,
'''            {sharedWalletContributions?.contributions.map((contribution) => <div key={contribution.member_id}><input aria-label={`${contribution.member_name}の入金予定額`} inputMode="numeric" placeholder={`${contribution.member_name} 現在 ${contribution.planned_amount ?? 0}`} value={sharedWalletMemberPlanDrafts[contribution.member_id] ?? ""} onChange={(event) => setSharedWalletMemberPlanDrafts((current) => ({ ...current, [contribution.member_id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateSharedWalletMemberPlan(contribution.member_id)}>{contribution.member_name}の予定額を更新</button></div>)}
''',
'''            {sharedWalletContributions?.contributions.map((contribution) => <label key={contribution.member_id} className="shared-wallet-settings__member"><span>{contribution.member_name}の予定額</span><div><input aria-label={`${contribution.member_name}の入金予定額`} inputMode="numeric" placeholder={`現在 ${contribution.planned_amount ?? 0}`} value={sharedWalletMemberPlanDrafts[contribution.member_id] ?? ""} onChange={(event) => setSharedWalletMemberPlanDrafts((current) => ({ ...current, [contribution.member_id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateSharedWalletMemberPlan(contribution.member_id)}>更新</button></div></label>)}
''')
replace(path,
'''          <div className="eyebrow composer__payer" style={{ marginRight: 2 }}>
''',
'''          <div className="eyebrow composer__payer">
''')

path = 'src/app/globals.css'
replace(path,
'''.chip--active {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

.chip--review {
''',
'''.chip--active {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

.chip--common {
  background: var(--common-bg);
  border-color: var(--common-bg);
  color: var(--common-text);
}

.chip--personal {
  background: var(--personal-bg);
  border-color: var(--personal-bg);
  color: var(--personal-text);
}

.chip--review {
''')
replace(path,
'''.chip--review {
  background: var(--review-bg);
  border-color: var(--review-text);
  color: var(--review-text);
}
''',
'''.chip--review {
  background: var(--review-bg);
  border-color: var(--review-bg);
  color: var(--review-text);
}

.chip--success {
  background: var(--success-bg);
  border-color: var(--success-bg);
  color: var(--success-text);
}

.chip--selected {
  border-color: currentColor;
  font-weight: 700;
}
''')
replace(path,
'''button {
  border: 0;
  background: none;
  color: inherit;
}
''',
'''button {
  border: 0;
  background: none;
  color: inherit;
}

button,
input,
select,
textarea {
  transition: border-color 120ms ease, background-color 120ms ease, color 120ms ease;
}

button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
}
''')
replace(path,
'''.chip--review {
  background: var(--review-bg);
  border-color: var(--review-bg);
  color: var(--review-text);
}
''',
'''.chip--review {
  background: var(--review-bg);
  border-color: var(--review-bg);
  color: var(--review-text);
}

.chip:hover,
.period-switcher__button:hover,
.tabbar__button:hover {
  background: var(--hover);
}

.chip--active:hover {
  background: var(--primary-hover);
}
''')
replace(path,
'''.composer__row {
  display: flex;
  align-items: center;
  gap: 10px;
}
''',
'''.composer__row {
  display: flex;
  align-items: center;
  gap: 10px;
}

.composer__payer {
  margin-right: 2px;
}
''')
replace(path,
'''.shared-wallet-settings__grid input {
  min-height: 40px;
  min-width: 0;
  padding: 0 10px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: #fff;
}
''',
'''.shared-wallet-settings__grid input {
  min-height: 40px;
  min-width: 0;
  padding: 0 10px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: #fff;
}

.shared-wallet-settings__member {
  display: grid;
  gap: 4px;
  color: var(--muted);
  font-size: 12px;
  font-weight: 600;
}

.shared-wallet-settings__member div {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 8px;
}
''')
PY

git diff --check
printf '\n-- status --\n'
git status --short --branch
git diff --check && printf '\n-- forbidden patterns --\n' && grep -nE 'background(-color)?:[^;]*(rgba|hsla)|linear-gradient|radial-gradient|backdrop-filter|border-radius:[[:space:]]*(999|[89]|1[0-9]|[2-9][0-9])px' src/app/globals.css || true && printf '\n-- token refs --\n' && for token in common-bg common-text personal-bg personal-text review-bg review-text success-bg success-text danger-bg danger-text; do printf '%s ' "$token"; grep -c "var(--$token)" src/app/globals.css; done
git diff --stat && printf '\n-- DESIGN diff --\n' && git diff -- DESIGN.md && printf '\n-- component diff --\n' && git diff -- src/components/kakeibo-dashboard.tsx
grep -RInE 'chip--success|chip--selected|shared-wallet-settings__member|composer__payer|focus-visible' src DESIGN.md && printf '\n-- tests touching labels --\n' && grep -RInE '葵の予定額を更新|葵の入金予定額|入金済み|共通費|個人費' tests
python3 - <<'PY'
from pathlib import Path
p = Path('tests/e2e/kakeibo.spec.ts')
text = p.read_text()
old = '''    page.getByRole("button", { name: "葵の予定額を更新", exact: true }).click(),
'''
new = '''    page.getByText("葵の予定額", { exact: true }).locator("..").getByRole("button", { name: "更新", exact: true }).click(),
'''
if old not in text: raise SystemExit('missing e2e button pattern')
p.write_text(text.replace(old, new, 1))
PY

git diff --check
printf '\n-- status --\n'
git status --short --branch
printf '\n-- diff stat --\n'
git diff --stat
grep -nE 'background(-color)?:[^;]*(rgba|hsla)|linear-gradient|radial-gradient|backdrop-filter|border-radius:[[:space:]]*(999|[89]|1[0-9]|[2-9][0-9])px' src/app/globals.css || true && printf '\n-- undefined vars --\n' && grep -o 'var(--[a-zA-Z0-9_-]*' src/app/globals.css | sed 's/var(//' | sort -u > /tmp/kakeibo-css-refs && grep -o -- '--[a-zA-Z0-9_-]*:' src/app/globals.css | sed 's/:$//' | sort -u > /tmp/kakeibo-css-defs && comm -23 /tmp/kakeibo-css-refs /tmp/kakeibo-css-defs
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage({ viewport: { width: 1280, height: 900 } });
  const errors = [];
  page.on('pageerror', error => errors.push(error.message));
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByText('今月の流れ', { exact: true }).waitFor({ timeout: 30000 });
  await page.getByRole('button', { name: '精算', exact: true }).click();
  const shared = page.getByRole('button', { name: '共通費', exact: true }).first();
  const personal = page.getByRole('button', { name: '個人費', exact: true }).first();
  const sharedStyle = await shared.evaluate(el => ({ background: getComputedStyle(el).backgroundColor, color: getComputedStyle(el).color }));
  const personalStyle = await personal.evaluate(el => ({ background: getComputedStyle(el).backgroundColor, color: getComputedStyle(el).color }));
  await page.getByRole('button', { name: '設定', exact: true }).click();
  const label = page.getByText('葵の予定額', { exact: true });
  await label.waitFor();
  const rowColumns = await label.locator('..').locator('div').evaluate(el => getComputedStyle(el).gridTemplateColumns);
  const targetInput = page.getByLabel('共通口座の入金目標額');
  await targetInput.focus();
  const focus = await targetInput.evaluate(el => ({ outline: getComputedStyle(el).outlineStyle, width: getComputedStyle(el).outlineWidth }));
  const css = await page.evaluate(() => [...document.styleSheets].flatMap(sheet => { try { return [...sheet.cssRules].map(rule => rule.cssText); } catch { return []; } }).join('\n'));
  const forbidden = /linear-gradient|radial-gradient|backdrop-filter:\s*blur/.test(css);
  if (errors.length || forbidden || focus.outline === 'none') throw new Error(JSON.stringify({ errors, forbidden, focus }));
  console.log(JSON.stringify({ sharedStyle, personalStyle, rowColumns, focus, errors, forbidden }));
  await browser.close();
})();
JS
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage({ viewport: { width: 1280, height: 900 } });
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded' });
  await page.getByText('今月の流れ', { exact: true }).waitFor();
  await page.getByRole('button', { name: '精算', exact: true }).click();
  for (const name of ['共通費', '個人費', '要確認']) {
    const items = await page.getByRole('button', { name, exact: true }).evaluateAll(buttons => buttons.map(button => ({ className: button.className, background: getComputedStyle(button).backgroundColor, color: getComputedStyle(button).color })));
    console.log(name, JSON.stringify(items));
  }
  await browser.close();
})();
JS
python3 - <<'PY'
from pathlib import Path
p = Path('src/components/kakeibo-dashboard.tsx')
text = p.read_text()
replacements = {
  'scopeClass(expense.scope, expense.scope === "shared")': 'scopeClass("shared", expense.scope === "shared")',
  'scopeClass(expense.scope, expense.scope === "personal")': 'scopeClass("personal", expense.scope === "personal")',
  'scopeClass(expense.scope, expense.scope === "review")': 'scopeClass("review", expense.scope === "review")',
}
for old, new in replacements.items():
    if old not in text: raise SystemExit(f'missing pattern: {old}')
    text = text.replace(old, new, 1)
p.write_text(text)
PY

git diff --check
printf '\n-- relevant diff --\n'
git diff -- src/components/kakeibo-dashboard.tsx | sed -n '1,180p'
git diff --check && printf '\n-- forbidden patterns --\n' && grep -nE 'background(-color)?:[^;]*(rgba|hsla)|linear-gradient|radial-gradient|backdrop-filter|border-radius:[[:space:]]*(999|[89]|1[0-9]|[2-9][0-9])px' src/app/globals.css || true && printf '\n-- status --\n' && git status --short --branch
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage({ viewport: { width: 1280, height: 900 } });
  const errors = [];
  page.on('pageerror', error => errors.push(error.message));
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByText('今月の流れ', { exact: true }).waitFor({ timeout: 30000 });
  await page.getByRole('button', { name: '精算', exact: true }).click();
  const firstRow = page.locator('.timeline-item').first();
  const styleOf = async name => firstRow.getByRole('button', { name, exact: true }).evaluate(el => ({ className: el.className, background: getComputedStyle(el).backgroundColor, color: getComputedStyle(el).color }));
  const scopes = { shared: await styleOf('共通費'), personal: await styleOf('個人費'), review: await styleOf('要確認') };
  await page.getByRole('button', { name: '設定', exact: true }).click();
  const label = page.getByText('葵の予定額', { exact: true });
  await label.waitFor();
  const row = label.locator('..');
  const rowColumns = await row.locator('div').evaluate(el => getComputedStyle(el).gridTemplateColumns);
  const updateButton = row.getByRole('button', { name: '更新', exact: true });
  if (!await updateButton.isVisible()) throw new Error('member update button is not visible');
  const targetInput = page.getByLabel('共通口座の入金目標額');
  await targetInput.focus();
  const focus = await targetInput.evaluate(el => ({ outline: getComputedStyle(el).outlineStyle, width: getComputedStyle(el).outlineWidth }));
  const css = await page.evaluate(() => [...document.styleSheets].flatMap(sheet => { try { return [...sheet.cssRules].map(rule => rule.cssText); } catch { return []; } }).join('\n'));
  const forbidden = /linear-gradient|radial-gradient|backdrop-filter:\s*blur/.test(css);
  if (errors.length || forbidden || focus.outline === 'none') throw new Error(JSON.stringify({ errors, forbidden, focus }));
  console.log(JSON.stringify({ scopes, rowColumns, focus, errors, forbidden }));
  await browser.close();
})();
JS
git diff --check && git status --short --branch && printf '\n-- diff stat --\n' && git diff --stat && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true
grep -RInE '未実装|TODO|給料日|収入登録|通知|認証|招待|レシート|コメント|リマインド' Docs --exclude-dir=activity_logs | sed -n '1,220p'
git add DESIGN.md src/app/globals.css src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts && git diff --cached --check && git diff --cached --stat && git commit -m "refactor: AI感の判定基準に沿ってUIを整える" -m "- DESIGN.mdにAI感の定義とレビュー用チェックリストを追加
- 区分と完了状態を淡いセマンティックカラーで表示
- 設定フォーム、hover、focus-visibleを整理" && git status --short --branch && git log --oneline --max-count=5
git switch main && git status --short --branch && git log --oneline --max-count=6
sed -n '1,150p' postgres/migrations/001_initial_schema.sql && sed -n '1,150p' supabase/migrations/202605240001_initial_schema.sql && sed -n '1,180p' src/app/api/shared-wallet-contributions/route.ts
sed -n '180,340p' tests/integration/api.integration.test.ts && sed -n '1360,1460p' src/components/kakeibo-dashboard.tsx && sed -n '145,180p' Docs/product-spec.md && sed -n '250,285p' Docs/api-security-spec.md && sed -n '250,320p' Docs/data-model.md
ls postgres/migrations supabase/migrations && printf '\n-- status --\n' && git status --short --branch && printf '\n-- issue --\n' && gh issue view 13 --json number,title,body,state,url
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:140]!r}')
    p.write_text(text.replace(old, new, 1))

migration = '''alter table household_members
  add column if not exists payday_day integer;

alter table household_members
  drop constraint if exists household_members_payday_day_range;

alter table household_members
  add constraint household_members_payday_day_range
  check (payday_day is null or payday_day between 1 and 31);
'''
Path('postgres/migrations/012_add_household_member_payday.sql').write_text(migration)
Path('supabase/migrations/202606030001_add_household_member_payday.sql').write_text(migration)

replace('postgres/migrations/002_prepare_local_environments.sh',
'''shared_wallet_member_plans_migration_file="/docker-entrypoint-initdb.d/011_add_shared_wallet_member_plans.sql"
''',
'''shared_wallet_member_plans_migration_file="/docker-entrypoint-initdb.d/011_add_shared_wallet_member_plans.sql"
household_member_payday_migration_file="/docker-entrypoint-initdb.d/012_add_household_member_payday.sql"
''')
replace('postgres/migrations/002_prepare_local_environments.sh',
'''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$shared_wallet_member_plans_migration_file"
''',
'''  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$shared_wallet_member_plans_migration_file"
  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\
    -f "$household_member_payday_migration_file"
''')
replace('postgres/migrations/003_seed_local_data.sql',
'''insert into household_members (id, household_id, user_id, display_name, role, income_amount, default_share_rate)
values
''',
'''insert into household_members (id, household_id, user_id, display_name, role, income_amount, default_share_rate, payday_day)
values
''')
replace('postgres/migrations/003_seed_local_data.sql',
'''    320000,
    0.55
''',
'''    320000,
    0.55,
    25
''')
replace('postgres/migrations/003_seed_local_data.sql',
'''    260000,
    0.45
''',
'''    260000,
    0.45,
    25
''')
replace('postgres/migrations/003_seed_local_data.sql',
'''  income_amount = excluded.income_amount,
  default_share_rate = excluded.default_share_rate;
''',
'''  income_amount = excluded.income_amount,
  default_share_rate = excluded.default_share_rate,
  payday_day = excluded.payday_day;
''')

api = '''import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const updateSchema = z.object({ memberId: z.string().uuid(), paydayDay: z.number().int().min(1).max(31) });
const householdQuery = `select id from households order by created_at asc limit 1`;

export async function GET() {
  try {
    const pool = db();
    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) return NextResponse.json({ members: [] });
    const members = (await pool.query<{ id: string; display_name: string; payday_day: number | null }>(`
      select id::text, display_name, payday_day
      from household_members
      where household_id = $1
      order by role asc, created_at asc
    `, [household.id])).rows;
    return NextResponse.json({ members });
  } catch (error) {
    console.error("Failed to load household member settings", error);
    return NextResponse.json({ error: "household_member_settings_failed" }, { status: 500 });
  }
}

export async function PATCH(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = updateSchema.safeParse(await request.json().catch(() => null));
    if (!parsed.success) return NextResponse.json({ error: "invalid_household_member_settings" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    const updated = await client.query<{ id: string }>(`
      update household_members
      set payday_day = $3, updated_at = now()
      where household_id = $1 and id = $2
      returning id::text
    `, [household.id, parsed.data.memberId, parsed.data.paydayDay]);
    if (updated.rowCount === 0) { await client.query("rollback"); return NextResponse.json({ error: "household_member_not_found" }, { status: 404 }); }
    await client.query("commit");
    console.info("Updated household member payday", parsed.data);
    return NextResponse.json({ memberId: parsed.data.memberId, paydayDay: parsed.data.paydayDay });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to update household member payday", error);
    return NextResponse.json({ error: "household_member_payday_failed" }, { status: 500 });
  } finally { client.release(); }
}
'''
Path('src/app/api/household-members/route.ts').parent.mkdir(parents=True, exist_ok=True)
Path('src/app/api/household-members/route.ts').write_text(api)

path='src/components/kakeibo-dashboard.tsx'
replace(path,
'''type SharedWalletContributions = {
''',
'''type HouseholdMemberSettings = { id: string; display_name: string; payday_day: number | null };

type SharedWalletContributions = {
''')
replace(path,
'''  const [sharedWalletPlanDraft, setSharedWalletPlanDraft] = useState("");
''',
'''  const [sharedWalletPlanDraft, setSharedWalletPlanDraft] = useState("");
  const [householdMemberSettings, setHouseholdMemberSettings] = useState<HouseholdMemberSettings[]>([]);
  const [paydayDrafts, setPaydayDrafts] = useState<Record<string, string>>({});
''')
replace(path,
'''  const saveSharedWalletPlan = async () => {
''',
'''  useEffect(() => {
    let active = true;
    void fetch("/api/household-members")
      .then((response) => { if (!response.ok) throw new Error("household_members_load_failed"); return response.json() as Promise<{ members: HouseholdMemberSettings[] }>; })
      .then((data) => { if (active) setHouseholdMemberSettings(data.members); })
      .catch(() => { if (active) setSharedWalletError("メンバー設定を読み込めませんでした。"); });
    return () => { active = false; };
  }, []);

  const updateMemberPayday = async (memberId: string) => {
    const paydayDay = Number(paydayDrafts[memberId]);
    if (!Number.isInteger(paydayDay) || paydayDay < 1 || paydayDay > 31) { setSharedWalletError("給料日は1日から31日で入力してください。"); return; }
    setSharedWalletError(null);
    try {
      const response = await fetch("/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId, paydayDay }) });
      if (!response.ok) throw new Error("household_member_payday_failed");
      setPaydayDrafts((current) => ({ ...current, [memberId]: "" }));
      setHouseholdMemberSettings((current) => current.map((member) => member.id === memberId ? { ...member, payday_day: paydayDay } : member));
    } catch { setSharedWalletError("給料日を更新できませんでした。"); }
  };

  const saveSharedWalletPlan = async () => {
''')
replace(path,
'''        <article className="panel settings-panel budget-goal-settings">
''',
'''        <article className="panel settings-panel payday-settings">
          <h3>給料日</h3>
          <div className="shared-wallet-settings__grid">
            {householdMemberSettings.map((member) => <div key={member.id}><input aria-label={`${member.display_name}の給料日`} inputMode="numeric" placeholder={`${member.display_name} 現在 ${member.payday_day ?? "未設定"}日`} value={paydayDrafts[member.id] ?? ""} onChange={(event) => setPaydayDrafts((current) => ({ ...current, [member.id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateMemberPayday(member.id)}>{member.display_name}の給料日を更新</button></div>)}
          </div>
        </article>
        <article className="panel settings-panel budget-goal-settings">
''')

replace('Docs/product-spec.md',
'''- 給料日通知、収入登録の更新は未実装
''',
'''- 設定画面からメンバーごとの給料日を登録する
- 給料日前後の確認表示、収入登録の更新は未実装
''')
replace('Docs/screens.md',
'''- 共通口座の入金目標: 選択月の目標額、メンバー別予定額
''',
'''- 共通口座の入金目標: 選択月の目標額、メンバー別予定額
- 給料日: メンバーごとの給料日
''')
replace('Docs/data-model.md',
'''default_share_rate
created_at
''',
'''default_share_rate
payday_day
created_at
''')
replace('Docs/data-model.md',
'''- `202606020007_add_shared_wallet_member_plans.sql`: メンバー別の共通口座入金予定と完了状態
''',
'''- `202606020007_add_shared_wallet_member_plans.sql`: メンバー別の共通口座入金予定と完了状態
- `202606030001_add_household_member_payday.sql`: メンバー別の給料日
''')
replace('Docs/api-security-spec.md',
'''### 5.15. 精算取り消し (`POST /api/settlements/reopen`)
''',
'''### 5.15. メンバー設定 (`GET /api/household-members`)

メンバーごとの表示名と給料日を返す。収入額は返さない。

### 5.16. メンバー給料日更新 (`PATCH /api/household-members`)

指定メンバーの給料日を1日から31日の範囲で更新する。

### 5.17. 精算取り消し (`POST /api/settlements/reopen`)
''')

path='tests/integration/api.integration.test.ts'
replace(path,
'''    await pool.query("update shared_wallet_monthly_plans set target_amount = 100000 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
''',
'''    await pool.query("update shared_wallet_monthly_plans set target_amount = 100000 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' and year = extract(year from current_date)::int and month = extract(month from current_date)::int");
    await pool.query("update household_members set payday_day = 25 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'");
''')
replace(path,
'''  it("updates recurring expense notification settings", async () => {
''',
'''  it("lists household member paydays without exposing income and updates them", async () => {
    const { GET, PATCH } = await import("../../src/app/api/household-members/route.ts");
    const listed = await GET();
    assert.equal(listed.status, 200);
    assert.deepEqual(await listed.json(), { members: [
      { id: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", display_name: "葵", payday_day: 25 },
      { id: "aaaaaaaa-2222-4222-8222-aaaaaaaa2222", display_name: "蓮", payday_day: 25 },
    ] });
    const updated = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 20 }) }));
    assert.equal(updated.status, 200);
    assert.deepEqual(await updated.json(), { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 20 });
    const invalid = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 32 }) }));
    assert.equal(invalid.status, 400);
  });

  it("updates recurring expense notification settings", async () => {
''')
PY

git diff --check
git status --short --branch
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '設定', exact: true }).click();
  const input = page.getByLabel('葵の給料日');
  await input.waitFor({ timeout: 30000 });
  const before = await input.getAttribute('placeholder');
  await input.fill('20');
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/household-members') && r.request().method() === 'PATCH' && r.ok()),
    page.getByRole('button', { name: '葵の給料日を更新', exact: true }).click(),
  ]);
  const after = await input.getAttribute('placeholder');
  if (!after.includes('20日')) throw new Error(`payday not refreshed: ${after}`);
  console.log(JSON.stringify({ before, after }));
  await browser.close();
})();
JS
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh >/tmp/kakeibo-seed.log && tail -n 8 /tmp/kakeibo-seed.log && printf '\n-- payday --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select display_name || ':' || payday_day from household_members where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' order by role asc;" && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true && printf '\n-- diff check --\n' && git diff --check && printf '\n-- status --\n' && git status --short --branch
git add Docs/api-security-spec.md Docs/data-model.md Docs/product-spec.md Docs/screens.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/012_add_household_member_payday.sql supabase/migrations/202606030001_add_household_member_payday.sql src/app/api/household-members/route.ts src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts && git diff --cached --check && git diff --cached --stat && git commit -m "feat: メンバーごとの給料日を登録できるようにする" -m "- household_membersへ給料日カラムを追加
- メンバー設定APIと設定画面の給料日更新を追加
- 収入額を返さないAPI契約、仕様書、integrationテストを更新" && git status --short --branch && git log --oneline --max-count=5
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:160]!r}')
    p.write_text(text.replace(old, new, 1))

path='src/app/api/household-members/route.ts'
replace(path,
'''const updateSchema = z.object({ memberId: z.string().uuid(), paydayDay: z.number().int().min(1).max(31) });
''',
'''const updateSchema = z.object({ memberId: z.string().uuid(), paydayDay: z.number().int().min(1).max(31).optional(), incomeAmount: z.number().int().nonnegative().optional() }).refine((value) => value.paydayDay != null || value.incomeAmount != null);
''')
replace(path,
'''    const updated = await client.query<{ id: string }>(`
      update household_members
      set payday_day = $3, updated_at = now()
      where household_id = $1 and id = $2
      returning id::text
    `, [household.id, parsed.data.memberId, parsed.data.paydayDay]);
    if (updated.rowCount === 0) { await client.query("rollback"); return NextResponse.json({ error: "household_member_not_found" }, { status: 404 }); }
    await client.query("commit");
    console.info("Updated household member payday", parsed.data);
    return NextResponse.json({ memberId: parsed.data.memberId, paydayDay: parsed.data.paydayDay });
''',
'''    const updated = await client.query<{ id: string }>(`
      update household_members
      set payday_day = coalesce($3, payday_day), income_amount = coalesce($4, income_amount), updated_at = now()
      where household_id = $1 and id = $2
      returning id::text
    `, [household.id, parsed.data.memberId, parsed.data.paydayDay ?? null, parsed.data.incomeAmount ?? null]);
    if (updated.rowCount === 0) { await client.query("rollback"); return NextResponse.json({ error: "household_member_not_found" }, { status: 404 }); }
    if (parsed.data.incomeAmount != null) {
      await client.query(`
        with household_totals as (
          select household_id, coalesce(sum(income_amount), 0) as total_income, count(*) as member_count
          from household_members
          where household_id = $1
          group by household_id
        )
        update household_members hm
        set default_share_rate = case when totals.total_income > 0 then hm.income_amount::numeric / totals.total_income else 1::numeric / totals.member_count end,
            updated_at = now()
        from household_totals totals
        where hm.household_id = totals.household_id
      `, [household.id]);
    }
    const shareRates = (await client.query<{ member_id: string; share_rate: number }>(`
      select id::text as member_id, default_share_rate::float8 as share_rate
      from household_members
      where household_id = $1
      order by role asc, created_at asc
    `, [household.id])).rows;
    await client.query("commit");
    console.info("Updated household member settings", { memberId: parsed.data.memberId, paydayDay: parsed.data.paydayDay, incomeUpdated: parsed.data.incomeAmount != null });
    return NextResponse.json({ memberId: parsed.data.memberId, ...(parsed.data.paydayDay == null ? {} : { paydayDay: parsed.data.paydayDay }), ...(parsed.data.incomeAmount == null ? {} : { shareRates: shareRates.map((item) => ({ memberId: item.member_id, shareRate: item.share_rate })) }) });
''')
replace(path,
'''    console.error("Failed to update household member payday", error);
''',
'''    console.error("Failed to update household member settings", error);
''')
replace(path,
'''    return NextResponse.json({ error: "household_member_payday_failed" }, { status: 500 });
''',
'''    return NextResponse.json({ error: "household_member_settings_update_failed" }, { status: 500 });
''')

path='src/components/kakeibo-dashboard.tsx'
replace(path,
'''  const [paydayDrafts, setPaydayDrafts] = useState<Record<string, string>>({});
''',
'''  const [paydayDrafts, setPaydayDrafts] = useState<Record<string, string>>({});
  const [incomeDrafts, setIncomeDrafts] = useState<Record<string, string>>({});
''')
replace(path,
'''  const saveSharedWalletPlan = async () => {
''',
'''  const updateMemberIncome = async (memberId: string) => {
    const incomeAmount = Number(incomeDrafts[memberId]);
    if (!Number.isInteger(incomeAmount) || incomeAmount < 0) { setSharedWalletError("収入額を0円以上で入力してください。"); return; }
    setSharedWalletError(null);
    try {
      const response = await fetch("/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId, incomeAmount }) });
      if (!response.ok) throw new Error("household_member_income_failed");
      setIncomeDrafts((current) => ({ ...current, [memberId]: "" }));
      const refreshed = await fetch(`/api/shared-wallet-contributions?year=${selectedPeriod.year}&month=${selectedPeriod.month}`);
      if (!refreshed.ok) throw new Error("shared_wallet_refresh_failed");
      setSharedWalletContributions(await refreshed.json() as SharedWalletContributions);
    } catch { setSharedWalletError("収入額を更新できませんでした。"); }
  };

  const saveSharedWalletPlan = async () => {
''')
replace(path,
'''          <h3>給料日</h3>
          <div className="shared-wallet-settings__grid">
            {householdMemberSettings.map((member) => <div key={member.id}><input aria-label={`${member.display_name}の給料日`} inputMode="numeric" placeholder={`${member.display_name} 現在 ${member.payday_day ?? "未設定"}日`} value={paydayDrafts[member.id] ?? ""} onChange={(event) => setPaydayDrafts((current) => ({ ...current, [member.id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateMemberPayday(member.id)}>{member.display_name}の給料日を更新</button></div>)}
          </div>
''',
'''          <h3>給料日と収入</h3>
          <p className="recurring-empty">収入額は提案比率の計算だけに使い、共有画面には表示しません。</p>
          <div className="shared-wallet-settings__grid">
            {householdMemberSettings.map((member) => <div key={member.id}><input aria-label={`${member.display_name}の給料日`} inputMode="numeric" placeholder={`${member.display_name} 現在 ${member.payday_day ?? "未設定"}日`} value={paydayDrafts[member.id] ?? ""} onChange={(event) => setPaydayDrafts((current) => ({ ...current, [member.id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateMemberPayday(member.id)}>{member.display_name}の給料日を更新</button><input aria-label={`${member.display_name}の収入額`} inputMode="numeric" placeholder={`${member.display_name}の収入額（非公開）`} value={incomeDrafts[member.id] ?? ""} onChange={(event) => setIncomeDrafts((current) => ({ ...current, [member.id]: event.target.value }))} /><button className="chip" type="button" onClick={() => void updateMemberIncome(member.id)}>{member.display_name}の収入額を更新</button></div>)}
          </div>
''')

replace('Docs/product-spec.md',
'''- 給料日前後の確認表示、収入登録の更新は未実装
''',
'''- 設定画面からメンバーごとの収入額を更新し、負担比率を再計算する
- 収入額は提案比率の計算だけに使用し、APIの取得結果と共有画面へ表示しない
- 給料日前後の確認表示は未実装
''')
replace('Docs/screens.md',
'''- 給料日: メンバーごとの給料日
''',
'''- 給料日と収入: メンバーごとの給料日、非公開の収入額入力
''')
replace('Docs/api-security-spec.md',
'''メンバーごとの表示名と給料日を返す。収入額は返さない。
''',
'''メンバーごとの表示名と給料日を返す。収入額は返さない。
''')
replace('Docs/api-security-spec.md',
'''指定メンバーの給料日を1日から31日の範囲で更新する。
''',
'''指定メンバーの給料日を1日から31日の範囲で更新する。`incomeAmount` を指定した場合は収入額を保存し、世帯内メンバーの負担比率を再計算する。収入額自体はレスポンスへ返さない。
''')

path='tests/integration/api.integration.test.ts'
replace(path,
'''    await pool.query("update household_members set payday_day = 25 where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'");
''',
'''    await pool.query("update household_members set payday_day = 25, income_amount = case id when 'aaaaaaaa-1111-4111-8111-aaaaaaaa1111' then 320000 when 'aaaaaaaa-2222-4222-8222-aaaaaaaa2222' then 260000 else income_amount end, default_share_rate = case id when 'aaaaaaaa-1111-4111-8111-aaaaaaaa1111' then 0.55 when 'aaaaaaaa-2222-4222-8222-aaaaaaaa2222' then 0.45 else default_share_rate end where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'");
''')
replace(path,
'''    const invalid = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 32 }) }));
    assert.equal(invalid.status, 400);
''',
'''    const incomeUpdated = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", incomeAmount: 260000 }) }));
    assert.equal(incomeUpdated.status, 200);
    assert.deepEqual(await incomeUpdated.json(), { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", shareRates: [
      { memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", shareRate: 0.5 },
      { memberId: "aaaaaaaa-2222-4222-8222-aaaaaaaa2222", shareRate: 0.5 },
    ] });
    const invalid = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 32 }) }));
    assert.equal(invalid.status, 400);
''')
PY

git diff --check
git status --short --branch
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  await page.getByRole('button', { name: '設定', exact: true }).click();
  const input = page.getByLabel('葵の収入額');
  await input.waitFor({ timeout: 30000 });
  const placeholder = await input.getAttribute('placeholder');
  if (!placeholder.includes('非公開')) throw new Error(`income privacy note missing: ${placeholder}`);
  await input.fill('260000');
  await Promise.all([
    page.waitForResponse(r => r.url().endsWith('/api/household-members') && r.request().method() === 'PATCH' && r.ok()),
    page.getByRole('button', { name: '葵の収入額を更新', exact: true }).click(),
  ]);
  const now = new Date();
  const body = await (await page.request.get(`/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`)).json();
  if (body.contributions.some(item => 'income_amount' in item || 'incomeAmount' in item)) throw new Error('income exposed');
  const proposals = body.contributions.map(item => item.proposed_amount);
  if (proposals[0] !== 50000 || proposals[1] !== 50000) throw new Error(`unexpected proposals: ${proposals}`);
  console.log(JSON.stringify({ placeholder, proposals, exposed: false }));
  await browser.close();
})();
JS
node - <<'JS'
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  const now = new Date();
  const body = await (await page.request.get(`http://127.0.0.1:3023/api/shared-wallet-contributions?year=${now.getFullYear()}&month=${now.getMonth() + 1}`)).json();
  if (body.contributions.some(item => 'income_amount' in item || 'incomeAmount' in item)) throw new Error('income exposed');
  const proposals = body.contributions.map(item => item.proposed_amount);
  if (proposals[0] !== 50000 || proposals[1] !== 50000) throw new Error(`unexpected proposals: ${proposals}`);
  console.log(JSON.stringify({ proposals, exposed: false }));
  await browser.close();
})();
JS
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh >/tmp/kakeibo-seed.log && tail -n 8 /tmp/kakeibo-seed.log && printf '\n-- rates --\n' && docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select display_name || ':' || income_amount || ':' || default_share_rate from household_members where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa' order by role asc;" && printf '\n-- ports --\n' && ss -ltnp | grep -E ':3022|:3023' || true && printf '\n-- diff check --\n' && git diff --check && printf '\n-- status --\n' && git status --short --branch
git add Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md src/app/api/household-members/route.ts src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts && git diff --cached --check && git diff --cached --stat && git commit -m "feat: 非公開の収入額から負担比率を再計算する" -m "- メンバー設定APIで収入額を更新可能にする
- 世帯内の収入額から負担比率をトランザクション内で再計算
- 収入額を取得APIと共有画面へ表示しない仕様を明記" && git status --short --branch && git log --oneline --max-count=5
python3 - <<'PY'
from pathlib import Path

def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f'missing pattern in {path}: {old[:160]!r}')
    p.write_text(text.replace(old, new, 1))

path='src/app/api/household-members/route.ts'
replace(path,
'''    return NextResponse.json({ members });
''',
'''    const now = new Date();
    const daysInMonth = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const reminders = members.flatMap((member) => {
      if (member.payday_day == null) return [];
      const paydayDay = Math.min(member.payday_day, daysInMonth);
      const daysUntilPayday = paydayDay - now.getDate();
      return Math.abs(daysUntilPayday) <= 3 ? [{ member_id: member.id, member_name: member.display_name, payday_day: member.payday_day, days_until_payday: daysUntilPayday }] : [];
    });
    return NextResponse.json({ members, reminders });
''')

path='src/components/kakeibo-dashboard.tsx'
replace(path,
'''type HouseholdMemberSettings = { id: string; display_name: string; payday_day: number | null };
''',
'''type HouseholdMemberSettings = { id: string; display_name: string; payday_day: number | null };
type PaydayReminder = { member_id: string; member_name: string; payday_day: number; days_until_payday: number };
''')
replace(path,
'''  const [householdMemberSettings, setHouseholdMemberSettings] = useState<HouseholdMemberSettings[]>([]);
''',
'''  const [householdMemberSettings, setHouseholdMemberSettings] = useState<HouseholdMemberSettings[]>([]);
  const [paydayReminders, setPaydayReminders] = useState<PaydayReminder[]>([]);
''')
replace(path,
'''      .then((response) => { if (!response.ok) throw new Error("household_members_load_failed"); return response.json() as Promise<{ members: HouseholdMemberSettings[] }>; })
      .then((data) => { if (active) setHouseholdMemberSettings(data.members); })
''',
'''      .then((response) => { if (!response.ok) throw new Error("household_members_load_failed"); return response.json() as Promise<{ members: HouseholdMemberSettings[]; reminders: PaydayReminder[] }>; })
      .then((data) => { if (active) { setHouseholdMemberSettings(data.members); setPaydayReminders(data.reminders); } })
''')
replace(path,
'''  const homeView = (
''',
'''  const paydayReminderPanel = paydayReminders.length === 0 ? null : (
    <section className="section recurring-panel" aria-label="給料日の入金確認">
      <div className="section__header"><div><h2 className="section__title">共通口座の入金確認</h2><p className="section__meta">給料日前後に、今月分の入金を確認する</p></div></div>
      <div className="recurring-list">{paydayReminders.map((reminder) => <article key={reminder.member_id} className="recurring-card"><div><strong>{reminder.member_name}</strong><p>{reminder.days_until_payday === 0 ? "今日は給料日です" : reminder.days_until_payday > 0 ? `給料日まであと${reminder.days_until_payday}日` : `給料日から${Math.abs(reminder.days_until_payday)}日経過`}</p></div><span className="pill pill--muted">毎月{reminder.payday_day}日</span></article>)}</div>
    </section>
  );

  const homeView = (
''')
replace(path,
'''        {featuredSavingsGoal ? <section className="section featured-savings-goal" aria-label="最優先の貯金目標">{savingsGoalCard(featuredSavingsGoal, true)}</section> : null}
        {analyticsPanel}
''',
'''        {featuredSavingsGoal ? <section className="section featured-savings-goal" aria-label="最優先の貯金目標">{savingsGoalCard(featuredSavingsGoal, true)}</section> : null}
        {paydayReminderPanel}
        {analyticsPanel}
''')

replace('Docs/product-spec.md',
'''- 給料日前後の確認表示は未実装
''',
'''- 給料日の3日前から3日後まではホーム画面へ共通口座の入金確認候補を表示する
- Push通知、メール配信は未実装
''')
replace('Docs/product-spec.md',
'''- 共通口座への入金状況、提案額、入金完了状態は管理できるが、給料日通知は未実装
''',
'''- 共通口座への入金状況、提案額、入金完了状態、給料日前後の確認候補は管理できるが、Push通知やメール配信は未実装
''')
replace('Docs/screens.md',
'''- 今月の定期支出: 予定日、予定日までの日数、金額、前回差分、通知ON/OFF、確定、スキップ
''',
'''- 給料日の入金確認: 給料日の3日前から3日後まで、メンバー別の確認候補
- 今月の定期支出: 予定日、予定日までの日数、金額、前回差分、通知ON/OFF、確定、スキップ
''')
replace('Docs/api-security-spec.md',
'''メンバーごとの表示名と給料日を返す。収入額は返さない。
''',
'''メンバーごとの表示名と給料日を返す。給料日の3日前から3日後までは入金確認候補も返す。収入額は返さない。
''')

path='tests/integration/api.integration.test.ts'
replace(path,
'''    assert.deepEqual(await listed.json(), { members: [
      { id: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", display_name: "葵", payday_day: 25 },
      { id: "aaaaaaaa-2222-4222-8222-aaaaaaaa2222", display_name: "蓮", payday_day: 25 },
    ] });
''',
'''    const listedBody = await listed.json();
    assert.deepEqual(listedBody.members, [
      { id: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", display_name: "葵", payday_day: 25 },
      { id: "aaaaaaaa-2222-4222-8222-aaaaaaaa2222", display_name: "蓮", payday_day: 25 },
    ]);
    assert.ok(Array.isArray(listedBody.reminders));
''')
replace(path,
'''    const invalid = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 32 }) }));
    assert.equal(invalid.status, 400);
''',
'''    const reminderDay = new Date().getDate();
    await pool.query("update household_members set payday_day = $1 where id = 'aaaaaaaa-1111-4111-8111-aaaaaaaa1111'", [reminderDay]);
    const withReminder = await GET();
    assert.ok((await withReminder.json()).reminders.some((reminder: { member_id: string; days_until_payday: number }) => reminder.member_id === "aaaaaaaa-1111-4111-8111-aaaaaaaa1111" && reminder.days_until_payday === 0));
    const invalid = await PATCH(new Request("http://localhost/api/household-members", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ memberId: "aaaaaaaa-1111-4111-8111-aaaaaaaa1111", paydayDay: 32 }) }));
    assert.equal(invalid.status, 400);
''')
PY

git diff --check
git status --short --branch
sed -n '1,240p' src/app/api/household-members/route.ts && sed -n '1,280p' src/components/kakeibo-dashboard.tsx
rg -n "updateMemberPayday|paydayReminderPanel|household-members" src/components/kakeibo-dashboard.tsx && sed -n '400,560p' src/components/kakeibo-dashboard.tsx && sed -n '900,1040p' src/components/kakeibo-dashboard.tsx
grep -nE 'updateMemberPayday|paydayReminderPanel|household-members' src/components/kakeibo-dashboard.tsx && sed -n '400,580p' src/components/kakeibo-dashboard.tsx && sed -n '900,1060p' src/components/kakeibo-dashboard.tsx
sed -n '332,410p' src/components/kakeibo-dashboard.tsx && sed -n '1216,1255p' src/components/kakeibo-dashboard.tsx
python3 <<'PY'
from pathlib import Path
route = Path('src/app/api/household-members/route.ts')
text = route.read_text()
old = '''    const now = new Date();
    const daysInMonth = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const reminders = members.flatMap((member) => {
      if (member.payday_day == null) return [];
      const paydayDay = Math.min(member.payday_day, daysInMonth);
      const daysUntilPayday = paydayDay - now.getDate();
      return Math.abs(daysUntilPayday) <= 3 ? [{ member_id: member.id, member_name: member.display_name, payday_day: member.payday_day, days_until_payday: daysUntilPayday }] : [];
    });
'''
new = '''    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const reminders = members.flatMap((member) => {
      if (member.payday_day == null) return [];
      const paydayDates = [-1, 0, 1].map((monthOffset) => {
        const monthStart = new Date(today.getFullYear(), today.getMonth() + monthOffset, 1);
        const daysInMonth = new Date(monthStart.getFullYear(), monthStart.getMonth() + 1, 0).getDate();
        return new Date(monthStart.getFullYear(), monthStart.getMonth(), Math.min(member.payday_day, daysInMonth));
      });
      const nearestPayday = paydayDates.reduce((nearest, payday) => Math.abs(payday.getTime() - today.getTime()) < Math.abs(nearest.getTime() - today.getTime()) ? payday : nearest);
      const daysUntilPayday = Math.round((nearestPayday.getTime() - today.getTime()) / 86400000);
      return Math.abs(daysUntilPayday) <= 3 ? [{ member_id: member.id, member_name: member.display_name, payday_day: member.payday_day, days_until_payday: daysUntilPayday }] : [];
    });
'''
assert old in text
route.write_text(text.replace(old, new))

dashboard = Path('src/components/kakeibo-dashboard.tsx')
text = dashboard.read_text()
old = '''      setPaydayDrafts((current) => ({ ...current, [memberId]: "" }));
      setHouseholdMemberSettings((current) => current.map((member) => member.id === memberId ? { ...member, payday_day: paydayDay } : member));
'''
new = '''      setPaydayDrafts((current) => ({ ...current, [memberId]: "" }));
      const refreshed = await fetch("/api/household-members");
      if (!refreshed.ok) throw new Error("household_members_refresh_failed");
      const data = await refreshed.json() as { members: HouseholdMemberSettings[]; reminders: PaydayReminder[] };
      setHouseholdMemberSettings(data.members);
      setPaydayReminders(data.reminders);
'''
assert old in text
dashboard.write_text(text.replace(old, new))
PY
git diff --check
git diff --stat
git diff -- src/app/api/household-members/route.ts src/components/kakeibo-dashboard.tsx
git diff -- Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md tests/integration/api.integration.test.ts
python3 <<'PY'
from pathlib import Path
path = Path('src/app/api/household-members/route.ts')
text = path.read_text()
text = text.replace('    if (!household) return NextResponse.json({ members: [] });', '    if (!household) return NextResponse.json({ members: [], reminders: [] });')
old = '''    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const reminders = members.flatMap((member) => {
      if (member.payday_day == null) return [];
      const paydayDates = [-1, 0, 1].map((monthOffset) => {
        const monthStart = new Date(today.getFullYear(), today.getMonth() + monthOffset, 1);
        const daysInMonth = new Date(monthStart.getFullYear(), monthStart.getMonth() + 1, 0).getDate();
        return new Date(monthStart.getFullYear(), monthStart.getMonth(), Math.min(member.payday_day, daysInMonth));
      });
      const nearestPayday = paydayDates.reduce((nearest, payday) => Math.abs(payday.getTime() - today.getTime()) < Math.abs(nearest.getTime() - today.getTime()) ? payday : nearest);
      const daysUntilPayday = Math.round((nearestPayday.getTime() - today.getTime()) / 86400000);
'''
new = '''    const now = new Date();
    const today = Date.UTC(now.getFullYear(), now.getMonth(), now.getDate());
    const reminders = members.flatMap((member) => {
      if (member.payday_day == null) return [];
      const paydayDates = [-1, 0, 1].map((monthOffset) => {
        const monthStart = new Date(now.getFullYear(), now.getMonth() + monthOffset, 1);
        const daysInMonth = new Date(monthStart.getFullYear(), monthStart.getMonth() + 1, 0).getDate();
        return Date.UTC(monthStart.getFullYear(), monthStart.getMonth(), Math.min(member.payday_day, daysInMonth));
      });
      const nearestPayday = paydayDates.reduce((nearest, payday) => Math.abs(payday - today) < Math.abs(nearest - today) ? payday : nearest);
      const daysUntilPayday = Math.round((nearestPayday - today) / 86400000);
'''
assert old in text
path.write_text(text.replace(old, new))
PY
git diff --check
npm test
python3 <<'PY'
from pathlib import Path
path = Path('src/app/api/household-members/route.ts')
text = path.read_text()
old = '''    const reminders = members.flatMap((member) => {
      if (member.payday_day == null) return [];
      const paydayDates = [-1, 0, 1].map((monthOffset) => {
'''
new = '''    const reminders = members.flatMap((member) => {
      const paydayDay = member.payday_day;
      if (paydayDay == null) return [];
      const paydayDates = [-1, 0, 1].map((monthOffset) => {
'''
assert old in text
text = text.replace(old, new)
text = text.replace('Math.min(member.payday_day, daysInMonth)', 'Math.min(paydayDay, daysInMonth)')
text = text.replace('payday_day: member.payday_day, days_until_payday', 'payday_day: paydayDay, days_until_payday')
path.write_text(text)
PY
git diff --check
npm run build
cat package.json
ss -ltnp
sed -n '1,240p' playwright.config.ts && find tests/e2e -maxdepth 2 -type f -print
node --input-type=module <<'JS'
import { chromium } from '@playwright/test';
const browser = await chromium.launch({ headless: true });
const page = await browser.newPage();
try {
  await page.goto('http://127.0.0.1:3023/', { waitUntil: 'networkidle' });
  await page.getByRole('button', { name: '設定' }).click();
  const payday = String(new Date().getDate());
  await page.getByLabel('葵の給料日').fill(payday);
  await page.getByRole('button', { name: '葵の給料日を更新' }).click();
  await page.getByRole('button', { name: 'ホーム' }).click();
  await page.getByRole('region', { name: '給料日の入金確認' }).waitFor();
  const text = await page.getByRole('region', { name: '給料日の入金確認' }).innerText();
  if (!text.includes('葵') || !text.includes('今日は給料日です')) throw new Error(`unexpected reminder panel: ${text}`);
  console.log(text);
} finally {
  await browser.close();
}
JS
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh && npm run test:e2e
sed -n '1,285p' tests/e2e/kakeibo.spec.ts
grep -RIn "settlement_period\|settled\|period" docker-entrypoint-initdb.d scripts supabase 2>/dev/null | head -n 120 && docker compose exec -T db psql -U kakeibo -d kakeibo_test -c "select year, month, status from settlement_periods order by year, month;"
npm run test:e2e -- --grep '支出入力は通常時に畳まれ'
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select display_name || ':' || payday_day || ':' || income_amount || ':' || default_share_rate from household_members order by role;"
ss -ltnp | grep -E ':(3021|3022|3023)\\b' || true
git add Docs/api-security-spec.md Docs/product-spec.md Docs/screens.md src/app/api/household-members/route.ts src/components/kakeibo-dashboard.tsx tests/integration/api.integration.test.ts && git commit -m "feat: 給料日前後に入金確認候補を表示する" -m "- 給料日の3日前から3日後まで確認候補を返す\n- ホーム画面へ共通口座の入金確認を表示する\n- 仕様書と結合テストを更新する" && git status --short --branch && git log --oneline --max-count=7
gh issue view 13 --json number,title,state,body,labels,url
gh issue view 3 --json number,title,state,body,labels,url
find . -maxdepth 3 -type f | sort | sed -n '1,260p'
sed -n '1,280p' docker-entrypoint-initdb.d/001_schema.sql 2>/dev/null || sed -n '1,280p' docker-entrypoint-initdb.d/001_initial_schema.sql && sed -n '1,260p' docker-entrypoint-initdb.d/003_seed.sql 2>/dev/null || true
find docker-entrypoint-initdb.d -maxdepth 2 -type f -print | sort && find src/app/api -type f -print | sort && find src/lib -type f -print | sort
grep -RIn "order by created_at asc limit 1\|from households\|household_id\|auth\|cookie\|session" src docker-entrypoint-initdb.d Docs --exclude-dir=node_modules | sed -n '1,260p'
sed -n '1,280p' docker-entrypoint-initdb.d/001_create_schema.sql 2>/dev/null || true && sed -n '1,300p' docker-entrypoint-initdb.d/003_seed_data.sql 2>/dev/null || true && sed -n '1,220p' Docs/architecture.md && sed -n '1,240p' Docs/data-model.md
find postgres supabase src/db -type f -print | sort
sed -n '1,500p' postgres/migrations/001_initial_schema.sql && sed -n '1,180p' src/db/README.md && sed -n '130,230p' Docs/api-security-spec.md
sed -n '1,520p' supabase/migrations/202605240001_initial_schema.sql && sed -n '1,120p' src/lib/db.ts && find src/app -maxdepth 3 -type f -print | sort
sed -n '1,260p' postgres/migrations/002_prepare_local_environments.sh && sed -n '1,160p' src/db/schema.sql && sed -n '1,140p' postgres/README.md
tail -n 120 Docs/data-model.md && tail -n 100 Docs/api-security-spec.md && sed -n '1,120p' tests/integration/api.integration.test.ts
sed -n '1,180p' supabase/migrations/202606030001_add_household_member_payday.sql && sed -n '1,120p' postgres/migrations/012_add_household_member_payday.sql && git status --short --branch
python3 <<'PY'
from pathlib import Path
Path('postgres/migrations/013_add_household_invitations.sql').write_text('''create table if not exists household_invitations (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households(id) on delete cascade,
  token_hash text not null unique,
  invited_by_member_id uuid not null,
  accepted_by_user_id uuid references app_users(id) on delete set null,
  expires_at timestamptz not null,
  accepted_at timestamptz,
  revoked_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint household_invitations_inviter_same_household
    foreign key (invited_by_member_id, household_id)
    references household_members(id, household_id)
    on delete cascade,
  constraint household_invitations_terminal_state
    check (accepted_at is null or revoked_at is null)
);

create index if not exists household_invitations_household_id_idx
  on household_invitations(household_id, created_at desc);

drop trigger if exists household_invitations_set_updated_at on household_invitations;
create trigger household_invitations_set_updated_at
before update on household_invitations
for each row execute function set_updated_at();
''')
Path('supabase/migrations/202606030002_add_household_invitations.sql').write_text('''create table public.household_invitations (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references public.households(id) on delete cascade,
  token_hash text not null unique,
  invited_by_member_id uuid not null,
  accepted_by_user_id uuid references auth.users(id) on delete set null,
  expires_at timestamptz not null,
  accepted_at timestamptz,
  revoked_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint household_invitations_inviter_same_household
    foreign key (invited_by_member_id, household_id)
    references public.household_members(id, household_id)
    on delete cascade,
  constraint household_invitations_terminal_state
    check (accepted_at is null or revoked_at is null)
);

create index household_invitations_household_id_idx
  on public.household_invitations(household_id, created_at desc);

create trigger household_invitations_set_updated_at
before update on public.household_invitations
for each row execute function public.set_updated_at();

alter table public.household_invitations enable row level security;

create policy "members can manage household invitations"
on public.household_invitations
for all
using (public.is_household_member(household_id))
with check (public.is_household_member(household_id));
''')

prep = Path('postgres/migrations/002_prepare_local_environments.sh')
text = prep.read_text()
text = text.replace('household_member_payday_migration_file="/docker-entrypoint-initdb.d/012_add_household_member_payday.sql"\n', 'household_member_payday_migration_file="/docker-entrypoint-initdb.d/012_add_household_member_payday.sql"\nhousehold_invitations_migration_file="/docker-entrypoint-initdb.d/013_add_household_invitations.sql"\n')
text = text.replace('  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$household_member_payday_migration_file"\n', '  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$household_member_payday_migration_file"\n  psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$database_name" \\\n    -f "$household_invitations_migration_file"\n')
prep.write_text(text)

data = Path('Docs/data-model.md')
text = data.read_text()
marker = '## payment_sources\n'
section = '''## household_invitations\n\n世帯へパートナーを招待するための期限付きトークン。招待URLへ載せるトークン自体は保存せず、照合用ハッシュだけを保持する。受諾済みまたは無効化済みの招待は再利用しない。\n\n```txt\nid\nhousehold_id\ntoken_hash\ninvited_by_member_id\naccepted_by_user_id\nexpires_at\naccepted_at\nrevoked_at\ncreated_at\nupdated_at\n```\n\n'''
assert marker in text
text = text.replace(marker, section + marker)
text = text.replace('- `202606030001_add_household_member_payday.sql`: メンバー別の給料日\n', '- `202606030001_add_household_member_payday.sql`: メンバー別の給料日\n- `202606030002_add_household_invitations.sql`: 世帯への期限付き招待\n')
data.write_text(text)

architecture = Path('Docs/architecture.md')
text = architecture.read_text()
text = text.replace('- Auth: MVPでは未確定。Supabase Authまたは独自Authを比較する', '- Auth: Supabase Auth。ローカルPostgreSQL単体テストでは `app_users` を代替として使う')
marker = '## 方針\n'
section = '''## 認証方針\n\n本番向け認証には Supabase Auth を採用する。既存の Supabase スキーマが `auth.users` と RLS を前提としており、独自認証を追加するよりも世帯単位のアクセス制御を一貫して適用しやすい。招待URLのトークンはDBへ平文保存せず、`household_invitations.token_hash` で照合する。\n\n素のPostgreSQLを使うローカル確認では、Supabase Authの代わりに `app_users` を利用する。\n\n'''
assert marker in text
architecture.write_text(text.replace(marker, section + marker))

security = Path('Docs/api-security-spec.md')
text = security.read_text()
marker = '### 3.2. 世帯（テナント）隔離の厳格化\n'
section = '''### 3.2. 世帯招待トークン\n\n- 招待URLへ載せるトークンは十分な長さのランダム値とし、DBにはハッシュ値だけを保存する。\n- 招待には有効期限を設け、受諾済みまたは無効化済みの招待は再利用しない。\n- 招待の一覧取得、再送、無効化は同じ世帯のメンバーだけに許可する。\n- 未参加ユーザーによる受諾は、今後追加するサーバー側APIでトークン照合後に処理する。\n\n'''
assert marker in text
text = text.replace(marker, section + '### 3.3. 世帯（テナント）隔離の厳格化\n')
security.write_text(text)

readme = Path('src/db/README.md')
text = readme.read_text()
text = text.replace('- `shared_wallet_contributions`\n', '- `shared_wallet_contributions`\n- `household_invitations`\n')
text = text.replace('MVPでは招待フローの詳細テーブルはまだ持たず、アプリ側で世帯作成後に参加メンバーを追加する想定。', '`household_invitations` は招待トークンを平文保存せず、照合用ハッシュ、有効期限、受諾・無効化状態を保持する。')
readme.write_text(text)
PY
git diff --check
git status --short
git diff -- Docs/api-security-spec.md Docs/architecture.md Docs/data-model.md src/db/README.md postgres/migrations/002_prepare_local_environments.sh
sed -n '1,240p' postgres/migrations/013_add_household_invitations.sql && sed -n '1,280p' supabase/migrations/202606030002_add_household_invitations.sql
sed -n '1,240p' postgres/migrations/013_add_household_invitations.sql
sed -n '1,280p' supabase/migrations/202606030002_add_household_invitations.sql
sed -n '1,160p' docker-compose.yml
docker compose exec -T db psql -v ON_ERROR_STOP=1 -U kakeibo -d kakeibo_test <<'SQL'
begin;
with source as (
  select household_id, id as member_id
  from household_members
  where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'
  order by role asc
  limit 1
)
insert into household_invitations (household_id, token_hash, invited_by_member_id, expires_at)
select household_id, 'integration-token-hash', member_id, now() + interval '7 days'
from source;
select count(*) as invitation_count from household_invitations where token_hash = 'integration-token-hash';
rollback;
SQL

docker compose exec -T db psql -v ON_ERROR_STOP=0 -U kakeibo -d kakeibo_test <<'SQL'
begin;
with source as (
  select household_id, id as member_id
  from household_members
  where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'
  order by role asc
  limit 1
)
insert into household_invitations (household_id, token_hash, invited_by_member_id, expires_at, accepted_at, revoked_at)
select household_id, 'invalid-terminal-state', member_id, now() + interval '7 days', now(), now()
from source;
rollback;
SQL
grep -n "delete from" postgres/migrations/003_seed_local_data.sql | sed -n '1,120p' && sed -n '1,120p' postgres/migrations/003_seed_local_data.sql
python3 <<'PY'
from pathlib import Path
path = Path('postgres/migrations/003_seed_local_data.sql')
text = path.read_text()
needle = '''insert into household_members (id, household_id, user_id, display_name, role, income_amount, default_share_rate, payday_day)
'''
insert = '''delete from household_invitations
where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa';

'''
assert needle in text
if insert not in text:
    text = text.replace(needle, insert + needle)
path.write_text(text)
PY
git diff --check
git diff --stat
git diff -- Docs/api-security-spec.md Docs/architecture.md Docs/data-model.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql src/db/README.md
git diff --stat && git diff --name-only
git add Docs/api-security-spec.md Docs/architecture.md Docs/data-model.md postgres/migrations/002_prepare_local_environments.sh postgres/migrations/003_seed_local_data.sql postgres/migrations/013_add_household_invitations.sql supabase/migrations/202606030002_add_household_invitations.sql src/db/README.md && git diff --cached --stat && git commit -m "feat: 世帯招待のDB基盤を追加する" -m "- add household invitation migrations for local PostgreSQL and Supabase\n- store invitation token hashes with expiry and terminal state constraints\n- document Supabase Auth and invitation token policy" && git status --short --branch
sed -n '1,180p' src/app/api/savings-goals/route.ts && sed -n '1,180p' src/app/api/budget-goals/route.ts
sed -n '120,320p' tests/integration/api.integration.test.ts
mkdir -p src/app/api/household-invitations
python3 <<'PY'
from pathlib import Path
Path('src/app/api/household-invitations/route.ts').write_text('''import { createHash, randomBytes } from "node:crypto";

import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const createSchema = z.object({ expiresInDays: z.number().int().min(1).max(30).optional().default(7) });
const revokeSchema = z.object({ invitationId: z.string().uuid(), action: z.literal("revoke") });
const householdQuery = `select id from households order by created_at asc limit 1`;
const inviterQuery = `select id from household_members where household_id = $1 and role = 'owner' order by created_at asc limit 1`;
const invitationsQuery = `
  select
    hi.id::text,
    hm.display_name as invited_by,
    hi.expires_at::text,
    hi.accepted_at::text,
    hi.revoked_at::text,
    hi.created_at::text,
    case
      when hi.accepted_at is not null then 'accepted'
      when hi.revoked_at is not null then 'revoked'
      when hi.expires_at <= now() then 'expired'
      else 'active'
    end as status
  from household_invitations hi
  join household_members hm on hm.id = hi.invited_by_member_id
  where hi.household_id = $1
  order by hi.created_at desc
`;

type InvitationRow = {
  id: string;
  invited_by: string;
  expires_at: string;
  accepted_at: string | null;
  revoked_at: string | null;
  created_at: string;
  status: "active" | "expired" | "accepted" | "revoked";
};

function hashInvitationToken(token: string) {
  return createHash("sha256").update(token).digest("hex");
}

function createInvitationToken() {
  return randomBytes(32).toString("base64url");
}

export async function GET() {
  try {
    const pool = db();
    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) return NextResponse.json({ invitations: [] });
    const invitations = (await pool.query<InvitationRow>(invitationsQuery, [household.id])).rows;
    return NextResponse.json({ invitations });
  } catch (error) {
    console.error("Failed to load household invitations", error);
    return NextResponse.json({ error: "household_invitations_failed" }, { status: 500 });
  }
}

export async function POST(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = createSchema.safeParse(await request.json().catch(() => ({})));
    if (!parsed.success) return NextResponse.json({ error: "invalid_household_invitation" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    const inviter = (await client.query<{ id: string }>(inviterQuery, [household.id])).rows[0];
    if (!inviter) { await client.query("rollback"); return NextResponse.json({ error: "household_inviter_not_found" }, { status: 404 }); }
    const token = createInvitationToken();
    const tokenHash = hashInvitationToken(token);
    const invitation = (await client.query<{ id: string; expires_at: string }>(`
      insert into household_invitations (household_id, token_hash, invited_by_member_id, expires_at)
      values ($1, $2, $3, now() + ($4::text || ' days')::interval)
      returning id::text, expires_at::text
    `, [household.id, tokenHash, inviter.id, parsed.data.expiresInDays])).rows[0];
    await client.query("commit");
    console.info("Created household invitation", { invitationId: invitation.id, expiresInDays: parsed.data.expiresInDays });
    return NextResponse.json({ invitation: { id: invitation.id, token, expires_at: invitation.expires_at } }, { status: 201 });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to create household invitation", error);
    return NextResponse.json({ error: "household_invitation_create_failed" }, { status: 500 });
  } finally { client.release(); }
}

export async function PATCH(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = revokeSchema.safeParse(await request.json().catch(() => null));
    if (!parsed.success) return NextResponse.json({ error: "invalid_household_invitation_action" }, { status: 400 });
    await client.query("begin");
    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];
    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }
    const invitation = (await client.query<{ id: string; revoked_at: string }>(`
      update household_invitations
      set revoked_at = now(), updated_at = now()
      where id = $1 and household_id = $2 and accepted_at is null and revoked_at is null
      returning id::text, revoked_at::text
    `, [parsed.data.invitationId, household.id])).rows[0];
    if (!invitation) { await client.query("rollback"); return NextResponse.json({ error: "household_invitation_not_found" }, { status: 404 }); }
    await client.query("commit");
    console.info("Revoked household invitation", { invitationId: invitation.id });
    return NextResponse.json({ invitationId: invitation.id, revokedAt: invitation.revoked_at });
  } catch (error) {
    await client.query("rollback");
    console.error("Failed to update household invitation", error);
    return NextResponse.json({ error: "household_invitation_update_failed" }, { status: 500 });
  } finally { client.release(); }
}
''')
PY
git diff --check
python3 <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
text = text.replace('import assert from "node:assert/strict";\n', 'import assert from "node:assert/strict";\nimport { createHash } from "node:crypto";\n')
text = text.replace('    await pool.query("delete from recurring_expenses where name = \'統合テスト定期支出\'");\n', '    await pool.query("delete from recurring_expenses where name = \'統合テスト定期支出\'");\n    await pool.query("delete from household_invitations where token_hash in (select token_hash from household_invitations where household_id = \'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa\' and created_at > now() - interval \'1 day\')");\n')
needle = '''  it("lists household member paydays without exposing income and updates them", async () => {
'''
test = '''  it("creates and revokes household invitations without exposing token hashes", async () => {
    const { GET, PATCH, POST } = await import("../../src/app/api/household-invitations/route.ts");
    const created = await POST(new Request("http://localhost/api/household-invitations", {
      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ expiresInDays: 3 }),
    }));
    assert.equal(created.status, 201);
    const createdBody = await created.json();
    assert.match(createdBody.invitation.token, /^[A-Za-z0-9_-]{40,}$/);
    assert.equal(typeof createdBody.invitation.id, "string");
    const tokenHash = createHash("sha256").update(createdBody.invitation.token).digest("hex");
    assert.deepEqual((await pool.query<{ token_hash: string; accepted_at: string | null; revoked_at: string | null }>("select token_hash, accepted_at::text, revoked_at::text from household_invitations where id = $1", [createdBody.invitation.id])).rows, [{ token_hash: tokenHash, accepted_at: null, revoked_at: null }]);

    const listed = await GET();
    assert.equal(listed.status, 200);
    const listedInvitation = (await listed.json()).invitations.find((invitation: { id: string }) => invitation.id === createdBody.invitation.id);
    assert.ok(listedInvitation);
    assert.equal(listedInvitation.status, "active");
    assert.equal(listedInvitation.token, undefined);
    assert.equal(listedInvitation.token_hash, undefined);

    const revoked = await PATCH(new Request("http://localhost/api/household-invitations", {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ invitationId: createdBody.invitation.id, action: "revoke" }),
    }));
    assert.equal(revoked.status, 200);
    assert.equal((await revoked.json()).invitationId, createdBody.invitation.id);

    const revokeAgain = await PATCH(new Request("http://localhost/api/household-invitations", {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ invitationId: createdBody.invitation.id, action: "revoke" }),
    }));
    assert.equal(revokeAgain.status, 404);

    const invalid = await POST(new Request("http://localhost/api/household-invitations", {
      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ expiresInDays: 31 }),
    }));
    assert.equal(invalid.status, 400);
  });

'''
assert needle in text
text = text.replace(needle, test + needle)
path.write_text(text)

security = Path('Docs/api-security-spec.md')
text = security.read_text()
marker = '### 5.17. 精算取り消し (`POST /api/settlements/reopen`)\n'
section = '''### 5.17. 世帯招待 (`GET /api/household-invitations`)\n\n世帯への招待一覧を返す。招待トークンと `token_hash` は返さない。\n\n### 5.18. 世帯招待作成 (`POST /api/household-invitations`)\n\n期限付き招待を作成する。`expiresInDays` は1〜30日の整数で、省略時は7日。レスポンスでは招待トークンを作成時に1回だけ返し、DBにはsha256ハッシュを保存する。\n\n### 5.19. 世帯招待無効化 (`PATCH /api/household-invitations`)\n\n未受諾かつ未無効化の招待を `revoke` する。受諾済みまたは無効化済みの招待は404として扱う。\n\n'''
assert marker in text
text = text.replace(marker, section + '### 5.20. 精算取り消し (`POST /api/settlements/reopen`)\n')
security.write_text(text)

product = Path('Docs/product-spec.md')
text = product.read_text()
marker = '### 給料日と共通口座\n'
section = '''### ユーザー登録と世帯招待\n\n- 本番向け認証は Supabase Auth を採用する\n- 世帯招待は期限付きトークンで作成し、DBにはトークンのハッシュだけを保存する\n- 招待の作成、一覧、無効化APIを提供する\n- 招待受諾、ログインUI、未認証拒否、他世帯アクセス拒否は未実装\n\n'''
assert marker in text
text = text.replace(marker, section + marker)
product.write_text(text)
PY
git diff --check
git diff --stat
grep -n "給料日\|ユーザー\|世帯\|共通口座" Docs/product-spec.md | sed -n '1,120p' && sed -n '130,190p' Docs/product-spec.md
sed -n '1,80p' tests/integration/api.integration.test.ts && sed -n '220,310p' tests/integration/api.integration.test.ts && sed -n '300,380p' Docs/api-security-spec.md
python3 <<'PY'
from pathlib import Path
route = Path('src/app/api/household-invitations/route.ts')
text = route.read_text()
text = text.replace('    const parsed = createSchema.safeParse(await request.json().catch(() => ({})));\n', '    const parsed = createSchema.safeParse(await request.json().catch(() => null));\n')
route.write_text(text)

product = Path('Docs/product-spec.md')
text = product.read_text()
marker = '### 共通口座への入金状況\n'
section = '''### ユーザー登録と世帯招待\n\n- 本番向け認証は Supabase Auth を採用する\n- 世帯招待は期限付きトークンで作成し、DBにはトークンのハッシュだけを保存する\n- 招待の作成、一覧、無効化APIを提供する\n- 招待受諾、ログインUI、未認証拒否、他世帯アクセス拒否は未実装\n\n'''
assert marker in text
if section not in text:
    text = text.replace(marker, section + marker)
text = text.replace('- 認証、世帯招待、世帯単位のアクセス制御は未実装\n', '- 招待の作成、一覧、無効化APIは提供するが、ログインUI、招待受諾、未認証拒否、世帯単位のアクセス制御は未実装\n')
product.write_text(text)
PY
git diff --check
git diff --stat
sed -n '1,260p' src/app/api/household-invitations/route.ts && git status --short --branch
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select count(*) from household_invitations where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa';"
git add Docs/api-security-spec.md Docs/product-spec.md tests/integration/api.integration.test.ts src/app/api/household-invitations/route.ts && git diff --cached --stat && git commit -m "feat: 世帯招待APIを追加する" -m "- add household invitation create/list/revoke endpoints\n- return raw invitation token only on creation and store sha256 hash\n- cover invitation lifecycle with integration tests" && git status --short --branch
mkdir -p src/app/api/household-invitations/accept
python3 <<'PY'
from pathlib import Path
Path('src/app/api/household-invitations/accept/route.ts').write_text('''import { createHash } from "node:crypto";

import { NextResponse } from "next/server.js";
import { z } from "zod";
import { db } from "../../../../lib/db.ts";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const acceptSchema = z.object({
  token: z.string().min(40).max(200),
  userId: z.string().uuid(),
  displayName: z.string().trim().min(1).max(50),
});

type Invitation = { id: string; household_id: string };

function hashInvitationToken(token: string) {
  return createHash("sha256").update(token).digest("hex");
}

function hasPgCode(error: unknown, code: string) {
  return typeof error === "object" && error != null && "code" in error && (error as { code: unknown }).code === code;
}

export async function POST(request: Request) {
  const pool = db();
  const client = await pool.connect();
  try {
    const parsed = acceptSchema.safeParse(await request.json().catch(() => null));
    if (!parsed.success) return NextResponse.json({ error: "invalid_household_invitation_accept" }, { status: 400 });
    await client.query("begin");
    const invitation = (await client.query<Invitation>(`
      select id::text, household_id::text
      from household_invitations
      where token_hash = $1 and accepted_at is null and revoked_at is null and expires_at > now()
      for update
    `, [hashInvitationToken(parsed.data.token)])).rows[0];
    if (!invitation) { await client.query("rollback"); return NextResponse.json({ error: "household_invitation_not_found" }, { status: 404 }); }
    const member = (await client.query<{ id: string }>(`
      insert into household_members (household_id, user_id, display_name, role)
      values ($1, $2, $3, 'partner')
      on conflict (household_id, user_id) do nothing
      returning id::text
    `, [invitation.household_id, parsed.data.userId, parsed.data.displayName])).rows[0];
    if (!member) { await client.query("rollback"); return NextResponse.json({ error: "household_member_already_exists" }, { status: 409 }); }
    await client.query("update household_invitations set accepted_by_user_id = $1, accepted_at = now(), updated_at = now() where id = $2", [parsed.data.userId, invitation.id]);
    await client.query("commit");
    console.info("Accepted household invitation", { invitationId: invitation.id, memberId: member.id });
    return NextResponse.json({ memberId: member.id }, { status: 201 });
  } catch (error) {
    await client.query("rollback");
    if (hasPgCode(error, "23503")) return NextResponse.json({ error: "invitation_user_not_found" }, { status: 404 });
    console.error("Failed to accept household invitation", error);
    return NextResponse.json({ error: "household_invitation_accept_failed" }, { status: 500 });
  } finally { client.release(); }
}
''')
PY
git diff --check
python3 <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
text = text.replace('    await pool.query("delete from household_invitations where token_hash in (select token_hash from household_invitations where household_id = \'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa\' and created_at > now() - interval \'1 day\')");\n', '    await pool.query("delete from household_invitations where token_hash in (select token_hash from household_invitations where household_id = \'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa\' and created_at > now() - interval \'1 day\')");\n    await pool.query("delete from household_members where user_id = \'33333333-3333-4333-8333-333333333333\'");\n    await pool.query("delete from app_users where id = \'33333333-3333-4333-8333-333333333333\'");\n')
needle = '''  it("lists household member paydays without exposing income and updates them", async () => {
'''
test = '''  it("accepts household invitations and prevents token reuse", async () => {
    await pool.query("insert into app_users (id, email, display_name) values ('33333333-3333-4333-8333-333333333333', 'invitee@example.local', '招待先') on conflict (id) do update set email = excluded.email, display_name = excluded.display_name");
    const invitationRoute = await import("../../src/app/api/household-invitations/route.ts");
    const acceptRoute = await import("../../src/app/api/household-invitations/accept/route.ts");
    const created = await invitationRoute.POST(new Request("http://localhost/api/household-invitations", {
      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ expiresInDays: 3 }),
    }));
    assert.equal(created.status, 201);
    const createdBody = await created.json();

    const accepted = await acceptRoute.POST(new Request("http://localhost/api/household-invitations/accept", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token: createdBody.invitation.token, userId: "33333333-3333-4333-8333-333333333333", displayName: "招待先" }),
    }));
    assert.equal(accepted.status, 201);
    const memberId = (await accepted.json()).memberId;
    assert.deepEqual((await pool.query<{ display_name: string; role: string }>("select display_name, role::text from household_members where id = $1", [memberId])).rows, [{ display_name: "招待先", role: "partner" }]);
    assert.deepEqual((await pool.query<{ accepted_by_user_id: string; accepted: boolean }>("select accepted_by_user_id::text, accepted_at is not null as accepted from household_invitations where id = $1", [createdBody.invitation.id])).rows, [{ accepted_by_user_id: "33333333-3333-4333-8333-333333333333", accepted: true }]);

    const reused = await acceptRoute.POST(new Request("http://localhost/api/household-invitations/accept", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token: createdBody.invitation.token, userId: "33333333-3333-4333-8333-333333333333", displayName: "招待先" }),
    }));
    assert.equal(reused.status, 404);

    const secondInvitation = await invitationRoute.POST(new Request("http://localhost/api/household-invitations", {
      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ expiresInDays: 3 }),
    }));
    const missingUser = await acceptRoute.POST(new Request("http://localhost/api/household-invitations/accept", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token: (await secondInvitation.json()).invitation.token, userId: "44444444-4444-4444-8444-444444444444", displayName: "未登録" }),
    }));
    assert.equal(missingUser.status, 404);
  });

'''
assert needle in text
text = text.replace(needle, test + needle)
path.write_text(text)

security = Path('Docs/api-security-spec.md')
text = security.read_text()
marker = '### 5.20. 精算取り消し (`POST /api/settlements/reopen`)\n'
section = '''### 5.20. 世帯招待受諾 (`POST /api/household-invitations/accept`)\n\n招待トークンをsha256ハッシュ化して照合し、有効期限内・未受諾・未無効化の招待だけを受諾する。受諾時に `household_members` へ `partner` として追加し、招待へ受諾ユーザーと受諾日時を記録する。受諾済みトークンの再利用は拒否する。\n\n'''
assert marker in text
text = text.replace(marker, section + '### 5.21. 精算取り消し (`POST /api/settlements/reopen`)\n')
security.write_text(text)

product = Path('Docs/product-spec.md')
text = product.read_text()
text = text.replace('- 招待の作成、一覧、無効化APIを提供する\n', '- 招待の作成、一覧、無効化、受諾APIを提供する\n')
text = text.replace('- 招待の作成、一覧、無効化APIは提供するが、ログインUI、招待受諾、未認証拒否、世帯単位のアクセス制御は未実装\n', '- 招待の作成、一覧、無効化、受諾APIは提供するが、ログインUI、未認証拒否、世帯単位のアクセス制御は未実装\n')
product.write_text(text)
PY
git diff --check
git diff --stat
python3 <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
needle = '''    assert.deepEqual((await pool.query<{ accepted_by_user_id: string; accepted: boolean }>("select accepted_by_user_id::text, accepted_at is not null as accepted from household_invitations where id = $1", [createdBody.invitation.id])).rows, [{ accepted_by_user_id: "33333333-3333-4333-8333-333333333333", accepted: true }]);

    const reused = await acceptRoute.POST(new Request("http://localhost/api/household-invitations/accept", {
'''
replacement = '''    assert.deepEqual((await pool.query<{ accepted_by_user_id: string; accepted: boolean }>("select accepted_by_user_id::text, accepted_at is not null as accepted from household_invitations where id = $1", [createdBody.invitation.id])).rows, [{ accepted_by_user_id: "33333333-3333-4333-8333-333333333333", accepted: true }]);
    await pool.query("delete from household_members where id = $1", [memberId]);

    const reused = await acceptRoute.POST(new Request("http://localhost/api/household-invitations/accept", {
'''
assert needle in text
path.write_text(text.replace(needle, replacement))
PY
docker compose exec -T db /docker-entrypoint-initdb.d/002_prepare_local_environments.sh
git diff --check
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select (select count(*) from household_invitations where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa') || ':' || (select count(*) from household_members where user_id = '33333333-3333-4333-8333-333333333333') || ':' || (select count(*) from app_users where id = '33333333-3333-4333-8333-333333333333');"
git add Docs/api-security-spec.md Docs/product-spec.md tests/integration/api.integration.test.ts src/app/api/household-invitations/accept/route.ts && git diff --cached --stat && git commit -m "feat: 世帯招待の受諾APIを追加する" -m "- accept active invitation tokens by sha256 hash\n- add invited users as household partners transactionally\n- prevent accepted token reuse with integration tests" && git status --short --branch
sed -n '1380,1485p' src/components/kakeibo-dashboard.tsx && sed -n '180,240p' src/components/kakeibo-dashboard.tsx
grep -n "settings\|sharedWalletError\|recurringError" src/app/globals.css | sed -n '1,160p' && sed -n '520,760p' src/app/globals.css
python3 <<'PY'
from pathlib import Path
path = Path('src/components/kakeibo-dashboard.tsx')
text = path.read_text()
text = text.replace('type PaydayReminder = { member_id: string; member_name: string; payday_day: number; days_until_payday: number };\n\ntype SharedWalletContributions = {', 'type PaydayReminder = { member_id: string; member_name: string; payday_day: number; days_until_payday: number };\ntype HouseholdInvitation = { id: string; invited_by: string; expires_at: string; accepted_at: string | null; revoked_at: string | null; created_at: string; status: "active" | "expired" | "accepted" | "revoked" };\n\ntype SharedWalletContributions = {')
text = text.replace('  const [sharedWalletMemberPlanDrafts, setSharedWalletMemberPlanDrafts] = useState<Record<string, string>>({});\n  const composerInputRef = useRef<HTMLInputElement>(null);', '  const [sharedWalletMemberPlanDrafts, setSharedWalletMemberPlanDrafts] = useState<Record<string, string>>({});\n  const [householdInvitations, setHouseholdInvitations] = useState<HouseholdInvitation[]>([]);\n  const [isHouseholdInvitationsLoading, setIsHouseholdInvitationsLoading] = useState(true);\n  const [householdInvitationError, setHouseholdInvitationError] = useState<string | null>(null);\n  const [createdInvitationToken, setCreatedInvitationToken] = useState("");\n  const [householdInvitationRefresh, setHouseholdInvitationRefresh] = useState(0);\n  const composerInputRef = useRef<HTMLInputElement>(null);')
needle = '''  useEffect(() => {
    let active = true;
    void fetch("/api/household-members")
      .then((response) => { if (!response.ok) throw new Error("household_members_load_failed"); return response.json() as Promise<{ members: HouseholdMemberSettings[]; reminders: PaydayReminder[] }>; })
      .then((data) => { if (active) { setHouseholdMemberSettings(data.members); setPaydayReminders(data.reminders); } })
      .catch(() => { if (active) setSharedWalletError("メンバー設定を読み込めませんでした。"); });
    return () => { active = false; };
  }, []);

  const updateMemberPayday = async (memberId: string) => {
'''
insert = '''  useEffect(() => {
    let active = true;
    void fetch("/api/household-members")
      .then((response) => { if (!response.ok) throw new Error("household_members_load_failed"); return response.json() as Promise<{ members: HouseholdMemberSettings[]; reminders: PaydayReminder[] }>; })
      .then((data) => { if (active) { setHouseholdMemberSettings(data.members); setPaydayReminders(data.reminders); } })
      .catch(() => { if (active) setSharedWalletError("メンバー設定を読み込めませんでした。"); });
    return () => { active = false; };
  }, []);

  useEffect(() => {
    let active = true;
    setIsHouseholdInvitationsLoading(true);
    setHouseholdInvitationError(null);
    void fetch("/api/household-invitations")
      .then((response) => { if (!response.ok) throw new Error("household_invitations_load_failed"); return response.json() as Promise<{ invitations: HouseholdInvitation[] }>; })
      .then((data) => { if (active) setHouseholdInvitations(data.invitations); })
      .catch(() => { if (active) { setHouseholdInvitations([]); setHouseholdInvitationError("招待を読み込めませんでした。"); } })
      .finally(() => { if (active) setIsHouseholdInvitationsLoading(false); });
    return () => { active = false; };
  }, [householdInvitationRefresh]);

  const createHouseholdInvitation = async () => {
    setHouseholdInvitationError(null);
    setCreatedInvitationToken("");
    try {
      const response = await fetch("/api/household-invitations", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({}) });
      if (!response.ok) throw new Error("household_invitation_create_failed");
      const data = await response.json() as { invitation: { token: string } };
      setCreatedInvitationToken(data.invitation.token);
      setHouseholdInvitationRefresh((current) => current + 1);
    } catch { setHouseholdInvitationError("招待を作成できませんでした。"); }
  };

  const revokeHouseholdInvitation = async (invitationId: string) => {
    setHouseholdInvitationError(null);
    try {
      const response = await fetch("/api/household-invitations", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ invitationId, action: "revoke" }) });
      if (!response.ok) throw new Error("household_invitation_revoke_failed");
      setHouseholdInvitationRefresh((current) => current + 1);
    } catch { setHouseholdInvitationError("招待を無効化できませんでした。"); }
  };

  const updateMemberPayday = async (memberId: string) => {
'''
assert needle in text
text = text.replace(needle, insert)
marker = '''        <article className="panel settings-panel payday-settings">
          <h3>給料日と収入</h3>
'''
panel = '''        <article className="panel settings-panel invitation-settings">
          <h3>パートナー招待</h3>
          <p className="recurring-empty">招待トークンは作成時だけ表示します。相手に共有したあとは、不要になった招待を無効化できます。</p>
          <div className="shared-wallet-settings__grid">
            <button className="chip chip--active" type="button" onClick={() => void createHouseholdInvitation()}>招待を作成</button>
            {createdInvitationToken ? <input aria-label="招待トークン" readOnly value={createdInvitationToken} /> : null}
          </div>
          {householdInvitationError ? <p className="recurring-empty">{householdInvitationError}</p> : null}
          {isHouseholdInvitationsLoading ? <p className="recurring-empty">読み込み中...</p> : householdInvitations.length === 0 ? <p className="recurring-empty">有効な招待はまだありません。</p> : (
            <div className="recurring-list invitation-list">
              {householdInvitations.map((invitation) => <article key={invitation.id} className="recurring-card"><div><strong>{invitation.status === "active" ? "招待中" : invitation.status === "accepted" ? "受諾済み" : invitation.status === "revoked" ? "無効化済み" : "期限切れ"}</strong><p>{new Date(invitation.expires_at).toLocaleDateString("ja-JP")}まで ・ 作成者 {invitation.invited_by}</p></div>{invitation.status === "active" ? <button className="chip" type="button" onClick={() => void revokeHouseholdInvitation(invitation.id)}>無効化</button> : <span className="pill pill--muted">{invitation.status}</span>}</article>)}
            </div>
          )}
        </article>
'''
assert marker in text
text = text.replace(marker, panel + marker)
path.write_text(text)
PY
git diff --check
git diff --stat
python3 <<'PY'
from pathlib import Path
screens = Path('Docs/screens.md')
text = screens.read_text()
marker = '- 共通口座設定: 月次目標額、メンバー別予定額、給料日、非公開収入額\n'
if marker in text:
    text = text.replace(marker, marker + '- パートナー招待: 招待作成、作成時のみのトークン表示、招待一覧、無効化\n')
else:
    fallback = '## 設定\n'
    assert fallback in text
    text = text.replace(fallback, fallback + '\n- パートナー招待: 招待作成、作成時のみのトークン表示、招待一覧、無効化\n')
screens.write_text(text)

product = Path('Docs/product-spec.md')
text = product.read_text()
text = text.replace('- 招待の作成、一覧、無効化、受諾APIを提供する\n', '- 招待の作成、一覧、無効化、受諾APIを提供する\n- 設定画面から招待を作成し、作成時だけ招待トークンを表示する\n')
product.write_text(text)

e2e = Path('tests/e2e/kakeibo.spec.ts')
text = e2e.read_text()
needle = '''test("分析ページで共通口座への入金状況を確認する", async ({ page }) => {
'''
test = '''test("設定画面でパートナー招待を作成して無効化する", async ({ page }) => {
  await page.goto("/");

  await page.getByRole("button", { name: "設定", exact: true }).click();
  const panel = page.locator(".invitation-settings");
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/household-invitations") && response.request().method() === "POST" && response.ok()),
    panel.getByRole("button", { name: "招待を作成", exact: true }).click(),
  ]);
  await expect(panel.getByLabel("招待トークン")).toHaveValue(/[A-Za-z0-9_-]{40,}/);
  await expect(panel).toContainText("招待中");
  await Promise.all([
    page.waitForResponse((response) => response.url().endsWith("/api/household-invitations") && response.request().method() === "PATCH" && response.ok()),
    panel.getByRole("button", { name: "無効化", exact: true }).click(),
  ]);
  await expect(panel).toContainText("無効化済み");
});

'''
assert needle in text
if '設定画面でパートナー招待を作成して無効化する' not in text:
    text = text.replace(needle, test + needle)
e2e.write_text(text)
PY
git diff --check
git diff --stat
npm run test:e2e -- --grep '設定画面でパートナー招待'
git add Docs/product-spec.md Docs/screens.md src/components/kakeibo-dashboard.tsx tests/e2e/kakeibo.spec.ts && git diff --cached --stat && git commit -m "feat: 設定画面に世帯招待管理を追加する" -m "- add invitation create/list/revoke controls to settings\n- show raw invitation token only immediately after creation\n- cover invitation management with Playwright smoke test" && git status --short --branch
git status --short --branch && git log --oneline --max-count=12
gh issue view 13 --json number,title,state,body,url && gh issue view 3 --json number,title,state,body,url
gh issue list --state open --limit 20 --json number,title,url
gh issue close 13 --comment "Implemented and pushed the shared wallet payday flow.\n\nCovered:\n- member payday registration\n- private income amount updates and share-rate recalculation\n- shared wallet proposed amount / planned amount / completion status\n- payday +/-3 day reminder candidates on home\n- income visibility documented as private\n\nPush notification delivery remains outside the current MVP boundary and is documented as unimplemented."
sed -n '1,220p' src/app/api/household-invitations/route.ts && sed -n '1,180p' src/app/api/household-invitations/accept/route.ts
sed -n '1,140p' tests/integration/api.integration.test.ts && sed -n '240,340p' tests/integration/api.integration.test.ts
sed -n '1,220p' Docs/api-security-spec.md && git status --short --branch
python3 <<'PY'
from pathlib import Path
Path('src/lib/household-context.ts').write_text('''import type { QueryResult } from "pg";

type Queryable = {
  query<T>(text: string, values?: unknown[]): Promise<QueryResult<T>>;
};

export type HouseholdContext = {
  householdId: string;
  memberId: string;
  userId: string | null;
};

function userIdFromCookie(cookieHeader: string | null) {
  if (!cookieHeader) return null;
  return cookieHeader.split(";").map((part) => part.trim()).find((part) => part.startsWith("kakeibo_user_id="))?.slice("kakeibo_user_id=".length) ?? null;
}

export function userIdFromRequest(request: Request) {
  return request.headers.get("x-kakeibo-user-id") ?? userIdFromCookie(request.headers.get("cookie"));
}

export async function resolveHouseholdContext(queryable: Queryable, request: Request): Promise<HouseholdContext | null> {
  const userId = userIdFromRequest(request);
  if (userId) {
    const member = (await queryable.query<{ household_id: string; member_id: string }>(`
      select household_id::text, id::text as member_id
      from household_members
      where user_id = $1
      order by role asc, created_at asc
      limit 1
    `, [userId])).rows[0];
    return member ? { householdId: member.household_id, memberId: member.member_id, userId } : null;
  }

  const demoMember = (await queryable.query<{ household_id: string; member_id: string }>(`
    select household_id::text, id::text as member_id
    from household_members
    order by role asc, created_at asc
    limit 1
  `)).rows[0];
  return demoMember ? { householdId: demoMember.household_id, memberId: demoMember.member_id, userId: null } : null;
}
''')

route = Path('src/app/api/household-invitations/route.ts')
text = route.read_text()
text = text.replace('import { db } from "../../../lib/db.ts";\n', 'import { db } from "../../../lib/db.ts";\nimport { resolveHouseholdContext } from "../../../lib/household-context.ts";\n')
text = text.replace('const householdQuery = `select id from households order by created_at asc limit 1`;\nconst inviterQuery = `select id from household_members where household_id = $1 and role = \'owner\' order by created_at asc limit 1`;\n', '')
text = text.replace('export async function GET() {\n  try {\n    const pool = db();\n    const household = (await pool.query<{ id: string }>(householdQuery)).rows[0];\n    if (!household) return NextResponse.json({ invitations: [] });\n    const invitations = (await pool.query<InvitationRow>(invitationsQuery, [household.id])).rows;\n', 'export async function GET(request: Request) {\n  try {\n    const pool = db();\n    const context = await resolveHouseholdContext(pool, request);\n    if (!context) return NextResponse.json({ error: "household_not_found" }, { status: 404 });\n    const invitations = (await pool.query<InvitationRow>(invitationsQuery, [context.householdId])).rows;\n')
text = text.replace('    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];\n    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }\n    const inviter = (await client.query<{ id: string }>(inviterQuery, [household.id])).rows[0];\n    if (!inviter) { await client.query("rollback"); return NextResponse.json({ error: "household_inviter_not_found" }, { status: 404 }); }\n', '    const context = await resolveHouseholdContext(client, request);\n    if (!context) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }\n')
text = text.replace('    `, [household.id, tokenHash, inviter.id, parsed.data.expiresInDays])).rows[0];', '    `, [context.householdId, tokenHash, context.memberId, parsed.data.expiresInDays])).rows[0];')
text = text.replace('    const household = (await client.query<{ id: string }>(householdQuery)).rows[0];\n    if (!household) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }\n', '    const context = await resolveHouseholdContext(client, request);\n    if (!context) { await client.query("rollback"); return NextResponse.json({ error: "household_not_found" }, { status: 404 }); }\n')
text = text.replace('    `, [parsed.data.invitationId, household.id])).rows[0];', '    `, [parsed.data.invitationId, context.householdId])).rows[0];')
route.write_text(text)

security = Path('Docs/api-security-spec.md')
text = security.read_text()
text = text.replace('  - API ルートで常に `limit 1` の最古の世帯IDを取得して処理しているため、マルチテナントとしての隔離がされておらず、本番環境への投入は不可能。', '  - 招待管理APIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。\n  - その他のAPIルートではまだ `limit 1` の最古の世帯IDを取得しているため、マルチテナントとしての隔離が未完了。')
text = text.replace('  すべての API ルートで、決め打ちのクエリを上記のようにログインユーザーのセッション ID（`$1`）に基づく所属世帯チェックに切り替える。', '  すべての API ルートで、決め打ちのクエリを `resolveHouseholdContext` ベースの所属世帯チェックに切り替える。')
security.write_text(text)
PY
git diff --check
git diff --stat
sed -n '1,220p' src/lib/household-context.ts && sed -n '1,180p' src/app/api/household-invitations/route.ts
grep -n "creates and revokes household invitations" -A45 tests/integration/api.integration.test.ts && git status --short --branch
python3 <<'PY'
from pathlib import Path
path = Path('tests/integration/api.integration.test.ts')
text = path.read_text()
text = text.replace('    await pool.query("delete from household_members where user_id = \'33333333-3333-4333-8333-333333333333\'");\n    await pool.query("delete from app_users where id = \'33333333-3333-4333-8333-333333333333\'");\n', '    await pool.query("delete from household_members where user_id = \'33333333-3333-4333-8333-333333333333\'");\n    await pool.query("delete from households where id = \'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa\'");\n    await pool.query("delete from app_users where id in (\'33333333-3333-4333-8333-333333333333\', \'55555555-5555-4555-8555-555555555555\')");\n')
needle = '''    const listed = await GET();
    assert.equal(listed.status, 200);
    const listedInvitation = (await listed.json()).invitations.find((invitation: { id: string }) => invitation.id === createdBody.invitation.id);
    assert.ok(listedInvitation);
    assert.equal(listedInvitation.status, "active");
    assert.equal(listedInvitation.token, undefined);
    assert.equal(listedInvitation.token_hash, undefined);

    const revoked = await PATCH(new Request("http://localhost/api/household-invitations", {
'''
replacement = '''    const listed = await GET(new Request("http://localhost/api/household-invitations"));
    assert.equal(listed.status, 200);
    const listedInvitation = (await listed.json()).invitations.find((invitation: { id: string }) => invitation.id === createdBody.invitation.id);
    assert.ok(listedInvitation);
    assert.equal(listedInvitation.status, "active");
    assert.equal(listedInvitation.token, undefined);
    assert.equal(listedInvitation.token_hash, undefined);

    await pool.query("insert into app_users (id, email, display_name) values ('55555555-5555-4555-8555-555555555555', 'other@example.local', '別世帯') on conflict (id) do update set email = excluded.email, display_name = excluded.display_name");
    await pool.query("insert into households (id, name, default_split_type) values ('bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '別世帯', 'equal') on conflict (id) do update set name = excluded.name");
    await pool.query("insert into household_members (id, household_id, user_id, display_name, role) values ('bbbbbbbb-1111-4111-8111-aaaaaaaa1111', 'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '55555555-5555-4555-8555-555555555555', '別世帯', 'owner') on conflict (household_id, user_id) do update set display_name = excluded.display_name");
    const listedOther = await GET(new Request("http://localhost/api/household-invitations", { headers: { "x-kakeibo-user-id": "55555555-5555-4555-8555-555555555555" } }));
    assert.equal(listedOther.status, 200);
    assert.deepEqual(await listedOther.json(), { invitations: [] });
    const createdOther = await POST(new Request("http://localhost/api/household-invitations", {
      method: "POST", headers: { "Content-Type": "application/json", "x-kakeibo-user-id": "55555555-5555-4555-8555-555555555555" }, body: JSON.stringify({ expiresInDays: 3 }),
    }));
    assert.equal(createdOther.status, 201);
    const otherInvitationId = (await createdOther.json()).invitation.id;
    assert.deepEqual((await pool.query<{ household_id: string }>("select household_id::text from household_invitations where id = $1", [otherInvitationId])).rows, [{ household_id: "bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa" }]);
    const crossRevoke = await PATCH(new Request("http://localhost/api/household-invitations", {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ invitationId: otherInvitationId, action: "revoke" }),
    }));
    assert.equal(crossRevoke.status, 404);
    const missingUser = await GET(new Request("http://localhost/api/household-invitations", { headers: { "x-kakeibo-user-id": "44444444-4444-4444-8444-444444444444" } }));
    assert.equal(missingUser.status, 404);

    const revoked = await PATCH(new Request("http://localhost/api/household-invitations", {
'''
assert needle in text
path.write_text(text.replace(needle, replacement))
PY
git diff --check
git diff --stat
python3 <<'PY'
from pathlib import Path
path = Path('src/lib/household-context.ts')
text = path.read_text()
text = text.replace('import type { QueryResult } from "pg";\n', 'import type { QueryResult, QueryResultRow } from "pg";\n')
text = text.replace('  query<T>(text: string, values?: unknown[]): Promise<QueryResult<T>>;\n', '  query<T extends QueryResultRow>(text: string, values?: unknown[]): Promise<QueryResult<T>>;\n')
path.write_text(text)
PY
git diff --check
npm run build
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select (select count(*) from household_invitations where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa') || ':' || (select count(*) from households where id = 'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa') || ':' || (select count(*) from app_users where id = '55555555-5555-4555-8555-555555555555');"
git add Docs/api-security-spec.md src/app/api/household-invitations/route.ts src/lib/household-context.ts tests/integration/api.integration.test.ts && git diff --cached --stat && git commit -m "feat: 招待APIを所属世帯ベースに切り替える" -m "- add request-based household context resolver\n- scope invitation list/create/revoke by resolved household\n- cover cross-household invitation isolation in integration tests" && git status --short --branch
git status --short --branch && gh issue view 13 --json number,state,title
sed -n '1,220p' src/app/api/dashboard/route.ts
grep -n "filters dashboard expenses" -A35 tests/integration/api.integration.test.ts && sed -n '1,90p' tests/integration/api.integration.test.ts
python3 <<'PY'
from pathlib import Path
route = Path('src/app/api/dashboard/route.ts')
text = route.read_text()
text = text.replace('import { db } from "../../../lib/db.ts";\n', 'import { db } from "../../../lib/db.ts";\nimport { resolveHouseholdContext } from "../../../lib/household-context.ts";\n')
old = '''// TODO(security): 本格的な認証の統合時に、ログインしているユーザーのセッションから所属世帯（household_id）を正しく参照するように変更する。
// 現状はMVP用の暫定仕様として、DB上で最古の世帯を暗黙的に選択しています。
const householdQuery = `
  select id
  from households
  order by created_at asc
  limit 1
`;

'''
assert old in text
text = text.replace(old, '')
old = '''    const pool = db();
    const householdResult = await pool.query<{ id: string }>(householdQuery);
    const household = householdResult.rows[0];

    if (!household) {
      return NextResponse.json({ members: [], expenses: [], period: { ...period, status: "open" } });
    }

    const [memberResult, expenseResult, settlementPeriodResult, settlementPeriodEventsResult] = await Promise.all([
      pool.query<DashboardMemberRow>(membersQuery, [household.id]),
      pool.query<DashboardExpenseRow>(expensesQuery, [household.id, period.year, period.month]),
      pool.query<{ status: SettlementPeriodStatus }>(settlementPeriodQuery, [household.id, period.year, period.month]),
      pool.query<SettlementPeriodEventRow>(settlementPeriodEventsQuery, [household.id, period.year, period.month]),
    ]);
'''
new = '''    const pool = db();
    const context = await resolveHouseholdContext(pool, request);

    if (!context) {
      return NextResponse.json({ error: "household_not_found" }, { status: 404 });
    }

    const [memberResult, expenseResult, settlementPeriodResult, settlementPeriodEventsResult] = await Promise.all([
      pool.query<DashboardMemberRow>(membersQuery, [context.householdId]),
      pool.query<DashboardExpenseRow>(expensesQuery, [context.householdId, period.year, period.month]),
      pool.query<{ status: SettlementPeriodStatus }>(settlementPeriodQuery, [context.householdId, period.year, period.month]),
      pool.query<SettlementPeriodEventRow>(settlementPeriodEventsQuery, [context.householdId, period.year, period.month]),
    ]);
'''
assert old in text
route.write_text(text.replace(old, new))

test = Path('tests/integration/api.integration.test.ts')
text = test.read_text()
old = '''    const body = await response.json();
    assert.deepEqual(body.expenses.map((expense: { id: string }) => expense.id), [testExpenseId]);

    const invalidResponse = await GET(new Request("http://localhost/api/dashboard?year=2098&month=13"));
'''
new = '''    const body = await response.json();
    assert.deepEqual(body.expenses.map((expense: { id: string }) => expense.id), [testExpenseId]);

    await pool.query("insert into app_users (id, email, display_name) values ('55555555-5555-4555-8555-555555555555', 'other@example.local', '別世帯') on conflict (id) do update set email = excluded.email, display_name = excluded.display_name");
    await pool.query("insert into households (id, name, default_split_type) values ('bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '別世帯', 'equal') on conflict (id) do update set name = excluded.name");
    await pool.query("insert into household_members (id, household_id, user_id, display_name, role) values ('bbbbbbbb-1111-4111-8111-aaaaaaaa1111', 'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '55555555-5555-4555-8555-555555555555', '別世帯', 'owner') on conflict (household_id, user_id) do update set display_name = excluded.display_name");
    const otherResponse = await GET(new Request(`http://localhost/api/dashboard?year=${testPeriod.year}&month=${testPeriod.month}`, { headers: { "x-kakeibo-user-id": "55555555-5555-4555-8555-555555555555" } }));
    assert.equal(otherResponse.status, 200);
    const otherBody = await otherResponse.json();
    assert.deepEqual(otherBody.members, [{ id: "bbbbbbbb-1111-4111-8111-aaaaaaaa1111", display_name: "別世帯" }]);
    assert.deepEqual(otherBody.expenses, []);
    const missingUserResponse = await GET(new Request(`http://localhost/api/dashboard?year=${testPeriod.year}&month=${testPeriod.month}`, { headers: { "x-kakeibo-user-id": "44444444-4444-4444-8444-444444444444" } }));
    assert.equal(missingUserResponse.status, 404);

    const invalidResponse = await GET(new Request("http://localhost/api/dashboard?year=2098&month=13"));
'''
assert old in text
test.write_text(text.replace(old, new))

security = Path('Docs/api-security-spec.md')
text = security.read_text()
text = text.replace('- 招待管理APIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。', '- 招待管理APIとダッシュボードAPIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。')
security.write_text(text)
PY
git diff --check
git diff --stat
docker compose exec -T db psql -U kakeibo -d kakeibo_test -Atc "select (select count(*) from households where id = 'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa') || ':' || (select count(*) from app_users where id = '55555555-5555-4555-8555-555555555555') || ':' || (select count(*) from household_invitations where household_id = 'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa');"
git add Docs/api-security-spec.md src/app/api/dashboard/route.ts tests/integration/api.integration.test.ts && git diff --cached --stat && git commit -m "feat: ダッシュボードAPIを所属世帯ベースに切り替える" -m "- resolve dashboard household from request user context\n- preserve demo fallback when no user context is provided\n- cover dashboard cross-household isolation in integration tests" && git status --short --branch
sed -n '1,240p' src/app/api/expenses/route.ts
grep -n "expense creation" -A42 tests/integration/api.integration.test.ts && grep -n "persists expense scope" -A35 tests/integration/api.integration.test.ts && git status --short --branch
python3 <<'PY'
from pathlib import Path
route = Path('src/app/api/expenses/route.ts')
text = route.read_text()
text = text.replace('import { db } from "../../../lib/db.ts";\n', 'import { db } from "../../../lib/db.ts";\nimport { resolveHouseholdContext } from "../../../lib/household-context.ts";\n')
text = text.replace('type HouseholdRow = { id: string };\n', '')
old = '''// TODO(security): 本格的な認証の統合時に、ログインしているユーザーのセッションから所属世帯（household_id）を正しく参照するように変更する。
// 現状はMVP用の暫定仕様として、DB上で最古の世帯を暗黙的に選択しています。
const householdQuery = `
  select id
  from households
  order by created_at asc
  limit 1
`;

'''
assert old in text
text = text.replace(old, '')
old = '''      const householdResult = await client.query<HouseholdRow>(householdQuery);
      const household = householdResult.rows[0];
      if (!household) {
        await client.query("rollback");
        return NextResponse.json({ error: "household_not_found" }, { status: 404 });
      }

      const period = (await client.query<SettlementPeriodRow>(currentPeriodSettledQuery, [household.id])).rows[0];
'''
new = '''      const context = await resolveHouseholdContext(client, request);
      if (!context) {
        await client.query("rollback");
        return NextResponse.json({ error: "household_not_found" }, { status: 404 });
      }

      const period = (await client.query<SettlementPeriodRow>(currentPeriodSettledQuery, [context.householdId])).rows[0];
'''
assert old in text
text = text.replace(old, new)
text = text.replace('client.query<DashboardMemberRow>(membersQuery, [household.id])', 'client.query<DashboardMemberRow>(membersQuery, [context.householdId])')
text = text.replace('client.query<CategoryRow>(categoryQuery, [household.id, category])', 'client.query<CategoryRow>(categoryQuery, [context.householdId, category])')
text = text.replace('client.query<CategoryRow>(createCategoryQuery, [household.id, category, scope])', 'client.query<CategoryRow>(createCategoryQuery, [context.householdId, category, scope])')
text = text.replace('client.query<PaymentSourceRow>(paymentSourceQuery, [household.id, payer.id])', 'client.query<PaymentSourceRow>(paymentSourceQuery, [context.householdId, payer.id])')
text = text.replace('client.query<PaymentSourceRow>(fallbackPaymentSourceQuery, [household.id])', 'client.query<PaymentSourceRow>(fallbackPaymentSourceQuery, [context.householdId])')
text = text.replace('        household.id,\n', '        context.householdId,\n')
route.write_text(text)

test = Path('tests/integration/api.integration.test.ts')
text = test.read_text()
needle = '''  it("rejects expense creation for a settled current period", async () => {
'''
insert = '''  it("creates expenses in the request user household", async () => {
    const { POST } = await import("../../src/app/api/expenses/route.ts");
    await pool.query("insert into app_users (id, email, display_name) values ('55555555-5555-4555-8555-555555555555', 'other@example.local', '別世帯') on conflict (id) do update set email = excluded.email, display_name = excluded.display_name");
    await pool.query("insert into households (id, name, default_split_type) values ('bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '別世帯', 'equal') on conflict (id) do update set name = excluded.name");
    await pool.query("insert into household_members (id, household_id, user_id, display_name, role) values ('bbbbbbbb-1111-4111-8111-aaaaaaaa1111', 'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '55555555-5555-4555-8555-555555555555', '別世帯', 'owner') on conflict (household_id, user_id) do update set display_name = excluded.display_name");
    await pool.query("insert into payment_sources (id, household_id, name, type, owner_member_id) values ('bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbb9', 'bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa', '別世帯カード', 'member_personal', 'bbbbbbbb-1111-4111-8111-aaaaaaaa1111') on conflict (id) do update set name = excluded.name");

    const created = await POST(new Request("http://localhost/api/expenses", {
      method: "POST",
      headers: { "Content-Type": "application/json", "x-kakeibo-user-id": "55555555-5555-4555-8555-555555555555" },
      body: JSON.stringify({ title: "別世帯支出", amount: 2500, paidBy: "a", scope: "personal", category: "テスト" }),
    }));
    assert.equal(created.status, 201);
    const expenseId = (await created.json()).expense.id;
    assert.deepEqual((await pool.query<{ household_id: string; memo: string }>("select household_id::text, memo from expenses where id = $1", [expenseId])).rows, [{ household_id: "bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa", memo: "別世帯支出" }]);

    const missingUser = await POST(new Request("http://localhost/api/expenses", {
      method: "POST",
      headers: { "Content-Type": "application/json", "x-kakeibo-user-id": "44444444-4444-4444-8444-444444444444" },
      body: JSON.stringify({ title: "存在しないユーザー", amount: 1200, paidBy: "a", scope: "personal", category: "テスト" }),
    }));
    assert.equal(missingUser.status, 404);
  });

'''
assert needle in text
text = text.replace(needle, insert + needle)
test.write_text(text)

security = Path('Docs/api-security-spec.md')
text = security.read_text()
text = text.replace('- 招待管理APIとダッシュボードAPIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。', '- 招待管理API、ダッシュボードAPI、支出作成APIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。')
security.write_text(text)
PY
git diff --check
git diff --stat
git add Docs/api-security-spec.md src/app/api/expenses/route.ts tests/integration/api.integration.test.ts && git diff --cached --stat && git commit -m "feat: 支出作成APIを所属世帯ベースに切り替える" -m "- resolve expense creation household from request user context\n- preserve demo fallback when no user context is provided\n- cover expense creation isolation in integration tests" && git status --short --branch
sed -n '1,280p' src/app/api/expenses/[id]/route.ts
grep -n "persists expense scope" -A90 tests/integration/api.integration.test.ts && grep -n "creates expenses in the request user household" -A40 tests/integration/api.integration.test.ts
python3 <<'PY'
from pathlib import Path
route = Path('src/app/api/expenses/[id]/route.ts')
text = route.read_text()
text = text.replace('import { db } from "../../../../lib/db.ts";\n', 'import { db } from "../../../../lib/db.ts";\nimport { resolveHouseholdContext } from "../../../../lib/household-context.ts";\n')
text = text.replace('type HouseholdRow = { id: string };\n', '')
old = '''// TODO(security): 本格的な認証の統合時に、ログインしているユーザーのセッションから所属世帯（household_id）を正しく参照するように変更する。
// 現状はMVP用の暫定仕様として、DB上で最古の世帯を暗黙的に選択しています。
const householdQuery = `
  select id
  from households
  order by created_at asc
  limit 1
`;

'''
assert old in text
text = text.replace(old, '')
old = '''      const household = (await client.query<HouseholdRow>(householdQuery)).rows[0];
      if (!household) {
        await client.query("rollback");
        return NextResponse.json({ error: "household_not_found" }, { status: 404 });
      }

      const expense = (await client.query<{ id: string; memo: string | null; expense_date: string }>(expenseQuery, [id, household.id])).rows[0];
'''
new = '''      const context = await resolveHouseholdContext(client, request);
      if (!context) {
        await client.query("rollback");
        return NextResponse.json({ error: "household_not_found" }, { status: 404 });
      }

      const expense = (await client.query<{ id: string; memo: string | null; expense_date: string }>(expenseQuery, [id, context.householdId])).rows[0];
'''
assert old in text
text = text.replace(old, new)
text = text.replace('settledPeriodQuery, [household.id, expense.expense_date, expenseDate ?? expense.expense_date]', 'settledPeriodQuery, [context.householdId, expense.expense_date, expenseDate ?? expense.expense_date]')
text = text.replace('client.query<MemberRow>(membersQuery, [household.id])', 'client.query<MemberRow>(membersQuery, [context.householdId])')
text = text.replace('client.query<CategoryRow>(categoryQuery, [household.id, category])', 'client.query<CategoryRow>(categoryQuery, [context.householdId, category])')
text = text.replace('client.query<CategoryRow>(createCategoryQuery, [household.id, category, scope])', 'client.query<CategoryRow>(createCategoryQuery, [context.householdId, category, scope])')
text = text.replace('client.query<PaymentSourceRow>(paymentSourceQuery, [household.id, payer.id])', 'client.query<PaymentSourceRow>(paymentSourceQuery, [context.householdId, payer.id])')
text = text.replace('client.query<PaymentSourceRow>(fallbackPaymentSourceQuery, [household.id])', 'client.query<PaymentSourceRow>(fallbackPaymentSourceQuery, [context.householdId])')
old = '''export async function DELETE(_request: Request, context: { params: Promise<{ id: string }> }) {
'''
new = '''export async function DELETE(request: Request, context: { params: Promise<{ id: string }> }) {
'''
assert old in text
text = text.replace(old, new)
old = '''    const pool = db();
    const household = (await pool.query<HouseholdRow>(householdQuery)).rows[0];
    if (!household) {
      return NextResponse.json({ error: "household_not_found" }, { status: 404 });
    }

    const expense = (await pool.query<{ id: string; expense_date: string }>(expenseQuery, [id, household.id])).rows[0];
'''
new = '''    const pool = db();
    const context = await resolveHouseholdContext(pool, request);
    if (!context) {
      return NextResponse.json({ error: "household_not_found" }, { status: 404 });
    }

    const expense = (await pool.query<{ id: string; expense_date: string }>(expenseQuery, [id, context.householdId])).rows[0];
'''
assert old in text
text = text.replace(old, new)
text = text.replace('settledPeriodQuery, [household.id, expense.expense_date, expense.expense_date]', 'settledPeriodQuery, [context.householdId, expense.expense_date, expense.expense_date]')
text = text.replace('deleteExpenseQuery, [id, household.id]', 'deleteExpenseQuery, [id, context.householdId]')
route.write_text(text)

test = Path('tests/integration/api.integration.test.ts')
text = test.read_text()
old = '''    assert.deepEqual((await pool.query<{ household_id: string; memo: string }>("select household_id::text, memo from expenses where id = $1", [expenseId])).rows, [{ household_id: "bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa", memo: "別世帯支出" }]);

    const missingUser = await POST(new Request("http://localhost/api/expenses", {
'''
new = '''    assert.deepEqual((await pool.query<{ household_id: string; memo: string }>("select household_id::text, memo from expenses where id = $1", [expenseId])).rows, [{ household_id: "bbbbbbbb-aaaa-4aaa-8aaa-aaaaaaaaaaaa", memo: "別世帯支出" }]);

    const expenseRoute = await import("../../src/app/api/expenses/[id]/route.ts");
    const crossPatch = await expenseRoute.PATCH(new Request(`http://localhost/api/expenses/${expenseId}`, {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ scope: "personal", title: "越境編集" }),
    }), { params: Promise.resolve({ id: expenseId }) });
    assert.equal(crossPatch.status, 404);
    const edited = await expenseRoute.PATCH(new Request(`http://localhost/api/expenses/${expenseId}`, {
      method: "PATCH", headers: { "Content-Type": "application/json", "x-kakeibo-user-id": "55555555-5555-4555-8555-555555555555" }, body: JSON.stringify({ scope: "personal", title: "別世帯編集後" }),
    }), { params: Promise.resolve({ id: expenseId }) });
    assert.equal(edited.status, 200);
    assert.deepEqual((await pool.query<{ memo: string }>("select memo from expenses where id = $1", [expenseId])).rows, [{ memo: "別世帯編集後" }]);
    const crossDelete = await expenseRoute.DELETE(new Request(`http://localhost/api/expenses/${expenseId}`, { method: "DELETE" }), { params: Promise.resolve({ id: expenseId }) });
    assert.equal(crossDelete.status, 404);
    const deleted = await expenseRoute.DELETE(new Request(`http://localhost/api/expenses/${expenseId}`, { method: "DELETE", headers: { "x-kakeibo-user-id": "55555555-5555-4555-8555-555555555555" } }), { params: Promise.resolve({ id: expenseId }) });
    assert.equal(deleted.status, 200);
    assert.equal((await pool.query("select id from expenses where id = $1", [expenseId])).rowCount, 0);

    const missingUser = await POST(new Request("http://localhost/api/expenses", {
'''
assert old in text
test.write_text(text.replace(old, new))

security = Path('Docs/api-security-spec.md')
text = security.read_text()
text = text.replace('- 招待管理API、ダッシュボードAPI、支出作成APIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。', '- 招待管理API、ダッシュボードAPI、支出作成・編集・削除APIでは `x-kakeibo-user-id` ヘッダーまたは `kakeibo_user_id` Cookie から所属世帯を解決する共通ヘルパーを使い始めている。')
security.write_text(text)
PY
git diff --check
git diff --stat
python3 <<'PY'
from pathlib import Path
path = Path('src/app/api/expenses/[id]/route.ts')
text = path.read_text()
text = text.replace('export async function DELETE(request: Request, context: { params: Promise<{ id: string }> }) {\n  try {\n    const { id } = await context.params;', 'export async function DELETE(request: Request, routeContext: { params: Promise<{ id: string }> }) {\n  try {\n    const { id } = await routeContext.params;')
path.write_text(text)
PY
git diff --check
git add Docs/api-security-spec.md src/app/api/expenses/[id]/route.ts tests/integration/api.integration.test.ts && git diff --cached --stat && git commit -m "feat: 支出編集削除APIを所属世帯ベースに切り替える" -m "- resolve expense update/delete household from request user context\n- reject cross-household update and delete requests\n- cover expense update/delete isolation in integration tests" && git status --short --branch
```

<!-- Conversation End: 019e7899-7332-7180-8c3d-49619e6b375d -->







---

<!-- Conversation Start: manual-1780401654183 -->
### 🤖 エージェント対話ログ (2026/06/02 21:00)

* **Conversation ID**: `manual-1780401654183`
* **作業スペース**: `kakeibo`

> [!NOTE]
> 対話アクティビティが記録されていないか、パースに失敗しました（フォールバック実行）。

<!-- Conversation End: manual-1780401654183 -->


---

<!-- Conversation Start: 9f93435b-a736-4a86-9d46-a1080225179d -->
### 🤖 エージェント対話ログ (2026/06/03 00:09)

* **Conversation ID**: `9f93435b-a736-4a86-9d46-a1080225179d`
* **作業スペース**: `uver-setlist-app`

#### 💬 ユーザーからの指示・要望
> <USER_REQUEST>
> 目的：
> サーバー上の開発記録を、private GitHub リポジトリ経由でローカル Obsidian から確認できるようにする。
> 
> Obsidian Vault：
> `/home/haya-ryoo/Obsidian`
> 
> 対象プロジェクト：
> `/home/haya-ryoo/dev/uver-setlist-app`
> 
> 要件：
> 1. Obsidian Vault の接続状況を確認する。
> 2. Vault の GitHub リモートが `PRIVATE` であることを `gh repo view` で確認する。
> 3. Vault の構成を以下に揃える。
> 
> ```text
> /home/haya-ryoo/Obsidian/
> ├── Daily/
> ├── Errors/
> │   ├── kakeibo/
> │   └── uver-setlist-app/
> └── Projects/
>     ├── kakeibo/
>     └── uver-setlist-app/
>         ├── README.md
>         └── Issues/
>             └── README.md
> 注意：
> 
> Errors/ は 1 つだけにする。
> 誤記の Erros/ を作らない。
> プロジェクトごとの記録は Errors/<project-name>/ と Projects/<project-name>/ に分ける。
> 空フォルダは Git に残らないため、必要なら README.md を置く。
> Agent Activity Logger の状態を確認する。
> 
> /home/haya-ryoo/dev/uver-setlist-app/.git/hooks/pre-push
> /home/haya-ryoo/.local/share/antigravity-logger/agent-logger.cjs
> /home/haya-ryoo/.local/share/antigravity-logger/install-logger-hook.cjs
> pre-push にロガーが未導入の場合のみ、対象プロジェクトのルートで以下を実行する。
> 
> node /home/haya-ryoo/.local/share/antigravity-logger/install-logger-hook.cjs
> Codex の会話 ID を認識できることを確認する。
> 
> CONVERSATION_ID
> CODEX_THREAD_ID
> pre-push が ${CONVERSATION_ID:-$CODEX_THREAD_ID} をロガーへ渡すこと
> 動作確認として、現在の会話 ID を使いロガーを手動実行する。
> 
> CONVERSATION_ID="${CONVERSATION_ID:-$CODEX_THREAD_ID}" \
> node /home/haya-ryoo/.local/share/antigravity-logger/agent-logger.cjs \
> --id "${CONVERSATION_ID:-$CODEX_THREAD_ID}"
> /home/haya-ryoo/Obsidian/Daily/YYYY-MM-DD.md に会話ログが保存されたことを確認する。
> 運用ルール：
> 
> Daily/: エージェントの会話ログ。自動保存する。
> Errors/uver-setlist-app/: 再利用できる障害対応を残す。
> Projects/uver-setlist-app/: 開発方針や判断事項を残す。
> Projects/uver-setlist-app/Issues/: 課題や仕様変更を 1 件 1 ファイルで残す。
> 会話ログをそのまま仕様書にせず、重要事項だけを Projects/ または Errors/ へ整理する。
> Vault は private リポジトリなので、Daily/ も同期対象にする。
> Git 操作：
> 
> 既存差分を勝手に変更しない。
> Vault に追加した運用ファイルと会話ログだけをコミットする。
> リモート更新がある場合は git pull --rebase origin main で取り込む。
> private であることを確認できた場合のみ origin/main へ push する。
> 出力：
> 
> 接続確認結果
> Logger の導入状況
> 追加・変更ファイル一覧
> 手動ログ生成の確認結果
> コミット ID
> push 結果
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-05-31T14:40:47+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 50
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Running terminal commands:
> - codex (in /home/haya-ryoo/dev/uver-setlist-app, running for 5h19m35s)
> </ADDITIONAL_METADATA>
> <USER_SETTINGS_CHANGE>
> The user changed setting `Model Selection` from None to Gemini 3.5 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
> </USER_SETTINGS_CHANGE>

> <USER_REQUEST>
> 開発記録ちゃんととれてる？
> あとチャットの記録
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T21:09:07+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 52
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Running terminal commands:
> - codex (in /home/haya-ryoo/dev/uver-setlist-app, running for 59h47m55s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> リモート同期のタイミングはいつ？
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T21:23:38+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 52
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Running terminal commands:
> - codex (in /home/haya-ryoo/dev/uver-setlist-app, running for 60h2m26s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> 変更を整理してコミット
> このプロダクトに関係なければignoreへ
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T21:45:12+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 52
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Running terminal commands:
> - codex (in /home/haya-ryoo/dev/uver-setlist-app, running for 60h24m0s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> エージェント側の回答がないね。
> そっちも残せるようにしたいね
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T21:53:12+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 52
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Running terminal commands:
> - codex (in /home/haya-ryoo/dev/uver-setlist-app, running for 60h32m0s)
> - git add .gitignore && git commit -m "chore: .gitignoreにエージェント... (in /home/haya-ryoo/dev/uver-setlist-app, running for 1m42s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> どういう状況？？
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T22:11:48+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 52
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Running terminal commands:
> - codex (in /home/haya-ryoo/dev/uver-setlist-app, running for 60h50m36s)
> - git add .gitignore && git commit -m "chore: .gitignoreにエージェント... (in /home/haya-ryoo/dev/uver-setlist-app, running for 20m18s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> どういう状況？？
> 
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:14:16+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 1
> Other open documents:
> - /home/haya-ryoo/.local/share/antigravity-logger/agent-logger.cjs (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> - /home/haya-ryoo/dev/uver-setlist-app/mcp.json (LANGUAGE_JSON)
> - /home/haya-ryoo/.local/share/antigravity-logger/README.md (LANGUAGE_MARKDOWN)
> - /home/haya-ryoo/.local/share/antigravity-logger/install-logger-hook.cjs (LANGUAGE_JAVASCRIPT)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> https://uver-setlist-archive.org/dashboard
> 本番環境のDBがつながっていない気がする
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:18:30+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 1
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/docs/agent_activity_logger.md (LANGUAGE_MARKDOWN)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/dump_schema.cjs (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/.gemini/antigravity-ide/scratch/dummy_transcript.jsonl (LANGUAGE_JSON)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/fetch_notion_spec.js (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/sync_notion_schema.cjs (LANGUAGE_JAVASCRIPT)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> 問題ない、よろしく
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:39:22+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 1
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/docs/agent_activity_logger.md (LANGUAGE_MARKDOWN)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/dump_schema.cjs (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/.gemini/antigravity-ide/scratch/dummy_transcript.jsonl (LANGUAGE_JSON)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/fetch_notion_spec.js (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/sync_notion_schema.cjs (LANGUAGE_JAVASCRIPT)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo fuser -k 8000/tcp
> 8000/tcp:            2390013
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo systemctl restart uver-app-prod
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo systemctl status uver-app-prod
> ● uver-app-prod.service - UVERworld Setlist App Production
>      Loaded: loaded (/etc/systemd/system/uver-app-prod.service; enabled; preset: enabled)
>      Active: active (running) since Tue 2026-06-02 23:46:52 JST; 5s ago
>    Main PID: 3243335 (node)
>       Tasks: 7 (limit: 9340)
>      Memory: 129.5M (peak: 129.7M)
>         CPU: 1.406s
>      CGroup: /system.slice/uver-app-prod.service
>              └─3243335 /usr/bin/node index.js
> 
> Jun 02 23:46:52 server01 systemd[1]: Started uver-app-prod.service - UVERworld Setlist App Production.
> Jun 02 23:46:52 server01 node[3243335]: [dotenv@17.2.4] injecting env (26) from .env -- tip: 🔄 add secrets lifecycle management: http>
> Jun 02 23:46:52 server01 node[3243335]: [dotenv@17.2.4] injecting env (0) from .env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.c>
> Jun 02 23:46:53 server01 node[3243335]: [dotenv@17.2.4] injecting env (0) from .env -- tip: 🔄 add secrets lifecycle management: https>
> lines 1-14
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:47:25+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 1
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/docs/agent_activity_logger.md (LANGUAGE_MARKDOWN)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/dump_schema.cjs (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/.gemini/antigravity-ide/scratch/dummy_transcript.jsonl (LANGUAGE_JSON)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/fetch_notion_spec.js (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/sync_notion_schema.cjs (LANGUAGE_JAVASCRIPT)
> Running terminal commands:
> - sudo systemctl status uver-app-prod (in /home/haya-ryoo/dev/uver-setlist-app, running for 28s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> Errorにまとめておこうか
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:52:15+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 1
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/docs/agent_activity_logger.md (LANGUAGE_MARKDOWN)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/dump_schema.cjs (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/.gemini/antigravity-ide/scratch/dummy_transcript.jsonl (LANGUAGE_JSON)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/fetch_notion_spec.js (LANGUAGE_JAVASCRIPT)
> - /home/haya-ryoo/dev/uver-setlist-app/.agent/scratch/sync_notion_schema.cjs (LANGUAGE_JAVASCRIPT)
> Running terminal commands:
> - sudo systemctl status uver-app-prod (in /home/haya-ryoo/dev/uver-setlist-app, running for 5m18s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> docsフォルダの仕様書にも、再発防止を
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:54:53+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 55
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> - /home/haya-ryoo/Obsidian/Errors/uver-setlist-app/cors_error_and_zombie_processes.md (LANGUAGE_MARKDOWN)
> Running terminal commands:
> - sudo systemctl status uver-app-prod (in /home/haya-ryoo/dev/uver-setlist-app, running for 7m55s)
> </ADDITIONAL_METADATA>

> <USER_REQUEST>
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo systemctl stop uver-app-prod
> sudo systemctl disable uver-app-prod
> Removed "/etc/systemd/system/multi-user.target.wants/uver-app-prod.service".
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo fuser -k 8000/tcp
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo systemctl enable uver-setlist
> sudo systemctl start uver-setlist
> Created symlink /etc/systemd/system/multi-user.target.wants/uver-setlist.service → /etc/systemd/system/uver-setlist.service.
> haya-ryoo@server01:~/dev/uver-setlist-app$ sudo systemctl status uver-setlist
> ● uver-setlist.service - UVERworld Setlist Archive - Node.js App
>      Loaded: loaded (/etc/systemd/system/uver-setlist.service; enabled; preset: enabled)
>      Active: active (running) since Tue 2026-06-02 23:57:49 JST; 4s ago
>    Main PID: 3268375 (node)
>       Tasks: 7 (limit: 9340)
>      Memory: 17.6M (peak: 17.9M)
>         CPU: 222ms
>      CGroup: /system.slice/uver-setlist.service
>              └─3268375 /usr/bin/node index.js
> 
> Jun 02 23:57:49 server01 systemd[1]: Started uver-setlist.service - UVERworld Setlist Archive - Node.js App.
> haya-ryoo@server01:~/dev/uver-setlist-app$ 
> </USER_REQUEST>
> <ADDITIONAL_METADATA>
> The current local time is: 2026-06-02T23:58:05+09:00.
> 
> The user's current state is as follows:
> Active Document: /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> Cursor is on line: 55
> Other open documents:
> - /home/haya-ryoo/dev/uver-setlist-app/.gitignore (LANGUAGE_UNSPECIFIED)
> - /home/haya-ryoo/Obsidian/Errors/uver-setlist-app/cors_error_and_zombie_processes.md (LANGUAGE_MARKDOWN)
> </ADDITIONAL_METADATA>

#### 🤖 エージェントからの回答
ご指定いただいた目的と要件に基づき、調査、設定確認、ロガーの手動実行、およびGitによるコミット・プッシュを完了いたしました。以下に結果をご報告いたします。

---

### 1. 接続確認結果
* **ディレクトリ存在確認**: `/home/haya-ryoo/Obsidian` ディレクトリが存在し、Git管理下にあることを確認しました。
* **プライベート確認**: `gh repo view --json isPrivate` コマンドにより、リモートリポジトリが**非公開（PRIVATE）**であることを確認いたしました。
  ```json
  {
    "isPrivate": true
  }
  ```
* **フォルダ構成確認**: Vault内の構成が要件通りに揃っていることを確認しました。
  * `Daily/` が存在し、対話ログが保存されています。
  * `Errors/kakeibo/` および `Errors/uver-setlist-app/` が存在します（誤記の `Erros/` は存在しません）。
  * `Projects/kakeibo/` および `Projects/uver-setlist-app/` が存在し、それぞれ配下の `Issues/README.md` を含めて必要ファイルがすべて揃っています。

### 2. Logger の導入状況
* **フック確認**: `/home/haya-ryoo/dev/uver-setlist-app/.git/hooks/pre-push` にロガーフックがすでに導入されていることを確認しました。
* **変数渡し**: フックは以下のように `${CONVERSATION_ID:-$CODEX_THREAD_ID}` がエスケープされつつ安全にロガーへ渡される記述になっていることを確認しました。
  ```sh
  node /home/haya-ryoo/.local/share/antigravity-logger/agent-logger.cjs --id "${CONVERSATION_ID:-$CODEX_THREAD_ID}"
  ```
  ※すでに正常に導入されていたため、新規インストールコマンドの実行はスキップしました（ロガーのインストーラ側でも二重登録防止でスキップされるロジックになっています）。

### 3. 追加・変更ファイル一覧
* **変更ファイル**:
  * `Daily/2026-05-31.md` （手動ログ生成によ
<truncated 72 bytes>


### 4. 手動ログ生成の確認結果
現在の会話ID `9f93435b-a736-4a86-9d46-a1080225179d` を環境変数および引数に指定し、ロガーを手動実行しました。
```bash
CONVERSATION_ID="9f93435b-a736-4a86-9d46-a1080225179d" \
node /home/haya-ryoo/.local/share/antigravity-logger/agent-logger.cjs \
--id "9f93435b-a736-4a86-9d46-a1080225179d"
```
* **実行ログ出力**:
  ```text
  [Logger] ログ資産化開始 (ID: 9f93435b-a736-4a86-9d46-a1080225179d)
  [Logger] Linuxネイティブ環境でWindows形式のパスを検出したため、パス解決をスキップします: C:\Users\oault\ドキュメント\Obsidian\ローカル\Daily
  [Logger] Linuxネイティブ環境用の標準フォールバックパスを使用します: /home/haya-ryoo/Obsidian/Daily
  [Logger] 新しい会話IDです。ログファイル末尾に追記します。
  [Logger] Obsidian 日誌に正常に保存されました: /home/haya-ryoo/Obsidian/Daily/2026-05-31.md
  [Logger] 新しい会話IDです。ログファイル末尾に追記します。
  [Logger] プロジェクトドキュメントに正常に保存されました: /home/haya-ryoo/dev/uver-setlist-app/Docs/activity_logs/2026-05-31.md
  ```
  フォールバックパスである `/home/haya-ryoo/Obsidian/Daily/2026-05-31.md` に、本対話セッションのログが正しく冪等に追記保存されたことを確認しました。

### 5. コミット ID
* **コミットID**: `8e6a384`
* **メッセージ**: `chore: 2026-05-31の対話ログを追加（手動実行）`

### 6. push 結果
* リモートが PRIVATE であることを実測確認した上で `git push origin main` を実行し、無事にプッシュが完了しました。
  ```text
  To https://github.com/hayaryoo519/obsidian-vault.git
     9e84232..8e6a384  main -> main
  ```

---
以上の通り、すべての手順が正常に完了し、ローカル Obsidian への開発記録の自動・手動資産化の仕組みが正常に動作することを確認いたしました。

I've queued the synchronization commands. I will wait for it to complete.

はい、**開発記録**および**チャット（対話ログ）の記録**ともに、正常にキャプチャ・保存され、リモートへ同期されていることを確認しました。

具体的な確認結果と対応は以下の通りです。

### 1. チャット（対話ログ）の記録確認
* **Obsidian 内の記録状況**:
  * `/home/haya-ryoo/Obsidian/Daily/` 配下に `2026-06-01.md` (約314KB) と `2026-06-02.md` (約391KB) が作成されていることを確認しました。
  * `2026-06-02.md` の末尾を確認したところ、直前のあなたの発言である「*開発記録ちゃんととれてる？ あとチャットの記録*」まで、リアルタイムに漏れなく記録されております。
* **リモートリポジトリとの同期**:
  * 手動実行によって作成・更新された以下のログファイルを Git にてコミットし、無事にプライベートリモートリポジトリ (`origin main`) へプッシュいたしました。
    * **対象ファイル**:
      * `Daily/2026-05-31.md` (他のプロジェクトでの更新を取り込んでマージ)
      * `Daily/2026-06-01.md` (新規追加)
      * `Daily/2026-06-02.md` (新規追加)
    * **コミットID**: `0ae91fb`
    * **プッシュ結果**: 正常完了

### 2. 開発記録（プロジェクト側）の確認
* **プロジェクト内の記録状況**:
  * プロジェクトルートの `.env` 設定（`AGENT_LOG_PROJECT_DIR="Docs/activity_logs"`）に基づき、`/home/haya-ryoo/dev/uver-setlist-app/Docs/activity_logs/2026-06-02.md` へも本セッションのログが正しく書き出されていることを確認しました。

---

開発記録もチャットログも漏れなくキャプチャされており、GitHubへのプッシュ同期も完了していますので、ローカルのObsidianからいつでも最新の記録を確認していただける状態です！

I will wait for the manual logger execution to finish to confirm the fix works.

リモート同期（GitHubへの自動プッシュ）が実行されるタイミングは、本来の設計では以下の**2つのタイミング**となっています。

1. **プロジェクト（`uver-setlist-app`等）で `git push` を実行した時**
   * プロジェクトのGitフック (`pre-push`) がトリガーとなり、自動的に会話ログの更新と、Obsidian Vault 側での `git add . && git commit && git push` が裏で実行されます。
2. **ロガーを手動実行した時**
   * コマンドによりロガーが実行された直後に、会話ログの更新と同時に GitHub へ自動同期されます。

---

### 💡 発生していたバグと修正のご報告
調査したところ、ロガーのプログラム（`agent-logger.cjs`）に以下のバグがあり、**自動同期処理がこれまで常にスキップされてしまっていた**ことが判明しました。

* **原因**: 
  ロガーが `.git` フォルダの存在チェックを行う際、親の `Obsidian/` ではなく `Obsidian/Daily/.git` を見に行ってしまっていたため、Gitリポジトリが存在しないと判定されて自動プッシュ処理がスキップされていました。
* **対応**: 
  先ほど `/home/haya-ryoo/.local/share/antigravity-logger/agent-logger.cjs` 内のパス判定ロジックを修正いたしました。

修正後の手動実行テストでは、以下の通り**自動同期が正常に機能する**ことを確認しております。
> `[Logger] Obsidian 日誌に正常に保存されました: /home/haya-ryoo/Obsidian/Daily/2026-06-02.md`
> `[Logger] [Git Sync] Obsidian の変更を GitHub に自動プッシュしました。`

今後は、**あなたがプロジェクト側で `git push` を行うか、あるいはロガーを実行した際**に、完全に自動でリモート GitHub に同期されるようになります。

I will wait for the git status check to complete.

I will wait for git status check to complete.

I will wait for the commit and push task to complete.

I will wait for the grep task to complete.
By the way, task-99 (git commit/push dev) finished. I'll read the notification or check later.

I will wait for the git push to complete.


### 1. 現在の状況（どういう状況？）

バックグラウンドで処理していた Git プッシュのタイミングで、一時的なネットワーク接続エラー（`curl 35 Send failure: Broken pipe`）が発生し、その直後に**開発サーバーの再起動**が走ったため、タスクが一時中断されておりました。

サーバー復帰後、直ちに以下のリカバリと処理を完了させています。
* **プロジェクト (`uver-setlist-app`) のプッシュ**:
  再起動後に `git push origin dev` を再試行し、**正常にプッシュを完了**しました。これにより、プロダクトに関係のない AI 協業スクリプト (`scripts/agent-collab/`) を `.gitignore` に追加したコミット (`e5c3d42`) がリモートの `dev` ブランチに反映されています。
* **Obsidian Vault の同期**:
  他プロジェクトからのリモート更新を取り込んで rebase 競合を解消し、`git push origin main` を手動実行してリモートへ**同期を完了**しました。

---

### 2. 「エージェント側の回答も残す」改修の完了

ご指摘の通り、これまでの対話ログではエージェント（AI）側の発言が記録されておらず、対話の流れが分からない状態でした。以下の通り改修を行い、動作確認いたしました。

* **ロガー (`agent-logger.cjs`) の改修**:
  システムが生成する会話ログ `transcript.jsonl` の内、AI が最終決定や回答を送る `PLANNER_RESPONSE` ステップから回答メッセージ (`content`) をパースして抽出する処理を追加しました。
  Markdown 変換時に **`#### 🤖 エージェントからの回答`** というセクションを設けて、ユーザーの指示のすぐ下に並ぶように変更しました。
* **動作確認**:
  ロガーを手動実行したところ、本日の日誌 `/home/haya-ryoo/Obsidian/Daily/2026-06-02.md` に、先ほど私がお送りしたステータス報告などの**エージェント側の回答がバッチリと保存されていること**を確認しました。

現在はバグが解消され、AI側の発言も含めて完全に会話履歴が保存＆自動同期されるクリーンな状態になっております！

I will wait for the journalctl logs output to complete.


I will wait for the logs output to complete.
_


I will wait for the service status check to complete.
_


I will wait for the curl output.
_



I will wait for the stats endpoint curl output.
_



本番環境のデータベースおよびプロセスの状態について調査いたしました。

結論から申し上げますと、**本番データベース自体は正常に稼働しており、データ（ライブ情報 1,061件など）も正しく保持されています。また、バックエンド (Node.js) も正常に応答しています。**

ユーザーから「DBがつながっていない」ように見える原因として、以下の**3つの問題（特にCORSエラー）**が判明いたしました。

---

### 🔍 調査結果と原因

#### 1. CORS設定の不足（最有力原因）
* **検証結果**:
  本番のAPIに対して、`Origin: https://www.uver-setlist-archive.org` (wwwあり) を指定してリクエストを投げたところ、`CORS policy violation` (500 Error) が返ってくることを実証しました。
  * **wwwなし** (`https://uver-setlist-archive.org`): 正常に応答 (200 OK)
  * **wwwあり** (`https://www.uver-setlist-archive.org`): CORSエラー (500 Error)
* **原因**:
  本番の環境変数ファイル (`/home/haya-ryoo/apps/uver-setlist-app/server/.env`) において、`ALLOWED_ORIGINS=https://uver-setlist-archive.org`（wwwなしのみ）と指定されているため、ブラウザが `www` ありのドメインでアクセスした際にAPIがブロックされ、画面にデータが一切表示されなくなっています。

#### 2. systemd サービスの重複稼働（ゾンビプロセス）
* **検証結果**:
  `uver-setlist.service` と `uver-app-prod.service` の2つのサービスが、同一のWorkingDirectoryで同時に `active (running)` になっています。
  * ポート 8000 は `uver-app-prod.service` がリスンしていますが、`uver-setlist.service` はポートの専有に失敗したものの起動し続け、裏でバックグラウンドモニター（`live_monitor.js`）などが二重に動き続けている状態です。

#### 3. 古いアセットのキャッシュ要求
* **検証結果**:
  nginx のアクセスログより、ユーザーのブラウザが古いビルドJS（`index-WHr_l98e.js`）を要求し、サーバー上に存在しないために `try_files` により `index.html` (HTMLテキスト) が 200 OK で返されていました。ブラウザ側で Javascript のパースエラー（`SyntaxError`）が発生し、画面の読み込みが止まっていた可能性があります。（※その後リロードなどにより正しいJSの読み込みには成功している形跡があります）

---

### 🛠️ 解決の提案（実行の許可要請）

本番サーバーの環境変更およびプロセスの再起動を伴うため、安全策ルールに基づき勝手には実行いたしません。以下の復旧作業を実行してもよろしいでしょうか？

1. **本番の `.env` を修正**:
   `/home/haya-ryoo/apps/uver-setlist-app/server/.env` の `ALLOWED_ORIGINS` を、wwwあり/なし両方を許可するように書き換えます。
   ```text
   ALLOWED_ORIGINS=https://uver-setlist-archive.org,https://www.uver-setlist-archive.org
   ```
2. **ゾンビプロセスのクリアとサービス再起動**:
   重複して動いているプロセスを整理し、本番のポート 8000 を一度解放した上で `uver-app-prod.service` を再起動します。
   （念のため不要な `uver-setlist.service` も一度停止します）

実行してよろしければ、指示をいただけますと幸いです！

本番の環境変数ファイル (`/home/haya-ryoo/apps/uver-setlist-app/server/.env`) にて、`ALLOWED_ORIGINS` の設定書き換え（wwwあり/なしの両ドメインの許可）は**完了いたしました**。

この設定変更を反映させ、かつゾンビプロセスを整理してサービスを復旧させるためには、systemd サービスの再起動が必要となります。
しかし、この操作には `sudo` 権限（パスワード入力）が必要であり、私の環境から非インタラクティブに実行することができませんでした。

大変お手数ですが、**ユーザー様ご自身のターミナルで、以下のコマンド群を実行していただけますでしょうか？**

```bash
# 1. 重複して動いている不要な uver-setlist サービスを停止・無効化
sudo systemctl stop uver-setlist
sudo systemctl disable uver-setlist

# 2. ポート 8000 を専有しているプロセスをクリアし、本番サービスを再起動
sudo fuser -k 8000/tcp
sudo systemctl restart uver-app-prod

# 3. 再起動後の起動ステータス確認
sudo systemctl status uver-app-prod
```

コマンドの実行が完了しましたら、その旨を教えていただけますと幸いです。こちらで API の応答テストなどを最終確認いたします！

ご協力ありがとうございました！無事に本番サービスの再起動が完了いたしました。

こちらで最終的な動作確認テスト（APIのCORSおよび疎通テスト）を行い、**無事に復旧したことを確認いたしました。**

---

### 🌐 最終確認テストの結果

再起動後の `uver-app-prod` に対して、`Origin` ヘッダーを指定して直接 API リクエストを送信し、応答を検証しました。

1. **wwwありドメイン** (`Origin: https://www.uver-setlist-archive.org`) 
   * **結果**: **`200 OK` (正常にJSONデータを返却)**
     * ※改修前は `CORS policy violation` (500 Error) になっていましたが、新設定が正しく反映され、正常に応答することを確認しました。
2. **wwwなしドメイン** (`Origin: https://uver-setlist-archive.org`)
   * **結果**: **`200 OK` (正常にJSONデータを返却)**

これにより、wwwあり/なしどちらの接続ルートからアクセスした場合でも、フロントエンドがAPI経由でデータベースからデータを正常に引き出せる状態に復旧いたしました。

---

### 📝 作業ログの自動資産化

今回の障害対応（ゾンビプロセスのクリアとCORSの修正）の一連のやり取りを、ロガーを通じて `Daily/2026-06-02.md` に最新化し、プライベートGitHubリポジトリへ**自動同期（コミット＆プッシュ）を完了**いたしました。
※今回の改修により、**エージェント側の回答テキストも正しく対話ログに残る**ようになっております。

お手元のブラウザから `https://uver-setlist-archive.org/dashboard` に再度アクセスし、ダッシュボードが正常に表示されるかご確認いただけますと幸いです！

ご指示に従い、今回発生した CORS エラーおよびゾンビプロセスによるアクセス障害についてのトラブルシューティングドキュメントを整理し、Obsidian の `Errors/` ディレクトリへ保存いたしました。

### 📁 作成・同期したファイル
* **パス**: [cors_error_and_zombie_processes.md](file:///home/haya-ryoo/Obsidian/Errors/uver-setlist-app/cors_error_and_zombie_processes.md)
* **コミット & プッシュ**:
  ロガーを連動実行し、上記のエラーログと会話日誌の更新を一括で自動コミット＆プッシュ完了しました。（作業ツリーはクリーンです）

---

### 📝 エラーログの要約 (Errorsにまとめた内容)

作成したドキュメントには、再利用性の高い構成（症状・原因・解決手順・再発防止策）で以下のように整理されています。

* **症状**: 
  本番ダッシュボードにアクセスしてもデータが表示されず、APIリクエスト（`/api/stats` 等）が CORS エラー (500) でブロックされる。
* **原因**:
  1. `server/.env` の `ALLOWED_ORIGINS` に www なし (`https://uver-setlist-archive.org`) のみが指定されており、`www` ありでのブラウザアクセスが拒否されていた。
  2. `uver-setlist.service` と `uver-app-prod.service` が同一ポート (8000) を競合して重複自動起動しており、プロセスがゾンビ化していた。
* **解決手順**:
  1. `server/.env` を書き換え、wwwあり/なしの両ドメインを許可。
  2. 重複している不要な `uver-setlist` サービスを `systemctl stop/disable`。
  3. `fuser -k 8000/tcp` で競合プロセスをクリアし、`uver-app-prod` を再起動。
* **再発防止**:
  1. 環境変数設定時のドメイン定義ルール（カンマ区切りでサブドメインを明示）の徹底。
  2. デプロイ後のポート専有プロセスと systemd サービス状態の確認。

本件にて、本番環境のDBアクセス及びAPI通信障害は完全に解消し、記録の保存まで正常にクローズいたしました！

I will wait for the commit and push to complete.
_



docs フォルダ内の仕様書（[environments.md](file:///home/haya-ryoo/dev/uver-setlist-app/docs/environments.md)）への再発防止策およびトラブルシューティング手順の追記と、`dev` ブランチへのコミット＆プッシュ（コミットID: `137d296`）を完了いたしました。

---

### ⚠️ 【重要】本番サービスの「正しいサービス名」への切り替えのお願い

自動デプロイ設定（`.github/workflows/deploy-production.yml`）を精査したところ、自動デプロイが完了した際に再起動するサービスとして、本来の **`uver-setlist.service`** が指定されていることが分かりました。

現在動かしている `uver-app-prod.service` のままだと、**次回のリリースデプロイ（GitHub Release 作成）時に、デプロイワークフロー側で `uver-setlist.service` が競合起動しようとしてエラー（デプロイ失敗）になる可能性が高い**です。

そのため、本来あるべき正しい本番構成（`uver-setlist.service` がポート8000を処理する状態）にするため、大変お手数ですが、**再度ご自身のターミナルで以下のコマンドの実行をお願いできますでしょうか？**

```bash
# 1. 誤って起動させた uver-app-prod を停止・無効化
sudo systemctl stop uver-app-prod
sudo systemctl disable uver-app-prod

# 2. ポート 8000 を一度クリア
sudo fuser -k 8000/tcp

# 3. 本来の正しい本番サービス uver-setlist を有効化・起動
sudo systemctl enable uver-setlist
sudo systemctl start uver-setlist

# 4. 起動ステータス確認
sudo systemctl status uver-setlist
```

何度もコマンド実行のお願いをしてしまい大変恐縮ですが、自動デプロイの動作維持のためにご協力をお願いいたします！

<!-- Conversation End: 9f93435b-a736-4a86-9d46-a1080225179d -->







---

<!-- Conversation Start: manual-1780405864412 -->
### 🤖 エージェント対話ログ (2026/06/02 22:12)

* **Conversation ID**: `manual-1780405864412`
* **作業スペース**: `uver-setlist-app`

> [!NOTE]
> 対話アクティビティが記録されていないか、パースに失敗しました（フォールバック実行）。

<!-- Conversation End: manual-1780405864412 -->


---

<!-- Conversation Start: manual-1780409680220 -->
### 🤖 エージェント対話ログ (2026/06/02 23:14)

* **Conversation ID**: `manual-1780409680220`
* **作業スペース**: `uver-setlist-app`

> [!NOTE]
> 対話アクティビティが記録されていないか、パースに失敗しました（フォールバック実行）。

<!-- Conversation End: manual-1780409680220 -->


---

<!-- Conversation Start: manual-1780412140226 -->
### 🤖 エージェント対話ログ (2026/06/02 23:55)

* **Conversation ID**: `manual-1780412140226`
* **作業スペース**: `uver-setlist-app`

> [!NOTE]
> 対話アクティビティが記録されていないか、パースに失敗しました（フォールバック実行）。

<!-- Conversation End: manual-1780412140226 -->
